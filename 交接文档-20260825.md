# 交接文档 2026-08-25：检测耗时优化与验证

> 交接给新会话。覆盖 2026-08-21 ~ 08-25 的检测流程耗时优化、实测数据、参数定稿、遗留问题与环境坑。
> 当前分支：`fix/multi-pintype-blob-parser` @ `e13e0f6`（工作区干净，仅 untracked：.workbuddy/ + 根目录分析脚本）

---

## 一、背景与目标

3D 扫描 + 视觉检测上位机（方案 215，8 检测位/排，可扩展 40+ 位连续运行）。目标：降低单检测位耗时。
基线（8/21）：单检测位中位 **7479ms**，40 位单排 **309s**（7mm/s 扫描）。

## 二、已落地优化（4 个提交）

| 提交 | 内容 | 实测收益 |
|---|---|---|
| `186171d`（8/24） | 1a 删移动前 delay200×2；2 删每拍 GetFiles×2 诊断；3 PauseAwareWaitAsync 改 WhenAny 零尾差 | A 段 -400ms 主力 |
| `4e1567d`（8/24） | Origin 图片容错：多余按创建时间删除 | 容错，无耗时收益 |
| `7c9e27f`（8/24） | 5 ReplaceTile 重拼+显示异步化；8 移动中心 X/Y+调Z 合并一次指令 | A 段 -100~500ms |
| `e13e0f6`（8/25） | SET_PATH 延时参数化（SetVmSavePathDelay）；调Z 延时参数化（PositionSnapshotDelay）×2 处；路径未生效诊断；删原图兜底死代码 | A 段 -130~320ms |

## 三、测试数据汇总（方案 215，同板）

| 轮次 | 扫描速度 | 参数 | 单检测位中位 | A段中位 | D段中位 | 跨度/节拍 |
|---|---|---|---|---|---|---|
| 8/21 基线（40位） | 7mm/s | 旧 | 7479ms | 2907ms | 4131ms | 309s / 7.73s |
| 8/24 17:25（41位） | 7mm/s | 旧 | 6627ms | 2082ms | 4204ms | 272s / 6.64s |
| 8/24 17:48（40位） | 12mm/s | 旧(100/100) | 5124ms | 2131ms | 2597ms | 211~232s |
| 8/25 10:53（39位） | 12mm/s | 50/40 | 5293ms | 2001ms | 2855ms | 235s / 6.03s |
| 8/25 11:04（39位） | 12mm/s | 25/40 | 5059ms | 1815ms | 2857ms | — |
| 8/25 11:12（39位） | 12mm/s | 0/40 | 4980ms | 1749ms | 2862ms | — |

**当前稳定态：单拍 ~5.0s，40 位单排 ~200s，对比 8/21 累计降 33%。**

### 关键结论（踩坑修正过，勿再踩）
- **速度-耗时自洽**：3D 扫描为纯速度限制（匀速反比），7/10/12/14mm/s 实测与理论偏差 ≤6%，14mm/s 偏差 0.7%。
- **SET_PATH 延时是"传播时间前置"**：延时 50ms 时传播与延时重叠、GRAB 后文件立即可见（等待 ~175ms）；0ms 时传播落入 GRAB 后文件等待（中位 271ms 且 max 602ms 长尾）。**50ms 最稳，25ms 无稳定收益（基线偏差假象），0ms 有长尾风险**。
- **8/25 各轮 A 段对比必须同位置**（前 8 位 A 段偏短、后位偏长），且轮间有机台漂移（10:53 轮前 8 位比 10:33 轮同参数高 373ms）——跨轮对比勿直接比均值。

## 四、参数定稿（已验证）

| 参数 | 值 | 验证 |
|---|---|---|
| 3D 扫描速度 | **12 mm/s**（界面"3D扫描速度"） | 14 曾跑无异常；12 为当前定值，14 需质量验证后再用 |
| SetVmSavePathDelay（保存路径生效延时） | **50ms** | 50/25/0 三轮 90+ 拍 0 条 [路径未生效]；50 最稳 |
| PositionSnapshotDelay（到位拍照延时） | **40ms** | 实测 35ms 是下限、40ms 清晰度最佳（用户目检确认） |

## 五、遗留问题（新会话关注）

1. **3D 偶发重试**：`关闭3D触发失败` / `3D数据超时，扫描成功但未收到数据` 偶发（8/24 17:13 轮 17890ms、8/25 11:04 轮 13042ms、8/21 多轮）。与 SET_PATH 无关（3D 链路独立）。**单次重试损失 5-20s，是连续产线最大隐忧，建议抓 3D 相机侧日志立项排查**。
2. **D 段偏高疑点**：8/25 轮 D 段中位 2855 vs 8/24 同速轮 2597（+258ms）——同位置同速应一致，疑似机台/板子差异，需同板多轮确认。
3. 偶发 `ERROR:Query timeout all boards`（GET_STATE 21Hz 轮询固有）、`ERROR:X axis busy`（重试轮出现）——已知，非本次改动引入。

## 六、环境坑（沙盒 git，重要）

- 本环境 git 写 `refs/heads/fix/` 目录被沙盒静默拦截：**pull/reset/commit/checkout 后分支可能变 unborn**（工作区全显示 staged，`git log` 报"does not have any commits yet"）。
- 修复：`mkdir -p .git/refs/heads/fix && printf '<完整SHA>' > .git/refs/heads/fix/multi-pintype-blob-parser`（SHA 从 `.git/FETCH_HEAD` 第一行取）。
- **每次 git 写 ref 操作后必须检查 `git log --oneline -1`**，异常即按上法修复。
- 远程 tracking ref（refs/remotes）写不进去 → `git branch -vv` 显示 `[origin/...: gone]`，不影响 push/pull（走 URL）。

## 七、分析脚本（项目根目录，可重跑）

| 脚本 | 用途 |
|---|---|
| `compare_0821_0824.py` | 8/21 vs 8/24 轮次总览（A/D/总耗时/排耗时） |
| `compare_detail.py` | 逐位 A/BC/D/E/total 明细 |
| `compare_40_41_xyz.py` | 40/41 位连续轮对比 + XYZ 到达顺序统计 |
| `xyz_order.py` | XYZ 到达顺序统计（399 组移动指令） |
| `analyze_gap.py` | 解析3D→下一检测位 耗时构成（TOTAL 锚定） |

日志位置：`bin/x64/Debug/Logs/log_*.txt`（每次启动新文件，格式 `[⏱] 检测位X A/B+C/D/E/总耗时`）。

## 八、下一步（详见《优化测试报告-20260825.md》）

1. 优化 11（ReadImage 19MB×2 异步化）——改动小已设计好，实机估省 200-400ms/拍
2. 扫描提速 12→14mm/s——省 ~410ms/拍，需同针高度精度对比验证
3. 3D 偶发重试排查立项
4. 微调项：调Z期间并行发 SET_PATH（省 100-150ms/针型位）、轴速 600→800mm/s（省 ~100ms）
