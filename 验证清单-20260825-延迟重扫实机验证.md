# 验证清单 2026-08-25：延迟重扫实机验证（主题①）

> 交接文档《交接文档-20260825-延迟重扫与2D重试方案.md》第三节主题①的执行清单。
> 前置：`_build.bat` 编译成功（改动含 `MotionIO.cs` 排队优化 + `DetectionFlow.cs` 延迟重扫，未编译）。
> 日志文件：`<程序目录>/Logs/log_{会话}.txt`（每轮一个会话文件，跑完一轮直接看最新那个）。
> 分支：`fix/multi-pintype-blob-parser`（未提交）。

---

## 〇、验证前确认（10 秒）

1. exe 时间 ≥ 源码 mtime（确认编译版本生效，`.workbuddy/` 已 ignore）
2. 场景触发条件：需要**至少一个 3D 数据异常位**（空点云/数据不来/轴失败）。现网位 6/21/25 轮发，或手动制造（拔 3D 数据线/堵光）。
3. 一轮即可：无需连续模式，单板跑完看三阶段日志。

---

## 一、验证清单（三阶段 + 替换 + 队列推进）

### 阶段① 主流程记录（期望：WARN 出现，数量 ≥1）

| # | 期望日志（关键词） | 代码位置 | 含义 |
|---|---|---|---|
| 1 | `已记录板末延迟重扫` | DetectionFlow.cs:829（轴失败）/ :1358（全匹配失败）/ :1438（数据超时） | 异常位已入 `_rescanQueue`，不再即时重扫 |

> 反证：若出现旧日志 `3D扫描失败` 后**无**"已记录板末延迟重扫"，说明轴失败分支没走到（检查 L822-830）。
> 阶段① 期间**不应**出现 `重扫` 字样（主流程已不即时重扫）。

### 阶段② 板末入口（期望：两条之一出现）

| # | 期望日志（关键词） | 代码位置 | 含义 |
|---|---|---|---|
| 2 | `主流程完成，进入板末统一重扫：检测位 N（队列剩余 M 位）` | :1575（End case） | 主流程在 End 内直接转入重扫 |
| 2' | `[延迟重扫] 主流程结束，循环外进入板末重扫：检测位 N（队列剩余 M 位）` | :1653（循环外兜底） | End case 未触发，循环外兜底接手（18:23:19 曾在此失败） |
| 3 | `[延迟重扫] End 收尾检查: _rescanPhase=..., 队列Count=..., i=..., total=...` | :1568 | End 每次到访都打，看 `i` 是否超出 total（判断游标异常） |

> **关键判定**：② 之后必须能连续看到阶段③的"重拍→2D→3D→合并"日志。
> 若只到②就出现 `流程未正常完成`/直接收尾——flowState 重置没生效，见第四节排查①。

### 阶段③ 重扫位完整流程（核心，日志必须逐条出现）

按重扫位在队列中的顺序，每位移出一组（以检测位 N 为例）：

| # | 期望日志（关键词） | 代码位置 | 含义 |
|---|---|---|---|
| 4 | `检测位 N 多针GRAB完成: 板面=..., 针型=... 共...个` | RunCommands.cs:1349 | **重拍 A 段**（完整重拍，非旧图） |
| 5 | `检测位 N: SCAN3D X..., Y...` | DetectionFlow.cs:600 | 重新发 SCAN3D |
| 6 | `检测位 N 2D检测完成，解析到 X 个针` | :730 | 重新 2D 检测 |
| 7 | `[⏱] 检测位 N 重扫 D(3D扫描完成)=...ms` | :792 | 重扫模式专属计时（主流程是 `D(3D扫描完成)` 无"重扫"前缀） |
| 8 | `[⏱] 检测位 N 重扫 总耗时=...ms ====` | :793 | 重扫位总耗时 |
| 9 | `[检测位 N 合并明细] ...` | :1368 | 重扫结果落表（**必须与 7/8 配对**） |

> 判定要点：**4→5→6→7→8→9 严格按序**，且 5 的 SCAN3D 与 3D 数据（`合并明细` 里的点数）一一对应——这就是"严格串行"的日志证据。
> 若 4 出现但 5 不出现 → 卡在 Start2D/MoveScanStart（检查点②③逻辑）；若 7 出现但 9 不出现 → Merge 分支问题。

### 阶段④ 重试与队列推进

| # | 期望日志（关键词） | 代码位置 | 含义 |
|---|---|---|---|
| 10 | `检测位 N 重扫第 X 次仍...（重拍重来/达到上限，接受降级结果）` | :1341-1343 / :1423-1424 | 重扫位仍不合格 → 重拍重来（<3 次）；≥3 次接受降级 |
| 11 | `检测位 N 板末重扫（队列剩余 M 位）` | :1523 | 队列内下一位移入（NextPos 重扫分支） |
| 12 | `板末重扫完成，队列已空` | :1529 | 全部重扫位处理完毕 |

> 多异常位场景期望：`队列剩余 M 位` 递减 → 0 → `队列已空`。

### 阶段⑤ 结果替换（日志不可见，看 UI/汇总）

- 重扫成功后该位 PinResults：旧降级（H=0 / NG）→ 新结果（H>0 或 OK 数增加），代码 DetectionFlow.cs:1382-1392（`_rescanPhase` 时先删旧 GridIndex 再插新）
- 叠加层该位 H/IsOK/PinId 刷新（:1394-1401）
- 汇总表/保存文件无重复 PinID（`[去重] 移除 X 个重复针` 数量正常，全局去重 :1662 附近）

---

## 二、一键 grep（跑完一轮直接执行）

```bash
# 最新会话日志（按需换成实际文件名）
LOG="<程序目录>/Logs/log_*.txt"   # 或直接给具体文件

# 三阶段总览（含行号，一次看全）
grep -nE "已记录板末延迟重扫|进入板末统一重扫|循环外进入板末重扫|多针GRAB完成|SCAN3D X|2D检测完成|重扫 D\(|重扫 总耗时|合并明细|板末重扫|队列已空|重扫第|End 收尾检查" $LOG

# 只看异常位（WARN/ERROR 上下文）
grep -nE "3D扫描失败|3D数据超时|延迟重扫|扫描成功但未收到数据" $LOG

# 串位回归检查（必须为空！重扫阶段不得出现"主流程位"的 SCAN3D 与重扫 SCAN3D 交错）
grep -nE "SCAN3D X" $LOG | awk '{print $2}' | uniq -c   # 每个检测位 SCAN3D 次数：正常流 1 次；被重扫位 2 次（主流程+重扫各1）——观察顺序是否交错
```

---

## 三、判定汇总表

| 检查项 | 通过标准 | 失败 → 排查路径 |
|---|---|---|
| ① 记录 | 至少 1 条 `已记录板末延迟重扫` | 异常位没触发重扫条件（Merge 判定 L1338/L1420） |
| ② 入口 | ② 或 ②' 至少一条，且后接阶段③ | 两条都没有 → End case 与循环外兜底都没进 → 查 `_rescanQueue` 是否在 Init 后又被 Clear（:335） |
| ③ 完整流程 | 4→5→6→7→8→9 按序成组 | 停在 4 后 → flowState 重置问题（见排查①）；停在 7 后 → Merge 未到 |
| ④ 队列推进 | `队列剩余` 递减 → `队列已空` | 卡在某位重复重试 → 该位 3D 持续异常（看 `重扫第 X 次`） |
| ⑤ 结果替换 | 旧降级→新高，无重复 PinID | 替换未生效 → Dispatcher 内先删后插（:1384-1388）竞态或 GridIndex 不匹配 |
| 回归-无串位 | 位31-34 类"3D 点数 vs 2D 针数严重不符"不再出现；重扫阶段无在途 SCAN3D 交错 | 出现 → 重扫模式仍有并发（`_rescanPhase` 分支被跳过） |

---

## 四、若重扫位流程仍不执行的排查路径（18:23:19 旧故障的复查）

按优先级：

1. **flowState 重置**：循环外兜底 L1655 `flowState = FlowRunState.Running;` —— 断点确认主流程结束后（End→BizStep 返回 true→Terminated）此句确实执行。若在 L1656 `await FlowLoopAsync(RunStep)` 内断点看到 RunStep 直接走 `case FlowRunState.Terminated`（L1633），说明 L1655 未生效（可能被前面 `if` 条件 `!_alarmStopping && bizState != CheckState.Err` 短路——确认 bizState 是 End 而非 Err）。
2. **重扫模式流转**：重扫位 CapturePos（L417）→ GrabTile（L442）全程**不走** `_skipAForNext`（NextPos 重扫分支 L1516-1533 直接设 CapturePos，绕过 L1536-1561 的跳过逻辑）——若日志出现"跳过A段"类行为，说明重扫位误走了 `_skipAForNext` 路径，需查 L1516 分支前是否有残留 true。
3. **Wait2D 尾分流**：L749 `bizState = _rescanPhase ? WaitScan3D : MergePending` —— 重扫模式必须走 WaitScan3D（同步取数直接 Merge）。若走了 MergePending，说明 `_rescanPhase` 在该位为 false（检查 :1569/:1649 置位时机）。
4. **重试死循环**：重扫位 Merge 持续不合格且 `_rescanRetries` 不递增 → 检查重试分支 `_rescanRetries < max3DRescanRetry` 条件（:1344/:1425）与 `_rescanRetries++` 位置。

---

## 五、实机关注点（记录时间线，供下轮分析）

- 重扫位 `重扫 总耗时` 与主流程同位的 `总耗时` 对比（重扫 = A 段重拍 ~1.7s + 2D + 3D，代价应集中在板末）
- ON→OK 直发后 p90 仍 440ms 的偶发（遗留问题#2，下位机侧）——本轮若复现，记时间戳
- 重扫期间 3D 数据到达时延（`重扫 D(...)` 值：<2s 正常；≈2s 说明数据异常空等了满窗口——这是主题②要压缩的点）
