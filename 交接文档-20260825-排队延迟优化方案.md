# 交接文档 2026-08-25：排队延迟优化方案讨论（下会话主题）

> 交接给下个会话。本次已完成：① Merge 后置优化实机验证（**达标**）；② 定位到下一个优化点——**轴到位→CAMERA3D_ON 排队延迟 ~400ms**。
> 下会话主攻：排队延迟优化的方案选型与实施。
> 分支：`fix/multi-pintype-blob-parser`（改动未提交）。改动文件：`ViewModels/MainViewModel.DetectionFlow.cs`、`ViewModels/MainViewModel.CollectionFlow.cs`。

---

## 一、本次验证结果：Merge 后置优化 ✅ 达标

**最新日志**：`bin/x64/Debug/Logs/log_20260825_170151.txt`（17:01 启动，39 位完整轮）

| 指标 | 基线(同步) | 16:00轮 | **最新17:01轮** | 结论 |
|---|---|---|---|---|
| A 中位 | 1838 | 1657 | **1656** | ✓ |
| **B+C 中位** | 2251 | 2582 | **2053** | ✓✓ 大幅回落，**低于基线** |
| D 中位 | 2380 | 2378 | **2383** | ⚠️ 未降（排队延迟仍在内） |
| **总中位** | 4625 | 4749 | **4456** | ✓ 较16:00轮 -293ms |

**关键证据**：
- **A行→2D发送 间隙：中位 12ms**（16:00 轮 247ms）→ Merge 计算彻底移出 B+C 段 ✅
- **重扫 0 次**，本轮干净
- 总耗时守恒式 `总 = B+C + D` 成立（2053+2383=4436≈4456）

**结论**：Merge 后置完全生效。总耗时 4625→4456（-169ms），未到 4280 理论值——因为 **D 段没降**，排队延迟 ~400ms 仍占着 D 段。

---

## 二、下一个优化点：轴到位→CAMERA3D_ON 排队延迟 ~400ms（主攻主题）

### 2.1 实测数据（最新轮逐位）

| 间隔 | 中位 | p90 | 说明 |
|---|---|---|---|
| **轴到位(B+C完成) → CAMERA3D_ON 发送** | **398ms** | 434ms | ⚠️ 主要损耗 |
| CAMERA3D_ON → ON_OK | 17ms | 53ms | 握手本身很快 |
| ON_OK → SCAN3D 发送 | 2ms | 66ms | 几乎立即 |

**异常佐证（同一轮内波动 5ms~430ms）**：位4=22ms、位6=5ms、位1/2/3/5/7=~410ms —— 证明正常路径可 <50ms，400ms 是**排队**而非固定开销。

### 2.2 根因链（已定位到代码）

```
CAMERA3D_ON 走 MotionIO 串行指令队列 EnqueueCommand（MotionIO.cs L833 → L562）
  ├─ GET_STATE 轮询（25Hz，RequestGetState L1400 也走同队列）
  ├─ 移动命令（MoveToAndWaitOK）、扫描命令 共用同一队列
  └─ 队列处理器单线程 ProcessQueue（L625）逐条执行
移动完成瞬间队列常积压 GET_STATE；遇命令超时（日志 "ERROR:Query timeout all boards"）
→ 后续 CAMERA3D_ON 全部排队 ~400ms
```

### 2.3 为什么值得修：这 400ms 计入 D 段

D 段公式 = `swPos.Elapsed − tBC`（tBC=移起点完成时刻）。CAMERA3D_ON 排队发生在 tBC 之后 → **全部计入 D 段**：
- 当前 D 中位 2383ms，其中 ~400ms 是"轴到位后等 3D 触发"空转
- 消除后：D → ~1980，**总耗时 4456 → ~4060，39 位 -15.6s**

### 2.4 候选方案（下会话选型，按性价比排序）

| # | 方案 | 思路 | 风险/代价 |
|---|---|---|---|
| 1 | **CAMERA3D_ON 提前到移动期间发** | MoveScanStart 移动开始前先 EnqueueCommand("CAMERA3D_ON")，触发开启与移起点并行 | 低：ON 不依赖轴位置；需确认 ON 提前对 CAM3D 信号语义无副作用（下位机侧） |
| 2 | **扫描关键段暂停 GET_STATE 轮询** | 移动→SCAN3D 期间 StatePollLoop 暂停 ~2s | 中：影响位置/IO 实时监控，需评估报警保护依赖 |
| 3 | **CAMERA3D_ON 走独立通道** | 不排队，fire-and-forget 发送，ON_OK 后台确认 | 中：破坏串行队列有序性，需验证与移动命令交错安全 |

**倾向方案 1**（一行移动代码位置，风险最低）。

---

## 三、下会话讨论要点

1. **方案选型**：1 vs 2 vs 3，或组合（1+2）；
2. **CAMERA3D_ON 提前的边界**：扫描失败/轴失败路径的 ON/OFF 配对是否受影响（现有代码：轴失败走弹窗降级，ON 已开需确保 OFF 收尾）；
3. **验证方式**：模拟器 + 实机各跑一轮，看"D 段是否降到 ~1980、到位→ON 间隔是否 <50ms"；
4. 若 400ms 全消除，**40 位排时 4456→~4060**，对照 8/21 基线 309s 累计 **-48% 左右**。

---

## 四、状态速览（本会话改动，均未提交）

1. **Merge 后置（已实施已验证）**：GrabTile→Start2D 直连、Wait2D→MergePending、NextPos _skipAForNext 恢复 rawPins2D + _rescanFlag 驱动重发 2D、重扫分支加 _vmDetectTask=null；
2. **删除单检测位完成日志** 2 处（保留整轮汇总）；
3. 待办：① 排队延迟方案（本文档主题）；② 更新日志/交接文档 v2 数据；③ 后续 3D 偶发重试（位23 历史问题）仍建议抓相机侧日志。

---

## 五、环境坑（沿用）

- git 写 `refs/heads/fix/` 被沙盒静默拦截 → 分支可能 unborn；`git log --oneline -1` 异常时用 reflog 取 SHA（`tail .git/logs/HEAD`）；
- 远程 tracking ref 写不进，push 走 URL；push 后 `git ls-remote origin <branch>` 验证。
