# 近几次检测耗时优化与测试数据总结（2026-08-21 ~ 08-25）

> 方案：215（8 检测位/排，后扩展至 40+ 位连续运行）
> 日志：`bin/x64/Debug/Logs/`；分析脚本：`compare_0821_0824.py` / `compare_detail.py` / `compare_40_41_xyz.py` / `analyze_gap.py` / `xyz_order.py`

---

## 一、优化清单（按提交顺序）

### 1. `186171d`（08-24）单检测位耗时优化第一波
| 项 | 内容 | 代码位置 |
|---|---|---|
| 1a | 删移动前 `Task.Delay(200)`（MoveToAndWaitOK 已等轴完成，纯白等） | RunCommands.cs GrabAndReplaceTileAsync + GrabOneAsync Blob 路径 |
| 2 | 删每拍诊断 `Directory.GetFiles×2`（8-14 排查遗留） | DetectionFlow.cs GrabTile |
| 3 | `PauseAwareWaitAsync` while→WhenAny 零尾差（暂停≤50ms/取消立即返回/异常上抛，语义不变） | DetectionFlow.cs |

### 2. `4e1567d`（08-24）Origin 图片数量容错改进
- 多余图片按**文件创建时间**删除（时间相同按文件名降序），比按文件名排序更准确反映采集新老。

### 3. `7c9e27f`（08-24）单检测位耗时优化第二波
| 项 | 内容 | 代码位置 |
|---|---|---|
| 5 | ReplaceTile 重拼+显示异步化（同步只更新缓存，Task.Run 纯计算 + UI 线程显示替换；全图采集同步版保留） | FullImageManager.cs（调用方零改动） |
| 8 | 移动中心(X/Y)+调Z 合并为一次指令 `MoveToAndWaitOK(x,y,z)`（原两段串行；z 仅 focusZ>0 传） | RunCommands.cs GrabOneAsync |

### 4. `e13e0f6`（08-25）SET_PATH 延时参数化 + 路径诊断（模拟器可测优化）
| 项 | 内容 |
|---|---|
| SET_PATH 延时参数化 | `Task.Delay(100)` → `Task.Delay(SetVmSavePathDelay)`（默认 100，可调 0；原 `ImageCaptureDelay` 改名，此前无调用点） |
| 调Z后稳定延时参数化 | `Task.Delay(100)` → `Task.Delay(PositionSnapshotDelay)`（默认 200，曝光前光学稳定保险） |
| 全图采集调Z延时参数化 | CollectionFlow.cs L191 区，与 L215 用法统一 |
| 路径生效诊断 | 新增 `_lastVmSavePath` 记录上次 SET_PATH 目录；文件就绪超时且 fallback 无文件时，若文件落在旧目录 → 打 `[路径未生效]` 显性日志 |
| 死代码清理 | 删除 DetectionFlow.cs 原图兜底残留（8-14 已改 Err 终止，分支不可达） |

> ⚠️ e13e0f6 **未编译**（沙箱拦截 MSBuild），待本机 `_build.bat` 编译 + 模拟器验证。

---

## 二、测试数据汇总（方案 215）

### 2.1 各轮次实测（同方案同板）

| 轮次 | 扫描速度 | 单检测位中位 | A段中位 | D段中位 | 连续跨度 | 节拍/位 |
|---|---|---|---|---|---|---|
| 8/21 基线（40位） | 7 mm/s | 7479ms | 2907ms | 4131ms | 309.0s | 7.73s |
| 8/24 17:25（41位） | 7 mm/s | 6627ms | 2082ms | 4204ms | 272.1s | 6.64s |
| 8/24 17:39（15位） | 10 mm/s | ~5700ms | ~2100ms | ~3040ms | — | — |
| **8/24 17:48（40位）** | **12 mm/s** | **5124ms** | **2131ms** | **2600ms** | **211.4~232.2s** | **5.3~5.8s** |

> 8/24 17:48 轮 A 段均值 2275ms；单检测位总耗时均值 5339ms（去检测位0 改参数重做后 5225ms）。
> 17:48 轮跨度含检测位0 因中途改速度（14→12mm/s）重做 ~21s，去重后 211.4s。

### 2.5 8/25 实机对比（e13e0f6 生效：SetVmSavePathDelay=50 / PositionSnapshotDelay=30，同 12mm/s）

| 指标 | 8/24 17:48（40位，旧参数） | 8/25（16位，新参数） | 变化 |
|---|---|---|---|
| A段 均值 | 2275ms | **1853ms** | **-422ms（-18.6%）** |
| A段 中位 | 2131ms | **1788ms** | **-343ms（-16.1%）** |
| D段 中位 | 2597ms | 2842ms | +245ms（位置/样本差异，同速） |
| 总耗时 均值 | 5339ms | **4965ms** | **-374ms（-7.0%）** |
| 总耗时 中位 | 5124ms | 5048ms | -76ms |

- **A 段收益归因**：SetVmSavePathDelay 100→50（每 GRAB 省 50ms×N）+ PositionSnapshotDelay 100→30（每次拍照调Z 省 70ms×M），合计实测 -343~422ms/拍。
- **无 `[路径未生效]`/文件超时日志** → SET_PATH 延时 50ms 够 VM 传播（首轮验证通过）。
- **无 3D 重试/超 7s 异常**（16 位 4195~5457ms）——对比 8/24 检测位0 9799ms、8/21 多轮重试，本轮干净。
- ⚠️ D 段中位略高（2842 vs 2597）：速度相同，疑似位置扫描长度分布差异（8/25 仅 16 样本），需更多样本确认；调Z 30ms 的光学稳定性需现场目检画面。

### 2.2 收益分解

| 贡献项 | 每检测位 | 说明 |
|---|---|---|
| A 段优化（1a 删 delay200×2 + 2 删诊断 + 8 合并X/Y+Z） | **-750~825ms** | 8/21 A=2907 → 8/24 A=2082 |
| 扫描提速 7→12 mm/s | **-1600ms**（D 段 4204→2600） | 匀速反比，线性收益 |
| **合计** | **-2355ms（-31.5%）** | 8/21 7479 → 8/24 5124 |

**40 位单排总时长：309s → 211~232s（-25%~-32%），约省 1 分 17 秒 ~ 1 分 38 秒。**

### 2.3 速度自洽性验证（证明 3D 扫描为纯速度限制，非 IO/通信瓶颈）

| 速度 | D段理论（7mm/s×4204反比） | 实测 | 偏差 |
|---|---|---|---|
| 10 mm/s | 4204×7/10 = 2943ms | ~3040ms | +3% |
| 12 mm/s | 4204×7/12 = 2452ms | ~2600ms | +6% |
| 14 mm/s | 4204×7/14 = 2102ms | 2117ms | +0.7%（精确） |

### 2.4 单检测位耗时构成（12mm/s，40位轮）

| 环节 | 中位 | 占比 | 优化空间 |
|---|---|---|---|
| A 段 移动中心+调Z（纯指令） | ~440ms | 21% | 已合并 X/Y+Z，无 |
| A 段 GRAB 拍照（板面+针型 2-3 次） | ~950ms | 46% | 受针型对焦 Z 高度不同约束，无法合并 GRAB |
| A 段 SET_PATH/调Z/交错间隙 | ~620ms | 29% | SET_PATH 可与拍照调Z并行，估省 100-150ms/针型位 |
| 3D 解析+决策衔接 | ~80ms | 4% | 无 |
| D 段 3D 扫描 | ~2600ms | — | 已随速度线性下降 |

---

## 三、XYZ 到达顺序（协议级观察）

- 归零 `X0,Y0,Z0`：**Z → Y → X**（Z 先到）
- X/Y 平面移动（388 组）：**330 组同毫秒到达**；57 组 Y 先（中位领先 3.2s，Z 型路径换行距离短）；X 先仅 1 次
- 合并指令 `X..,Y..,Z..`：X/Y 未动时仅回 Z_COMPLETE；POS 反馈总带四轴位置
- 偶发：`ERROR:X axis busy`（8/24 17:13 重试轮）、`ERROR:Query timeout all boards`（GET_STATE 21Hz 轮询时偶现）

---

## 四、待验证 / 待办

1. **e13e0f6 编译 + 模拟器验证**（未编译）：`SetVmSavePathDelay` 调 0 后 A 段应降（每拍省 (1+N)×100ms）；`PositionSnapshotDelay` 默认 200 暂比旧 100 慢 100ms，可测 0/50 找最佳值
2. `[路径未生效]` 诊断验证（模拟器改 GRAB 落点）
3. 3D 偶发重试：8/24 17:13 轮（total 17890ms）、8/21 多轮仍有 3D 超时重试，与耗时优化无关，待单独排查
4. 优化 11（ReadImage 19MB×2 异步化）：模拟器 -63ms，实机估 200-400ms，待实机数据

---

## 五、环境备注（沙盒 git 坑）

- 本环境 git 写 `refs/heads/fix/` 目录会被沙盒静默拦截：`pull/reset/commit` 后分支可能变 unborn（工作区全显示 staged）。
- 修复：`mkdir -p .git/refs/heads/fix && printf '<完整SHA>' > .git/refs/heads/fix/multi-pintype-blob-parser`（SHA 从 FETCH_HEAD 取）。
- 每次 pull 后需检查 `git log` 是否正常，是则无需处理。
