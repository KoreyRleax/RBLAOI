# 更新日志

- **时间**：2026-08-11
- **标题**：设备监控层（DeviceMonitor）—— 轴报警实时监控，仿插针机 ioAndDataMonitor 分层设计

## 背景

轴报警处理最初直接写在信号工具类 `IoSignalMonitor` 的事件 handler 里（弹窗 + 蜂鸣器 + 标志）。用户指出：报警处理是**业务逻辑**，不应住在信号层工具类；参照插针机（C++ 插件机）的设计——独立监控线程 `dataRefresh` 主循环按序调用各业务检查函数（`ioAndDataUpdate` → `sensorUpdate` → `chkAppearance` → `chkAxisAlarm` → `keyJog` → `machineStateUpdate`），每个检查独立成函数，便于维护与扩展。先讨论定案再实施（用户确认：独立线程循环驱动、报警联动回零标志、蜂鸣器报警消失自动停）。

## 解决方案

### 分层架构（仿插针机）

```
采集层  MotionIO.StatePollLoop(GET_STATE 轮询,已有,不动)
信号层  IoSignalMonitor(只做喂入:全部 IO 位 + 轴报警位去抖计时,纯信号,无业务)   ← 报警检查已移出
监控层  DeviceMonitor(新增,仿 ioAndDataMonitor):独立后台线程循环(20ms),
        每周期按序执行各业务检查函数,报警动作(弹窗/蜂鸣器/置标志)全在此层
流程层  MainViewModel 状态机:查询 DeviceMonitor 标志(NeedHomeAny 等)
```

### 1. `Core/Device/DeviceMonitor.cs`（新建，csproj 已注册）

- 单例 + **独立后台线程**（IsBackground + 最高优先级，仿 ioThread），`Start()` 由 MainViewModel 构造调用；
- 每周期（20ms）`OnCycle()` 按序调用检查函数：`CheckAxisAlarm()`（未来扩展 CheckSafety/CheckAppearance/UpdateMachineState 只需挂入调用）；
- **CheckAxisAlarm（仿 chkAxisAlarm）**：
  - 用信号层 `IoSignalMonitor.AlarmX/Y/Z/H.IsOnFor(200)` 去抖判定任一轴报警 ON 持续 0.2s；
  - 报警触发：置 `AxisAlarm` 标志 + 按轴置 `NeedHomeX/Y/Z/H`（仿 isNeedHome）+ 弹窗（Dispatcher 调度 UI 线程，非阻塞）+ 蜂鸣器 ON（异步 SetIO）+ `[Alarm]` 日志；**防重**（`_alarmReported`，报警持续期间只报一次）；
  - 报警全部消失：复位标志 + 蜂鸣器 OFF（自动解除）。

### 2. `IoSignalMonitor` 回退为纯信号层

- 移除报警判定/弹窗/蜂鸣器代码及不再需要的 using（System/System.Windows）；
- 保留 `AlarmX/Y/Z/H` 去抖喂入（业务层的数据源）。

### 3. MainViewModel 接入

- 构造函数：`DeviceMonitor.Instance.Start()`（内部先 `IoSignalMonitor.EnsureInit()` 保证信号层就位）；
- `ServoHome` 回零成功后 `ClearNeedHomeFlags()`（清除回零标志）；
- 主要动作入口（运送基板/全图采集/检测）检查 `NeedHomeAny`：轴报警后未回零则提示"需先执行伺服回零"并拒绝执行。

## 总结

报警等设备业务监控从信号工具类迁移到独立监控层：信号层（喂入去抖）→ 监控层（业务检查，独立线程循环）→ 流程层（查询标志）三层各司其职。后续扩展（安全光幕/急停/气压检查、设备状态机）只需在 `DeviceMonitor.OnCycle` 挂入新检查函数，不动采集层与信号层。

## 验证

- 编译通过（AnyCPU；x64 需程序关闭后复制）。
- 上机验证：触发某轴报警 → 0.2s 后弹窗 + 蜂鸣器 ON + 日志；解除报警 → 蜂鸣器自动停；报警后未回零执行运送基板/全图采集/检测 → 提示需先回零；执行伺服回零 → 标志清除可继续操作。

---

## 补充更新（同日）：报警锁存与复位确认模型 + 监控合并到轮询线程

### 背景

用户明确报警模型要求：弹窗必须模态；**点掉对话框 ≠ 清除报警**（信号仍 ON 时蜂鸣器不应关）；报警清除靠**复位按钮**——复位只清除软件报警状态，不代表硬件信号消失，信号仍在时用**状态栏文字持续提示**兜底。并决策：IO 监控与设备监控**合并到一个轮询线程**（仿插针机单线程全包，无线程/无锁），报警复位**复用现有复位按钮**（主界面运行栏"复位"→ResetMachine、IO 界面"🔧 复位"→BtnReset_Click，两处都接入）。

### 解决方案

1. **DeviceMonitor 改为事件驱动**（不再独立线程）：订阅 `IOStateUpdated`，与 IoSignalMonitor 喂入同在 GET_STATE 轮询线程顺序执行（先喂入后检查）；检查频率 = IO 轮询间隔（默认 20ms），去抖 0.2s 误差 ±20ms；
2. **报警锁存模型**：
   - 触发沿（信号 ON 持续 0.2s 且未锁存）→ 锁存：**模态弹窗一次**（Dispatcher 调度 UI 线程）+ 蜂鸣器 ON + AxisAlarm/NeedHome 置位；
   - 锁存中：点掉弹窗不清除（蜂鸣器继续响）；报警信号消失 → 自动解除（蜂鸣器停）；
   - **ResetAlarm()（复位按钮）**：清除锁存 + 蜂鸣器 OFF；若信号仍 ON → 进入"已复位待消除"（`_resetPending` 防立即重新锁存），不弹窗不响铃，**状态栏持续提示**；
3. **状态栏兜底提示**：主窗口机器状态栏新增红色 `txtAlarm`（DispatcherTimer 500ms 刷新 `DeviceMonitor.AlarmText`），显示"⚠ 轴报警: X" / "⚠ 轴报警已复位(信号未消失): X"，不依赖业务状态栏（不被"移动中..."覆盖）；
4. 复位按钮接入：`MainViewModel.ResetMachine`（主界面）与 `AxisIOControlWindow.BtnReset_Click`（IO 界面）均调用 `ResetAlarm()`。

### 总结

报警从"信号消失自动恢复"升级为"锁存 + 复位确认"：弹窗仅提示、蜂鸣器跟随锁存、复位按钮为唯一软件清除手段、状态栏兜底提示未消失的信号——操作员处理流程闭环。监控层与信号层同线程顺序执行，无并发问题，后续扩展检查函数只需挂入 OnCycle。

### 验证

- 编译通过（AnyCPU；x64 需程序关闭后复制）。
- 上机验证：报警 → 模态弹窗 + 蜂鸣器响；点掉弹窗信号仍在 → 蜂鸣器继续响；点复位（主界面/IO 界面）→ 蜂鸣器停 + 状态栏红色提示；信号消失 → 状态栏清除；再次报警 → 重新弹窗。

---

## 补充更新（同日）：合并 IoSignalMonitor 至 DeviceMonitor（单一设备状态入口）

### 背景

IoSignalMonitor（喂入去抖）与 DeviceMonitor（业务检查）订阅同一个 IOStateUpdated 事件，事件 handler 本来就是顺序执行，分开两个 Monitor 只是心理分层、无执行隔离——冗余。

### 解决方案

1. **删除 `Core/Utility/IoSignalMonitor.cs`**（及 csproj 注册），`OnOffSignal` 工具类与全部 IO 位去抖字段（Input/Output/Alarm 共 33 个，命名与 IOState 属性一致）移入 `Core/Device/DeviceMonitor.cs`；
2. **DeviceMonitor 成为设备信号唯一入口**：`OnCycle` 内按序执行 ① `FeedSignals`（喂入去抖，仿 sensorUpdate）→ ② `CheckAxisAlarm`（业务检查），单线程顺序执行；
3. 流程层查询改为静态访问：`DeviceMonitor.InputWork.IsOnFor(...)`（原 `IoSignalMonitor.InputWork`）。

### 总结

信号数据维护与业务监控统一宿主，少一个类/少一层；"报警业务不混入信号层"的边界仍然清晰——`OnOffSignal`（去抖计时）是工具类，喂入（FeedSignals）是数据维护，报警动作（弹窗/蜂鸣器/标志）在 `CheckAxisAlarm`。

### 验证

- 编译通过（AnyCPU + x64）。

---

## 补充更新（同日）：多报警源 + 急停/气压 + 设备状态机与红绿黄灯

### 背景

报警需求升级：多报警并存（如 X 轴、急停、气压不足同时发生）；弹窗各自独立（无法预知何时报警，不合并），状态栏用 `>` 追加显示；急停信号 ON 0.2s 触发报警；设备报警状态禁止 6 类动作（全图采集/采集FOV/检测/运送基板/送入口出口/调整轨道/手动XYZH）；按设备状态控制红绿黄灯。

### 解决方案

1. **多报警源**（DeviceMonitor.CheckAlarms）：`AlarmItem` 列表注册 6 源（X/Y/Z/H 轴 + 急停 InputEmergency + 气压 InputAirPressure），每源信号 ON 持续 0.2s 独立触发：**各自模态弹窗一次**（不合并）、状态栏按注册顺序 `>` 追加显示（如"X轴驱动器报警>急停已按下>气压不足"）、蜂鸣器任一激活即响（仅状态变化时下发指令，不刷串口）；`AxisAlarm` = 任一激活；
2. **设备状态机**（仿插针机 MachineStatus，六态）：`DeviceState { NotReady, Ready, Manual, Running, Pause, Alarm }`，`UpdateMachineState` 按优先级转移（Alarm > NotReady(未回零) > Pause(检测暂停) > Running(流程中) > Ready(待机位)/Manual(不在待机位)，待机位用 Xmm/Ymm/Zmm 与 StandbyX/Y/Z 对比 ±10mm）；流程状态来源由 MainViewModel 注册委托（isWorkFlowRun / _pauseCheckEvent）；
3. **红绿黄灯**（仿插针机）：Alarm=红灯、Running/Pause=绿灯、Ready/Manual/NotReady=黄灯，仅状态变化时下发；
4. **报警禁止动作**：`AlarmBlocked(action)` 入口检查，覆盖 全图采集/采集FOV/检测/运送基板/送到入口/送到出口/调整轨道 + AxisIOControlWindow 手动 XYZH 12 个点动按钮（ManualMoveBlocked 拦截按下）；
5. **状态栏 UI**：底部状态栏"● 就绪"/"全图采集："静态文字删除，报警显示为**深红底白字大字**（20px，DispatcherTimer 刷新，无报警透明），橙背景上对比清晰。

### 总结

报警从单源升级为多源独立触发 + 状态栏合并显示；设备状态机统一了"报警禁止动作"与"红绿黄灯"的控制依据；手动操作（点动按钮）在报警时被拦截，保护设备。

### 验证

- 编译通过（AnyCPU；x64 需程序关闭后复制）。
- 上机验证：同时触发 X 轴报警+急停 → 两个弹窗依次弹出（各自独立）+ 状态栏"X轴驱动器报警>急停已按下" + 红灯亮；复位 → 蜂鸣器停、状态栏仍提示；信号消失 → 状态栏清、黄灯亮（回待机位后）。

---

## 补充更新（同日）：状态栏三段式显示 + 动态变色 + 分隔线改造

### 背景

状态栏原为文本符号 `|` 分隔,字体渲染导致对不齐;信息区报警时覆盖运行信息,无法同时查看;状态文字颜色单一。

### 解决方案

1. **三段式显示**:`状态 | 运行信息 | 报警信息` 独立三列(StateText / StatusText / AlarmText),报警不再覆盖运行信息,无报警时报警段留空;
2. **动态变色**(500ms 定时刷新,静态画笔复用不新建):报警→红 #FF0000,未就绪→橙 #EF6C00,其余(待机/运行/暂停/手动)→绿 #558B2F;报警段固定红;
3. **分隔线改造**:文本 `|` → 1px Border 垂直分隔线,替换 StatusBar/StatusBarItem(Border 容器消除其默认模板 VerticalContentAlignment=Center 导致的分隔线只占 22px 问题),通高 28px;两侧间距由文字 Margin 控制,严格对称 10px。

### 验证

- 编译通过(x64,程序关闭后复制 exe)。

---

## 补充更新（同日）：固定按钮区 + 功能/运行菜单精简

### 背景

右侧原"机器状态栏"信息文字与底部状态栏重复；检测类按钮分散在功能/运行两个菜单中，操作入口不集中；伺服归零/H轴回零命名与功能不符。

### 解决方案

1. **右侧固定按钮区**：原机器状态栏改为 4 个固定快捷按钮——开始(检测，暂停中点击则继续)/停止(暂停检测)/复位(原运行菜单复位)/归零(全轴归零含H轴)，风格仿 IO 面板快捷按钮但更大(46px、15号字、圆角6)；
2. **功能栏**：删除自动检测/手动检测/停止；"伺服归零"改名"相机归零"(HomeXYZ)；新增"轨道归零"(Home("H")，仅H轴回零，成功只清H轴回零标志)；
3. **运行栏**：删除停止检测/连续运行/单步运行/复位，保留开始检测/暂停检测/演示模式/精度检测/精度报告；
4. **新增 ViewModel 方法**：`StartOrResumeCheck`(检测暂停中→继续，否则启动)、`HomeAllAxes`(HomeXYZH 全轴归零)、`OrbitHome`(H轴归零)；DeviceMonitor 新增 `ClearNeedHomeH`(按轴清标志，不影响XYZ)；
5. **运行信息显示**：原右侧状态文字删除，`OnStatusChanged` 改到底部状态栏运行信息段显示。

### 验证

- 编译通过(x64，程序关闭后复制 exe)。

---

## 补充更新（同日）：工具栏图标统一 + 菜单微调 + 固定按钮文字化

### 背景

Halcon 上方工具栏图标为 emoji/文本字符混排，刷新"↻"字符过小、emoji 有边框，板面/查看按钮语义不清；保存/刷新图标挤在标签栏右侧；固定按钮区 emoji 图标与楷体文字需求冲突。

### 解决方案

1. **工具栏图标统一为 Segoe MDL2 Assets 线条风格**（无边框、字号可控）：刷新 Refresh(E72C)、删除 Delete(E74D)、清空 Clear(E711)；查看栏板面按钮 = 放大镜(Search E721)+ PCB 板 Path(板框/走线/引脚手绘)，针型查看按钮 = 放大镜+粗体字母；修复放大镜字符码点错写(E921→E721，横线问题根因)；
2. **菜单调整**：单元测试1/2/3 从设置栏移至帮助栏；刷新方案按钮从标签栏右侧移至标记工具栏(删除按钮左侧)；删除标签栏右侧保存按钮(文件栏已有保存方案)；
3. **固定按钮文字化**：右侧固定按钮去掉 emoji，纯文字(开始/停止/复位/归零)，楷体 FontSize30 约占按钮65%，保留圆角6。

### 验证

- 编译通过(x64，程序关闭后复制 exe)。
