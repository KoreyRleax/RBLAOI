# 更新日志 2026-08-24：Origin 图片数量容错校验（多余自动清理，仅不足弹窗）

> 需求：允许 Origin 图片数 > 计算数（不再弹窗）；仅当 实际 < 预期 时弹窗"图片数量不一致"；
> 勾选检测位 / 重载方案时若发现 实际 > 预期，自动删除**文件创建时间最新**的多余图片（如 154 张→删最新 4 张恢复 150 张），保证方案正常加载并显示底图。

## 背景

- 原 `ValidateImageCount` 为严格相等校验（`actual == expected`），任意不等即弹窗并中止加载；
- 现场存在因文件操作失败（如 GRAB 残留/复制中断）导致 Origin 目录图片数多于预期的偶发场景，此时方案被"卡死"无法加载底图，只能手动清目录或重新采集全图；
- 多余图片为文件操作失败残留的"最新"图片，删除后拼图顺序不受影响，属安全可自动修复的脏数据。

## 改动（3 个文件位置）

### 1. `ViewModels/MainViewModel.Project.cs` — `ValidateImageCount` 容错化（L278）
- 返回签名：`(bool isValid, int expected, int actual, int removed)`
- 图片数 > 预期：**按文件创建时间（`File.GetCreationTime`）降序取最新的** `actual - expected` 张删除（创建时间相同时按文件名降序保证确定性），删除前 `File.SetAttributes(FileAttributes.Normal)` 防只读，单个失败记 `Log.Warning` 不阻断，成功后记 `Log.Warning("Origin图片数量容错: 预期 X 张，实际 Y 张，已按创建时间自动删除最新多余 Z 张")`；返回 `isValid=true`
- 图片数 < 预期：返回 `isValid=false`（调用方弹窗）
- 相等：`isValid=true`
- 注：删除按**文件创建时间**判定"最新"（2026-08-24 用户修正，原实现按文件名排序取末尾）；删除后数量恢复 expected，加载仍按文件名排序，拼图顺序不受影响

### 2. `ViewModels/MainViewModel.Project.cs` — `LoadProjectImagesAndDisplayAsync`（L162）
- 仅当 `isValid=false`（图片不足）时弹窗，文案统一为：
  `图片数量不一致！预期 {expected} 张，实际仅 {actual} 张。请重新采集全图。`（标题"图片数量不一致"）
- 多余图片在 `ValidateImageCount` 内已清理，此处直接放行正常加载
- 覆盖入口：启动加载 / 重载方案 / 菜单切换方案（共用此方法）

### 3. `ViewModels/MainViewModel.cs` — `EnsureSelectionModeReady`（L862）
- 同上：仅不足时弹窗；多余自动清理后放行（勾选检测位→进入选择模式入口）

## 边界处理

- 删除前清只读属性，失败仅记日志、不抛异常、不阻断加载；
- 删除按**文件创建时间**降序取最新（`File.GetCreationTime`），创建时间相同按文件名降序兜底；读取创建时间失败按 `DateTime.MinValue` 排最前、不会被优先删；
- 不动 `ResolveImageDir()`（优先 Origin 子目录、兼容旧 Images/ 根目录）的目录选择逻辑。

## 验证

1. 复制 4 张多余图到 Origin → 重载方案 → 日志出现"已按创建时间自动删除最新多余 4 张"、无弹窗、底图正常显示 150 格
2. 删至 145 张 → 重载方案 → 弹窗"图片数量不一致！预期 150 张，实际仅 145 张。请重新采集全图。"
3. 数量恰为 150 → 无弹窗、正常加载
4. 勾选检测位（进入选择模式）场景重复 1~3 验证
5. 验证删除对象为"创建时间最新"的图片：查看 Origin 目录确认被删文件为最近写入/复制的文件

## 总结

图片数量校验从"严格相等"改为"允许上溢、自动清理、仅下溢弹窗"：多余的脏图（文件操作失败残留）由系统在加载前按**文件创建时间**自动清理最新几张，方案不再因多余文件被卡住；真实缺图仍弹窗提示重新采集全图。
