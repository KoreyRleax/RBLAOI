# 更新日志：IO 位表对接 F401 四轴电机板（布局4：每轴8位）

- **时间**：2026-08-04
- **标题**：IOState 位 32-63 按 F401 四轴板布局重写 + IO 面板 H 轴指示灯补全

## 背景

现场设备由旧双板（F103 XYZ 三轴板 + F103 H 单轴板，3+1 布局）更换为**单块 F401 XYZH 四轴电机板**。新板电机 IO 为固定 32 位布局：X/Y/Z/H 各占一个字节，字节内按统一顺序排 +限位、-限位、原点、报警、驱动器到位(INP)、报警清除输出激活、预留×2。旧上位机按"3+1 双板"硬编码的位表与新布局完全错位（仅 X 轴限位/原点碰巧一致），必须按下位机协议文档（MOTOR_IO_LAYOUT_AXIS_BYTE_V1 / io_layout=4）重写。

位表（综合位 = 32 + 轴序号×8 + 信号偏移；轴序号 X=0,Y=1,Z=2,H=3；信号偏移 0=+限位,1=-限位,2=原点,3=报警,4=到位,5=报警清除）：

| 轴 | +限位 | -限位 | 原点 | 报警 | 到位(INP) | 报警清除 |
|----|------|------|------|------|-----------|----------|
| X  | 32   | 33   | 34   | 35   | 36        | 37       |
| Y  | 40   | 41   | 42   | 43   | 44        | 45       |
| Z  | 48   | 49   | 50   | 51   | 52        | 53       |
| H  | 56   | 57   | 58   | 59   | 60        | 61       |

## 解决方案

### 1. IOState.cs — 位表重写（布局分发）

- 位 0-31（主板本地 IO：传感器/执行器）不变，提取为 `ParseInputOutputBits` 两种布局共用；
- 位 32-63 新增 `ParseMotorBitsAxisByte`（布局4：每轴 8 位）；旧 F103 双板位表保留为 `ParseMotorBitsLegacy`（回归用）；
- 新增静态 `IoLayout`（默认 4），`ParseFromHex64` 按布局分发；未知布局不猜测位序（文档要求）；
- 新增 H 轴属性：`LimitHPos`(56)、`LimitHNeg`(57)、`OrgH`(58)、`AlarmH`(59)、`AxisHDone`(60)、`AlarmClearH`(61)；
- 属性语义校正：`EmergencyStop` 属性删除（F401 电机板无急停位，急停由主板位 8 `InputEmergency` 承载，`IsEmergencyStop` 改用仅该位）；`HasAlarm` 含四轴；`AllAxesDone` 明确为四轴 INP 到位，与软件运动完成事件（X_COMPLETE 等）不同义；
- `ToHexString64` 打包逻辑同步重写（与解析对称）。

### 2. AxisIOControlWindow — H 轴指示灯补全

- **H 轴控制面板**：原单个"限位"灯拆为"**H+ 正限位** / **H- 负限位**"两灯，与 X/Y/Z 轴一致（`rectLimitHPos`/`rectLimitHNeg`）；
- **输入信号面板**：Grid 扩为 4 列，报警行新增 **H 轴报警**（`rectAlarmH`），到位行新增 **H 轴到位**（`rectAxisHDone`）；
- 指示灯绑定（`UpdateAllIO`/`UpdateIfChanged`）：H 轴原点、正/负限位、报警、到位全部接入。

## 总结

- IO 面板 X/Y/Z/H 四轴的限位（±）、原点、报警、到位指示灯全部按新位表正确显示；
- 位 1 恒为固件归一化后的"有效"，上位机不做极性反转；未接线信号占位返回 0 不重排；预留位忽略；
- 旧 F103 双板解析保留（`IoLayout=1/2` 时启用）用于回归；接入 GET_CONFIG 后按 `io_layout` 设置即可；
- 固件与上位机必须同批联调发布（旧版上位机无法解析布局 4）；
- 编译通过。

## 修订（同日）：输入信号面板布局优化 + 删除查询按钮

**背景**：输入信号面板 17 项信号（9 传感器 + 4 报警 + 4 到位）混在同一 Grid 中过于密集；位置已实时显示，"📍 查询"按钮冗余。

**改动**：
- **输入信号面板拆为两个分组**："📥 输入信号"（9 项传感器，3×3）与"🛠 轴状态（电机板）"（四轴报警 + 到位，4×2），组间加小标题分隔，视觉上区分"主板传感器"与"电机板状态"两类信号；
- **指示灯行距加大**：`IndicatorStyle` Margin 0,4 → 0,6（全局，输出信号面板同步受益）；
- **删除查询按钮**：快捷操作区移除 "📍 查询"（`btnQueryPos`/`BtnGetPos_Click`），UniformGrid 5 列 → 4 列；`GetPosAndDisplay` 保留（JOG 按钮松手后仍刷新显示）；
- **删除 G 代码指令输入框**：移除"G代码发送"面板（`txtGCode`/`btnSendGCode`/`BtnSendGCode_Click`）——该功能是直发任意文本的调试口，绕过封装 API、无等待无校验、生产环境误触有撞机风险；底层 `SendCommandAsync` 保留供程序内部使用，调试需求可用外部串口工具；
- **Z/H 轴运动指示灯**：左侧轴控制面板补齐 `rectTestMovingZ`/`rectTestMovingH`（与 X/Y 一致，忙=红/闲=绿），四轴运动状态一目了然；
- **到位灯颜色统一**：`UpdateAllIO` 与 `UpdateIfChanged` 到位灯语义不一致（绿 vs 红）的 bug 修复，四轴到位灯统一为"触发=红/未触发=绿"，与限位/报警一致；
- 编译通过。
