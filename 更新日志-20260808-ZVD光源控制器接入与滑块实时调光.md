# 更新日志

- **时间**：2026-08-08
- **标题**：ZVD 光源控制器接入 —— 光源调试界面滑块实时调光

## 背景

方案界面中已有光源调试对话框（`LightControlWindow`，4 通道滑块 0-100%），但此前仅有界面与参数保存，串口通信待协议确定。现 ZVD-24XXXD-XT 光源控制器协议（RS232 / 19200-8N1 / ASCII 文本协议，多通道帧 `S+每通道[3位亮度+T/F]+C#`）已实测确认，需要接入串口，实现**拖动滑块时实时改变光源亮度**。

## 解决方案

1. **新建 `Core/LightSource/LightSourceController.cs`**（核心服务，独立于 MotionIO）：
   - 单例串口服务：Open/Close/SendCommand（单字符回复即完整，超时返回空串）；
   - 协议构建：`BuildMultiChannel`（完整帧，含亮灭 T/F）、`BuildSingleChannelBrightness`（4 位补零纯亮度）、`BuildSingleChannelState`；
   - 补充 `SetAll(LightChannelState[])` 重载，便于直接传状态数组；
   - 在 `RBLAOI.csproj` 注册编译项。

2. **改造 `Views/LightControlWindow.xaml.cs`**：
   - 窗口打开即按方案串口号连接（`SetChannelCount(4)` + `Open(port)`），失败弹提示但参数仍可编辑保存；
   - 滑块/输入框（`UpdateSourceTrigger=PropertyChanged`）变化 → 仅更新"待发送快照"，由后台线程每 10ms 轮询发送**最新完整帧**：UI 不阻塞、拖动中间帧自动丢弃、串口指令由 lock 串行化无乱序；
   - 0-100% 换算 0-255；亮度 0 自动发 `F`（灭），>0 发 `T`（亮）—— 符合"亮灭必须走多通道帧"实测结论；
   - 发送无回复/异常记日志并降频重试；对话框关闭时释放串口（`_light.Close()`），其他模块可再次打开。

## 总结

光源调试界面已从"纯参数编辑"升级为"所见即所得"实时调光：打开窗口即点亮当前方案亮度，拖动滑块实时下发 ZVD 完整帧。串口与 330 运动板完全隔离（独立 SerialPort），未触碰 `MotionIO.cs`。检测流程联动（抓图前 `SetAll`、结束 `AllOff`）按文档 5.5 留待后续按业务需要接入。

## 验证

- 编译通过（Debug，无新增警告）。
- 硬件验证待上机：打开方案 → 光源调试 → 拖动滑块观察灯珠亮度实时变化；串口无回复时检查交叉线/COM 号/波特率。

---

## 补充更新（同日）：串口号不固定 —— 加载方案时检查连接并弹窗

### 背景

串口号不固定（插拔/换机后 COM 号会变），原实现只在点击"光源控制"按钮打开调试窗口时才检查连接，用户重新打开程序后灯不亮且无从得知原因。

### 解决方案

1. **MainViewModel 新增 `CheckLightSourceConnected()`**：加载方案后检查光源串口 —— 已连接且端口与方案一致则复用；否则尝试 `Open(方案端口)`，失败弹窗提示（提示用户可在 方案→光源调试 中重新选择串口）。调用点覆盖两个加载入口：`AutoLoadLastProject`（启动自动加载上次方案）与 `OpenProject`（手动打开方案）。
2. **串口生命周期归 MainViewModel 管理**：加载方案时建立连接，`Cleanup()`（退出）时 `Close()`；`LightControlWindow` 不再自行关闭串口（启动已连接时直接复用）。
3. **调试窗口内切换串口号立即重连**：combo 选择变化时关闭旧连接、按新端口重连并立即按当前通道值重新下发，改完端口马上生效。

### 总结

光源串口检查从"点击按钮才触发"前移到"打开程序/方案即检查、失败即弹窗"，串口号更换后（改方案端口或窗口内切端口）均立即生效，无需重启程序。

---

## 补充更新（同日）：亮度单位改为 0-255 + 每通道亮灭开关（复选框）

### 背景

光源控制器硬件亮度本就是 0-255，界面此前用 0-100% 百分比需要换算，且无法控制亮灭（T/F 位）。

### 解决方案

1. **亮度数值体系改为 0-255**：滑块 0-255、输入框直接输入硬件值，不再换算；
2. **每通道新增亮灭复选框**：勾选=亮（T）、取消=灭（F），勾选状态随滑块/输入实时下发多通道完整帧（符合"亮灭必须走多通道帧"的实测结论）；
3. **方案参数新增 `LightOn1~4`**（bool，默认 true），`LightChannel1~4` 语义改为 0-255（默认 128），全链路透传：ProjectData → EditProjectWindow → LightControlWindow → MainViewModel 回写。

### 注意

- **旧方案兼容**：旧方案 XML 中 `LightChannel` 存的是百分比（0-100），与 0-255 数值空间重叠、无法自动区分，**旧方案的光源亮度需打开 光源调试 重新设置一次**；
- 新方案/缺失字段时默认 128 亮。

### 总结

光源调试界面数值与硬件完全一致（所见即所得），亮灭开关补齐了 T/F 控制能力，滑块/开关/输入框三者联动实时下发。

---

## 补充更新（同日）：修复光源串口号被覆盖成 COM1 的 bug

### 背景

重新打开程序后光源串口号永远是 COM1，方案保存的端口丢失。

### 原因

`LightControlWindow` 构造函数中，方案端口不在当前串口列表（串口未插/换机器）时执行 `SelectedIndex = 0` 强制选中第一项——ComboBox 的 `SelectedItem` 为 TwoWay 绑定，此操作把 `LightPortValue` 覆盖为列表第一项（通常 COM1），点确定后方案被**永久改写成 COM1** 并保存。

### 解决方案

1. **构造时不再自动改选第一个串口**：方案端口未检测到时也加入列表并选中（值保持方案原端口），用户可看到当前配置的端口并手动重选；
2. **切换串口重连失败时回滚选择**：弹窗提示后回滚到之前连接成功的端口，避免误保存无效串口号。

### 注意

已被旧版本覆盖为 COM1 的方案文件无法自动恢复，需在 光源调试 中重新选择正确串口并保存一次。

---

## 补充更新（同日）：光源串口号迁移至系统参数，检查时机改为程序启动

### 背景

光源串口号对所有方案都是一样的（同一台设备只有一套光源），不应放在方案里。原实现在加载方案时检查连接，逻辑上不合适，且换方案时反复重连。

### 解决方案

1. **串口号迁移到系统参数**：`SysParamData.LightSourceSerialPort`（默认 COM1），`SysParam.LightSourceSerialPort` 便捷属性赋值即保存；所有方案共用，不再随方案走；
2. **检查时机与下位机/网口一致**：程序启动时 `ConnectMotion()`（下位机串口）后立即 `ConnectLightSource()`，与设备连接检查同时进行，不再依赖方案加载；
3. **移除方案级端口链路**：EditProjectWindow/LightControlWindow 不再传递端口；光源调试窗口内切换串口号成功即写回系统参数（自动保存）；
4. `ProjectData.LightPort` 保留但标记 `[Obsolete]`（仅兼容旧方案 XML 反序列化，不再使用）。

### 总结

光源串口号成为系统级配置（与下位机串口号同级），启动时统一检查连接、失败弹窗，换方案不再重连；旧方案文件因保留字段可正常加载。

---

## 补充更新（同日）：方案级光源通道数（解决 2 通道控制器收到 4 段帧无法解析）

### 背景

ZVD 控制器按自身通道数严格解析指令，多通道帧的段数必须与实际通道数一致：给 2 通道控制器发 4 段帧（原实现固定 `SetChannelCount(4)`）指令无法解析、无回复。

### 解决方案

1. **方案新增 `LightChannelCount` 参数**（2/4，默认 4）：光源调试窗口"全局设置"新增 通道数 下拉框；
2. **发送帧段数 = 实际通道数**：切换通道数 → `SetChannelCount(N)` + 按 N 构建快照帧（快照长度 = 通道数，多余通道行隐藏）；
3. 通道数随方案保存/加载（方案级参数，与亮度开关同级）。

### 验证

2 通道控制器上选择"通道数=2"后，发送帧为 `SxxxTxxxC#`（2 段），可正常解析调光；需保持 4 通道时选 4。

---

## 补充更新（同日）：光源串口配置移入 系统参数→连接管理，光源调试界面只留通道调光

### 背景

串口号/波特率/通道数均为系统级配置（所有方案共用），原分布在光源调试窗口内，入口隐蔽且与方案参数混存（通道数曾临时放方案级，导致两处来源冲突）。

### 解决方案

1. **系统参数新增**：`LightSourceBaudRate`（默认19200）、`LightSourceChannelCount`（默认4，与 `LightSourceSerialPort` 同区）；
2. **系统参数界面"连接管理"新增"光源控制"区**（风格与下位机一致）：串口号、波特率、通道数 三个输入框 + 连接/断开按钮 + 状态文本（已连接/未连接），`[SysParam]` 反射自动同步与保存；
3. **`LightSourceController.Open` 支持波特率参数**（默认19200）；
4. **光源调试界面移除串口号/通道数下拉**，只保留 4 通道亮度+亮灭开关；通道行显示与发送帧段数一律跟随系统参数通道数；
5. **方案级 `LightChannelCount` 废弃**（`[Obsolete]` 保留兼容旧方案XML），通道数唯一来源为系统参数；
6. `MainViewModel.ConnectLightSource` 启动时按系统参数（端口+波特率+通道数）连接。

### 总结

光源连接配置与下位机/视觉连接完全同构：技术人员在 系统参数→连接管理 一次配好（串口号/波特率/通道数），调试窗口专注调光；帧段数永远匹配控制器硬件，无两处来源冲突。

---

## 补充更新（同日）：设备连接弹窗统一 —— 启动后聚合一次提示

### 背景

光源/下位机在 MainViewModel 构造函数中连接失败即弹模态框（主窗口未显示，时机差）；2D/3D 由 MainWindow.Loaded 异步连接，失败曾走 MessageBox（模态）或状态栏（非模态），三种连接的弹窗时机与形态不一致。

### 解决方案

统一为：**启动只尝试连接不弹窗，窗口就绪后把所有未连接设备聚合为一次模态提示**。

1. `ConnectMotion` / `ConnectLightSource` / `ConnectVision` 内部**不再弹窗**，仅尝试连接 + 记日志；
2. 新增 `CheckDeviceConnectionsAsync`（挂在 `StartPostInitTasks` 开头，主窗口 Loaded 后执行）：等待 2D/3D 连接完成 → 检查 下位机/光源/2D相机/3D相机 四项 → 未连接项**聚合为一条消息弹窗一次**（含指引"系统参数→连接管理"）；
3. `ConnectVision` 改为返回共享 Task（`_visionConnectTask`）：MainWindow.Loaded 发起连接、聚合检查 await 同一 Task，避免重复 ConnectAsync；
4. 系统参数界面手动连接按钮的即时反馈弹窗保留（用户主动操作需要明确结果）。

### 总结

启动时的设备连接提示从"最多 3 个弹窗、时机各异"统一为"1 个聚合弹窗、窗口就绪后弹出"；运行中状态变化仍走状态栏（非模态），手动连接仍即时反馈。

---

## 补充更新（同日）：光源通道数移至"其他"分组

### 背景

光源区原为 4 列（串口号/波特率/通道数/按钮），TextBox 固定宽 110px 导致 3 输入列溢出、文字覆盖边框；改为两行布局后用户认为通道数属硬件配置而非连接参数，不宜放连接管理区。

### 解决方案

1. **光源控制区回归与下位机完全一致的 3 列一行**：串口号 | 波特率 | 连接按钮；
2. **通道数移至 "⚙️ 其他" 分组**（与 IO 状态轮询间隔并列，UniformGrid 两列），标签"光源通道数 (2/4)"。

### 总结

连接管理区只保留连接操作与连接参数（与下位机/视觉区同构）；通道数作为硬件配置归类到其他参数区。

---

## 补充更新（同日）：训练拍照功能（临时）—— 光源界面曝光滑块 + 独立目录拍照

### 背景

视觉训练需要采集不同光源×曝光组合下的图片。方案比较后采用"光源界面加曝光滑块"方式：视觉训练是高频组合调参场景，滑块实时下发+同窗拍照的工作流效率最高；方案 `CameraExposureTime` 链路现成（方案级参数，EditProjectWindow 已持有）。

### 解决方案

1. **光源控制界面新增"训练拍照"区**：
   - **曝光滑块** 0-30000μs（拖动实时下发 `SET_EXPOSURE` 到 VM，300ms 防抖只发最新值；与光源滑块同窗联动）；
   - **存储路径** 行：只读显示 + "浏览..."按钮（FolderBrowserDialog 选择）；
   - **拍照按钮**：按当前光源值+曝光拍照。
2. **拍照流程**（不影响瓦片）：`SET_EXPOSURE` → `SET_PATH:训练目录` → `GRAB` → 等VM存图 → 文件改名为 **`曝光-通道1值-通道2值.bmp`**（按系统参数通道数拼接）→ finally 恢复 `SET_PATH` 为方案 Origin 目录；
3. 曝光值经 EditProjectWindow 与方案 `CameraExposureTime` 同步（确定后回写保存），拍照不写入 _imageManager、不触发拼接。

### 总结

训练采集工作流闭环：光源界面一处完成"调光源→调曝光→选路径→拍照"，文件名自带参数便于训练集管理；拍照后 VM 路径自动恢复，不影响正常瓦片采集。

---

## 补充更新（同日）：协议修正 —— 按实测串口日志改用单通道指令

### 背景

实测光源串口日志（调试上位机）：

```
[TX] SA0128#  →  [RX] A   （通道1亮度128）
[TX] SB0128#  →  [RX] B   （通道2亮度128）
[TX] SA0100#  →  [RX] A
[TX] SA0000#  →  [RX] A   （0亮度有效，即可关灯）
[TX] SB0000#  →  [RX] B
```

与说明书示例（多通道完整帧 `S...T...TC#`）不同，实际控制器使用**单通道指令**：`S + 通道字母(A-H) + 4位亮度 + #`，回复对应通道字母；**0 亮度即灭，无需 T/F 位**（推翻此前"关灯必须走多通道帧"的推断）。

### 解决方案

`LightSourceController.SetChannel` 改为发送单通道指令（`BuildSingleChannelBrightness`，4 位补零），亮灭由亮度值控制（灭 = 0）；`SetAll`/`AllOff` 改为逐通道下发。滑块/复选框/拍照链路调用方式不变（仍走 SetAll → 逐通道）。

### 结论

- **通道数设置不再需要**（协议上）：单通道指令与通道数无关（SA~SH 独立生效）；
- 通道数参数**保留**但作用降级为纯界面用途（控制滑块行显示、训练文件名拼接段数），不影响协议。

### 验证

上机：拖动滑块 → 日志应为 `SA0100#`/`SB0128#` 等单通道指令 + 对应字母回复；取消勾选 → `Sx0000#` 灯灭。

---

## 补充更新（同日）：训练拍照路径修复与持久化 + 通道整数显示 + 方案编辑"移动到"按钮

### 背景与问题

1. 训练拍照实测：选择存储路径后，图片仍保存到方案文件夹 `Images/Pin` 下——`SET_PATH` 指令在 VM 端不生效（存图路径由 VM 方案内配置决定）；
2. 训练存储路径每次打开窗口都要重新选；
3. 通道数值框可输入小数；
4. 方案编辑界面针尖列表无快捷调焦入口。

### 解决方案

1. **拍照文件定位兜底**：`GRAB` 后不再假定文件在训练目录，`LocateVmImage` 按候选目录查找（训练目录 → 方案 Images/Pin 及子目录（实测位置）→ Images/Origin → D:\GrabImage），找到后 Move 到训练目录并改名，日志记录实际来源目录；
2. **训练路径持久化**：新增系统参数 `LightTrainDir`，浏览选择后立即写回（自动保存），窗口打开自动带出；
3. **通道数值整数化**：`Channel1~4` setter 强制 `Math.Round`，滑块/输入框只保留整数亮度（0-255）；
4. **方案编辑"操作"列**：DataGrid 新增"操作"表头，每行 📍 按钮 → Z 轴 `MoveToAndWaitOK(z: 该针尖焦距)`，成功/失败提示。

### 总结

训练拍照路径不再依赖 `SET_PATH` 生效与否，从实际存图位置取文件；路径选一次长期有效；界面数值与焦距调整操作更顺手。

---

## 补充更新（同日）：方案界面与光源控制窗口布局优化

### 背景

修改方案界面基础设定为 3 列、板面焦距独立 box、检测参数表格文字不居中且字号偏小、窗口高度不足出现滚动条；光源控制窗口为模态、内容需滚动。

### 解决方案

1. **修改方案界面（EditProjectWindow）**：
   - 基础设定改 **5 列**一行（板宽/板高/重叠率/插件特征/曝光）；
   - **光源控制按钮**移到 基础设定 标题行右侧（不再在板面焦距 box 内）；
   - **板面焦距**输入框移至 检测参数 标题行（与"检测参数"水平对齐，位于表格上方），删除独立 box；
   - 检测参数表格：所有列应用居中文本样式（`ElementStyle` 引用全局 `DataGridTextColumnStyle`），**字号 18→21（+3px）**，操作按钮居中；
   - 窗口高度 600→720，默认不再出现滚动条。
2. **光源控制窗口（LightControlWindow）**：
   - **改为非模态**（`Show()` 打开）：通道亮度/开关/曝光变化**实时同步回方案编辑窗口**（构造传入 EditProjectWindow 引用，属性 setter 直接回写），关闭后点"确定"统一保存；
   - 曝光输入框 **只显示整数**（`Math.Round`）；
   - 窗口 430×420 → **620×520 可放大**（ResizeMode=CanResize），内容全部可见，**去掉滚动条**。

### 总结

方案编辑与光源调试两个界面高度内敛：无滚动条、参数集中（基础设定 5 列）、表格居中大字；光源控制非模态化后可与方案编辑窗口并排操作，参数实时联动。
