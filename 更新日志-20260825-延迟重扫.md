# 更新日志 2026-08-25：3D 延迟重扫（板末统一重扫不合格位）

> 分支：`fix/multi-pintype-blob-parser`。改动文件：`ViewModels/MainViewModel.DetectionFlow.cs`（单文件，未编译，待 `_build.bat`）。

## 背景：即时重扫的两个连锁 bug（实机实证 log_20260825_174937）

1. **3D 数据串位 → 后续位静默全 NG**：即时重扫跳转时，`StartScan3D` 异步发出的"下一检测位 SCAN3D"仍在物理执行（`_3dScanTask=null` 只丢句柄）→ 重扫 SCAN3D 与在途 SCAN3D 双发交错 → VM 3D 数据推送串位/丢失。本轮位 31-34 连续 4 位合并 OK=0 静默落表（位32 特征：3D 18 点 > 2D 6 针；位31：3D 9 点 < 2D 24 针），且不满足原重扫条件（`mergeValidH3D==0`）故不重扫。
2. **丢弃在途 2D → 下一检测位 0 针 → 叠加层残留**：`_vmDetectTask=null` 丢弃的是下一检测位的 2D（MergePending 后置后）→ 位 27 第一次 2D 0 针（日志实证）→ Wait2D 跳过叠加层刷新 → 画面"不是当前的"。

## 方案：延迟重扫（主流程不重扫，板末统一处理）

- **主流程**：3D 数据异常（`mergeValidH3D==0` / `scan3DNoData`）不再即时重扫跳转，只 `_rescanQueue.Add(i)` 记录，正常降级落表（H=0/NG）。
- **板末**（End 状态）：队列非空 → 进入重扫模式（`_rescanPhase=true`），逐个重扫不合格位：**完整重拍 A 段**（复用 GrabTile 全流程，图路径/pinPaths 零重建风险）→ Start2D → MoveScanStart → StartScan3D → Wait2D → WaitScan3D（**同步等扫描+数据**）→ Merge。
- **严格串行**：重扫模式 Wait2D 后**跳过 MergePending**（不挂起/不异步），WaitScan3D 同步取数直接 Merge——重扫位之间、与主流程之间绝无在途 SCAN3D 交错。
- **重试**：重扫位 Merge 仍不合格 → `_rescanRetries++` < 3 → 回 GrabTile 重拍重来；≥3 → 接受降级结果（语义同原即时重扫上限）。
- **结果替换**：重扫成功 → Dispatcher 内先删该位旧 PinResults（GridIndex）再插新；叠加层由 GrabTile 重拍（RemoveAll+重建）+ Merge 更新 H 自然刷新。

## 改动点（DetectionFlow.cs 单文件 9 处）

| # | 位置 | 改动 |
|---|---|---|
| 1 | 字段区 | +`_rescanQueue`/`_rescanPhase`/`_rescanRetries` |
| 2 | Init | 每板重置队列/阶段 |
| 3 | Wait2D 尾 | `bizState = _rescanPhase ? WaitScan3D : MergePending` |
| 4 | WaitScan3D | +重扫分支：同步等扫描+数据 → Merge（严格串行） |
| 5 | Merge 重扫条件①（全匹配失败） | 主流程记录队列 / 重扫模式重试 |
| 6 | Merge 重扫条件②（3D 数据超时） | 同上 |
| 7 | Merge 落表 ×2 | 重扫模式先删该位旧结果再插新 |
| 8 | NextPos | 重扫模式取队列下一个 / 队列空→End |
| 9 | End | 队列非空 → 进入重扫模式 |

`_rescanFlag` 已无写入点（即时重扫删除），读取处走 else 正常路径，死代码保留无害。

## 预期效果

- 位 31-34 类"静默全 NG"消失（主流程 SCAN3D 严格串行）
- 位 27 类 0 针/叠加层残留消失（不再丢弃在途 2D）
- 重扫集中在板末：主流程节拍完全稳定；代价 = 每重扫位 +A 段重拍 ~1.7s（本轮 6 次重扫 ≈ +10s，板末统一承担）

## 验证

1. `_build.bat` 编译
2. 实机一轮：① 无"3D 数据串位"特征（3D 点数 vs 2D 针数一致）；② 重扫位日志显示"已记录板末延迟重扫"→"进入板末统一重扫"→"板末重扫完成"；③ 重扫位结果被替换（旧降级→新高）；④ 叠加层显示正常
3. 关注：重扫模式日志 `[⏱] 检测位{i} 重扫 D(...)` 与 `合并明细` 正确配对
