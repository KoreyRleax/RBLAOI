# 更新日志 2026-08-20：叠加层 PinId 重复 bug 修复

## 问题描述

当检测方案中存在**不同针型**（A/B/C...）时，叠加层显示的 PinId（`#编号` 标注）会出现重复；
而检测结果列表（PinResults）中的 PINID 显示正常。

## 根因分析

merge 阶段将 `PinResult` 回填给叠加层（`VmOverlayItem`）时，只按 `PinIndex` 单字段匹配：

```csharp
var r = newResults.FirstOrDefault(n => n.PinIndex == overlay.PinIndex);
```

而 VM 多针型方案中，**各针型检测模块输出的 PinIndex 是各自独立编号的**（每个针型模块内部从 0 开始计数）。
同一检测位内，A 针和 B 针都会出现 `PinIndex = 0` 的针，于是：

1. 两个不同针的 overlay 通过 `FirstOrDefault` 都命中结果列表中的第一条（A 针结果）→ 叠加层显示重复 PinId；
2. B 针的同 PinIndex 结果永远不会被任何 overlay 引用到。

结果列表本身用 `globalPinId++` 全局自增生成 PINID（唯一），所以显示正常。

## 修复内容

| 文件 | 修改 |
|------|------|
| `ViewModels/MainViewModel.cs` | `VmOverlayItem` 增加 `PinType` 字段（旧存档反序列化为 null，用于触发兜底匹配） |
| `ViewModels/MainViewModel.DetectionFlow.cs` | 构建叠加层时填入 `PinType`（Wait2D 阶段，第 673 行） |
| `ViewModels/MainViewModel.DetectionFlow.cs` | 新增 `MatchOverlayPinResult()` / `MatchOverlayByPad()` 匹配辅助方法 |
| `ViewModels/MainViewModel.DetectionFlow.cs` | merge 两处回填（3D 匹配成功分支、3D 超时纯 2D 分支）改用 `MatchOverlayPinResult()` |
| `ViewModels/MainViewModel.DetectionFlow.cs` | `SyncVmOverlayResults()` 同步逻辑同样修复 |

## 匹配策略（新）

1. **首选**：`PinType + PinIndex` 联合匹配（调用方已按 GridIndex 过滤结果集）；
2. **兜底**：旧存档 `VmOverlays.json` 无 PinType 时，按焊盘坐标匹配（`PadX/PadY` 与 `PadMmX/PadMmY` 距离 < 0.2mm，同一 FOV 内焊盘坐标唯一，且两侧数值来自同一次 Round(3) 计算，精确相等）。

## 影响范围

- 叠加层 `#编号` 标注（DrawVmText）——修复后每个针显示唯一的全局 PINID；
- 叠加层 H / IsOK 回填——同根因，一并修复；
- 结果列表、去重逻辑、精度统计（按坐标匹配，不受 PinIndex 影响）——无影响。

## 验证

- 本地安全策略拦截 MSBuild，未能在命令行完成编译验证；代码已逐处复查（方法签名、lambda 捕获、partial 类内静态方法引用均正确）。
- 建议：VS 中编译运行，用含两种针型的方案跑一次检测，确认叠加层编号无重复、与结果列表一一对应。
