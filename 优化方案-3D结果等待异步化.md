# 优化方案：3D 结果等待异步化（v2 —— 数据驱动立即显示 + 重扫不重拍）

> 日期：2026-08-25（v2 修订）｜ 改动文件：`ViewModels/MainViewModel.DetectionFlow.cs`（单文件）
> 状态：**方案待确认，未编码**
> v2 修订：① 结果"数据到就立即显示"（并行消费任务，不推迟）；② 重扫回退后**跳过已拍的 A 段**（不重拍）。

---

## 一、v2 核心模型

```
检测位 i 的 WaitScan3D: 等扫描完成 → 创建数据监听(早开防漏) → 启动"消费任务 Consume(i)"(后台, fire-and-forget) → 立即 NextPos
检测位 i+1 的 CapturePos/GrabTile: 移动+拍照（A段 ~1.8s）
   ├─ 期间 Consume(i): 数据到达(X_COMPLETE+137ms) → 立即 Merge(i) + 落表 + 显示   ← 数据到就显示，零推迟
   └─ Start2D(i+1) 前【同步点】: await Consume(i)（正常已瞬间完成，零等待）→ 保证数据消费早于下一拍 SCAN3D（防串位）
```

**两个关键性质**：
- **显示不延迟**：数据到达（X_COMPLETE+137ms）→ Consume 后台立即处理（+30ms）→ 显示。与现在同步版的显示时机（137+30）**几乎一致**——收益全部来自"等待期间轴在移动"。
- **节拍收益增加**：等数据 137ms + Merge 30ms 全部被 A 段（~1.8s）隐藏 → **每拍省 ~167ms（-3.6%）**，40 位排 -6.7s。

## 二、状态机改动（5 处）

### 2.1 新增字段
```csharp
Task<string> _pending3DDataTask;      // 数据监听（原 WaitScan3D 局部变量提升；扫描完成前创建，防漏）
Task _pendingConsumer;                // Consume(i) 句柄（同步点 await 用）
bool _pendingRescan;                  // Consume 判重扫 → 状态机在同步点响应（volatile）
int _rescanIdx;                       // 需重扫的检测位号
bool _skipAForNext;                   // 重扫返回后跳过下一检测位 A 段（图已拍）
List<PinResult> _rawPins2DSnapshot;   // 2D 结果快照（Wait2D 完成后复制，防被下一检测位覆盖）
```

### 2.2 `WaitScan3D(i)`（L717-782）：只等扫描完成 + 启动消费
```csharp
// 保留：检查点、await _3dScanTask（scanOk）、扫描失败弹窗
// 删除：WhenAny 等数据段（L742-760）
if (scanOk && _vision3DClient.IsConnected)
{
    _pending3DDataTask = _vision3DClient.WaitForPrefixNonEmptyAsync("DETECT_3D_OK:", _3dDataCapMs, token);  // 早开监听
    _rawPins2DSnapshot = rawPins2D?.ToList();        // 快照 i 的 2D 结果（Wait2D 已完成，数据属于 i）
    _pendingConsumer = Consume3DResultAsync(i, token);   // 后台消费，不 await
}
bizState = CheckState.NextPos;
```

### 2.3 新增 `Consume3DResultAsync(i)`（数据驱动，核心）
```csharp
private async Task Consume3DResultAsync(int i, CancellationToken token)
{
    string response = "TIMEOUT";
    if (_pending3DDataTask != null)
    {
        // 结果窗口从扫描完成起算压 2s（同现语义）；A 段期间数据已到 → 立即返回
        var resultWait = Task.WhenAny(_pending3DDataTask, Task.Delay(Math.Min(Wait3DResultTimeout, 2000), token));
        if (await resultWait == _pending3DDataTask) response = await _pending3DDataTask;
        else if (token.IsCancellationRequested) return;   // 复位：放弃
        else response = "TIMEOUT";
    }
    _pending3DDataTask = null;

    // ===== Merge(i) 主体：从现 Merge(L784-1355) 原样搬入，rawPins2D → _rawPins2DSnapshot =====
    // 解析 3D + 2D 合并 + 判定 + Dispatcher 落表 + UpdateStatistics + RefreshOverlayDisplayAsync
    // 重扫判定（现 L1218-1227 / L1282-1290）：需要重扫 →
    if (需要重扫(i))
    {
        retry3DPos++;                                  // 状态机不回退重扫时 CapturePos 重置，重扫 i 的计数不被清 ✓
        _pendingRescan = true; _rescanIdx = i;
        return;                                        // 不落表不显示（同现逻辑）
    }
    // 正常：落表 + 显示 + UpdateStatus（全部 Dispatcher/UI 线程，与状态机 UI 操作串行 ✓）
}
```

**竞态隔离**：
| 共享对象 | 隔离方式 |
|---|---|
| rawPins2D | `_rawPins2DSnapshot` 快照（Consume 启动前复制） |
| pos/GrabIndex | `DetectPositions[i]`（只读数组） |
| PinResults/统计/状态栏 | 全部 `Dispatcher.InvokeAsync`（UI 线程串行） |
| globalPinId | 仅 Consume 写（同一时刻只有一个 Consume 挂起） |
| retry3DPos | Consume 写重扫计数；CapturePos 重置发生在 Consume 完成（同步点）之后 ✓ |
| 重扫标志 | volatile bool + 状态机同步点读取 |

### 2.4 同步点（防数据串位）：`Start2D(i+1)` 前
```csharp
// GrabTile(i+1) 完成后、Start2D(i+1) 前：
if (_pendingConsumer != null) { await _pendingConsumer; _pendingConsumer = null; }
if (_pendingRescan)
{
    _pendingRescan = false;
    i = _rescanIdx;                       // 回退
    InitPosParams();                      // 重算 startX/endX/moveY（抽自 CapturePos L397-405）
    bizState = CheckState.MoveScanStart;  // 移回 i 扫描起点重扫
    break;
}
bizState = CheckState.Start2D;
```

### 2.5 重扫返回：`NextPos` 跳过已拍 A 段（用户诉求 2）
```csharp
// NextPos(i 重扫完成后):
i++;
if (i >= totalPositions) { bizState = CheckState.End; break; }   // 末位（End 前已由同步点消费 N-1 ✓）
if (_skipAForNext)
{
    _skipAForNext = false;
    InitPosParams();                     // swPos.Restart / retry3DPos=0 / pos / 扫描参数
    bizState = CheckState.Start2D;       // 跳过 CapturePos/GrabTile：i 的图已拍过，直接 2D 检测
    break;
}
bizState = CheckState.CapturePos;
```
- **为什么可以跳过**：重扫 i 期间，i+1 的 A 段（移动+拍照）**已经完成**（同步点在 A 段之后）——`capturedImagePath`/`_currentPositionPinPaths` 未被重扫流程覆盖（重扫只走 MoveScanStart→StartScan3D→Wait2D→WaitScan3D），DETECT2D_BLOB 直接读已拍文件 ✓；
- 重扫 i 完成后 i+1 的 2D 检测用已拍图，**零重拍**；
- 轴位置：重扫结束在 i 扫描终点，Start2D(i+1) 后的 MoveScanStart(i+1) 发绝对移动会正确到位 ✓。

## 三、收益（v2 修正）

| 项 | v1 方案 | **v2 方案** |
|---|---|---|
| 每拍节省 | 137ms | **~167ms**（等数据 + Merge 均被 A 段隐藏） |
| 结果显示延迟 | +1.8s（用户否决） | **≈0**（数据到即显示，与同步版几乎一致） |
| 重扫代价 | i 的 A 段重拍（+2s） | **不重拍**（用已拍图） |
| 40 位排 | -5.5s | **-6.7s** |

## 四、风险与对策

| 风险 | 对策 |
|---|---|
| Consume 与状态机并发写（rawPins2D 覆盖） | 快照 `_rawPins2DSnapshot` |
| 数据慢到（>A 段时长）时同步点阻塞 | 同步点 await Consume（数据兜底 2s）；A 段 ~1.8s + 同步点剩余 ~200ms，偶发多等可接受 |
| 重扫 i 时 `retry3DPos` 计数 | 重扫走 WaitScan3D→Consume（不经过 CapturePos），计数不被清，3 次上限保护 ✓ |
| 暂停：A 段暂停时 Consume 继续跑 | 数据到 → Merge+显示（无害）；重扫标志在恢复后同步点响应；复位 → token 取消 Consume ✓ |
| 末位消费 | NextPos(i=N) → End 前由同步点 await Consume(N-1) ✓ |
| 首位 | 无特殊处理（Consume(0) 正常消费第一位）✓ |

## 五、验证清单

1. 模拟器：单拍 3 位 + 连续 2 轮（数据不串位、末位落表、显示时机）；
2. 人为构造空 3D 数据触发重扫：验证 i 回退 + 跳过 A 段（i+1 不重拍、结果正确）；
3. 暂停/恢复 + 复位：无残留监听、无崩溃；
4. 实机 16mm/s 完整轮：总耗时中位 4625 → 预期 ~4460；日志无 DETECT_3D 串位；
5. 内存/线程：长时间运行无任务堆积（每个 Consume 有界：数据到或 2s 超时即结束）。

## 六、回退

单文件状态机内部改动，可整体 revert；无协议/数据格式变更。
