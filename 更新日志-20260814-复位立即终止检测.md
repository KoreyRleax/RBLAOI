# 更新日志：连续模式复位立即终止检测（2026-08-14）

## 时间
2026-08-14

## 标题
连续模式下点击复位，检测应立即终止（不再等到在途指令超时）

## 背景
用户反馈：连续模式下点击复位，检测流程没有立即终止，仍在运行（移动/拍照/扫描等长操作继续等到自身超时才退出）。根因分析：
1. 检测流程的长操作（`MoveToAndWaitOK`/`Scan3DToAndWaitOK`/`WaitForFileReadyAsync` 等）**不带取消 token**，复位取消后仍等到自身超时（几秒~几十秒）才返回，检查点才能转 Terminated；
2. `MotionIO.Flush()` 只清指令队列中**未执行**的项，**在途指令**（已发送等待回复）不受影响；且 `DiscardInBuffer` 可能丢弃在途指令的回复，等待者只能等满超时；
3. GRAB（带 token）取消时抛 `OperationCanceledException`，被 `StartCheckFlow` 的 `catch (Exception)` 捕获 → 误弹"检测失败"错误框（应静默终止）；
4. 拍照被取消返回 null 时，会误走"拍照失败"Err 分支弹窗（应走 Terminated）。

## 解决方案
改动集中在 `ViewModels/MainViewModel.cs`（不动 MotionIO.cs，遵守禁改规则）：
1. 新增**取消感知等待**辅助方法 `WaitOrCancelAsync<T>(task, token)`：任务完成或 token 取消时返回（取消时立即返回 default，不等在途指令超时；在途指令由 Flush/自身超时收敛）；
2. 检测流程所有**无 token 长操作**改用 `WaitOrCancelAsync` 包装：
   - 拍照移动（`GrabAndReplaceTileAsync`/`GrabOneAsync` 中心点、调焦）——含取消短路（取消时返回 null 而非记失败日志）
   - VM 文件等待（`WaitForFileReadyAsync` × 2 处）——取消时不等兜底
   - 移到 3D 扫描起点（`MoveScanStart`）
   - 3D 扫描等待（`WaitScan3D` 的 `_3dScanTask`）——取消时直接 Terminated，不弹"3D扫描失败"
3. `GrabTile` 状态：拍照返回后**先检查 token**（取消优先于"拍照失败"Err 分支），取消 → Terminated 静默退出；
4. `StartCheckFlow` 新增 `catch (OperationCanceledException)`：复位/停止/报警取消静默终止，不弹"检测失败"框（finally 照常清理流程标志）。

## 总结
复位/停止/报警取消后，检测流程在所有长操作点（移动/拍照/调焦/等文件/3D扫描）**立即退出**，不再等超时；取消不再误弹错误框（"检测失败"/"3D扫描失败"/"拍照失败"均只走 Err 时弹）。编译通过 0 错误。注：串口在途指令仍由自身超时收敛（物理机制，不影响流程退出观感）；全图采集流程的同类长操作（GRAB 不带 token）本次未改，如需一并处理请告知。
