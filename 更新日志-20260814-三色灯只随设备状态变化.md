# 更新日志 2026-08-14：三色灯只随设备状态变化

## 时间
2026-08-14 下午

## 标题
三色灯规则简化：只受设备状态驱动，去掉流程运行中设置的逻辑

## 背景
原三色灯逻辑（DeviceMonitor.UpdateLights）依赖流程层委托（flowBizRunning/flowError）：红灯=报警或流程Err、绿灯=流程业务执行中、黄灯=其余（含准备/暂停/收尾等）。流程层（MainViewModel 全图采集/检测状态机）内部有约 20 处"业务执行中/报错"标志赋值用于灯色。用户要求：**三色灯只受设备状态变化**，去掉流程运行中设置的逻辑。

## 解决方案
1. **DeviceMonitor.UpdateLights 改为纯设备状态映射**：
   - 报警(Alarm) → 红
   - 未就绪(NotReady) → 黄
   - 其余（就绪/手动/自动/暂停/演示）→ 绿
   - 仍保持"仅状态变化时下发指令"（防刷串口）
2. **删除灯色专用委托**：`_flowBizRunning`/`_flowError` 字段及 `RegisterFlowStateProviders` 对应参数（flowRunning/flowPaused/flowDemo 保留——设备状态机仍依赖）
3. **MainViewModel 清理**：删 `_grabBizRunning`/`_checkBizRunning`/`_flowError` 字段声明、注册调用中的 flowBizRunning/flowError lambda、全图采集/检测状态机内全部灯色赋值点（约 20 处，含复合行）

## 总结
- 灯色语义：未就绪=黄、就绪/手动/自动=绿、报警=红；暂停/演示归绿（非报警非未就绪即绿）。
- 流程状态机代码不再含灯色副作用，灯色完全由设备状态（UpdateMachineState 每周期评估）驱动。
- 编译 0 错误。已验证：重启未回零=黄灯、回零完成就绪=绿灯。
