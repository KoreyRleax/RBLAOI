# 更新日志：F407 协议会话握手 + RESTART 重握手

**时间**：2026-08-11  
**标题**：F407 会话握手与 RESTART 重握手实施（下位机交接文档 B0 阶段）

## 背景

现场 F407 主板 RESTART 后持续收到 `ERROR:SESSION_REQUIRED`。下位机同事交接文档（2026-08-11-rblaoi-restart-session-state-polling.md）定位根因：RBLAOI 打开串口后从不发送 PING 建立协议会话；重启按钮只发一次 RESTART，不暂停轮询、不重新握手；F407 重启后会话回到 UNLOCKED，普通请求全部返回 SESSION_REQUIRED，形成持续错误循环。

**已核实**：交接文档描述的旧代码（GET_IO 轮询 + 50ms 钳制 + 无 GET_STATE）在当前工作树已不存在——2026-08-04 提交 a7c8c90 已完成 GET_STATE 单飞轮询重构。本次实施聚焦文档方案 B 的核心缺失项：会话握手、RESTART 生命周期、generation 隔离、SESSION_REQUIRED 有界恢复、急停按钮接线错误。

**授权**：项目负责人批准对 MotionIO.cs 做最小可回滚修改；后台采样沿用现有 IoPollInterval=20ms（50Hz）。

## 解决方案

### 1. 协议会话状态机（MotionIO.cs 新增）

`MotionSessionState { Closed, Handshaking, Ready, Recovering, Restarting, Failed }` + `SessionState` / `IsSessionReady` / `SessionGeneration` 属性 + `SessionStateChanged` 事件。会话未 Ready 时禁止普通查询与动作。

### 2. Open() 接入握手（全部 4 个连接入口自动覆盖）

打开串口 → 启动解析/队列 → **两次 PING 握手**（每次 2s，任意本代完整行=设备响应）→ 成功才 Ready 并启动 GET_STATE 轮询；失败关闭串口返回 false（MainViewModel / AxisIOControlWindow / SysParamWindow / IAPWindow 现有失败路径自然生效）。

PING 直写串口 + 专用 handler（PingDirectAsync），**不走命令队列**：恢复期间队列可能被旧代在途请求阻塞，握手不能被队列拖死；代次校验防旧会话残留回复误完成。

### 3. RESTART 统一生命周期（RestartAndRehandshakeAsync）

单实例锁防重复点击：停轮询 → Flush 队列 → 发 RESTART → 等 2s 设备重启 → 新一代 PING 握手 → 成功恢复轮询；失败保持 Failed 并明确报错"主板已复位，但重新握手失败"，不形成 SESSION_REQUIRED 风暴。

### 4. SESSION_REQUIRED 有界恢复

ParseDataTask 检测到该错误且状态为 Ready 时，Interlocked 门保证**只触发一个**恢复任务（RecoverSessionAsync）：停轮询 → 新一代握手 → 恢复。恢复失败轮询保持停止，不会每轮重复触发。

### 5. generation 代次隔离

每次握手 `SessionGeneration++`；CommandItem 记录入队代次，ExecuteCommandAsync 匹配回复时校验代次——RESTART 前的旧代迟到回复不得完成新请求。

### 6. 队列确定性完成

Close()/Flush() 清队列时逐个 `TrySetResult("ERROR")`，不再静默丢弃悬挂等待者；EnqueueCommand 在会话未 Ready 时返回 `"NOT_READY"` 拒绝入队。

### 7. 急停按钮接线修正

IO 窗口"急停"按钮原误发 RESTART（不是停止命令，F407 无 PC ASCII 全局 STOP）：XAML 禁用 + ToolTip 说明"软件急停未提供，请使用独立硬件安全链"，点击兜底提示；"重启"按钮改调 RestartAndRehandshakeAsync。

### 8. UI 绘制分频

后台 GET_STATE 保持 ~50Hz（IoPollInterval 可配置），AxisIOControlWindow 两个事件加 50ms 节流合并，UI 刷新 ~20Hz；DeviceMonitor 订阅保持全速率（去抖精度不受影响）。

### 9. 快照无效保守修正

GetAllAxisStatus 无快照时返回全 true（视为运动中），与 IsMoving 一致，防止连接瞬间误判"全轴已停止"。

## 总结

补齐了 F407 协议会话生命周期：任何连接入口都先握手再轮询，RESTART 自动重握手，SESSION_REQUIRED 有界恢复不风暴，代次隔离杜绝迟到回复串代；误发 RESTART 的"急停"按钮禁用。未改动 GET_STATE 轮询主体、线协议与固件侧保护。

## 验证

- 编译通过（x64，程序关闭后复制 exe）。
- 逻辑走查：握手失败关闭串口路径、SESSION_REQUIRED 单实例门、generation 校验、RESTART 防重复、PING 直写绕队列。
- **待现场无运动验收**（按交接文档第 9 节）：冷启动首帧必须为 PING；空闲点击重启无 SESSION_REQUIRED 风暴、新 PING 成功后恢复状态流；拔线/握手失败时轮询停止且 UI 明确报错。

---

## 补充更新（同日）：调试与联调修复（虚拟串口联调 + 真机验收准备）

### 背景

虚拟串口联调发现多个问题：IO 面板连接始终失败、连接成功后不轮询、指示灯灰、刷新率受限等。

### 解决方案与根因

1. **Open() 死锁**：Open 在 UI 线程同步等待握手（GetResult），握手链路 await 捕获 UI 上下文互相等待 → 全部 await 加 ConfigureAwait(false)；
2. **连接成功不轮询**：Open 握手成功后漏 SetSessionState(Ready)，StatePollLoop 首轮检查未就绪立即退出 → 补 Ready 状态；
3. **IO 指示灯全灰**：IO 位置快照与指示灯共用 50ms 节流器，快照事件先触发把节流额度占走 → 各自独立节流；
4. **IO 面板连不上（核心）**：IO 面板 OnDataReceived 用 Dispatcher.Invoke 同步等待 UI 线程，而握手时 UI 线程被 Open 阻塞 → ParseDataTask 线程被卡，PONG 匹配延迟 2s 超时 → 全部改 InvokeAsync（非阻塞）；系统参数窗口不订阅 DataReceived 所以一直正常；
5. **SPEED JOG 干扰握手**：IO 面板连接事件立即发 SPEED JOG 直写串口插队到 PING 前 → SendCommand 加会话门（未 Ready 仅允许 PING/RESTART）；
6. **PING 宽匹配误判**：任意完整行算响应会把设备主动推送数据当 PONG，时好时坏 → 精确匹配 PONG（下位机确认回复格式）；
7. **IO 面板连接参数**：端口列表默认选中系统配置串口（原取列表第一项）；波特率下拉默认 460800 + TryParse 兜底（原空文本 FormatException）；
8. **刷新率受限说明**：实际刷新率 = 1000/(RTT+间隔)，虚拟串口 RTT≈31ms 是链路特性（真实设备固件直回 RTT 3-10ms，可达 90-100Hz）；IO 面板新增 RTT 显示，右上角"刷新率 + RTT"；串口区左对齐避免遮挡。

### 验证

- 编译通过（x64）；虚拟串口联调握手/重启/轮询正常。
- 真机验收（待现场）：冷启动 PING 首帧、RESTART 无 SESSION_REQUIRED 风暴、间隔 5-10ms 时刷新率与 RTT 对照。

---

## 补充更新（同日）：PING 回复格式确认（PONG → PING:<us>）

### 背景

下位机确认 F407 实际 PING 回复格式为 `PING:<us>`（如 PING:123us，us 为固件处理耗时），非 PONG。

### 解决方案

`PingDirectAsync` 回复匹配从 `PONG` 前缀改为 `PING:` 前缀（忽略大小写）；`PING:` 前缀足够独特，设备主动推送数据不会被误判为握手成功。固件耗时可在真机验收时与上位机 RTT 显示值对照，差值为传输/上位机开销。

### 验证

- 编译通过（x64）。
