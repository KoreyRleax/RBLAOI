# 更新日志 2026-08-13：删除采集设置功能

## 时间
2026-08-13

## 标题
删除采集设置窗口与拍照位遍历模式（Z/S/U），遍历顺序固定为 Z 型默认

## 背景
采集设置窗口（CaptureSettingsWindow）仅提供两个参数：移动模式（Z 逐行/S 蛇形/U 列向）和起始角（TopLeft/BottomRight）。用户决定删除：只保留默认模式（Z 型）的拍照位遍历顺序，理由为方便维护——S/U 模式节省的耗时不值得多一套参数和窗口的维护成本。

## 解决方案
1. 删除 `Views/CaptureSettingsWindow.xaml(.cs)` 窗口文件 + `RBLAOI.csproj` 的 Compile/Page 注册（旧式 csproj 显式注册）
2. 删除 `MainWindow.xaml.cs` 的 `capture_settings` 菜单项（AddSubButton）与 `_actionMap` 分发
3. 删除 `MainViewModel.ShowCaptureSettings()`
4. 删除 `GetGrabOrder()`（Z/S/U + TopLeft/BottomRight 四组合生成遍历顺序），`CalculateGrabPositions` 改为固定双重循环（Z 型：自上而下逐行从左到右），日志去掉模式参数
5. 删除系统参数 `GrabMoveMode`（Z/S/U）与 `GrabStartCorner`（TopLeft/BottomRight）：`SysParamData.cs` 与 `SysParam.cs` 包装属性

## 总结
采集设置入口、窗口、参数、遍历逻辑全部移除；拍照位遍历固定为 Z 型默认顺序（与删除前的默认配置行为一致，无行为变化）。编译通过（0 错误）。
