# 优化测试报告 2026-08-25：检测耗时优化效果与剩余优化空间

> 本报告含：优化效果实测数据 + 剩余优化空间与代码位置，供其他电脑/新会话实施修改。
> 项目：RBLAOI 3D 扫描+视觉检测上位机，方案 215，日志 `bin/x64/Debug/Logs/`。

---

## 一、优化效果实测（截至 8/25 11:12，参数 12mm/s + 50/40ms）

| 指标 | 8/21 基线 | 8/24（优化后 7mm/s） | 8/25（12mm/s+新参数） | 累计降幅 |
|---|---|---|---|---|
| 单检测位总耗时中位 | 7479ms | 6627ms | **~5050ms** | **-32.5%** |
| A 段（移动+拍照）中位 | 2907ms | 2082ms | **1815~2001ms** | -31~38% |
| D 段（3D 扫描）中位 | 4131ms | 4204ms | **~2855ms** | -31% |
| 40 位单排跨度 | 309s | 272s | **~200~235s** | -24~35% |

**收益分解（每检测位）**：
- A 段优化（1a 删 delay200 + 2 删诊断 + 8 X/Y+Z 合并 + e13e0f6 延时参数化）：**-900~1100ms**
- 扫描提速 7→12mm/s（D 段）：**-1300ms**
- 合计：**-2400ms/拍**

**参数验证**（SetVmSavePathDelay 50/25/0ms 三轮 90+ 拍）：0 条 [路径未生效]、0 条文件超时；50ms 文件等待 175ms 最稳，0ms 有 602ms 长尾。**定稿 50ms**。

---

## 二、剩余优化空间（按优先级，含代码位置）

### 🔴 高优先（收益大、风险可控）

#### 1. 优化 11：ReadImage 19MB×2 异步化
- **代码位置**：检测流程读取板面图 + 针型图处（2D 图片加载，19MB×2，当前同步读取）
- **收益**：实机估 **200-400ms/拍**（模拟器已实测 -63ms）
- **做法**：e13e0f6 更新日志中"已评估未做"项——读图任务 Task.Run 异步化，与检测流程并行；需保证后续图像处理拿到完整数据
- **风险**：低（改动局部）

#### 2. 扫描提速 12→14 mm/s
- **参数位置**：界面"3D扫描速度"（SPEED SCAN3D 指令 X/Y 值，5000/0.1mm 粒度）
- **收益**：D 段 2855×(1-12/14) = **~410ms/拍**（匀速线性，已验证速度-耗时自洽）
- **前置验证**：8/24 曾用 14mm/s 跑过（D=2117ms 无异常），但需**同一针测 12 vs 14 的高度精度/标准差对比**（轮廓噪点风险），通过后再固化
- **风险**：中低（质量验证通过即可）

#### 3. 3D 偶发重试排查（连续性最大隐患）
- **现象**：`关闭3D触发失败` / `3D数据超时，扫描成功但未收到数据` 偶发 → 单次重试损失 **5-20s**
- **案例**：8/24 17:13 轮 17890ms、8/25 11:04 轮 13042ms、8/21 多轮
- **方向**：抓 3D 相机侧日志（CAMERA3D_ON/OFF 时序、VM 3D 模块输出），确认是触发冲突还是数据丢失；与 2D SET_PATH 链路无关
- **收益**：消灭偶发尖峰，稳定产线节拍

### 🟡 中优先（改动小）

#### 4. 调Z 期间并行发 SET_PATH
- **代码位置**：RunCommands.cs GrabAndReplaceTileAsync / GrabOneAsync（调Z `MoveToAndWaitOK(z)` 与 `SET_PATH` 串行段）
- **收益**：**100-150ms/针型位**（SET_PATH 150ms 与调Z 40ms 重叠）
- **注意**：SET_PATH 是 2D VM 指令与轴运动无关，但需验证 Z 运动中切 VM 路径不丢图（小范围试）

#### 5. 轴速 600→800 mm/s
- **代码位置**：运动速度设置（"已发送轴速度: 600 mm/s"，SPEED PTP 300000）
- **收益**：~100ms/位（A 段移动中心 325ms + B+C 移起点 300ms 提速）
- **注意**：振动/到位精度，低风险试一轮

### ⚫ 不建议（有依据地排除）

| 项 | 排除理由 |
|---|---|
| GRAB 合并（板面+B+C 一次拍） | 各针型对焦 Z 高度不同（B=25000/C=23500），物理约束，硬做糊图 |
| 文件等待∥移起点 | GRAB 文件未就绪就移轴，时序风险高 |
| 扫描路径合并/跨位扫描 | 架构级改动，收益不确定 |

---

## 三、预期

三项高优先全做：单拍 **5.05s → ~4.2s（-17%）**，40 位单排 **~168s**，对比 8/21 累计降 **45%**。

**实施顺序建议**：#1（立即见效）→ #2（质量验证后）→ #3（立项，不急但重要）；#4/#5 调试空档顺手试。

---

## 四、实施注意事项

1. **对比必须同位置同板**：8/25 发现轮间 A 段有机台漂移（同参数差 373ms），跨轮对比只信中位趋势、不信绝对值
2. **改参数后验证三件事**：无 [路径未生效]/文件超时；画面清晰（调Z 延时）；A 段数值
3. **e13e0f6 之后的代码未推远程**：本地分支 `fix/multi-pintype-blob-parser` 与远程一致（e13e0f6 已推送），新改动需自行提交推送
4. 环境坑（若在沙盒 git 环境）：pull/reset 后检查 `git log`，分支 unborn 时手动重建 ref（见交接文档第六节）
