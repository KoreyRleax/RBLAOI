# 更新日志 2026-08-21：3D扫描超时解耦 + 轴等待快照兜底

## 背景
连续多天实测日志（analyze_3d_delay.py）显示 3D 扫描存在 20%~100% 的双扫描重试：
每拍第一次 SCAN3D 后 X_COMPLETE 完成信号偶发丢失 → `WaitAxisComplete` 等满超时
（8~10s）→ `RetryAsync` 重扫一次。19:30 日志实锤：X_COMPLETE 在超时窗口内已到达
（36.755 < 40.2），VM 也已推送真实数据，但等待未被唤醒 → 假超时 + 重复扫描。

另发现 `Wait3DResultTimeout` 与扫描重试强耦合：数据监听在 `WaitScan3D` 进入时就启动，
其 15s 窗口被 `_3dScanTask` 内部的重试（最多 2×Scan3DTimeout）吃掉，极端情况下
扫描成功但数据监听已退订 → 误降级纯 2D。

## 改动

### A. 轴等待超时快照兜底（Core/Motion/MotionIO.cs）
- `Scan3DToAndWaitOK`：保存目标脉冲 `targetXPulse/targetYPulse`；
  轴等待超时后先用 GET_STATE 快照（19~21Hz 刷新）确认失败轴已到位
  （`IsFresh && !Busy && |位置-目标|≤500脉冲`，容差同 `NeedMove`）→ 按成功处理，
  避免白等超时+重扫；快照缺失/过期/未到位保持失败（重试只留给真失败）。
- 新增 `AxisArrivedBySnapshot(string axis, long targetPulse)` 辅助方法。
- 新增两条 Warning 日志：快照确认成功 / 快照未确认（含 Fresh/X/Y/Busy/目标值），
  用于定位完成信号丢失点。

### B. 3D结果等待超时解耦（ViewModels/MainViewModel.DetectionFlow.cs, WaitScan3D）
- `_3dDataTask` 监听仍在扫描前创建（防漏 VM 提前推送），内部超时改为大上限
  `Scan3DTimeout×2 + Wait3DResultTimeout`；
- 扫描完成后用 `Task.WhenAny(_3dDataTask, Task.Delay(Wait3DResultTimeout, token))`：
  **结果窗口只从扫描完成起算**，重试次数不再连累结果等待；
- 取消语义保持：token 取消时立即 `MarkAlarmCancel + Terminated`，不走超时降级
  （延续 08-14 复位立即终止修复）。

### C. 参数调整
- `Wait3DResultTimeout`：15000 → **5000**（SysParam.xml + SysParamData.cs 默认值 10000→5000）
  ——解耦后该参数只覆盖"扫描完成→VM推送"，实测 23ms~1.5s，5s 富余；
- `Scan3DTimeout`：维持 8000（实测扫描运动 2.7~4.6s，A 落地后它只是真失败兜底）。

### D. 重试二次短超时（新增系统参数 Scan3DRetryTimeout）
- 最坏等待分析：两次真失败原来 = 2×8s+0.2s ≈ 16.2s（OFF 回执超时最坏 ~20s），用户不可接受；
- 新增系统参数 `Scan3DRetryTimeout`（默认 3000ms，界面"⏱ 超时时间"组可调）：
  第一次尝试用 `Scan3DTimeout`（正常扫描运动），重试用 `Scan3DRetryTimeout`——
  重试时轴已到位：假失败瞬间过，真失败短超时快速判定；
- `StartScan3D` 改用新增私有方法 `Scan3DWithRetryAsync(endX, moveY)`（替代 RetryAsync 包裹），
  取消语义不变（外层 WaitOrCancelAsync 兜底）；
- 改动文件：Models/SysParamData.cs、ViewModels/SysParamViewModel.cs、
  Views/SysParamWindow.xaml（绑定"3D重试超时(ms)"）、bin Config/SysParam.xml、
  ViewModels/MainViewModel.DetectionFlow.cs。
- 最坏等待由 ~16.2s 降至 **8+0.2+3 ≈ 11.2s**（OFF 回执正常）。

## 验证
- 跑一轮检测后执行：`分析3D延迟.bat`（analyze_3d_delay.py）
  - `SCAN3D发送/成拍` 应趋近 1:1，双扫率应归 0；
  - 出现 `[Scan3DToAndWaitOK] 轴等待超时但快照确认已到位` 即兜底生效；
  - 出现 `轴等待超时且快照未确认到位` 则为真失败（需查固件 X_COMPLETE 上报）。
