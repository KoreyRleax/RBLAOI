# 交接文档 2026-08-27：精度检测完善 + 流程改造 + 实时取流方案评估

> 交接给下个会话。分支：`fix/multi-pintype-blob-parser` @ 8161bc5。
> ⚠️ **2026-08-27 14:52 用户反馈更新**：① 五B「量纲错配」为**误判**，实机匹配正常，勿改上位机；② 验证清单 1-7 **全部完成**；③ 长期遗留删除：检测位12 偏差 / VM 空推送 / WaitForVmSaveTimeout 下调 / 耗时优化剩余项；④ 10:27 已 commit+push（0ad8175），14:28 本地 `_build.bat` 编译通过（bin/x64/Debug/RBLAOI.exe）。
> 需求来源：《上位机改造方案》（Task1 精度检测 a-f / Task2 流程 a-d / Task3 实时取流评估 + 3 个 bug 修复）。

---

## 一、Task1 精度检测完善（已实施，未编译验证）

### 改动文件
- `Models/PrecisionStat.cs`：`PrecisionPinStat` 新增 `PinType`（kind）+ `Samples`（每轮明细 List<PrecisionSample>）；新增 `PrecisionSample`（RunIndex/Dx/Dy/H/Matched）；`PrecisionReportSummary` 新增 `KindRanges`（List<KindRangeStat>）；新增 `KindRangeStat`（按 kind 的极差分布）
- `ViewModels/MainViewModel.RunCommands.cs`：`ComputePrecisionStats` 重写 + `StartPrecisionCheck` 扩展
- `Views/PrecisionReportWindow.xaml/.cs`：报告窗口重构

### 各子项实现
| 项 | 实现 | 说明 |
|---|---|---|
| a) 以 X/Y 判定同一针 | **原有逻辑保留**（L858-901 最近距离<1mm 匹配，非 PinId） | 未改动；第一轮为参考轮，后续轮按 X/Y 距离匹配。边界：参考轮漏检的针不会进统计（沿用原行为） |
| b) 高度0排除 | hList 过滤 `h>0` 后再算 HMean/HStdDev/HRange/HMin/HMax；全 0 时统计置 0 | 仅影响 H 统计；dx/dy 与每轮明细保留原始值 |
| c) 每轮存历史 | 每轮完成后存 `Result/Result_{ts}_Run{n}/`（DetectResults.json + VmOverlays.json + DetectPositions.json） | 时间戳前置保证历史窗口排序/解析；`HistoryResultWindow` 解析改为正则提取时间戳 |
| d) 每轮明细表 | 每针一块：左单元格 "id kind Pos X Y"（垂直居中）+ 右侧 10 行 dx dy h（漏检轮空） | 见窗口下半区 |
| e) 汇总格式 | 汇总明细表：id kind Pos X Y ˉdx ˉdy ˉh ∂dx ∂dy ∂h（ˉ=σ 标准差，∂=极差） | 已去掉 PINID 列 |
| f) 按 kind 细分 | 极差分布表动态列：行=等级（良好→严重），列=全部+A/B/C... | 旧报告 KindRanges 为 null → 仅显示"全部"列 |

### ⚠️ 待用户确认/验证
- **旧报告兼容**：旧 report.json 打开时 Samples/KindRanges 为 null，窗口已容错（每轮明细空行、分布表仅全部列）
- 每轮明细的 H 列展示原始值（含 0）；汇总 σh/∂h 已排除 0 —— 与需求 b) 一致，但明细中 0 值轮次是否要标灰/隐藏待实机确认
- **汇总表 H=0 排除后的 σh 数值**可能比旧版小很多，属预期（需求 b 目的）

## 二、Task2 流程改造（已实施）

| 项 | 实现 | 位置 |
|---|---|---|
| a) End阶段重试 3→2 | `max3DRescanRetry = 3 → 2`（板末重扫每次失败重拍的上限，覆盖 GrabTile/Merge 全部重扫分支） | `DetectionFlow.cs:283` |
| b) 结果表头加 kind | 表头改为 `pinId,kind,dx,dy,h,rs(ok/ng),pinX,pinY,padX,padY,pos（检测位号）,pinIdx（位号）` | `MainWindow.xaml` dgPinResults |
| c) 全图采集前置基板判断 | 抽出 `RunBoardEntryGateAsync`（规则a-d）供检测/全图采集共用；`GrabFullImage` 改走前置流程 | `RunCommands.cs` + `TransportFlow.cs:862` |
| d) init 顶板→100ms→阻挡 | 检测流程与全图采集两处：`Task.WhenAll(Clamp1,Clamp2,路径,曝光)` → `Task.Delay(100)` → `Block` | `DetectionFlow.cs:398-407`、`CollectionFlow.cs:176-182` |

### ⚠️ c) 需实机验证的点
- **运送后回零顺序**：全图采集前置流程运送基板 → `StartFullImageGrabFlow` 内部 `CheckAndHomeAxesAsync(true)` 回零检查。轴已回零时无动作（正常）；若轴未回零会**在板已就位后回零**，可能扰动板位——原"直接采集"路径也有此风险，非本次引入，但走运送路径后概率场景不同，实机注意
- 全图采集与检测共用前置流程：`isWorkFlowRun[TransportBoard]` 与采集互斥判定在 CheckSafeOperation/StartFullImageGrabFlow 内，实机确认运送期间点「开始」检测的互斥表现

## 三、Bug 修复（已实施）

| # | 问题 | 修复 |
|---|---|---|
| 1 | "共多少针"显示最后一个 PinId | 改为去重后 `PinResults.Count`（去重 Dispatcher 已 await）`DetectionFlow.cs:1859-1864` |
| 2 | 历史结果保留总针数 | **设计方案已与用户确认**：JSON 加 PinCounts（存 `ResultMeta.json`，随 DetectResults.json 一起写），显示在**检测结果列表标题旁**（`检测总数：N`，MainWindow.xaml 标题行）；`LoadResultFromDir`/`LoadPinResults` 读取，旧历史回退结果数 |
| 3 | 检测位标记 A/B | ① 显式 `box=false` 无背景（配置 MarkerTextBgColor 才带背景）；② 字符间距用新参数 `MarkerTextMargin`（原硬编码 18px）；③ 个性化设置新增 针型字号大小(已有 MarkerFontSize)/针型文字背景(MarkerTextBgColor)/针型文字边距(MarkerTextMargin)；④ 个性化设置窗口 520→660 高（用户已确认指此窗口） |

### ⚠️ Bug3 实机反馈修正（2026-08-27 当天，最终方案=回退 disp_text box）
- **最终方案（性能优先，已定稿）**：`MainViewModel.cs:1103-1137` 用 **disp_text 原生 box**（非自绘）：
  - 未配置 `MarkerTextBgColor` → `box=false` 无背景；配置后 → `box=true + box_color`
  - 字符间距 = `基准字号 + MarkerTextMargin`（固定图像坐标值，不乘缩放因子 → 缩放不漂移、焊在图上）
  - **box 按字体行盒计算、比大写字母大，为 Halcon 固有行为，接受此观感**
- **修正1（保留）**：字符间距固定图像坐标值（`MarkerFontSize + MarkerTextMargin`），不用换算后字号——缩放时焊在图上（原 `mi*18` 同性质）
- **修正2/3/4（已回退，勿再采用）**：自绘背景方案（`get_string_extents`+`DispRectangle1`、`set_tposition`+`write_string`）虽能精确贴合文字/焊在图上，但**每字符多次算子调用，缩放/拖动窗口时性能差**（用户实测后回退）。其技术要点记录于 git diff/当日日志，如未来需要精确贴合背景且性能允许再评估

## 四、Task3 实时取流方案评估（讨论结论，未改代码）

**结论：SDK 实时取流与 VM 占用相机存在硬冲突，不可同时；降级方案（SET_PATH+GRAB 轮询）技术可行但帧率仅 ~2-3fps，可作"手动微调模式"用，非视频级实时。**

### 冲突本质
- 海康 MVS SDK 打开相机默认独占（GigE 控制权/USB 独占）；VM 相机模块同样经 SDK 打开同一相机 → 两进程同时打开必冲突（控制权互抢/设备占用报错）
- GigE Vision 理论上控制/流通道分离，但 MVS SDK 不开放"仅订阅流"的裸流 API → 无法绕开

### 降级方案（SET_PATH + GRAB 轮询 + Halcon 窗口）可行性
| 维度 | 结论 |
|---|---|
| 帧率 | **~2-3fps 硬上限**：GRAB 同步指令 VM 处理实测 ~300-400ms（偶发 4.5s，见 8-25 交接），50ms 间隔发 GRAB 会命令堆积；实际单帧全链路（GRAB+等文件就绪 185ms）~400-600ms |
| 存图目录 | **必须用专用目录**（如 D:\LiveGrabCache），严禁用 Origin——实时存图会污染 Origin 图片计数（ValidateImageCount 容错校验 + 瓦片排序） |
| 文件清理 | 可行：GRAB 前删旧图→GRAB→等就绪→读图显示→删图，形成单帧循环，目录恒 ≤2 张 |
| 互斥 | 实时取流期间 VM 忙 GRAB，2D 检测必须暂停 → 独立"微调模式"，退出恢复 SET_PATH |
| 与 3D 链路 | 3D 扫描走独立相机/下位机，不受影响 |

### 备选（如需真视频级）
1. VM 连续取流方案 + VM 窗口句柄嵌入上位机（依赖 VM 能力，需向海康确认）
2. 上位机 MVS SDK 直连取流 + Halcon 显示，与 VM 检测**互斥切换**（切换需重开相机 ~数秒）
3. 与海康确认相机是否支持双应用/多播流共享

**建议**：若用途为看板/找料/视野微调（慢交互）→ 降级方案够用，落地时按上文要点实现；若要视频级 → 立项与海康确认方案 1/3。

## 五、本次未做 / 遗留
- **编译/提交**：✅ 2026-08-27 14:28 用户本地 `_build.bat` 通过（bin/x64/Debug/RBLAOI.exe，含 PrecisionReportWindow 新 XAML / RunBoardEntryGateAsync / MainWindow.xaml 新列 / 检测耗时 / validStats 过滤）；10:27 已 commit `0ad8175` 推送，其后新增"检测耗时显示(11:44)+精度报告 validStats 过滤(14:10)"4 文件（DetectionFlow.cs/RunCommands.cs/MainViewModel.cs/MainWindow.xaml）**未提交**，下会话先提交
- Task3 未改代码（讨论结论如上，落地需新会话单独排期）
- 精度检测参考轮漏检边界（第一轮漏检的针不进统计）——如需按"所有轮去重聚合"改进，另立项（用户 14:52 询问含义，未定是否做）
- 模拟器数据 4 个可疑点待修（见五B，不阻塞上位机）
- 环境坑沿用：git 写 refs/heads/fix/ 被沙盒拦截，`git log --oneline -1` 异常时 reflog 重建

## 五B、模拟器数据优化 + ✅ 量纲问题澄清（2026-08-27 用户确认，勿再修上位机）

### 模拟器改造（已实施，`C:\Users\59538\Desktop\f407_gui_simulator.py`）
- **分针型**：`_rand_2d_blob` 按 `PIN_TYPE_CFG`（A/B/C/D，各型 2-4 针，焊盘尺寸不同）生成 `padA|padB|..|blankA|blankB|..|pinA|pinB|..` 多段（上位机正则按标记分组已兼容）
- **高度有数据**：`_rand_3d` 与最近一次 2D BLOB 正常针**一一对应**生成（记录 `_last_2d_pins`）：
  - 坐标反推：`VmX(mm) = px×mmPerPx − actFovW/2`、`VmY(mm) = actFovH/2 − py×mmPerPx`（与上位机 2D mm 换算公式对齐 → 匹配 dist≈0.05-0.13mm）
  - 高度按针型：A=1.0 / B=1.5 / C=2.0 / D=0.8mm ±0.05mm（须与方案 PinTypeParams.IdealHeight 对应，否则 hOk 判 NG）
  - 常量 `MM_PER_PX=0.004780825`、`CAM_RES_W/H` 与上位机系统参数对齐；无 2D 记录时回退旧随机行为
- 验证：等价逻辑脚本通过（分针型分段 ✓ 匹配距离<1mm ✓ 高度按针型 ✓）

### ✅ 量纲问题澄清（2026-08-27 14:52 用户确认）：实机匹配正常，上位机**无 bug，勿改**
- **更正此前误判**：代码核实 `Wait2D` 内 L755-773 `rawPins2D = vmConverted` 已将 X/Y 覆盖为 **mm 坐标**（`X = displayX = fovLeft + px×mmPerPx`，Y 同理），因此：
  - L1200-1204 匈牙利匹配 `p2.X`(mm) vs `c.BoardX`(mm) → **同量纲** ✓（非"像素 vs mm"）
  - L1315-1316 `pin2D.Dx`（L758 已为 dxMm，mm）vs `xyTol`(mm) → **同量纲** ✓
- **实机匹配正常**（8-21 至今实测 dist 平均 ~1.57mm）；模拟器数据与实机不一致才是根因，**要修的是模拟器**
- **模拟器数据可疑点**（下次改模拟器时处理，不阻塞上位机）：
  1. 3D 生成未包含 `VmToBoardOffsetX/Y`（上位机 L1167-1168 会加，若系统参数非 0 → 匹配距离=|offset|）
  2. 针尖偏移 `±0.3×焊盘宽`（L802-803）→ C 型 50px≈±0.072mm / D 型 25px≈±0.036mm，可超 `XyTolerance`（≈0.05mm）→ 部分针误判 NG
  3. 2D y 像素范围 `[1824−2000, 1824+2000]=[−176, 3824]` 可越界/负值 → 越界针被上位机有效区过滤（丢针）
  4. 无 2D 记录时 3D 回退 `±20000µm` 随机点（L844-846）→ 与 2D 完全对不上 → 匹配必失败 H=0

## 六、验证清单（用户实机/本地）
1. ✅ `_build.bat` 编译通过（2026-08-27 14:28，无 CS 错误）
2. ✅ 精度检测 10 轮：每轮历史目录生成（Result_{ts}_Run1..10）、报告汇总无 PINID、σh/∂h 明显小于旧版（H=0 排除生效）、每轮明细 10 行、kind 分布表分列正确
3. ✅ 检测完成状态栏"共 N 个针"= 去重后数量；历史结果加载后标题旁显示检测总数
4. ✅ 检测位选择：标记无背景、间距随 针型文字边距 变化、字号生效
5. ✅ 全图采集：工作位有料直接开始；入口有料自动运送后采集；入口无料等待放板
6. ✅ init 时序：顶板→100ms→阻挡（可看 IO 日志/示波器）
7. ✅ 连续模式 End 重扫最多 2 次（构造 3D 异常位验证）

> 以上 1-7 项**全部完成**（用户 2026-08-27 14:52 确认）。
