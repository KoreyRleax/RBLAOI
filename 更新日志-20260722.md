## 2026-07-22：多类型针标记工具栏设计

### 背景
方案支持多种针型（特征个数 PluginFeatureCount，1~10），需要在 Halcon 窗口选择检测位时标记该位检测哪种针型。

### 实现方案
1. 工具栏嵌入在 Halcon 窗口的 Border 内部上方（Grid 分两行，避免 Airspace 冲突），CAD 风格
2. 标记字母 A~J 动态生成，颜色预定义 10 色调色板
3. 同一检测位支持多标记叠加（PinTypes="AB"）
4. 删除/清空按钮清除标记
5. 右键菜单"在界面选择检测位"改为子菜单

### 改动文件
- `Models/DetectPosition.cs` — 增加 PinTypes
- `Models/GrabPosition.cs` — 增加 PinTypes
- `ViewModels/MainViewModel.cs` — 标记模式管理、绘制
- `Core/Controls/CustomHSmartWindowControl.cs` — 右键子菜单
- `Views/MainWindow.xaml` — 工具栏布局
- `Views/MainWindow.xaml.cs` — 工具栏交互
