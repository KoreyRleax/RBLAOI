# 检测流程状态机重构 —— 交接文档

> 首次生成：2026-08-19 ｜ 最近更新：2026-08-25（单检测位耗时优化 1a/2/3/5/8 全部实施 + 模拟器验证）
> 状态：**文件级拆分 ✅ ｜ 暂停/复位问题 ✅ ｜ 单检测位耗时优化 ✅（1a/2/3/5/8 已实施，模拟器 A 段 2013→1517ms）｜ 类级抽取（Core/Flows）待做**
> 分支：`fix/multi-pintype-blob-parser`（HEAD=`7c9e27f`，**已同步远程**；8-21 另有 `交接文档-20260821.md` 记录 3D 超时/快照兜底；8-24 耗时优化见 §9 与 `更新日志-20260824-单检测位耗时优化.md`）
> 本文件是重新开会话时的"出生证明"：先读 §7 约束与风险（git bug/编译/沙箱），再读 §3 现状与 §6 剩余待办

---

## 1. 背景与目标

RBLAOI（AOI 视觉检测，WPF / .NET 4.7.2）的 `MainViewModel` 曾达 **8755 行**（God Class），其中**检测流程状态机约 2144 行**、传送带/连续模式状态机约 1438 行，全部内聚在 ViewModel 内。

架构审视结论（2026-08-19 会话）：
- ✅ 分层（Core/Models/ViewModels/Views）清晰，问题只在 **MainViewModel 过载**
- 重构三步走：**① 清理废弃功能（已完成）→ ② 文件级拆分 partial（已完成）→ ③ 把状态机抽成 Core/Flows 独立类（本任务，未做）**

**本任务目标**：把检测流程状态机（及传送带流程状态机）从 `MainViewModel` 中抽为 `Core/Flows/` 下的独立类，行为零变化。暂停/复位已先期完成（见 §2/§6），类化时保持新机制不变即可。

### 为什么是现在（"多了再抽"的触发点）
项目既有决策（`.claude/memory/flow-state-machine-pattern.md`）：状态机内聚 VM 是为直接访问 UI 上下文（Dispatcher/绑定属性/弹窗/状态栏），"**代码多到内聚成本 > 接口化成本时再抽**"。当前：
- 检测流程单文件已 **> 2400 行**（拆分后最大文件，且 8-19 加了暂停感知代码）
- 检测流程状态机与检测动作/3D 解析混在一个 partial
- 暂停/复位已动作级化（PauseAwareWaitAsync 模式），类化时接口面更清晰

---

## 2. 已完成的工作（git `fe770df` + `c3ec443`，均已推送）

### 2.1 清理 + 文件级拆分（fe770df，2026-08-19）
| 项 | 内容 |
|---|---|
| 清理 | 删除 YOLO 检测（YoloDetector/best.pt/yolo_detect_server.py/pin6.py + VM 内全部 YOLO 分支）、IAP 固件升级（IAPWindow/IAPService/残留/菜单）、SysParam 检测模式去 "YOLO方案" |
| 拆分 | MainViewModel 8269 行 → 主文件 1510 行 + 7 个领域 partial（见 §3.1） |
| 归类 | `UserGlobalScript.cs` → `VmScripts/`；csproj 移除 `Core\Flows\` 空文件夹引用 |

### 2.2 暂停/复位动作级大修 + 演示运板 + 图标（c3ec443，2026-08-19）
| 项 | 内容 |
|---|---|
| 暂停立即停止 | 新增 `PauseAwareWaitAsync<T>(task, token, isPaused?)`（50ms 轮询暂停标志）下沉到**动作级**：移动/调Z/GRAB/2D等待/等文件全部可中断（≤50ms 转 Paused）；**3D 扫描不打断**（保证扫完） |
| 抓图流程 | GrabOneAsync / GrabAndReplaceTileAsync 内部每步暂停检查（暂停中断返回 null，外层先查暂停再判 Err）；GrabAndReplaceMultiAsync 板面图后/针尖图间检查（暂停不再抓后续图） |
| 复位立即终止 | ResetMachine **不提前置 `_continuousCts=null`**（修复二次复位把 `_continuousAbort` 覆盖回 false 的竞态）+ `_continuousCts` 加 volatile + 补 `StopMotorAsync()` 停传送带 |
| 单板运送阶段暂停 | PauseCheck/PauseCheckOnly 判定扩展到 TransportBoard/SendToExit；StartOrResumeCheck 支持运送阶段暂停恢复 |
| 演示模式运板 | DemoLoopAsync 复用 `SingleBoardCheckFlowAsync(token)`（规则a-d：工作位有料→直接检测；入口有料→运板→检测；入口无料→等待放板→运板→检测）；`SingleBoardCheckFlowAsync` 加可选 CancellationToken（演示取消后不启动新检测）；演示分支暂停恢复判定同步扩展 |
| 全图采集 | MoveToPos/GrabImage 加暂停感知；**GRAB 补传 token**（原来无 token，复位时要等拍照超时） |
| 图标 | 主界面顶部栏 `TitleBarControl` 绑定 `Assets/AppIcon.png`（`<Window.Resources>` 定义 BitmapImage + `IconSource="{StaticResource AppIcon}"`；子窗口表情图标不受影响） |

编译/验证：**devenv.com 编译未跑**（沙箱拦截），代码通过 **Roslyn 语义验证**（见 §7.4）。**待用户用 devenv.com 编译 + 模拟器/实机回归**。

---

## 3. 现状：代码位置与结构

### 3.1 相关文件（拆分后 + 8-19 修改后）
| 文件 | 行数(约) | 内容 |
|---|---|---|
| `ViewModels/MainViewModel.cs` | 1510 | 类头、字段/属性/事件、串口通信、连接检查、ResetMachine、WaitOrCancelAsync、辅助方法 |
| `ViewModels/MainViewModel.DetectionFlow.cs` | **~2400+** | **检测流程状态机**（StartCheckFlow）+ VM 3D 解析/坐标转换 + 检测动作 + 结果解析/统计/叠加层持久化；含 `PauseAwareWaitAsync` |
| `ViewModels/MainViewModel.CollectionFlow.cs` | ~530 | 全图采集三层状态机；**含共享枚举 `FlowRunState`/`CheckState`/`GrabState`**（partial 类私有成员跨文件可见） |
| `ViewModels/MainViewModel.TransportFlow.cs` | ~872 | 传送带流程状态机（`TransportState`/`ExitState`/`EntranceState`）+ 轨宽检查 + FlowLoopAsync + CheckSafeOperation |
| `ViewModels/MainViewModel.RunCommands.cs` | ~1500 | 运行命令 + **连续模式状态机（`ContinuousState`）** + 精度检测 + GrabOneAsync/GrabAndReplaceTileAsync/GrabAndReplaceMultiAsync + SingleBoardCheckFlowAsync + DemoLoopAsync |
| `ViewModels/MainViewModel.MenuCommands.cs` / `Project.cs` / `VisionOverlay.cs` | 719/748/290 | 菜单命令 / 方案+VM+检测位管理 / 叠加层绘制 |
| `Views/MainWindow.xaml` | — | Window.Resources 定义 AppIcon；TitleBarControl 绑定 IconSource |

### 3.2 检测流程状态机结构（类化前必须理解）
- **入口**：`StartCheckFlow(bool isSubFlow = false)`（DetectionFlow.cs）
- **三层状态**（互不混用，既定决策）：
  1. 设备状态 `DeviceMonitor.DeviceState`（全局呈现，轮询线程更新）
  2. 壳层 `FlowRunState { Init, Running, Paused, Terminated }`——管生命周期（暂停等待 + 终止 CTS）
  3. 业务层 `CheckState`（Init→CapturePos→GrabTile→Start2D→MoveScanStart→StartScan3D→WaitScan3D→Wait2D→Merge→NextPos→End/Err）
- **驱动模式**：一个方法内两个局部函数——`RunStep`（壳层 switch，调 `BizStep`）、`BizStep`（业务 switch，每拍一件事）；`FlowLoopAsync(RunStep)` 公共 while 驱动
- **跨状态传参**：**闭包局部变量**（`pos`/`detectX`/`rawPins2D`/`_vmDetectTask`/`_3dScanTask`/`response`/`scan3DTimeout`/`pendingPauseTask` 等）——类化时须改为类字段或方法参数
- **并行**：StartScan3D 不 await（`_3dScanTask` 句柄），WaitScan3D 等待；2D 检测 `_vmDetectTask` 与 3D 并行；3D 扫描带重试（总尝试 2 次）
- **暂停（8-19 新机制）**：
  - `PauseAwareWaitAsync<T>(task, token, isPaused=null)`（DetectionFlow.cs 类级方法）：50ms 轮询暂停标志（默认 `_pauseCheckEvent`，可注入 `_pauseGrabEvent`），暂停/取消时立即返回 default；在途任务不打断（后台收尾）
  - 暂停中断后转 Paused 前把在途任务记入 `pendingPauseTask`，**壳层 Paused 恢复前等其收尾 ≤3s**（防恢复后整拍重做与旧任务并发写同一图片）
  - 长动作：移动/调Z/GRAB/2D等待/等文件均用 PauseAwareWaitAsync；3D 扫描等待仍用 WaitOrCancelAsync（不响应暂停）
- **终态陷阱**：返回条件用 `endDone` 标志，**不能写 `bizState == End`**（Err 分支同理，弹窗/日志在循环外统一处理）
- **枚举共享**：`FlowRunState`/`CheckState` 定义在 CollectionFlow.cs（partial 类私有成员跨文件可见）

### 3.3 暂停/复位/停止按钮语义（勿漂移）
- 固定"**开始**"=检测专用；"**停止**"=纯暂停（不切换）；"**复位**"=先 Terminated（壳层取消+唤醒）再物理复位，不等待流程退出
- 暂停实现：`_pauseCheckEvent.Reset()`（ManualResetEventSlim）+ 各长动作 PauseAwareWaitAsync 轮询中断；恢复 `Set()`
- 复位实现（MainViewModel.cs ResetMachine）：取消各 CTS（_checkCts/_grabCts/_continuousCts/_demoCts/_precisionCts）+ 唤醒暂停等待（Set 两事件）+ `_continuousAbort` 置位（**不置 null**）+ `ResetWorkFlowStates()` + 报警复位 + Flush + StopMotorAsync + ResetIO + 回待机位
- 报警：DeviceMonitor 轮询线程触发 → 置标志 + 取消各 CTS + 唤醒暂停等待 + Flush

---

## 4. 目标架构（Core/Flows 独立类）

```
Core/Flows/
├── DetectionFlow.cs      // 检测流程状态机（由 DetectionFlow partial 抽出）
├── TransportFlow.cs      // 传送带流程状态机（由 TransportFlow partial 抽出）
└── (可选) ContinuousFlow.cs  // 连续模式状态机
```

**类化方案**（保留既定"直接调用+局部函数+FlowLoopAsync"模式，勿改成轮询框架）：

1. **定义上下文接口**（解决"UI 上下文够不着"的核心问题）：
   - `IFlowContext`：Dispatcher（UI 线程调度）、UpdateStatus、弹窗（MessageBox）、暂停事件（_pauseCheckEvent/_pauseGrabEvent 或方法化）、按钮状态
   - `IFlowServices`（可构造注入）：MotionIO / Vision2DClient / Vision3DClient / DeviceMonitor / SysParam / ProjectManager / FullImageManager
   - 建议拆小接口而非一个大胖接口
2. **先抽零 UI 依赖部分**（零风险第一步）：VM 3D 结果解析/坐标转换（`ParseDetect2DResponse`/`ParseDetect2DBlobResponse`/`ParseVMDetect3DResponse`/`MatchTipToBoard` 等纯计算）→ 普通静态类 `Core/Vision/DetectResultParser.cs`
3. **检测流程类化**：`DetectionFlow` 构造注入服务 + IFlowContext；闭包变量→类字段；BizStep/RunStep 局部函数→类方法；`StartCheckFlow` 保留为公开入口；**`PauseAwareWaitAsync`/`pendingPauseTask` 机制随迁并保持**
4. **MainViewModel 瘦身**：按钮命令（开始/暂停/复位/停止）委托给 `DetectionFlow`/`TransportFlow`；保留 UI 绑定属性
5. **传送带/连续模式类化**：同模式；注意与检测流程的互斥（`isWorkFlowRun` 位集）与状态联动

---

## 5. 建议实施步骤（渐进、每步可编译可验证）

1. **§4.2 纯解析类抽取**（0 UI 依赖，风险最低）→ 编译 → 模拟器回归
2. **定义 IFlowContext/IFlowServices 接口**（先按 DetectionFlow 实际依赖反推）
3. **DetectionFlow 类化**（主体搬迁，行为不变，含暂停新机制）→ 编译 → 模拟器回归（检测/暂停/复位）
4. **TransportFlow/ContinuousFlow 类化**（如有余力）
5. **移除临时启动诊断**：`App.xaml.cs:22-28` 的 `startup_error.txt` 落盘代码（沙箱段错误已确认是环境问题、用户桌面运行正常，可安全移除）
6. 全量模拟器回归 + 写更新日志

---

## 6. 待办与已知问题

### 6.1 ✅ 已解决（2026-08-19，c3ec443，**待用户实机/模拟器回归确认**）
| 问题 | 解决方式 |
|---|---|
| 暂停不立即停止（单板/连续） | PauseAwareWaitAsync 动作级中断（移动/调Z/拍照/2D等待 ≤50ms）；3D 扫描保证扫完 |
| 连续模式复位无法终止 | ResetMachine 竞态修复（不提前置 null + volatile）+ 复位停传送带 |
| 单板运送/出板阶段暂停无效 | PauseCheck/PauseCheckOnly/StartOrResumeCheck 判定扩展 |
| 演示模式跳过运板 | DemoLoopAsync 复用 SingleBoardCheckFlowAsync（规则a-d + 取消令牌） |
| 全图采集拍照不响应复位 | GRAB 补传 token |
| 主界面顶部栏未应用图标 | MainWindow.xaml 绑定 AppIcon（Assets/AppIcon.png） |

### 6.2 ⬜ 剩余待办（核心重构，§4/§5）
1. 纯解析类抽取（DetectResultParser）
2. IFlowContext/IFlowServices 接口
3. DetectionFlow 类化（Core/Flows）
4. TransportFlow/ContinuousFlow 类化
5. 移除 App.xaml.cs 临时诊断
6. 全量模拟器回归 + 更新日志
7. **单检测位执行时间优化**（分析 8-24 §9；**1a/2/3/5/8 已实施并推送 7c9e27f**，模拟器 A 段 2013→1517ms；剩余 1b/9/10/11/6/7 见 §9.2）——**待用户实机验证**：画面目视（优化5）、XY/Z 并发机构安全（优化8）、暂停/复位回归（优化3）

---

## 7. 约束与风险（必读）

1. **`MotionIO.cs` 禁止修改**（除新增运动指令封装，且须在更新日志注明）——动作中断若需新指令须先与用户确认
2. **状态机模式是既定决策**：直接调用 + 局部函数 + FlowLoopAsync；**不建轮询框架**；流程级并行须用户确认（记忆 flow-state-machine-pattern.md）
3. **终态分支陷阱**：用 `endDone` 标志，勿用 `bizState == End` 做返回条件
4. **按钮语义固定**：开始=检测、停止=纯暂停、复位=先 Terminated 再物理复位；勿漂移
5. **三色灯只随设备状态**（2026-08-14 拍板）：红灯=报警、黄灯=未就绪/就绪/手动、绿灯=运行/暂停/演示
6. **UI 线程**：Dispatcher 调用必须保留（叠加层绘制、PinResults 集合操作等）
7. **git 环境 bug（本机 PortableGit 1.2.0，实测，严重）**：
   - commit 写 `.git/refs/heads/` 静默失败 → commit 后必须 `git rev-parse HEAD` 验证，失败则手动写 ref（hash 从 `.git/logs/HEAD` 尾行取）
   - **`git push`/`fetch`/`pull` 都可能删除 refs 目录**（8-24 实测：fetch 删 `.git/refs/remotes/origin/`、pull 删 `.git/refs/heads/fix/`，`rev-parse HEAD` 报 unknown revision）→ 每次写操作后检查 `git rev-parse HEAD` + `find .git/refs -type f`；缺失时 mkdir 重建 + 手写文件：
     - 本地分支 ref：hash 从 `.git/logs/HEAD` 尾行取 → `mkdir -p .git/refs/heads/fix && printf '<hash>\n' > .git/refs/heads/fix/<branch>`
     - 远程跟踪 ref：hash 从 `.git/FETCH_HEAD` 取（未 fetch 时从 packed-refs 取）→ `mkdir -p .git/refs/remotes/origin && printf '<hash>\n' > .git/refs/remotes/origin/<branch>`
     - ⚠️ **ref 文件必须写全量 40 位 hash**——写短 hash（如 78e6ffb）会导致 `rev-parse HEAD` 仍报 unknown revision（8-24 实测）
   - **禁用 `git rm`**（连带删同目录文件）；**避免 `git checkout <commit>`/`git stash`**（会删整个 refs/ 目录甚至对象库，8-19 两次翻车重建仓库）
   - 删除文件用普通 `rm` + `git add -A`
   - **每次 git 写操作后检查 refs 目录完整性**（`ls .git/refs/heads/` 与 `ls .git/refs/remotes/origin/`）
8. **编译方式**：`devenv.com RBLAOI.sln /Build "Debug|x64"`（MSBuild.exe 被 WorkBuddy 沙箱 LOLBin 策略拦截；dotnet build 对旧式 WPF 有 GenerateResource 任务宿主限制）——**沙箱内无法代跑完整编译，须用户本地执行**
9. **沙箱内代码验证**：Roslyn 语义验证可行（见 §7.4），替代完整编译
10. **推送**：须用户确认后才推送；当前 `7c9e27f` 已同步（本地=远程）

### 7.4 沙箱内验证代码的方法（Roslyn，已用 3 次）
- MSBuild/devenv 被沙箱拦截 → 用 `dotnet` + Roslyn 做语法/语义级验证：
  1. 临时目录建 `net9.0` console 项目，加 `Microsoft.CodeAnalysis.CSharp 4.8.0`
  2. 收集项目全部 `.cs`（排除 obj/bin），引用 `C:\Program Files (x86)\Reference Assemblies\...\v4.7.2\*.dll` + `bin\Debug\*.dll`
  3. `CSharpCompilation` 编译，过滤环境噪声（`GlobalCameraModuleCs` 等第三方命名空间缺失、非托管 DLL 的 CS1509/CS0009），检查**目标文件**错误数 = 0
  4. 用完删除临时目录
- 注意：XAML 错误（MainWindow.xaml 等）Roslyn 查不到，改动 XAML 后须用户编译确认

---

## 8. 相关资源

- 架构决策：`.claude/memory/flow-state-machine-pattern.md`（状态机三层架构/按钮语义/终态陷阱）
- 会话检查点：`.claude/memory/session-checkpoint-20260814.md`
- 模拟器手册：`.claude/memory/auto-test-playbook-20260814.md`、`simulator-test-rules-20260814.md`
- 项目说明：`CLAUDE.md`（项目结构/环境警告/配色/记忆索引）
- 工作日志：`.workbuddy/memory/2026-08-19.md`（清理拆分/暂停复位/图标/演示运板/推送+git bug 全过程）、`2026-08-20.md`、`2026-08-24.md`（同步远程/git bug 修复/单检测位耗时分析）
- 旧项目参考：`D:\MyWork\Account\1_BEST\RBLWorkMin\Rayble\Common\CMcRblR9AoiMotionIO.cpp`（RivetPin2019 暂停机制：1ms 扫描线程 + 非阻塞状态机 + 状态跳转暂停，本机 PauseAwareWaitAsync 即向其靠拢）
- 代码：`ViewModels/MainViewModel.DetectionFlow.cs`（主战场，~2400+ 行）、`CollectionFlow.cs`（枚举与三层模式参照）、`TransportFlow.cs`、`RunCommands.cs`（连续模式/精度/抓图动作/演示/单板前置）

---

## 9. 单检测位执行时间优化（2026-08-24 分析；1a/2/3/5/8 已实施，模拟器 A 段 2013→1517ms）

> 结论先行：**可优化空间约 1.0~1.8s/检测位（占 7.5s 的 13%~24%），全部在"拍照→扫描前"A/C 链路的非物理开销；SCAN3D 4.1s 为硬件扫描时长（23mm @ 7mm/s），不动扫描速度不可压缩。**

### 9.1 时间线（基于 8-21 实测 7.1~7.8s/位）
| 段 | 位置 | 耗时 | 性质 |
|---|---|---|---|
| 移动中心点 | RunCommands.cs:987 | ~0.8s | 物理 |
| GRAB 拍照 | RunCommands.cs:1000-1004 | ~0.6s（含固定 delay 300ms） | 部分可压缩 |
| 等文件+替换+拼接重绘 | RunCommands.cs:1019-1113 | ~0.7s | 部分可压缩 |
| 移到 3D 扫描起点 | DetectionFlow.cs:559 | ~0.5s（与 2D 并行） | 物理 |
| SCAN3D 扫描 | MotionIO.cs:828 | **~4.1s** | 物理瓶颈 |
| 3D 结果+合并 | DetectionFlow.cs:766-808 | ~30ms | 已最优 |

已做的优化（勿回退）：2D 与 3D 并行、显示刷新异步化（8-21）、快照轮询提前判完成（8-21）、数据等待 2s 窗口（8-21）。

### 9.2 已识别的优化点（按建议顺序）
> ✅ 2026-08-24 已实施：**1a/2/3/5/8**（详见 `更新日志-20260824-单检测位耗时优化.md`）。模拟器验证（Blob 方案）A 段演进：**2013（8-19 基线）→2002（1a 漏 Blob）→1580（1a Blob 补删）→1517（+优化5）**。
> ⚠️ **优化 5 收益澄清**：模拟器上 TileImages 重拼本来就快（<100ms），异步化仅 -63ms——A 段真瓶颈是**移动/调Z + ReadImage 19MB×2 同步缓存更新 + delay100×4**。优化 8 已实施待验证（**需确认 XY/Z 并发机构安全**）。
| # | 优化点 | 位置 | 收益/拍 | 风险 | 状态 |
|---|---|---|---|---|---|
| 1a | 删移动前 `Task.Delay(200)`（纯白等，MoveToAndWaitOK 已等轴完成） | RunCommands.cs:985 | **200ms 确定性** | 零 | ✅ 已实施 |
| 2 | 删诊断 `Directory.GetFiles`×2（8-14 排查遗留，Origin 目录全枚举） | DetectionFlow.cs:424,476 | 10~400ms（目录规模相关，期望 ~100ms） | 零（仅影响日志） | ✅ 已实施 |
| 3 | `PauseAwareWaitAsync` 50ms 轮询尾差 → `Task.WhenAny` 立即返回 | DetectionFlow.cs:1665-1677 | 平均 150ms（6 调用点/拍 × 平均 25ms，Blob 模式更多） | 中（全流程共享方法：检测/采集/传送带 14 处，须回归暂停/复位/取消） | ✅ 已实施 |
| 5 | ReplaceTile 重拼+显示异步化（同步只更新瓦片缓存；后台 Task.Run 纯计算 + UI 线程显示替换；全图采集同步版保留） | FullImageManager.cs（调用方零改动） | 模拟器 ~60ms（重拼本来就快），实机待测 | 中（画面时序；_currentStitchedImage 读写收敛 UI 线程） | ✅ 已实施 8-24 16:40 |
| 8 | **移动中心(X/Y)+调Z 合并为一次 `MoveToAndWaitOK(x,y,z)`**（原两段串行；状态栏合并；调Z仅 focusZ>0 参与） | RunCommands.cs:1127（GrabOneAsync） | 实机估 100-500ms/拍 | 中（**需确认 XY/Z 并发无机构干涉**） | ✅ 已实施 8-24 16:55，待验证 |
| 1b | `Task.Delay(100)`（SET_PATH→GRAB 传播保险）+ 路径不变缓存跳过 SET_PATH | RunCommands.cs:1001,1000 | 0~100ms | 低（删过头 VM 会用旧路径存图，先确认 VM 是否有 SET_PATH 回执） | ⬜ 未做 |
| 9 | 调Z后 `Task.Delay(100)`×2 删除（MoveToAndWaitOK 已等 Z_COMPLETE） | RunCommands.cs:1151 | ~200ms | 低-中（可能是曝光前光学稳定保险，删前实测对比） | ⬜ 未做 |
| 10 | SET_PATH 路径不变缓存跳过（与 1b 合并） | RunCommands.cs:1000/1161 | 50~100ms | 低-中（需确认 VM SET_PATH 生效机制） | ⬜ 未做 |
| 11 | ReadImage 19MB×2（ReplaceTile 同步缓存更新）异步化 | FullImageManager.cs + RunCommands.cs | 200~400ms（实机待测） | 中（需串行后台链+竞态处理） | ⬜ 未做 |
| 6 | 文件就绪等待与"移到起点"并行 | RunCommands.cs:1019 + DetectionFlow.cs:559 | 模拟器 ~110ms，实机待定 | 中（结构改动大） | ⬜ 未做 |
| 7 | 提高 Scan3DSpeed（7→10mm/s） | 系统参数 | ~1.2s | 高（影响采样密度/精度，且 P2-3 标定偏差未解决，暂不动） | ⬜ 未做 |

### 9.3 收益量化（模拟器实测 + 剩余空间）
- A 段实测演进（Blob 方案）：**2013ms（8-19 基线）→ 2002 → 1580（1a 补删）→ 1517（+优化5）**，已实现 ~500ms/拍
- 优化 5 收益澄清：模拟器上重拼（TileImages）本来就快（<100ms），异步化仅 -63ms；**A 段剩余大头 = 移动/调Z + ReadImage 19MB×2 同步缓存更新 + delay100×4**（其中 9+10 低风险约 300-400ms 可挖）
- 17 检测位整板（~127s）：当前优化 ~8.5s/板，剩余可挖（9/10/11/6）约 0.5~1s/拍

### 9.4 建议实施顺序
1. **1a + 2**（确定性 ~300ms/拍，零风险）—— ✅ 已实施 2026-08-24
2. **3**（统计性 ~150ms，公共方法须全场景回归）—— ✅ 已实施 2026-08-24，**回归待做**
3. **5**（重拼异步化，画面时序待目视）—— ✅ 已实施 2026-08-24，收益被高估已澄清（模拟器 -63ms）
4. **8**（移动+调Z 合并）—— ✅ 已实施 2026-08-24，**机构安全确认待做**
5. **9+10**（低风险 ~300-400ms，下一步候选）；**11**（ReadImage 异步化，实机测过再定）；**6**（结构改动大）；**7**（待 P2-3 精度问题解决后与硬件一起决策）
