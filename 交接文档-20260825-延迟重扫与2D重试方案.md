# 交接文档 2026-08-25：延迟重扫实施现状 + 2D/GRAB 超时纳入重试队列方案（下会话主题）

> 交接给下个会话。分支：`fix/multi-pintype-blob-parser`（改动**未提交**）。
> 改动文件：`ViewModels/MainViewModel.DetectionFlow.cs`（延迟重扫 + 主题②A/C + End case 修复）、`ViewModels/MainViewModel.RunCommands.cs`（主题②A：文件就绪超时快查分流）、`Core/Motion/MotionIO.cs` + `Models/SysParamData.cs` + `ViewModels/SysParamViewModel.cs`（排队延迟优化）。
> 已实机验证：延迟重扫全链路通过（18:34 轮 4 位重扫 + 18:51 轮 1 位重扫均成功自愈，结果替换生效）。
> 下会话主攻：① **主题②改动的实机验证**（A 快查分流/GrabTile失败入队/2D超时入队/孤儿清扫，未编译未验证）；② **WaitForVmSaveTimeout 下调评估**（现 15s，A 方案后 3-5s 足够）。

---

## 一、已完成改动（未编译/未完整验证）

### 1. 排队延迟优化（ON/OFF 直发 + 短超时确认，设计稿《优化方案-20260825-排队延迟优化.md》）
- `MotionIO.cs` 新增 `SendCommandAndWaitDirectAsync`（直发不入队，Generation 校验 + finally 退订）；`Scan3DToAndWaitOK/Stop` 共 10 处 ON/OFF 改直发（:881/:912/:919/:958/:967/:980/:1000/:1008/:1017）
- `SysParamData.cs` 加 `Camera3DTriggerTimeout=500`；`SysParamViewModel.cs` 加 `[SysParam]`
- **实测达标超预期**（17:49 轮）：D 中位 2383→1776（-607ms）、总耗时 4456→3935；ON→OK 中位 30ms（排队消除）。遗留：ON→OK p90=440ms（下位机侧偶发处理延迟，与 RS-485 查询超时同源，见第五节）

### 2. 延迟重扫第一版（即时重扫 → 板末统一重扫，`DetectionFlow.cs` 9 处 + 后续修复）
- **主流程**：3D 数据异常（`mergeValidH3D==0` / `scan3DNoData` / 轴失败）不再即时重扫，`_rescanQueue.Add(i)` 记录 + 正常降级落表
- **板末**：End case + 循环外双入口进入重扫模式（`_rescanPhase`）：重扫位完整重拍 A 段（复用 GrabTile）→ Start2D → MoveScanStart → StartScan3D → Wait2D → WaitScan3D（**同步等扫描+数据，跳过 MergePending，严格串行**）→ Merge → NextPos 取队列
- **flowState bug（已修，未验证）**：主流程结束时 flowState=Terminated，循环外第二次驱动 `FlowLoopAsync(RunStep)` 直接走 Terminated 分支退出，重扫不执行（18:23:19 日志"循环外进入板末重扫：检测位 6"后直接"流程未正常完成"）。**修复：循环外驱动前 `flowState = FlowRunState.Running;`**
- **弹窗去除（已改，未验证）**：`WaitScan3D` 轴失败分支（L820-827）删 MessageBox（实测阻塞 5.3s），改 `_rescanQueue.Add(i)`（带 Contains 防重）
- **位2 条件放宽（已改，未验证）**：重扫条件 `mergeValidH3D == 0 && (mergeTotal2D > 0 || (vmRaw3D?.Count ?? 0) > 0)`——3D 有数据但 2D 全无（2D 检测失败）也记录重扫
- 产出《更新日志-20260825-延迟重扫.md》

---

## 二、MergePending 机制（下会话理解用）

```
StartScan3D(i) ──发 SCAN3D（不 await）──┐
Wait2D(i) ──解析2D──► MergePending(i-1)   │ 上一检测位数据后台到达
   └─ 恢复 i-1 上下文(_pending*) → 取数(剩余窗口) → Merge(i-1)
WaitScan3D(i) ──只等扫描完成（不等数据！）→ 快照 _pending(i) → NextPos
CapturePos(i+1) → GrabTile A段(~2s) ──期间 i 的数据到达──► ...
```

- 位置：`Wait2D` 后、`WaitScan3D` 前（`DetectionFlow.cs:839`）
- **剩余等待窗口** = `Math.Max(0, Math.Min(Wait3DResultTimeout, 2000) - 已过时间)`——数据在 A 段已到时等待≈0；数据异常（3D 数据不来）时**轴空等最长 2s**（位6 D=4633ms 实证）
- 重扫模式 `_rescanPhase` 时 Wait2D 后**跳过 MergePending**（严格串行），WaitScan3D 内同步取数

---

## 三、主题①：重扫位流程实机验证（**已通过**，log_20260825_183451）

**验证结果（18:34 轮 39 位，4 个异常位 5/6/15/23 + 18:51 轮 1 个异常位 34）**：
- 阶段①记录 → 阶段②循环外兜底入口 → 阶段③完整重拍（GRAB完成→SCAN3D→2D→[⏱]重扫D→合并明细）→ 阶段④队列推进至空 → 阶段⑤结果替换（位6 匹配3D=0→2、位23 0针→28针OK=27、位34 3D扫描失败重扫后恢复）
- **flowState 修复生效**（循环外兜底 L1655 `flowState=Running` 正常驱动重扫）
- **新发现（已修）**：End case 是死代码路径（BizStep L1595 同轮返回 → End 本体永不执行，"End 收尾检查"日志缺失实证）→ 加 `_endHandled` 修复（未编译）
- 效率：D 中位 1747ms（基线 2383→1778→1747 持续改善）；总耗时中位 4107（vs 3969，轮间波动无回归）；重扫 4 位合计 17.8s（跨位移动额外 ~2.1s，占比 ~1.2%）

验证方法（日志关键词）：
1. 主流程：`已记录板末延迟重扫`（WARN，记录位）
2. 板末：`[延迟重扫] 主流程结束，循环外进入板末重扫：检测位 N（队列剩余 M 位）`
3. 重扫位完整流程（关键）：`检测位 N 多针GRAB完成`（重拍）→ `检测位 N: SCAN3D` → `检测位 N 2D检测完成` → `[检测位 N 合并明细]`
4. 结果替换：该位 PinResults 从降级变新结果；`[去重]`/保存汇总无重复
5. 队列推进：`队列剩余 1 位` → 空 → `板末重扫完成，队列已空`

---

## 四、主题②：2D/GRAB 超时纳入重试队列（19:07 已实施 A+C，未编译验证）

### 背景（实测）
- **GRAB 响应偶发慢**：位13 板面 GRAB 4.5s（18:21:11.097 发 → 18:21:15.600 回复，正常 ~300-400ms）→ A 段 7606ms，轴停在拍照位等
- **存图等待**：文件就绪 184 样本中位 185ms / p95 342ms / max 1828ms（18:34 轮，0 超时）——但**位0 首拍偶发 15s 空等**（用户 WaitForVmSaveTimeout=15000，实机复现"停止再开始马上继续"= VM 存图了但上位机未感知）
- **2D 检测超时**：`GrabTimeout=10000` 内 VM 无响应 → 当前 GrabTile 拍照失败走 **Err 终止**（原 L493-497），整板流程中止——**这是最重的失败模式**

### 已实施改动（未编译，待 `_build.bat`）
1. **A 方案（核心 bug 修复）**：`RunCommands.cs` GrabAndReplaceTileAsync/GrabOneAsync 文件就绪超时后 **File.Exists 快查分流**——文件已在目标路径（阶段2 独占打不开/超时边缘写完=存图成功未感知）→ 按就绪继续；真不存在 → 路径未生效诊断（查 `_lastVmSavePath`）+ 返回 null → 入队板末重扫。**删除了 D:\GrabImage fallback**（用户确认 VM 无兜底存图机制，只存 ImageSavePath 指定路径）
2. **GrabTile 拍照失败不再 Err 终止**（DetectionFlow）：主流程 → `_rescanQueue.Add(i)`（防重）+ 该位跳过 2D/3D → 板末重扫重拍自愈；**重扫模式内失败 → 重试计数（≤max3DRescanRetry）后接受降级，不入队防死循环**
3. **2D 检测超时入队**（Wait2D TIMEOUT 分支）：主流程 → 入队；**重扫模式内不重拍**（该位 SCAN3D 在途，重拍会双发交错）→ 由 Merge 的"3D数据异常"分支统一重试（3D 扫完后安全）
4. **C 方案孤儿清扫**：`CleanupOrphanGrabbedFiles()` 在循环外终态收尾调用——删除 `GRAB_OK_*` 且不在 `GetImagePaths()` 索引内的残留文件（瓦片本身也是 GRAB_OK_ 前缀，必须索引过滤；防瓦片排序错位 + ValidateImageCount 容错误删）
5. **End case 死代码修复**（另见第三节）：`_endHandled` 标志——End case 执行完收尾才返回 true（原 L1595 同轮返回导致 End 本体永不执行，重扫入口只剩循环外兜底）
6. **Err 终止仅保留**：移动失败（MoveToAndWaitOK 重试 3 次仍失败）等物理性失败（未改）

### 待验证（下会话重点）
- 位0 类"首拍 15s 空等"是否消失（A 方案核心场景）
- GrabTile 失败位主流程跳过 → 板末重扫重拍自愈（日志：`拍照失败，已记录板末延迟重扫` → `板末重扫` → 重扫 GRAB 成功）
- 重扫模式失败防死循环（`重扫重拍失败第 N 次` → 上限接受降级）
- 孤儿清扫（`[孤儿清扫] 清理 N 个...`）
- End 收尾检查日志恢复出现（End case 修复生效）
- **WaitForVmSaveTimeout 建议下调**：正常 185ms/p95 342ms，A 方案后 3-5s 足够，15s 空等上限可去除（界面参数调整，不需改代码）

### 风险点（遗留）
- GrabTile 失败跳过后该位无降级落表（主流程 PinResults 缺位）——板末重扫补，连续模式汇总时该位延迟出现（待验证汇总计数）
- 路径未生效（SET_PATH 传播）持续时重扫也失败 → 接受降级（重扫重试上限兜底）
- 孤儿清扫在连续模式每板收尾执行一次（无在途 GRAB 时安全）

---

## 五、遗留问题（非本次范围，长期跟踪）

| # | 问题 | 现状 | 方向 |
|---|---|---|---|
| 1 | **3D 偶发数据异常**（空点云/数据不来，待办③） | 位6/21/25 轮发；每次触发重扫 + 主流程 2s 空等 | 查 VM/3D 相机侧日志；下位机 RS-485 偶发超时（`IO_TIMEOUT_MS=100×2`，`D:\CJL\330\transport_rs485.c:571`）疑同源 |
| 2 | **GET_STATE 偶发超时** | 上位机在途 GET_STATE 卡 500ms；ON 直发后 p90 仍 440ms（18:34 轮累计 217/40500≈0.5%） | 下位机侧：主控板等电机板 RS-485 回复期间处理新命令延迟 |
| 3 | 位13 类 GRAB 4.5s / 位19 D=3982ms 类 3D 慢 | VM/相机偶发慢 | 主题②：超时→重试队列，不干等（已实施待验证） |
| 4 | 检测位 2 类"2D 0 针但 3D 有点" | 条件已放宽（记录重扫） | 验证重扫后 2D 恢复 |
| 5 | **位0 首拍偶发 15s 空等**（WaitForVmSaveTimeout=15000） | VM 存图了但上位机未感知（停止/重启后"马上继续"） | **已修**：主题② A 快查分流（文件就绪超时→File.Exists→存在即继续）；验证后 WaitForVmSaveTimeout 可下调 3-5s |

---

## 六、环境坑（沿用）

- git 写 `refs/heads/fix/` 被沙盒静默拦截 → 分支可能 unborn；`git log --oneline -1` 异常时 reflog 取 SHA（`tail .git/logs/HEAD`），`fix` 目录可能消失需 `mkdir -p .git/refs/heads/fix` 后 echo SHA 写 ref
- 远程 tracking ref 写不进，push 走 URL；push 后 `git ls-remote origin <branch>` 验证
- 沙盒无 VS/MSBuild → 编译验证由用户 `_build.bat`；exe 时间与源码 mtime 对比可确认编译版本
- `.gitignore` 已补 `.workbuddy/`
