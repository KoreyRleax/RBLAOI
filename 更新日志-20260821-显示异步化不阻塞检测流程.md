# 更新日志 2026-08-21 — 显示刷新异步化：不再阻塞检测流程

> 用户决策：不做显示提速（免拷贝/直绘已回退），改为**让显示刷新不阻塞检测流程**——
> 显示渲染在后台间隙执行，检测流程直接推进下一个检测位。

## 一、背景

- 实测扫描完成→显示落表平均 ~200-250ms，偶发尖峰 0.5~1.6s；其中解析+匈牙利匹配仅 20-30ms，慢在 UI 全量重绘（拼接大图 DispObj + 全量画框）。
- 原实现：检测位循环内 Wait2D 后 / Merge 后两处 `await Dispatcher.InvokeAsync(...)` 全量刷新——**UI 渲染完成前检测流程干等**（卡住下一检测位启动）。

## 二、改动（2 个文件）

### 1. MainViewModel.VisionOverlay.cs
- 新增 `RefreshOverlayDisplayAsync()`：
  - **fire-and-forget**：`Dispatcher.InvokeAsync(...)` 不 await，发起渲染后立即返回；
  - **快照**：渲染前 `_vmOverlays.ToList()` 浅拷贝——渲染排队期间即使下一检测位已 RemoveAll/Add 修改 _vmOverlays，本帧仍按发起时刻的快照绘制，不会画出后续检测位的内容；
  - 拼接图克隆 `GetCurrentStitchedImage()` 与 `si.Dispose()` 均在渲染任务内完成，不泄漏；
  - `snapshot.Count == 0` 直接 return（与调用点原条件一致：无叠加层不刷新，保持原画面）。
- `DrawVmRects` / `DrawVmText` 增加带 `List<VmOverlayItem> overlays` 参数的重载（供快照绘制）；原无参重载保留转发 `_vmOverlays`，其余调用点（End 收尾、全图显示、手动刷新等）不受影响。

### 2. MainViewModel.DetectionFlow.cs
- **Wait2D 后显示段**（(3.5)）：`await Dispatcher.InvokeAsync(全量刷新)` → `RefreshOverlayDisplayAsync()`；
- **Merge 后显示段**：同样替换；
- **Merge 段 overlay.H/IsOK/PinId 更新移出 InvokeAsync**：原在 `PinResults.Add` 的同一 UI 委托内执行，现拆出——`PinResults.Add` 留在 UI 委托，overlay 属性赋值（纯数据对象，线程无关）放流程上下文执行，保证下方异步刷新的快照拍到**最终 H**（若不移出，快照时机在 UI 委托内更新前，渲染会画 H=0）。

## 三、行为与收益

- 检测流程不等渲染：单拍从"显示 200ms 阻塞→才进下一拍"变为"流程直接推进，UI 线程在 await 间隙（运动/扫描等待）执行渲染"。
- 渲染顺序 FIFO：Wait2D 帧 → Merge 帧，最终显示 Merge 状态，与原先一致。
- 内存：每拍至多 2 张克隆图在途，拍间约 4s ≫ 渲染 200ms，不会积压。
- 快照元素与 _vmOverlays 元素对象共享但渲染只读、流程顺序执行（流程运行于 UI 线程上下文），无数据竞争崩溃风险。

## 四、验证

- ⚠️ 沙箱拦截 MSBuild（LOLBin 策略），**未在本环境编译**；需本机跑 `_build.bat` 验证。
- 关注点：
  1. 编译通过、显示画面与原先一致（框/文字/颜色）；
  2. `[⏱] 检测位总耗时` 不应再包含显示等待（预计显著下降）；
  3. 画面刷新是否跟得上（2D 帧短暂显示后 Merge 帧覆盖——正常）；
  4. 连续模式、暂停/恢复、复位/停止、3D 自动重扫场景下显示正常。
- 回退：恢复两处 `await Dispatcher.InvokeAsync` 原块即可（本次改动集中、无备份需求；如需存档节点可自行备份两文件）。

## 五、涉及文件

- `ViewModels/MainViewModel.VisionOverlay.cs`
- `ViewModels/MainViewModel.DetectionFlow.cs`
