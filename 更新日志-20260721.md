## 2026-07-21：3D检测协议 — 针尖↔板面Z差计算高度

### 背景
VM 3D检测现在返回两组数据：针尖位置(x1,y1,z1)和板面位置(x2,y2,z2)，中间用 `||` 分隔。两组数据的顺序无关，VM未做配对。需要上位机自行匹配后计算 `h = |z1 - z2|`，替代原来的单Z值作为针的实际高度。

### 解决方案
1. 修改 `ParseVMDetect3DResponse()`，检测 `||` 分隔符，分别解析针尖组和板面组
2. 新增 `MatchTipToBoard()` 方法，对针尖↔板面两组点按xy距离执行匈牙利匹配，计算 h = |z_tip - z_board|
3. 将计算后的 h 回填到 VmZ，下游代码（坐标转换、2D↔3D匹配、判定、显示）无需改动

### 改动文件
- `ViewModels/MainViewModel.cs` — 修改解析和匹配逻辑

### 总结
利用项目中已有的匈牙利算法，针尖↔板面匹配后以 |z1 - z2| 作为实际针高，流程侵入最小。
