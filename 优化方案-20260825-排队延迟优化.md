# 优化方案 2026-08-25：CAMERA3D_ON 排队延迟优化（直发 + 短超时确认）

> 状态：**已实施（2026-08-25 17:40，未编译/未验证）**。改动：MotionIO.cs 直发方法 :714 + OK/Stop 共 10 处 ON/OFF :881/:912/:919/:958/:967/:980/:1000/:1008/:1017；SysParamData.cs `Camera3DTriggerTimeout=500`；SysParamViewModel.cs `[SysParam]`。编译由 _build.bat，验证按第四节。分支 `fix/multi-pintype-blob-parser`（已含 25bbee3 推送）。
> 目标：消除"轴到位→CAMERA3D_ON 发送"排队延迟 ~400ms（D 段 2383→~1985，总耗时 4456→~4060，39 位 -15.5s）。

---

## 一、根因（已定位，下位机代码佐证）

```
CAMERA3D_ON 走 MotionIO 串行指令队列（MotionIO.cs:833 → EnqueueCommand :602）
  └─ 在途 GET_STATE（StatePollLoop 单条在途，_isStatePollPending 保证）
       └─ 主控板转发 RS-485 查询电机板（IO_TIMEOUT_MS=100ms×重试1次，
           D:\CJL\330\main_board\User\Inc\user_defines.h:17 + transport_rs485.c:571-596）
            └─ 电机板偶发不回复 → 主控板重试后放弃（"ERROR:Query timeout all boards"）
                 → 上位机在途 GET_STATE 卡满 500ms → 队尾 CAMERA3D_ON 陪绑 ~400ms
```

**关键事实**：
- 队列深度正常 ≤1（轮询严格单条在途），400ms 是"在途单条超时"的陪绑，**不是积压** → 任何"插队/让路"方案都无法绕过已出队的在途命令（ProcessQueue 单线程阻塞在等回复上）→ 方案 2'（高优插队）**放弃**
- `CAMERA3D_ON/OFF` = 主控板 GPIO PB10 电平（`app_io.c:34`，us 级完成），OK 回复仅"命令送达确认"，非设备就绪确认
- ON 提前到移动期间发（方案1）：PB10 在 PTP 变速移动段也拉高 → 轮廓仪若电平/持续触发则数据污染 → **放弃**（用户确认触发语义风险不可赌）
- 实测 ON→ON_OK 往返：**中位 17ms / p90 18-40ms / 正常轮 max<100ms**（8/4~8/25 全部日志 3000+ 样本）→ 2000ms 超时严重过量

## 二、定稿方案：直发 + 短超时确认

- **ON/OFF 移出队列直发**（与 MOV/SCAN3D 直发先例一致，`MotionIO.cs:774,869`），保留 ON_OK/OFF_OK 确认（防 ON 静默丢失 → 空点云 → 重扫 5-20s）
- **确认超时参数化**：默认 500ms（= 正常往返 17ms 的 30 倍余量，覆盖排队/异常长尾；真丢失 500ms 内发现并快速重试）
- GET_STATE 轮询、快照兜底、IO 监测**完全不动**（IO 监测连续性不受影响）

| | 现状（队列+2000ms） | 直发+500ms 确认 |
|---|---|---|
| 排队 400ms | 存在（计入 D 段） | **消除**（写锁 ms 级 + 往返 17ms） |
| ON 失败发现 | 2s | **500ms** |
| IO 监测 | 轮询被业务命令排队影响 | 不受影响 |
| 收益 | 0 | **-398ms/拍**（39 位 -15.5s） |

## 三、代码级设计（4 文件）

### ① MotionIO.cs：新增直发方法（放 ExecuteCommandAsync :704 之后）

```csharp
/// <summary>
/// 发送指令并等待匹配的响应（直发，不经过指令队列）。
/// 2026-08-25 排队延迟优化：CAMERA3D_ON/OFF 若走队列会陪绑在途 GET_STATE 的 RS-485 偶发
/// 超时（100ms×2 重试）→ 排队 ~400ms 计入 D 段。直发后仅受串口写锁（ms 级）与回复往返
/// （实测中位 ~17ms）约束；GET_STATE 轮询照常，IO 监测不受影响。
/// 回复匹配与队列命令并行安全：DataReceived 多播，各 handler 按 expectedResponse 独立匹配，
/// 互不吞并；仅需避免 expectedResponse=null（PING 握手）同期待回（正常检测流程无此场景）。
/// </summary>
private async Task<string> SendCommandAndWaitDirectAsync(string cmd, string expectedResponse, int timeoutMs)
{
    if (!IsSessionReady && cmd != "PING")
        return "NOT_READY";

    var tcs = new TaskCompletionSource<string>();
    var cts = new CancellationTokenSource(timeoutMs);
    int gen = SessionGeneration;   // 入队时快照代次，旧代迟到回复不完成本请求

    void handler(string data)
    {
        foreach (var segment in data.Split(new[] { "\r\n" }, StringSplitOptions.RemoveEmptyEntries))
        {
            if (segment.StartsWith(expectedResponse, StringComparison.OrdinalIgnoreCase) &&
                (gen < 0 || gen == SessionGeneration))
            {
                tcs.TrySetResult(segment);
                return;
            }
        }
    }

    DataReceived += handler;
    try
    {
        if (!await SendCommandAsync(cmd))   // 写失败立即返回，不等超时
        {
            tcs.TrySetResult("SEND_FAIL");
            return "SEND_FAIL";
        }
        cts.Token.Register(() => tcs.TrySetResult("TIMEOUT"));
        return await tcs.Task.ConfigureAwait(false);
    }
    finally
    {
        DataReceived -= handler;   // 同 ExecuteCommandAsync :701 模式，防订阅泄漏
        cts.Dispose();
    }
}
```

### ② MotionIO.cs：Scan3DToAndWaitOK（L828）/ Scan3DToAndWaitStop（L927）ON/OFF 改直发

| 位置 | 现值 | 改后 |
|---|---|---|
| :833 ON | `SendCommandAndWaitAsync("CAMERA3D_ON","CAMERA3D_ON_OK",2000)` | `SendCommandAndWaitDirectAsync("CAMERA3D_ON","CAMERA3D_ON_OK",(int)SysParam.Instance.Data.Camera3DTriggerTimeout)` |
| :864/:871/:910/:919 OFF | 队列版 2000 | 直发版 + Camera3DTriggerTimeout（:919 保持 fire-and-forget `_ =`） |
| :932/:952/:960/:969 OFF | 队列版 2000 | 直发版 + Camera3DTriggerTimeout（:969 保持 fire-and-forget `_ =`） |

失败路径语义不变：ON 失败（TIMEOUT/SEND_FAIL）→ return false → `Scan3DWithRetryAsync`（DetectionFlow.cs:1721）重试时再直发 ON（ON 未成功无需 OFF）；SCAN3D 发送/轴等待失败 → fire-and-forget OFF 收尾。

### ③ SysParamData.cs：参数（L136 后）

```csharp
public double Camera3DTriggerTimeout { get; set; } = 500;   // CAMERA3D_ON/OFF 确认超时（直发版, 2026-08-25; 实测往返中位17ms/p90~40ms, 500ms 覆盖排队与异常长尾）
```

### ④ SysParamViewModel.cs：界面绑定（L182 后）

```csharp
[SysParam] public double Camera3DTriggerTimeout { get; set; }
```

### 不动
- `AxisIOControlWindow.xaml.cs:615-621`（手动 ON/OFF 按钮保持队列版，非性能路径，避免并行直发语义变化）
- GET_STATE 轮询 / StatePollLoop / 快照兜底 / `Scan3DWithRetryAsync` 逻辑

## 四、验证

1. `_build.bat` 构建
2. 模拟器一轮：ON/OFF 正常、无异常
3. 实机一轮（39 位）：看 `[⏱] 检测位{i} B+C=...ms` 与 D 段——**到位→ON 间隔 <50ms、D 中位 2383→~1985、总耗时 4456→~4060**
4. 若日志出现 ON 超时：暴露 RS-485 偶发问题（与待办③ 3D 偶发重试同源，单独议题）

## 五、风险与边界

| 项 | 评估 |
|---|---|
| 直发与队列并发回复匹配 | 多播独立匹配，ON_OK 不命中 STATE: handler，安全；边界=队列在途 expectedResponse=null（PING）场景，正常检测流程不存在 |
| 订阅泄漏 | finally 退订（:701 模式），安全 |
| 代次错配 | handler 校验 gen == SessionGeneration，RESTART 后旧代回复不完成新请求 |
| ON 静默丢失 | 500ms 超时发现 → 快速重试，不会演变成空点云重扫 |
| 写锁竞争 | ON 写入晚几 ms（GET_STATE 在途时），可忽略 |
