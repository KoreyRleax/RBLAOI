# 更新日志：GET_STATE 综合状态轮询改造

- **时间**：2026-08-04
- **标题**：GET_STATE 综合快照轮询替代 GET_IO / GET_POS / GET_STATUS 三路分开查询

## 背景

330 设备固件已实现 `GET_STATE` 综合状态快照指令，一次回复同时携带 IO 位图、X/Y/Z/H 四个轴的位置脉冲值和忙闲状态。原上位机仍是三路分开轮询：

- `IoPollLoop` 每 50ms+ 发一次 `GET_IO`（仅 IO）；
- `AxisIOControlWindow` 的 200ms 定时器先发 `GET_STATUS`（轴运动指示）、再发 `GET_POS`（位置显示）；
- 三路各自独立发送，串口通信量浪费，且 IO、位置、运动状态来自不同时刻的回复，互相不一致。

固件实测基线：ASCII GET_STATE 平均 RTT 3.938ms（253Hz）、Binary 2.453ms（406Hz），综合轮询完全够用。

## 解决方案

按《GET_STATE 上位机轮询优化说明》逐项改造（用户已确认解除 MotionIO.cs 的改动限制）：

### 1. MotionIO.cs — 新增综合快照机制

- 新增 `StateSnapshot` 类（IO 位图 + X/Y/Z/H 位置脉冲 + X/Y/Z/H 忙闲 + ReceivedAtUtc + IsFresh + RttMs）与 `StateSnapshotUpdated` 事件、`LastStateSnapshot` 缓存；
- `TryParseGetState`：按文档正则解析，IO 支持 1~16 位十六进制（可带 0x，兼容 F401/F103 的 8 位与 F407 网关的 16 位），POS/STATUS 按 X/Y/Z/H 字段名查找（不依赖逗号下标），IO + 4 位置 + 4 忙闲共 9 项全部成功才提交，不允许部分提交；
- 快照成功后**原子更新** `LastStateSnapshot`、`CurrentIOState`，一次性触发 `StateSnapshotUpdated` 与 `IOStateUpdated`。

### 2. MotionIO.cs — StatePollLoop 唯一综合轮询（替代 IoPollLoop）

- 同一时刻最多一条 GET_STATE 在途（`_isStatePollPending` 标志），收到完整回复后再发下一条；
- 请求经现有命令队列串行执行，不绕过队列直写串口；控制指令（STOP/JOG/PTP/G28/IO_SET 等）不被轮询饿死；
- **能力探测**：从未成功时连续 3 次失败 → 判定为旧固件，明确告警并回退到旧 `IoPollLoop`（GET_IO），回退路径同样单请求在途；
- 运行期连续 20 次失败（约 1 秒）→ 临时回退并告警；
- 超时/坏帧 → 保留上一份有效快照并标记 `IsFresh=false`（stale），不解释成"位置 0"或"设备空闲"；
- 每 500 轮输出一次统计日志：成功/超时次数、平均 RTT、估算刷新率（Hz）。

### 3. MotionIO.cs — 串口碎片处理

- `ParseDataTask` 改为**按行广播**：最后一个未以换行结尾的片段保留在缓冲区等待下一批数据拼接，防止 GET_STATE 长回复被 `ReadExisting` 拆成两半后后半段丢失导致解析失败；异常保护（无换行且缓冲 >1024 字节时强制取出）；
- 指令队列 `EnqueueCommand` 增加 `completeLineOnly` 完整行匹配模式（StartsWith），GET_STATE 不再被半条回复的前缀误匹配。

### 4. MotionIO.cs — 三个查询 API 改为快照优先（兼容 API 保留）

- `GetCurrentPosition` / `GetAllAxisStatus` / `GetAll_IOState`：综合模式（`_supportsGetState=true`）下直接读快照缓存，回退模式/未启用时回退直查 GET_POS / GET_STATUS / GET_IO；
- `IsMoving` 改为 `GetAllAxisStatus` 封装（`WaitAxesStop` 的 100ms 轮询不再独立发 GET_STATUS）；
- `GetIOState(OutSignal/InputSignal)` 自动继承快照优先；写 IO 后不再追加独立 GET_IO 确认，等待下一次 GET_STATE 即可；
- `SendCommand` 的 Debug 日志过滤掉 GET_STATE 刷屏。

### 5. AxisIOControlWindow — 删除重复定时器，订阅同一个快照事件

- 删除 200ms `_movingStateTimer`（先 GET_STATUS 再 GET_POS 的重复查询）；
- 订阅 `StateSnapshotUpdated`：同一份快照一次性更新四轴位置、3D 相机位置和 X/Y 运动指示；
- 删除"全零过滤"（0,0,0,0 是合法位置，不再当作超时/失败标志）；stale 快照保留最后已知显示，不闪 0；
- `GetPosAndDisplay` 保留（内部 `GetCurrentPosition` 已改为快照优先）。

## 总结

- 周期刷新路径不再独立发送 GET_IO / GET_POS / GET_STATUS，串口轮询请求从"最多 3 条/周期"降为"1 条 GET_STATE/周期"；
- IO、位置、忙闲状态来自同一份快照，界面不再出现"IO 已是新值而位置还是上一轮"的错位；
- 超时/坏帧明确标记 stale，不再伪装成 0 或 IDLE；
- 编译通过（仅存既有无关警告）。

## 修订（同日）：移除 GET_IO / GET_POS / GET_STATUS 全部直发路径

现场固件已确认支持 GET_STATE，按用户要求删除代码中剩余的所有旧指令直发，统一为纯 GET_STATE：

- **删除旧固件回退路径**：`StartIoPolling` / `StopIoPolling` / `IoPollLoop`（GET_IO 轮询）、能力探测与回退分支、`_supportsGetState` 标志全部移除；GET_STATE 连续失败仅保留旧快照并标记 stale（IsFresh=false），并输出限频告警（第 3 次及此后每 300 轮约 15 秒一次）；
- **`GetCurrentPosition`**：只读 `LastStateSnapshot`，无快照返回全 0（与旧 GET_POS 查询失败语义一致），不再直发 GET_POS；
- **`GetAllAxisStatus`**：只读快照忙闲，无快照返回全 false，不再直发 GET_STATUS；
- **`IsMoving`**：直接读快照，**状态未知（无快照/过期）时保守判定为"运动中"**（返回 true），避免 `WaitAxesStop` 提前放行导致移动未完成就继续流程；
- **`GetAll_IOState`**：只读 `CurrentIOState`，未收到过快照返回 null（`GetIOState` 随之返回 false），不再直发 GET_IO；
- 上述兼容 API 保留原签名（`Task<T>` 返回，内部 `Task.FromResult`），调用方无需改动；`timeoutMs` 参数仅为兼容保留不再使用；
- `SendCommand` 日志过滤只保留 GET_STATE；
- 代码中已无 GET_IO / GET_POS / GET_STATUS 指令发送（grep 验证仅剩注释说明）；
- 编译通过。

## 修订（同日）：IO 面板右上角增加数据刷新率实时显示

- **`MotionIO.CurrentStateRateHz`**：新增刷新率属性，按最近 50 次成功快照的到达间隔滑动平均计算（`1000 / 平均间隔ms`），StatePollLoop 每轮更新，断开时重置为 0；
- **AxisIOControlWindow 右上角**（串口连接栏右侧）：实时显示 `xx.x Hz`，断开显示 `--`；
- **轮询间隔下限 50ms → 20ms**：原代码 `Math.Max(IoPollInterval, 50)` 把系统参数默认 20ms 抬到了 50ms（参数与代码不一致），现改为 20ms 使设置真正生效，实际刷新率从约 18Hz 提升到约 41Hz（ASCII RTT≈4ms 时）；
- 编译通过。

## 修订（同日）：刷新率上不去的根因修复（面板 20Hz → 目标 ~35Hz）

**现象**：系统参数间隔已设 1ms，日志显示平均 RTT 28ms ≈ 36Hz，但 IO 面板刷新率始终 ≤20Hz。

**根因（三层）**：
1. **轮询间隔下限 20ms 钳制**：`Math.Max(IoPollInterval, 20)` 把用户设置的 1ms 抬到 20ms；
2. **Windows 定时器精度 ~15.6ms**：系统默认定时器分辨率为 15.625ms，`Task.Delay(20)` 实际约 31ms，`Task.Delay(1)` 也至少 ~15.6ms，间隔无法按设置兑现；
3. **日志的"≈36Hz"是误导**：该值 = `1000/平均RTT` 是"仅按 RTT 的极限频率"，不是实际刷新率；实际 = `1000/(RTT + 间隔)` = `1000/(28+31)` ≈ 17Hz，正是面板显示值。
4. 现场 RTT 实测 28ms（F407 网关内部轮询多块电机板，高于文档直连电机板的 3.9ms 基线）。

**解决方案**：
- 去掉轮询间隔下限：`Math.Max(IoPollInterval, 1)`，尊重系统参数设置；
- Open 时调用 `timeBeginPeriod(1)`（winmm.dll）把系统定时器分辨率提升到 1ms，`Task.Delay(1)` 才能真正 ≈1ms；Close/Dispose 时 `timeEndPeriod(1)` 成对恢复；
- 修正统计日志：改为与面板同源的滑动窗口实际刷新率，不再输出误导性的 RTT 极限 Hz；
- `ParseDataTask` 广播周期 10ms → 5ms：减少回复到达后等待广播匹配的延迟（叠加进 RTT）。

**预期效果**：周期 ≈ RTT(≈25ms) + 间隔(1ms) ≈ 26ms → 刷新率 ≈ 33~37Hz，面板与日志一致。

## 修订（同日）：断线重连/设备恢复后状态不恢复的修复

**现象**：运行中断开串口并重连，或下位机停止回复后再恢复，IO 面板一直持有旧数据；日志显示 GET_STATE 持续失败（连续 6900 次）。

**关键线索**：失败节奏为 ~31ms/轮（300 次 ≈ 9.4 秒），不是 200ms 超时的节奏（那应为 60 秒）——说明设备每轮都回了数据但**解析持续失败**，而不是"设备无响应"。

**修复**：
- **`Close()` 清空命令队列 + 接收缓冲**：重连后旧会话残留的半条数据会拼到新回复上污染解析；队列中旧命令作废不得重连后重新执行；
- **`ProcessQueue` 通用异常保护**：原只 catch `OperationCanceledException`，任何其他异常会使命令队列永久死亡（所有走队列命令包括 GET_STATE 全部失效）——这是"重连后不恢复"的潜在根因之一，已加固；
- **失败诊断日志**：连续失败告警附带上位机实际收到的原始回复（限频），可区分"设备无响应(超时)"和"回复格式错误(解析失败)"，下次复现可直接定位设备侧问题；
- **自愈机制**：连续失败每 60 轮（约 2 秒）清空一次接收缓冲，若解析失败源于残留片段污染可自动恢复，不影响命令队列；
- **面板 stale 提示**：快照过期时刷新率显示橙色"过期"，位置保留最后已知值；设备恢复后自动恢复正常显示（原逻辑直接 return，无法区分过期与正常）。

## 修订（同日）：STATUS 忙闲解析放宽 —— 根因修复

**诊断日志定位**（上一节的诊断机制直接命中）：设备实际回复为
`STATE:IO=0x0000080701040D80;POS:X0,Y0,Z0,H0;STATUS:X10000,Y0,Z0,H0`

**根因**：设备 STATUS 段的"忙"值不是文档所述的 `1`，而是内部 mode 状态码 `10000`（空闲为 `0`）。原 `TryParseAxisBool` 只接受 `0`/`1`，X 轴一旦忙碌回复 `X10000` 即解析失败 → 整份快照被丢弃 → 持续 stale → 面板冻结（此前"正常"仅因测试时轴恰好空闲）。

**修复**：STATUS 解析放宽为 **0 = IDLE，任意非 0 值 = 忙**（`long.TryParse` 成功后 `value = v != 0`）；非数字值仍解析失败（保留旧快照，不猜测）。

**验证**：设备忙闲交替（如 JOG 移动 X 轴）时快照持续成功，不再因 STATUS:X10000 中断。

