# 更新日志：检测流程拍照异常/运送暂停/暂停计时冻结（2026-08-14）

## 时间
2026-08-14

## 标题
检测流程三项修正：拍照超时直接异常终止、运送基板时暂停立即生效、暂停期间冻结超时计时

## 背景
用户现场反馈四个问题（2026-08-14 任务批次）：
1. 检测流程拍照超时（一般是 VM 问题）后仍"使用已存储图片检测"并继续 3D 扫描，应直接进入异常状态；
2. 检测暂停时三色灯应亮黄灯而非灭灯（**暂缓处理，用户要求先不管**）；
3. 单板/连续模式在运送基板时点暂停，会等运送完+检测开始才暂停，用户要求运送基板时就暂停；
4. 在入口等板或出口防堵时暂停流程，再继续会立刻报超时。

## 解决方案
改动全部集中在 `ViewModels/MainViewModel.cs`：

### 1. 拍照失败 → 直接异常终止（不再用旧图继续）
- `GrabTile` 状态：`capturedImagePath == null`（拍照超时/移动失败/文件等待超时）时，原逻辑 `Log.Warning("采集失败，使用已存储图片检测")` 继续 → 改为 `err = "检测位 X 拍照失败（VM超时/移动失败）"` + `bizState = CheckState.Err`（红灯 + Err 收尾统一弹窗 + 部分结果落盘）；
- `GrabAndReplaceTileAsync` / `GrabOneAsync`：移除 GRAB 超时处的"拍照超时"弹窗（避免与 Err 收尾弹窗双弹），保持返回 null 由状态机统一转 Err；
- 全图采集流程原本已是超时→Err，未改动。

### 2. 运送基板时暂停立即生效（问题 3）
- `TransportBoard.Step()` 顶部（报警检查/连续复位检查之后、switch 之前）新增暂停响应：
  `_continuousPaused || !_pauseCheckEvent.IsSet`（单板=检测暂停事件，连续=段间暂停标志）→ `StopMotorAsync()` 停传送带 → 冻结 `sw` 计时 → 等待恢复 → `sw.Start()` 后从当前状态继续（当前点位重做，恢复后重新读传感器）；
- 暂停等待循环由 `_pauseCheckEvent.Set()`（复位/报警/继续按钮）唤醒，不会卡死。

### 3. 暂停期间冻结超时计时（问题 4）
- 连续模式 `ContinuousRunInternal.Step()` 暂停等待块：`WaitOutletClear`（出口防堵）且已开始计时时，暂停期间 `sw.Stop()`、恢复后 `sw.Start()`——暂停不再消耗防堵超时时间；
- `TransportBoard` 的 `WaitEntry/WaitBoard` 计时由第 2 条的暂停块统一冻结（覆盖单板"等待入口放板"和连续模式入口等板场景）。

## 总结
问题 1/3/4 已实施并编译通过（0 错误；部署因 `bin\x64\Debug\RBLAOI.exe` 被运行中的进程（PID 29160）锁定而暂缓，需关闭运行中的程序后重新编译部署）。问题 2（暂停/复位灯色）暂缓，待用户补充观察细节后再处理。行为变更确认：拍照失败不再用旧图兜底检测，运送基板中暂停立即停带并冻结计时，恢复后继续当前动作。
