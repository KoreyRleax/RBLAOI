## 2026-07-22：多类型针检测完整实现

### 阶段一：数据模型 + 方案编辑UI

#### 1.1 新增 PinTypeParameter 模型
- 文件：`Models/PinTypeParameter.cs`（新文件）
- 属性：Label(string), Focus(double), XyTolerance(double), IdealOffsetX(double), IdealOffsetY(double), HeightTolerance(double), IdealHeight(double)

#### 1.2 修改 ProjectData.cs
- 删除：FocusParams、XAllowedDeviation、YAllowedDeviation、IdealPinHeight、AllowedHeightDeviation、RelativeOffsetX、RelativeOffsetY
- 新增：`PinTypeParam[] PinTypeParams { get; set; }`

#### 1.3 修改 SysParamData.cs
- 删除对应的全局参数（XAllowedDeviation、YAllowedDeviation 等）
- 保留 BoardFocus 和基本参数

#### 1.4 修改 EditProjectWindow
- 删除"焦距设定"按钮和 FocusSettingWindow 调用
- 在检测参数区域，根据 PluginFeatureCount 动态生成 N 行
- 每行格式：标记字母 | 焦距 | XY允许偏差 | 理想针尖X | 理想针尖Y | 高度允许偏差 | 理想高度
- 删除"恢复系统参数"按钮

#### 1.5 删除 FocusSettingWindow.xaml + .xaml.cs

### 阶段二：检测流程重构

#### 2.1 MainViewModel 参数引用改造
所有引用 `ProjectIdealPinHeight`、`ProjectXAllowedDeviation` 等全局参数的地方，改为按 PinType 读取

#### 2.2 GrabAndReplaceMultiAsync
- 泛化抓图：`GrabOneAsync(focus, saveSubDir)` → 调焦 → SET_PATH → GRAB → 路径
- 根据 PinTypes 循环抓取每个针型

#### 2.3 DETECT2D_BLOB 协议
- 新格式：`DETECT2D_BLOB:{boardPath}|A:{pathA}|B:{pathB}`（|分隔，字母:路径）

#### 2.4 ParseDetect2DBlobResponse
- 适配新返回格式：`padA:x,y,w,h,ang|blankA:x,...|pinA:x,...||padB:...`

### 阶段三：VM全局脚本 + 查看模式工具栏

#### 3.1 UserGlobalScript
- 解析 `A:path` 格式，设置 isA/isB 全局变量和图像源

#### 3.2 查看模式工具栏
- MainWindow 工具栏增加 `[🔲板面] [👁A] [👁B] [👁C]` 按钮
- 点击切换拼图瓦片显示
