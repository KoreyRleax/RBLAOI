# 更新日志 2026-08-25：3D 结果等待异步化

> 分支：`fix/multi-pintype-blob-parser`（e5a4f7f 之上新增，未提交）
> 改动文件：`ViewModels/MainViewModel.DetectionFlow.cs` + `MainViewModel.CollectionFlow.cs`（枚举）
> 状态：**代码完成，未编译**（待用户 `_build.bat` + 实机验证）
> 方案：见《优化方案-3D结果等待异步化.md》（v2：数据到即显示 + 重扫不重拍）

## 一、目标

原流程 `WaitScan3D` 在扫描完成后**同步等待 DETECT_3D_OK 数据（中位 137ms / p90 191ms / 偶发 1.5s）**，期间轴空闲。本次把等待与"移动到下一检测位"并行，**数据到即显示、零推迟**，节拍收益 ~167ms/拍（等数据 + Merge 均被 A 段 ~1.8s 隐藏）。

## 二、机制（快照+回退，Merge 570 行零改动）

```
WaitScan3D(i): 只等扫描完成 → 快照 i 上下文 + 早开数据监听 → NextPos（不等数据）
CapturePos(i+1)/GrabTile(i+1): A段移动+拍照（期间 D_i 后台到达）
MergePending(i+1 A段后): 恢复 i 快照 → 取数(剩余窗口，数据已到=零等待) → Merge(i)
   └ 正常 → NextPos(i++) → _skipAForNext → 跳过已拍A段 → Start2D(i+1)
   └ 重扫 → i 已恢复 → MoveScanStart(i)（用快照的 startX/endX）→ 重扫 → 同上
末位: NextPos(i=N) → MergePending 补 N-1 的 Merge → End
```

## 三、改动点

| # | 位置 | 内容 |
|---|---|---|
| 1 | CollectionFlow.cs 枚举 | 新增 `MergePending`（Merge 前）；检查点 6→7 处 |
| 2 | DetectionFlow.cs 字段区 | 新增 `_pendingMergeIdx/_pendingPos/_pendingDetectX/Y/_pendingScanHalfWidth/_pendingStartX/EndX/MoveY/_pendingPins2D/_pending3DDataTask/_pendingScanDoneUtc/_skipAForNext` |
| 3 | BizStep 内 | 新增局部函数 `SnapshotPending3D(idx, pins2D)`（快照挂起上下文） |
| 4 | WaitScan3D case | 重写：**监听早开**（await 扫描前创建，防 VM 提前推送漏数据）→ 只等扫描完成 → 快照 → NextPos；扫描失败丢监听（scan3DNoData=false 不重扫）；总耗时日志移位至此（swPos 基准正确，自动体现 -167ms 收益） |
| 5 | 新增 MergePending case | 检查点⑦：恢复 i 上下文 → 取数（WhenAny 剩余窗口 = max(0, 2s-已过时间)，总窗口语义不变）→ 写 scan3DTimeout/scan3DNoData → 置 _skipAForNext → Merge；无挂起 → Start2D/End |
| 6 | GrabTile 末尾 | `Start2D` → `MergePending`（A 段完成先处理上一位挂起） |
| 7 | Merge case | 删两处"总耗时"日志（已移位）；重扫分支（2 处）补 `swPos.Restart()`（重扫段独立计时，修正 swPos 基准被下一检测位占用） |
| 8 | NextPos case | `i++` 后：`i>=N` → MergePending（末位补 Merge）；`_skipAForNext` → 清标志 → MergePending（跳过已拍 A 段，**不重拍**）→ Start2D |

## 四、关键设计点

1. **监听早开**（WaitScan3D 内 await 扫描前创建 `_pending3DDataTask`）——数据可能在扫描完成前已到（实测 23ms~1.5s），晚开会漏；
2. **取数串行**：MergePending 在 Start2D/SCAN3D 之前执行 → 上一位监听先消费，绝不吃掉下一位 DETECT_3D_OK；
3. **剩余窗口**：`waitMs = max(0, 2000 - (UtcNow - _pendingScanDoneUtc))`——总窗口从扫描完成起算仍压 2s，与原语义一致；
4. **重扫零改动**：Merge 重扫分支直接跳 MoveScanStart——i 已由 MergePending 恢复、startX/endX/moveY 已恢复，重扫路径原样工作；
5. **不重拍**：`_skipAForNext`——MergePending 处理挂起时 i+1 的 A 段已拍完（图已就绪、路径未被覆盖），NextPos 跳过 CapturePos/GrabTile 直接 Start2D；
6. **首拍/末位**：首拍无挂起直接 Start2D；末位 NextPos(i=N) 经 MergePending 补最后一位 Merge。

## 五、预期收益

- 每拍 ~167ms（-3.6%）：数据等待 137ms + Merge ~30ms 全部被 A 段移动隐藏；
- 偶发 1.5s 数据长尾被 A 段（~1.8s）完全覆盖；
- **显示时机不变**：数据到（扫描完成+137ms）→ MergePending 立即处理（A 段期间）→ 显示，与同步版几乎一致；
- 40 位排：190s → ~183s。

## 六、验证清单（实机）

1. 单拍 3 位 + 连续 2 轮：检测位数据不串位、末位落表正确、每拍结果即时显示；
2. 人为构造空 3D 数据触发重扫：i 回退正确、i+1 **不重拍**、结果无错位；
3. 暂停/恢复 + 复位：无残留监听、无崩溃；
4. 16mm/s 完整轮：总耗时中位 4625 → 预期 ~4460；日志总耗时打印在"扫描完成"位置（D 段日志后）；
5. 日志格式变化：E(3D结果获取) 行已移除（原 WaitScan3D 打），总耗时行移到扫描完成时——分析脚本需适配。

## 七、风险与回退

- 最高风险：重扫 i 回退 + _skipAForNext 组合路径（偶发场景，实机验证频次低）——模拟器可用模拟空 3D 数据触发；
- 单文件状态机内部改动（+1 枚举值），可整体 revert；无协议/数据格式变更。
