# 更新日志：方案编辑界面优化 + 方案级曝光 + 光源控制界面

- **时间**：2026-08-04
- **标题**：EditProjectWindow 交互优化；方案级相机曝光时间；光源控制对话框（界面+参数保存）

## 背景

1. 方案编辑界面（EditProjectWindow）的检测参数 DataGrid 单击单元格即进入编辑态，长列表难以用 Tab 顺畅跳转；"板面焦距"是全局参数却与 DataGrid 混在同一 Border，易误认为表格一行；
2. 相机曝光时间只有全局系统参数，不同方案（不同板种/针型）无法差异化曝光；
3. 光源控制完全未实现（仅 csproj 引用了 LightControlCs/LightModuleCs DLL 但零调用，CreateProjectDialog 有废弃的"光源模板"下拉框）。

## 解决方案

### Task1 — 方案编辑界面优化

- **DataGrid 交互**：`SelectionUnit="Cell"` → `"FullRow"`——单击选中整行（不进入编辑），双击单元格才进入编辑，Tab 键可在行/列间顺畅跳转；说明文案同步更新；
- **板面焦距独立成行**：从检测参数 Border 中取出，独立成"板面焦距"区块（含用途说明），与检测参数、备注平级。

### Task2 — 方案级相机曝光时间

- `ProjectData` 新增 `CameraExposureTime`（double，默认 10000μs，与系统参数一致；`<=0` 时回退系统参数）；
- EditProjectWindow 基础设定新增"相机曝光时间 (μs)"输入框，构造传入/确定回写；
- `MainViewModel.SetVmCameraExposure()`：**优先应用当前方案曝光时间**（>0 用方案值），未设置回退系统参数；全图采集、检测流程、检测位重采三个调用点自动生效；日志标注曝光来源（方案/系统参数）；
- **检测参数按标记字母升序**：EditProjectWindow 初始化 PinTypeParams 时 `OrderBy(Label)` 排序，界面行序稳定。

### Task3 — 光源控制（方案 a：按钮 + 独立对话框，仅界面+参数保存）

- `ProjectData` 新增方案级光源字段：`LightPort`（串口号，默认 COM1）+ `LightChannel1..4`（亮度 0-100%）；
- 新建 `Views/LightControlWindow` 对话框（按需求设计）：
  - 顶部"全局设置"区：**串口号**下拉框（枚举可用串口，当前值不在列表时默认选第一个）；
  - 底部"光源通道"区：**通道1-4 四行**，每行 = 右对齐标签 + 细长 Slider + 数字输入框，**滑块与输入框双向绑定**（拖动滑块实时更新数字，输入数字滑块同步移动）；
  - 输入框仅允许数字和小数点（PreviewTextInput 校验），确定时收敛 0-100；
  - 左右对齐、垂直居中的网格化布局，与需求规格一致；
- EditProjectWindow"板面焦距"行右侧新增"💡 光源控制"按钮 → 弹出对话框，确定后回写方案参数；
- 光源参数随方案 XML 自动持久化（ProjectManager 序列化，旧方案缺字段取默认值，兼容安全）；
- 串口通信待光源控制器协议确定后接入（当前仅界面+参数保存）。

## 总结

- 方案编辑界面：单击不误触编辑、双击编辑、Tab 顺畅导航；板面焦距独立区块不再混淆；
- 曝光时间支持按方案差异化，未配置时回退全局参数，向后兼容；
- 光源控制界面完整实现（串口下拉 + 4 通道滑块/输入框联动），参数随方案保存；通信层预留；
- 新窗口已注册进 RBLAOI.csproj（Page + Compile）；
- 编译通过。
