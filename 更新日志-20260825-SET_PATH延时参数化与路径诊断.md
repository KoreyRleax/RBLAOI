# 更新日志 2026-08-25：SET_PATH 传播延时参数化 + 到位延时参数化 + 路径生效诊断（模拟器可测优化）

> 依据 2026-08-25 会话决策：优先实施"模拟器上就能验证效果"的优化（交接文档 ③ 1b/9/10 族）。
> **方案定稿（09:39）**：SET_PATH **不做回执**——回执只确认"全局变量已写"，不确认 VM 保存模块（输出图像1
> 订阅 ImageSavePath）已应用新路径（异步传播窗口），属假确认；改用"参数化延时兜底传播 + GRAB 后文件落点诊断"。
> ⚠️ 沙箱拦截 MSBuild，**未编译**；改动小且直白，待本机 `_build.bat` 编译 + 模拟器对比验证。

## 改动（2 个上位机文件）

### 1. 上位机：SET_PATH 后延时保留 + 参数化（`ViewModels/MainViewModel.RunCommands.cs`）
- L1002 区（GrabAndReplaceTileAsync）与 L1164 区（GrabOneAsync）：`Task.Delay(100)` → `Task.Delay((int)_sysParam.Data.SetVmSavePathDelay, token)`
- 语义：SET_PATH 是 fire-and-forget（无回执），"VM 变量传播→保存模块应用"窗口由该延时兜底；
  `SetVmSavePathDelay`（保存路径生效延时，默认 100，此前 `ImageCaptureDelay` 无调用点，09:50 改名）正式启用，用户可调小找最佳值；补 token 使复位可立即中断
- ⚠️ 改名同步 4 文件：`SysParamData.cs`/`SysParamViewModel.cs`/`SysParamWindow.xaml`（LabelText"保存路径生效延时 (ms)"）/`RunCommands.cs`；持久化影响：旧配置中 `ImageCaptureDelay` 字段失效回默认 100——该参数此前无调用点、默认与旧固定一致，无实际影响

### 2. 上位机：SetVmSavePath 记录上次路径（`ViewModels/MainViewModel.Project.cs`）
- 保持 `SendCommandAsync` fire-and-forget 不变（**不加回执**）；
- 新增字段 `_lastVmSavePath`：每次 SET_PATH 记录目录，供文件落点诊断使用（与回执无关的纯诊断能力）

### 3. 上位机：调Z后稳定延时参数化（`ViewModels/MainViewModel.RunCommands.cs` L1144）
- `if (focusZ > 0) await Task.Delay(100);` → `if (focusZ > 0) await Task.Delay((int)_sysParam.Data.PositionSnapshotDelay, token);`
- 语义：曝光前光学稳定保险保留，延时由"到位拍照延时"（PositionSnapshotDelay，默认 200）控制，界面可调参；补 token

### 4. 上位机：全图采集调Z后延时参数化（`ViewModels/MainViewModel.CollectionFlow.cs` L191 区）
- `await Task.Delay(100);` → `await Task.Delay((int)_sysParam.Data.PositionSnapshotDelay, token);`，与 L215 用法统一

### 5. 上位机：路径未生效诊断（`ViewModels/MainViewModel.RunCommands.cs` 文件就绪失败分支 ×2）
- 文件就绪超时且 fallback 无文件时，检查文件是否落在**上次 SET_PATH 目录**（`_lastVmSavePath`）：
  - 命中 → `[路径未生效] {文件名} 落在上次SET_PATH目录({旧})，本次期望({新})——VM保存模块未应用新路径，请检查ImageSavePath订阅/延时`
  - 价值：路径未生效场景从"静默超时"变为"显性定位"（诊断 SET_PATH 传播/VM 订阅问题）

### 6. 上位机：删除"原图兜底"残留死代码（`ViewModels/MainViewModel.DetectionFlow.cs` GrabTile case，原 L481-498）
- 原"拍照失败时用瓦片库 GrabIndex 对应原图继续 2D/3D"的兜底逻辑：主流程已在 2026-08-14 改为 Err 终止
  （L467 注释"不再用旧图继续 2D/3D 扫描（2026-08-14 用户要求）"），残留的 `imagePath==null` 查找分支
  实际不可达（capturedImagePath==null 时已 break 进 Err），10:00 已删除，消除"仍可兜底"的误导
- 采集失败唯一语义：**return null → CheckState.Err → 异常终止弹窗**（不重试、不用旧图）

## 预期收益（模拟器可量化）
| 场景 | 每张图 | 说明 |
|---|---|---|
| SetVmSavePathDelay 延时参数化 | 100ms（默认，可调小至 0） | 原固定 100ms 改为可调参数，用户定值 |
| 调Z延时参数化 | 由调参决定 | 默认 200 与旧 100 相比暂慢 100ms，测 0/50 时省 |
| 合计（调参到 0） | 每拍约省 (1+N)×100ms | Blob 板面+针尖 N 图/拍 |

## 验证方法（模拟器）
1. 本机编译 `_build.bat`（本次改动 5 处、4 文件含 XAML，未编译）
2. 跑模拟器 ≥10 拍（Blob 方案），观察：
   - `[⏱] 检测位i A(移动+拍照)` 均值 vs 8-24 基线（Blob A 段 1517ms）——SetVmSavePathDelay 调 0 后应显著下降
   - 路径正常：上位机 `已设置VM拍照路径: {path}` + 文件就绪通过（不应有 [路径未生效]）
3. 调参对比：界面把"保存路径生效延时"（SetVmSavePathDelay）依次设 100/50/0，各跑 ≥5 拍记录 A 段均值，确认延时生效且单调
4. 诊断验证（可选）：改模拟器让 GRAB 复制到固定目录（模拟保存模块未应用新路径），确认上位机打出 `[路径未生效]` 日志
5. 回归：暂停/复位在延时中可立即中断（带 token）、全图采集调Z分支正常

## 已评估未做
- **SET_PATH 回执（SET_PATH_OK）**：VM 保存模块（输出图像1）订阅 ImageSavePath 变量，"设置变量"→"模块应用"是异步传播，脚本立即回执属假确认——不做；若需语义最正确的确认，须 VM 方案支持在输出图像1 后插入脚本节点（待确认 VM 4.4.40 能力）
- 优化 11（ReadImage 19MB×2 异步化）：模拟器收益小（-63ms 已实测），实机收益 200-400ms，待实机计时数据
- 优化 6（文件等待∥移起点）：结构改动大，模拟器文件等待本来就快测不出收益，待讨论
