# 更新日志

## 2026-08-24 新增 — Origin 图片数量容错校验（多余自动清理，仅不足弹窗）

### 背景
原校验要求 Origin 图片数严格等于计算数，任意不等即弹窗并中止加载；文件操作失败残留的多余图片会让方案被"卡死"无法显示底图。

### 改动
- `ViewModels/MainViewModel.Project.cs` `ValidateImageCount`：容错化，返回 `(isValid, expected, actual, removed)`；实际 > 预期时按**文件创建时间**自动删除最新多余图片（防只读+失败记日志），实际 < 预期时返回无效
- `LoadProjectImagesAndDisplayAsync`（方案加载/重载入口）：仅不足时弹窗"图片数量不一致！预期 X 张，实际仅 Y 张。请重新采集全图。"
- `ViewModels/MainViewModel.cs` `EnsureSelectionModeReady`（勾选检测位入口）：同上

### 总结
允许图片数上溢并自动清理脏图，方案不再因多余文件卡住；仅真实缺图（下溢）弹窗提示重新采集全图。

详见 [更新日志-20260824-Origin图片数量容错校验.md](更新日志-20260824-Origin图片数量容错校验.md)

## 2026-07-23 新增 — IO界面H+/H-禁止 + 当前位置实时轮询

### 背景
1. H轴(H+/H-)在工作位有板时仍可操作，存在撞板风险
2. IO界面的当前位置需手动点击"查询"按钮才刷新，操作不便

### 改动文件
`Views/AxisIOControlWindow.xaml.cs`

### 改动内容

**任务1：H+/H- 禁止**
- 新增 `UpdateHButtonsState()` 方法，检测 `_lastState?.InputWork`（工作位传感器）
- 当工作位有板时，`btnLineAdd(H-)` 和 `btnLineDel(H+)` 自动禁用
- 在3处触发更新：连接成功时、IO状态变更时、控件重新启用时
- 保护逻辑：未连接时固定禁用，防止误操作

**任务4：当前位置实时显示**
- `_movingStateTimer` 间隔从 100ms 改为 200ms，保持低频率轮询
- `RefreshMovingState()` 集成 `GetCurrentPosition()`（GET_POS）调用
- 轮询周期内依次执行：GET_STATUS（轴运动指示）→ GET_POS（轴位置更新）
- 通过 MotionIO 指令队列串行执行，不冲突
- 窗口关闭时 Timer 自动停止

### 验证要点
1. 连接串口后，工作位传感器 ON → H+/H- 按钮自动置灰
2. 工作位传感器变为 OFF → H+/H- 恢复正常可用
3. 未连接时 H+/H- 始终禁用
4. 位置显示每 200ms 自动刷新，无需手动点击查询

## 2026-07-06 新增 — 项目记忆同步
在 `.claude/memory/` 中创建项目记忆文件（git同步），CLAUDE.md 添加记忆路径说明

## 2026-06-27 重构 — 3D协议解析与坐标匹配

### 背景
VM 3D检测结果协议改为新格式 `DETECT_3D_OK:x,y,z;x,y,z;...`，需重新实现坐标转换和2D-3D匹配逻辑。

### 解决方案
1. **新协议解析**：ParseVMDetect3DResponse 解析逗号+分号格式，µm→mm转换
2. **迭代校准法**：两遍匹配解决3D FOV > 2D FOV导致的均值偏移问题
   - Pass 1：全部点组内均值对齐 + 粗匹配 → 计算校准偏移
   - Pass 2：应用校准偏移 + 精匹配（欧氏距离）
3. **坐标映射**：vmX → boardX, vmY → boardY（VM的X方向对应板面X方向）
4. **清理**：删除过时的 BoardPointZ、VmFovToBoardY、IsVmPointIn2DFovX

### 验证
两批测试数据均 3/3 正确匹配，匹配精度 0.002~0.024mm

## 2026-06-29 优化 — 3D匹配阈值调整

### 背景
旧算法Pass 1用8mm阈值导致部分场景PINID丢失匹配，新算法Pass 1无阈值+Pass 2阈值1mm更准确。

### 改动
1. Pass 1：去掉8mm阈值 → 无阈值，所有2D针强制匹配算校准偏移
2. Pass 2：5mm → 2mm

### 验证
5组测试数据全部通过，正确匹配距离均<0.05mm

## 2026-06-24 修复 — 精度检测匹配 Bug

### 背景
精度检测报告只匹配到 20/62 针，大部分针脚未被纳入统计。

### 根因：Async Void 竞态条件
`StartCheckFlow()` 是 `async void`，执行到 `await CheckAndHomeAxesAsync()` 时就返回调用方。但 `isWorkFlowRun[WorkFlowType.Check] = true` 要在 `await` 之后才执行。精度检测的 `while (isWorkFlowRun[Check])` 循环看到 flag 为 false，**误判检测已结束**，立刻拍下空快照。

### 修复
- 添加 `_checkFlowCompleted` ManualResetEventSlim，在 `StartCheckFlow` 的 `finally` 块中可靠设置
- 精度检测 / 演示模式改用 `_checkFlowCompleted.Wait(200)` 替代轮询 `isWorkFlowRun[Check]`
- `_checkFlowCompleted` 在进入 `try` 块前 Reset，在 `finally` 中 Set，消除所有时序间隙

### 诊断增强
- `ComputePrecisionStats` 添加每轮针数日志
- 匹配失败详细日志：输出参考针坐标、最近目标坐标、距离（mm）

## 2026-06-24 — 新增演示模式 + 精度检测功能

### 演示模式
**背景：** 需要在运行菜单中添加循环检测功能，方便展示设备运行流程。

**解决方案：**
- MainViewModel 新增 `ToggleDemoMode()` 方法，通过 `_isDemoMode` 标志控制循环
- 点击"演示模式"按钮启动循环 → `StartCheckFlow()` → 等待完成 → 自动下一轮
- 再次点击"演示模式"（按钮文字变为"停止演示"）→ `StopCheckFlow()` 取消当前检测并退出循环
- 使用 `_demoCts` CancellationTokenSource 控制循环生命周期
- DemoModeStateChanged 事件驱动 UI 按钮文字动态切换

### 精度检测（Task3）
**背景：** 需要定量评估设备检测重复性，通过重复检测 N 次并统计 dx/dy/h 的标准差、极差等指标来评估机器精度。

**解决方案：**
- **SysParamData** 新增 `PrecisionRepeatCount` 参数（默认10），配置于系统参数界面
- **SysParamViewModel** 新增 `[SysParam] PrecisionRepeatCount` 属性，通过反射自动同步
- **MainViewModel** 新增 `StartPrecisionCheck()` + `ComputePrecisionStats()` + `SampleStdDev()` 方法
- **核心流程：**
  1. 重复执行 `StartCheckFlow()` N 次（精度模式下跳过历史保存和目录创建）
  2. 每轮结束后快照 `PinResults`
  3. 以第一轮为基准，通过坐标 (X,Y) 最近邻匹配同一针脚（阈值 1mm）
  4. 每个针脚计算 dx/dy/h 的均值、标准差、极差、最小/最大值
  5. 全局汇总：σdx均值、σdy均值、σh均值、综合重复性指标
- **PrecisionReportWindow** 全新报告窗口：
  - 汇总区（检测次数/匹配针数/耗时/σ均值）
  - DataGrid 详细统计表格
  - CSV 导出功能
- **MainWindow** 运行菜单新增"精度检测"按钮 + 📏 图标
- 添加 `_isPrecisionMode` 标志控制 `StartCheckFlow` 跳过非必要的保存操作

**匹配策略：** 使用坐标而非 PinID 匹配同一针脚，避免因排序变化导致 ID 漂移

**涉及文件：** MainViewModel.cs, MainWindow.xaml.cs, PrecisionReportWindow(.xaml/.cs), PrecisionStat.cs, SysParamData.cs, SysParamViewModel.cs, SysParamWindow.xaml, RBLAOI.csproj

## 2026-06-09 v24

### 新增文字标注纵向显示（绕针中心顺时针旋转90度）

**背景**：当同一水平线上有多个针脚时，横向标注文字会重叠。新增"显示方向"选项，设为"Vertical"时文字绕针中心顺时针旋转90度显示。

**实现**：通过 Halcon `set_font` 的 escapement=2700 参数实现字体旋转。

| 文件 | 改动 |
|------|------|
| `Models/SysParamData.cs` | 新增 `TextDirection` 字段 |
| `ViewModels/PersonalizationViewModel.cs` | 新增绑定属性 + `TextDirectionOptions` 列表 |
| `Views/PersonalizationWindow.xaml` | 文字样式区域新增"显示方向"ComboBox |
| `ViewModels/MainViewModel.cs` | `DrawYoloText`/`DrawVmText` 中 TextDirection=Vertical 时 SetFont 加 escapement=2700 |

**注意**：SetFont 包裹在 try-catch 中，若 Halcon 版本不支持字体旋转参数则会静默降级为横向。

---

## 2026-06-09 v20

### 回退 v19：DEL_CAM_CACHE 方案不适用于 3D 线扫相机

**背景**：v19 将缓存清除（`TriggerMode=Off` + 软件触发）合并到 GlobalScript，并在检测循环中发送 `DEL_CAM_CACHE`。实测发现该方案干扰了 3D 线扫相机的内部状态机，导致帧号卡在 1、失败数从 1 恶化到 2。

**根因**：3D 线扫相机依赖硬件触发（编码器脉冲）采集行数据，`IMVS_PF_SetParamValue_CS("TriggerMode", "Off")` 会切断相机内部触发链路，即使 CAMERA3D_OFF 已通过 IO 切断硬件信号，VM 相机模块还是会因为 TriggerMode 状态变更而重置内部行计数器/帧同步状态。

**回退内容**：
1. `bin\x64\Debug\VmSolution\GlobalScript.cs` — 恢复为仅含 SET_PATH + DETECT_2D 的原始版本
2. `C:\Program Files\VisionMaster4.4.40\Applications\GlobalScript\GlobalScript.temp` — 恢复为仅含 SET_PATH + DETECT_2D 的原始版本
3. `ViewModels/MainViewModel.cs` — 移除 DEL_CAM_CACHE 发送代码

**经验**：3D 线扫相机的缓存不能通过 VM SDK 的 `TriggerMode/TriggerSoftware` API 清除。如需缓存清除，后续应探索 MVS SDK `ClearImageBuffer()` + 相机非独占模式，或下位机硬件复位方案。

---

## 2026-06-09 v23 — 已回退

### 文字标注：去掉水平垂直对齐，改为横向/纵向显示方向 — **已回退**

方向功能（`TextDirection`）因 Halcon 字体旋转兼容性问题未达到预期效果，已完整回退。文字标注保持原样。

**回退内容**：`SysParamData.cs`、`PersonalizationViewModel.cs`、`PersonalizationWindow.xaml`、`MainViewModel.cs` 中所有方向相关改动已移除。

---

### OK/NG 判定统一：dx、dy、h 全部达标才 OK，不再豁免 H=0

**改动**：移除所有场景中 `h == 0` 的高度豁免逻辑，dx/dy/h 三条件缺一不可。

| 场景 | 修改前 | 修改后 |
|------|--------|--------|
| A1 (2D+3D合并) | H=0 时跳过高度检查 | 必须 `\|H - idealHeight\| <= allowedHDev` |
| A2 (纯2D, H=0) | 只判 dx/dy | 加判 `\|0 - idealHeight\| > allowedHDev` → NG |
| B (2D超时, dx/dy=TIMEOUT) | 只判高度 | 直接 NG（TIMEOUT 不可能达标） |
| RecalculatePinResults | H=0 时跳过高度检查 | 必须检查高度 |

**改动文件**：
- `ViewModels/MainViewModel.cs` — 5 处判定逻辑统一

---

## 2026-06-09 v21

### 修复 3D 超时时 2D 检测结果不显示的问题

**背景**：当 3D 高度数据超时（`response == "TIMEOUT"`），2D 检测结果（VM/YOLO）不会被添加到 `PinResults` 列表中，导致界面上不显示任何结果。这是因为合并逻辑中缺少"2D有效 + 3D无数据"的处理场景。

**改动**：在 `MainViewModel.cs` 的合并逻辑中新增 **场景 A2**：

```
合并逻辑完整覆盖 4 种场景：
  ├─ 场景 A1：2D有效 + 3D有坐标 → 2D-FIRST坐标匹配（已有）
  ├─ 场景 A2：2D有效 + 3D无数据 → 纯2D结果显示 √ NEW
  ├─ 场景 B： 2D超时 + 3D有坐标 → dx/dy=TIMEOUT（已有）
  └─ 场景 else：全无效 → 跳过（已有）
```

场景 A2 逻辑：
- 遍历 `validPins2D`（排除空针），直接生成 `PinResult`
- H=0，不判高度
- OK/NG 仅依据 dx/dy 偏差
- 日志警告标记：`"3D数据无效（超时/空），仅显示2D结果"`

**改动文件**：
- `ViewModels/MainViewModel.cs` — 在 `if (has3DToMerge && has3DCoordinates)` 后新增 `else` 分支

---

## 2026-06-09 v20

### VM GlobalScript 合并：加入 DEL_CAM_CACHE 缓存清除功能

**背景**：根据 v18 的规划，3D 相机缓存清除将从 C# 端 CameraController 迁移到 VM 自身 GlobalScript 中触发。原来 VM 安装目录中已有独立的 `DEL_CAM_CACHE` 缓存清除脚本（`ef2fbfae`），但与当前使用的 GlobalScript（含 SET_PATH/DETECT_2D 处理）分离。需要将两者合并为一个统一的 GlobalScript。

**解决方案**：
1. 合并 `bin\x64\Debug\VmSolution\GlobalScript.cs` — 项目输出目录的脚本源文件
2. 合并 `C:\Program Files\VisionMaster4.4.40\Applications\GlobalScript\GlobalScript.temp` — VM 当前激活的 GlobalScript 配置

**合并内容**（保留原有 SET_PATH + DETECT_2D，新增）：

| 功能 | 指令 | 说明 |
|------|------|------|
| 设置保存路径 | `SET_PATH:` | 保留，加日志 |
| 2D 检测触发 | `DETECT_2D:` | 保留，加日志 |
| 清除 3D 相机缓存 | `DEL_CAM_CACHE` | 新增，完整实现 |

**缓存清除原理**（`ClearCameraCache()`）：
1. 保存当前触发模式（`IMVS_PF_GetParamValue_CS` 读取 TriggerMode）
2. 关闭硬件触发（`TriggerMode=Off`），切到软件触发
3. 软件触发 3 帧并丢弃（`TriggerSoftware=Execute`）
4. 恢复原来的触发模式

**⚠️ 关键时序设计**：DEL_CAM_CACHE 不能放在 3D 扫描启动前（会切断硬件触发导致线扫丢帧）。必须放在 **CAMERA3D_OFF 之后**，此时硬件触发已被 IO 信号切断，TriggerMode 切换不会丢失任何线扫帧：

```
正确时序：
  CAMERA3D_ON → 移动扫描中 → CAMERA3D_OFF → [硬件触发已断] → DEL_CAM_CACHE → DETECT_3D_OK
  
错误时序（原方案，已修正）：
  移动到起点 → DEL_CAM_CACHE → CAMERA3D_ON → ...  ← 硬件触发被误切，丢帧
```

**改进**：
- `DiscardImageFrame()` 从占位符改为实际调用 `IMVS_PF_GetImage` + `IMVS_PF_FreeImage` API（旧脚本中为注释状态）
- 日志路径改为 VM 标准日志目录 `log/GlobalScript/ClearCache.log`
- 缓存清除状态通过全局变量 `var0` 回传（1=开始, 2=成功, 3=失败）
- 新增 `using System.Threading`、`using System.IO` 支持

**改动文件**：
1. `bin\x64\Debug\VmSolution\GlobalScript.cs` — 完整重写，合并缓存清除逻辑
2. `C:\Program Files\VisionMaster4.4.40\Applications\GlobalScript\GlobalScript.temp` — 更新 ScriptContent 为合并后版本
3. **`ViewModels/MainViewModel.cs`** — 检测循环中移动到3D扫描起点后、启动扫描前发送 `DEL_CAM_CACHE` 指令

---

## 2026-06-08 v18

### VM 通信迁移：SET_PATH + DETECT_2D 从 TCP 指令改为 VM SDK 直调

**背景**：原有架构通过 TCP 发送指令字符串，VM GlobalScript 解析后再调 SDK。MVS SDK 直连相机（CameraController）与 VM 占用冲突，无法同时打开。

**解决方案**：SET_PATH 和 DETECT_2D 从 TCP 改为 C# 端 VM SDK 直调。

```
迁移前:  C# ──TCP──→ VM GlobalScript ──SDK──→ VM 模块
迁移后:  C# ──SDK──→ VM 模块  (省去 TCP 上行)
```

| 操作 | 迁移前 | 迁移后 |
|------|--------|--------|
| 设置拍照路径 | TCP `SET_PATH:` → GlobalScript → SetGlobalVar | SDK `SetGlobalVar("ImageSavePath", path)` |
| 触发2D检测 | TCP `DETECT_2D:` → GlobalScript → SetImagePath + proc.Run | SDK `SetImagePath` + `VmProcedure.Run` |
| 检测结果接收 | TCP `DETECT_2D_OK:...` | TCP `DETECT_2D_OK:...`（不变） |

**性能**：省去 TCP 上行延迟 ~5-20ms，核心耗时不变，并行性不变。

**删除**：检测循环中的 `CameraController` 缓存清除调用（CameraController.cs 保留不动）。

**改动文件**：`ViewModels/MainViewModel.cs`
- 新增 `SetVmSavePath()`（SDK 直调全局变量）替代 3 处 TCP `SET_PATH`
- DETECT_2D 改为 `WaitForPrefixAsync` + SDK `SetImagePath` + `VmProcedure.Run`
- 删除 `_clearCacheTask`，简化移动等待逻辑
- 新增 `using GlobalVariableModuleCs;` `using ImageSourceModuleCs;`

**总结**：SET_PATH 和 DETECT_2D 从 TCP 指令改为 VM SDK 直调，省去 TCP 上行开销。CameraController 缓存清除调用已移除（底层代码保留），后续缓存清除将通过 3D VM 自身 GlobalScript 触发。

---

## 2026-06-08 v17

### 检测流程优化：移除无效回退场景 + 空针 #NULL 标记 + 3D 连接强制检查

**背景**：
1. 场景 A2（3D 旧格式，按 PinIndex 匹配）和 A3（无 3D 数据，纯 2D 结果）在当前系统中已不存在，不应保留无效回退逻辑
2. 空针（2D 返回 HasPin=0）的 3D 高度数据无效，不应独立显示；且叠加层中需要明确标记 `#NULL`
3. 2D 超时极少发生，发生时 dx/dy 应显示 "TIMEOUT"
4. 3D 客户端未连接时不允许开始检测（之前被注释掉）

**解决方案**：

1. **移除 A2/A3 场景**：删除旧格式 PinIndex 匹配和纯 2D 结果的合并逻辑
2. **修改场景 B**：2D 超时时用 3D 坐标创建 PinResult，DiffX/DiffY = "TIMEOUT"；空针不创建 PinResult（高度无效）
3. **空针叠加层**：空针也加入叠加层，绘制焊盘框 + 理想针框（空针专属颜色，默认橙黄色），文字标注 `#NULL`
4. **新增个性化设置**：空针框颜色 `EmptyPinColor`（默认 "orange"）、显示开关 `ShowEmptyPinBox`
5. **3D 连接检查**：`StartCheckFlow()` 入口恢复 `CheckVision3DConnected()` 检查

**改动文件**：
- `Models/SysParamData.cs` — 新增 `EmptyPinColor`、`ShowEmptyPinBox` 字段
- `ViewModels/PersonalizationViewModel.cs` — 新增绑定属性
- `Views/PersonalizationWindow.xaml` — 新增空针框颜色选择 + 显示开关
- `ViewModels/MainViewModel.cs` — ①启用 3D 连接检查 ②删除 A2/A3 代码 ③场景 B 改为 2D 超时+TIMEOUT 处理 ④空针加入叠加层 ⑤`DrawVmRects` 空针框绘制 ⑥`DrawVmText` #NULL 标注 ⑦日志措辞更新

**总结**：移除了已不存在的 A2（旧格式 PinIndex 匹配）和 A3（纯 2D 无 3D）合并场景；场景 B 从"纯 3D 高度"改为"2D 超时 → dx/dy=TIMEOUT"；空针在拼图上显示焊盘框+理想针框（橙黄色）并标记 `#NULL`，无实际针框；3D 未连接时禁止开始检测。

---

## 2026-06-08 v16

### 修复 3D 相机缓存清除：`EnsureCameraOpened()` 只尝试第一个设备导致打不开 3D 相机

**背景**：检测流程中，清除 3D 相机缓存前需要先打开相机。系统中有 2 个 MVS 相机（2D + 3D），`EnsureCameraOpened()` 默认只尝试打开枚举到的第一个设备。但排在前面的恰好是已被 VM 占用的 2D 相机（SN: `00DA7797928`），返回错误码 `0x80000203`（MV_E_ACCESS_DENIED），代码没有继续尝试第二个设备（3D 相机），导致缓存清除整体失败。日志中表现为：

```
找到 2 个相机设备
打开第一个可用相机: [0] (SN: 00DA7797928)
打开设备失败，错误码: 0x80000203   ← 2D相机已被VM占用
清除缓存失败: 相机未打开              ← 3D相机根本没被尝试
```

**解决方案**：修改 `EnsureCameraOpened()` 的 fallback 逻辑，从"只尝试第一个"改为"逐个尝试所有设备"：

```csharp
// 修改前：只打开第一个
Log.Info($"打开第一个可用相机: {devices[0]}");
return OpenDevice(devices[0]);

// 修改后：逐个尝试，跳过被占用的
foreach (var device in devices)
{
    Log.Info($"尝试打开相机: {device}");
    if (OpenDevice(device))
        return true;
    Log.Warning($"相机 {device.SerialNumber} 打开失败（可能已被占用），尝试下一个...");
}
Log.Error("所有相机均无法打开");
return false;
```

**改动文件**：
- `Core/Vision/CameraController.cs` — `EnsureCameraOpened()` 方法：逐个尝试所有枚举到的相机设备

**总结**：`EnsureCameraOpened()` 从只尝试第一个设备改为遍历所有设备逐个尝试打开，解决了 2D 相机被 VM 占用时无法自动切换到 3D 相机的问题。修复后，2D 相机打开失败时会自动尝试 3D 相机，缓存清除可以正常执行。

---

## 2026-06-05 v15

### 新增 CameraController + 3D 相机缓存清除方案

**背景**：3D 激光扫描仪每次扫描行程和实际移动行程不一致，导致下一次扫描图像被上一次残留数据影响。经分析，发现需要每次扫描前清除相机内部缓存和 VM 模块缓存。

**解决方案**：
1. **CameraController 封装**：基于 `MvCameraControl.Net` SDK 封装完整相机管理类
   - `EnumerateCameras()` — 枚举 MVS 相机设备（GigE/USB）
   - `OpenCameraByIndex/SN()` — 按索引或序列号打开相机
   - `StartGrabbing/StopGrabbing()` — 开始/停止采集
   - `ClearCameraCache()` — 清除相机缓存（Stop → Delay → Start 重置内部 FIFO）
   - `EnsureCameraOpened()` — 自动查找并打开第一个可用相机
   - `ResetCameraAsync()` — 完全复位相机（Close → Delay → Reopen）
2. **检测流程集成**：在每个检测位的 3D 扫描前自动调用缓存清除
3. **VM SDK 辅助刷新**：通过曝光时间参数重载触发 VM 相机模块刷新

**改动文件**：

1. **`Core/Vision/CameraController.cs`**（新建）— MVS 相机控制器，单例模式
2. **`ViewModels/MainViewModel.cs`** — 检测循环中集成缓存清除

---

## 2026-06-05 v14

### 通信架构全面优化（上行/下行）— 消除运动超时 + IO独立通道

**背景**：上位机通信时常出现几百毫秒延迟，运动完成回复偶发丢失导致8-10秒超时。经对比桌面下位机(330)源码和上位机(RBLAOI)源码，发现三个核心问题：①缺少行缓冲器导致回复被切断 ②IO轮询和运动命令共用串行队列互相阻塞 ③部分硬编码和低效通信模式。

**解决方案**：
1. **行缓冲器**（P0）：`OnSerialDataReceived` 追加到 `_lineBuffer`，`ParseDataTask` 只提取完整 `\r\n` 行，不完整内容等下次。彻底消除 X_COMPLETE 被切断丢失的问题
2. **前缀分发**（P1）：收到数据后按 `IO:`/`POS:`/`SPEED:`/完成信号前缀分流到 `IOStateUpdated`、`MotionEventReceived`、`PositionReceived` 等独立事件
3. **IO独立通道**（P1）：`IoPollLoop` 直接 `SendCommand("GET_IO")` 不走命令队列，IO回复通过 `IOStateUpdated` 事件接收
4. **IO轮询间隔**：默认从 200ms → **20ms**（最小值同步从 50→20），急停响应提升10倍
5. **`WaitAxisComplete`**：订阅 `MotionEventReceived` 替代 `DataReceived`，减少非运动数据扫描
6. **`WaitAxesStop`**（P3）：用 `GetAllAxisStatus` 一次查询代替逐轴发 `GET_STATUS`，减少75%通信量
7. **`PositionInfo`**（P2）：`Xmm/Ymm/Zmm/Hmm` 改用 `SysParam.Instance.Data.PulseEquivalent`，消除硬编码500
8. **下位机固件**（P5）：① 主板+电机板 `protocol_dispatch` 未识别命令返回 `ERROR:Unknown cmd\r\n` ② 电机板 `UART_RX_BUF_SIZE` 64→256

**改动文件**：

1. **`Core/Motion/MotionIO.cs`**（核心重构）：
   - 新增 `_lineBuffer` 行缓冲器，`ParseDataTask` 仅提取完整行
   - 新增 `DispatchLine()` 按前缀分发到独立事件
   - 新增 `MotionEventReceived`、`PositionReceived`、`SpeedReceived`、`SystemEventReceived` 事件
   - `IoPollLoop` 改为直接发 `GET_IO` + 事件接收，不走命令队列
   - `WaitAxisComplete` 改用 `MotionEventReceived` 订阅
   - `WaitAxesStop` 改用 `GetAllAxisStatus` 一次性查询
   - 移除未使用的 `_receiveBuffer` 字段
   - `PositionInfo` 属性改为 `SysParam.Instance.Data.PulseEquivalent`

2. **`Models/SysParamData.cs`**：
   - `IoPollInterval` 默认值 200→20

3. **下位机 `330 主板程序 5.15/User/Src/app_comm.c`**：
   - `protocol_dispatch` 未识别命令返回 `ERROR:Unknown cmd\r\n`

4. **下位机 `motor_330 5.15/User/Src/app_comm.c`**：
   - `protocol_dispatch` 未识别命令返回 `ERROR:Unknown cmd\r\n`

5. **下位机 `motor_330 5.15/User/Inc/user_defines.h`**：
   - `UART_RX_BUF_SIZE` 64→256

---

## 2026-06-05 v13

### 2D/3D 合并逻辑重构：2D-First + 坐标校准 + 排除NG空针

**背景**：上机测试发现合并逻辑有严重问题——当前遍历3D结果找2D匹配（3D-first），但3D轮廓仪坐标与2D相机像素坐标存在系统偏移，导致最近邻匹配失败。结果变成：3D有数据时所有dx=0、dy=0（只有高度），3D无数据时才有2D的dx/dy。另外3D测了2D已过滤的NG空针，数量对不上。

**解决方案**：
1. **合并方向反转**：改为2D-first，以2D有效结果（排除空针）为基础遍历
2. **坐标系统偏差校准**：通过2D点集与3D点集的中心差自动估算偏移量（`ΔX=avg2D_X-avg3D_X`），校准后再做最近邻匹配
3. **一对一匹配**：已匹配的3D针脚从候选池移除，防止多个2D针脚匹配到同一个3D
4. **NG空针自动过滤**：3D中匹配不到2D的针脚视为2D已过滤的空针/歪针，跳过
5. **每检测位明细日志 + 全流程汇总**：输出合并统计和最终检测结果

**改动文件**：

1. **`ViewModels/MainViewModel.cs`**（替换合并逻辑，+日志）：
   - 合并部分：从3D-first遍历改为2D-first遍历（4种场景覆盖）
   - 新增坐标校准：`offsetX/Y = avg2D - avg3D`，输出日志
   - 新增 `mergeMatched3D`/`mergeNoMatch3D`/`mergeUnmatched3DSkipped` 跟踪
   - 新增每检测位明细日志 `[检测位 i 合并明细]`
   - 新增全流程总汇总（总针脚、2D+3D完整/仅2D、OK/NG、合格率）

**总结**：
彻底扭转了合并方向，解决坐标不匹配导致的dx/dy丢失问题，并增加了完整的可追溯日志体系。
- `validPins2D` 排除空针 → `mergeMatched3D` 一对一填高度 → `mergeUnmatched3DSkipped` NG空针跳过
- 运行后查看日志：先看每检测位的 `2D/3D坐标校准偏移` 判断ΔX/ΔY是否合理，再看 `[检测位 i 合并明细]` 确认匹配数量，最后看 `========== 检测汇总 ==========` 全局结果

## 2026-06-05 v13.1

### bugfix: 3D Y坐标转换公式修正 + 3D重叠过滤与2D对齐

**背景**：上机实测发现两个问题：
1. 3D坐标 `Y = phys_x/1000` 公式缺少负号，导致Y轴方向与板面相反，3D#0(H=3.886)匹配到2D#2而不是2D#0，后续匹配级联错误
2. 3D重叠过滤未应用重叠率（`project.Overlap`），使用全宽扫描范围，与2D有效区不一致，3D比2D多出针脚

**解决方案**：
1. **Y轴公式修正**：`y = -phys.x / 1000.0`（加负号），使扫描方向与板面Y方向对齐
2. **3D重叠率对齐**：使用 `effHalfW = scanHalfWidth * (1 - overlap)` 替代 `scanHalfWidth`，使3D有效区与2D一致
3. **独占区分界优化**：相邻检测位的分界点改为 `(本有效区右边界 + 下个有效区左边界) / 2`

**改动文件**：

1. **`ViewModels/MainViewModel.cs`**（2处）：
   - `ParseDetect3DResponse()`：`y = phys.x/1000` → `y = -phys.x/1000`
   - 3D重叠区过滤：全宽扫描 → `scanHalfWidth * (1 - project.Overlap)`，命中率输出到日志

2. **`检测流程.md`**：
   - 第5.4节坐标转换表：`Y_rel = phys_x/1000` → `Y_rel = -phys_x/1000`

**总结**：
两个bug都是在实测中发现的坐标映射问题：Y轴方向反了导致匹配级联错误，重叠率不一致导致2D/3D过滤结果数量不匹配。修正后经手工验算，匹配关系和H值完全正确。

**背景**：之前的上位机通过 TCP 发送 `DETECT_3D` 指令主动查询 3D 结果（请求-响应模式）。改为硬触发模式后，3D 相机在运动过程中自动采集，扫描完成后 3D VM 通过网口主动推送 `DETECT3D_OK:{数据}` 给上位机。

**解决方案**：在 `Vision3DClient` 中新增 `WaitForPrefixAsync(string prefix, int timeoutMs)` 方法——只监听不发送，封装 TCS/Handler 生命周期，与现有 `SendCommandAndWaitAsync` 风格一致。上位机不再发送 `DETECT_3D` 查询，改为等待推送结果。

**改动文件**：

1. **`Core/Vision/Vision3DClient.cs`**（新增）：
   - `WaitForPrefixAsync(prefix, timeoutMs)`：等待指定前缀的主动推送数据，内部管理 TCS 和事件订阅/取消，超时返回 "TIMEOUT"
   - 与 `SendCommandAndWaitAsync` 区别：只监听不发送，`finally` 块保证 handler 及时清理

2. **`ViewModels/MainViewModel.cs`**（2 处）：
   - `StartCheckFlow()`：移除 `DETECT_3D` 发送+等待调用，改为 `_vision3DClient.WaitForPrefixAsync("DETECT3D_OK:", 3000)`
   - `ParseDetect3DResponse()`：前缀剥离逻辑增加 `DETECT3D_OK` 支持（优先匹配新推送格式，旧 `DETECT_3D_OK` 向后兼容）

3. **`检测流程.md`**（4 处）：
   - 5.1 通信协议表更新为被动推送模式
   - 5.2/5.3 新增推送格式说明，旧查询格式降级为 5.3
   - 5.6 触发流程图更新
   - 版本历史追加 v12

**总结**：
从主动查询改为被动推送，仅涉及 3D 数据的获取方式变化，数据格式和合并逻辑完全不变。
- `Vision3DClient` 新增 `WaitForPrefixAsync` 通用方法
- `MainViewModel` 检测循环替换为一行调用
- `ParseDetect3DResponse` 兼容新旧两种前缀
- `检测流程.md` 协议文档同步更新

## 2026-06-04

### 2. 3D 物理坐标解析改用转置映射 + (x&y&z) 原生格式

**背景**：上机实测发现 3D 轮廓仪返回的数据格式为 `(x&y&z)(x&y&z)...` 原生输出，包含像素坐标前半和物理坐标后半。之前假设的逗号分隔格式与实际不符。同时需要正确建立 3D 物理坐标到板面坐标的映射关系。

**数据分析**（三组实测数据验证）：
- 物理坐标（phys_x, phys_y, phys_z）µm → 通过转置映射可精确匹配 2D 结果
- phys_y = -pixel_y × 11µm（验证通过），phys_x = pixel_x × 11µm - 偏移量
- 像素坐标可忽略，直接使用物理坐标

**转置映射验证（所有数据误差 < 0.1mm）**：
- 轮廓仪扫描方向(phys_x) → 板面Y方向（公式：`Y = phys_x/1000`）
- 轮廓仪传感器方向(phys_y) → 板面X方向（公式：`X = -phys_y/1000`）
- 高度：`H = phys_z/1000`

**原点对齐方案**：由于 3D 坐标原点依赖于相机停靠位置（每个检测位不同），采用**组内均值对齐法**：
- 计算该检测位所有 3D 针脚坐标的组内均值
- 以 detectX/detectY 为参考点，`absX = detectX + (pin3D.X - meanX)`, `absY = detectY + (pin3D.Y - meanY)`
- 验算结果：匹配误差 <0.5mm，全部在 2mm 阈值内

**改动文件**：

1. **`ViewModels/MainViewModel.cs`**（2 处）：
   - `ParseDetect3DResponse()`：完全重写，支持 `(x&y&z)` 格式提取 + 物理/像素分离 + 转置映射；向后兼容旧逗号格式
   - `StartCheckFlow()` 合并逻辑：改用组内均值对齐法计算 3D 绝对坐标；更新重叠区过滤公式

2. **`检测流程.md`**（4 处）：
   - 4.2 坐标计算：注释更新为"板面Y方向"
   - 5.1-5.5 3D TCP 协议：完全重写，新格式 `(x&y&z)` 说明 + 转置映射表 + 数据对齐方式
   - 6.2 合并逻辑：更新为 v11 转置映射+组内均值对齐示例代码
   - 6.3 特殊情况：增加 v11/v10/v9- 各类格式说明
   - 版本历史追加 v11

**总结**：3D 物理坐标解析改用转置映射（扫描方向→板面Y，传感器方向→板面X），格式升级为 `(x&y&z)` 原生输出（取后半物理坐标），组内均值对齐法替代固定偏移量。旧格式向后兼容。

---

### 1. 3D 扫描行程固定 26mm + 相邻检测位重叠区过滤 + 基于坐标的 2D/3D 合并

**背景**：
- 问题1：3D 相机扫描行程只能设为整数（26mm），而 2D FOV 宽度为 26.15mm（浮点数），若按 FOV 边界对齐每个检测位累积 0.15mm 误差。
- 问题2：相邻检测位步长约 21mm，小于 3D 扫描行程 26mm，重叠区内的针脚会被两个检测位重复检测。

**解决方案**：

问题1（累积误差）→ 3D 扫描不以 FOV 边界对齐，改为以检测位中心点 (detectX, detectY) 对齐：
- `startX = detectX - 13.0 + offsetX`（固定 ±13mm）
- `endX   = detectX + 13.0 + offsetX`
- 不再使用 `project.FovWidth` 计算扫描范围
- 2D 和 3D 都以 detectX 为中心，坐标映射用相对偏移量，误差不累积

问题2（重复检测）→ 按相邻扫描范围中点划分独有区域：
- 当前检测位独有区域 = [detectX-13.0, 分界点)
- 分界点 = (当前3D右边界 + 下一个3D左边界) / 2
- 在解析 3D 返回的针脚数据时，按物理 X 坐标过滤，只保留落在独有区域内的针

合并逻辑改进：
- 3D 响应格式增加坐标字段：`DETECT_3D_OK:pinIdx,relXmm,relYmm,height;...`
- 3D 针脚绝对坐标 = (detectX-13.0) + relXmm, detectY + relYmm
- 合并方式从 PinIndex 匹配改为基于物理坐标的欧氏距离最近匹配（阈值 2mm）
- 2D 判定的空针，其 3D 高度数据也一并剔除

**改动文件**：

1. **`ViewModels/MainViewModel.cs`**（3 处）：
   - `StartCheckFlow()`：startX/endX 计算公式改为 detectX ± 13.0 + offsetX
   - `ParseDetect3DResponse()`：返回类型增加 X/Y 坐标字段，支持新旧两种格式
   - `StartCheckFlow()` 合并逻辑：重叠区过滤 + 基于坐标匹配 + 空针剔除

2. **`检测流程.md`**（3 处）：
   - 4.2 坐标计算更新为 ±13mm 公式
   - 5.x 新增新 3D 格式说明（含坐标）
   - 5.3/5.4 响应格式和数据对齐更新
   - 6.2/6.3 合并逻辑和特殊场景更新
   - 版本历史追加 v10

3. **`测试数据.md`**（3 处）：
   - 所有检测位的 startX/endX 计算更新
   - 3D 响应示例更新为新格式（含坐标）
   - 合并结果表添加坐标匹配步骤说明

**总结**：3D 扫描行程固定 26mm（detectX±13mm），避免 2D/3D FOV 不匹配的累积误差；相邻检测位以中点划分独有区域，过滤重复针脚；合并逻辑改为基于坐标匹配，空针高度数据一并剔除。旧格式（无坐标）向后兼容。

---

## 2026-05-30

### 1. VM空针数据不再显示/保存 + 移除空针标注设置

**背景**：VM方案检测时，`{焊盘数据}&{NG}&{针脚数据}` 格式中的 NG 代表空针（有焊盘无针脚）。空针本身无实际检测意义，不需要显示在界面、不需要保存、不需要标注。

**改动**：

1. **`SysParamData.cs`** — 移除 `NullColor` 字段（空针颜色设置不再需要）
2. **`PersonalizationViewModel.cs`** — 移除 `NullColor` 属性
3. **`PersonalizationWindow.xaml`** — 移除"空针 #NULL 颜色"颜色选择控件
4. **`MainViewModel.cs`**（共5处）：
   - VM叠加层构建：空针不加入 `_vmOverlays`（不产生空针标注）
   - 2D+3D合并结果：空针跳过 `continue`，不加入 `PinResults`
   - 仅2D结果：空针跳过 `continue`，不加入 `PinResults`
   - `DrawVmText`：移除空针分支代码（不再绘制 `VM #N NULL` 橙色文字）
   - `LoadVmOverlays`：加载时过滤 `Where(r => !r.IsEmptyPin)` 清除旧数据中的空针

**效果**：空针数据不再出现在结果列表和统计中，Halcon窗口不再显示空针标注，个性化设置中不再有空针颜色选项。

### 2. IAP 固件升级插件

**背景**：需要在上位机集成 IAP 固件升级功能，支持对主板(F407)和电机板(F103)进行固件升级。不动 MotionIO.cs，通过关闭 MotionIO 释放串口、IAPService 自建 SerialPort 通信、完成后重启 MotionIO 的方式实现。

**改动**：

1. **`Core/IAP/IAPService.cs`**（新建）— IAP二进制帧协议服务类：
   - 完整的 PING → INFO → ERASE → WRITE → VERIFY → JUMP 升级流程
   - Intel HEX 文件解析器
   - CRC16-CCITT 校验（多项式 0x8408，初值 0xFFFF）
   - 基于 LEN 定界的帧读取器
   - 进度回调 `Action<ProgressInfo>` 供 UI 绑定

2. **`Views/IAPWindow.xaml`**（新建）— 固件升级界面：
   - 升级流程说明
   - 固件文件选择（.hex）+ 浏览按钮
   - 升级目标选择（主板 F407 / 电机板 F103）
   - 进度条 + 当前阶段状态文字
   - 日志输出（Consolas 字体，只读TextBox，自动滚动）

3. **`Views/IAPWindow.xaml.cs`**（新建）— 升级流程控制：
   - `MotionIO.SendCommandAsync("IAP_UPDATE"/"IAP_MOTOR")` → 发升级命令
   - `MotionIO.Close()` → 释放串口
   - `Task.Delay(5000)` → 等待 MCU 复位进 Bootloader
   - `new SerialPort(portName, baudRate)` → 自建串口（DtrEnable/RtsEnable=false）
   - `new IAPService(port).Upgrade()` → 执行升级
   - `motionIO.Open(portName, baudRate)` → 恢复串口通信

4. **`MainViewModel.cs`** — 新增 `ShowFirmwareUpgrade()` 方法，打开 IAPWindow

5. **`MainWindow.xaml.cs`** — `InitializeActionMap` 新增 `firmware_upgrade` 按钮映射；`AddSettingSubMenu` 新增"固件升级"子按钮

**效果**：设置菜单新增"固件升级"按钮，点击打开固件升级对话框，支持选择 .hex 文件和升级目标，执行完整 IAP 升级流程并显示进度和日志。

**影响范围**：
- `Core/IAP/IAPService.cs`（新建）
- `Views/IAPWindow.xaml`（新建）
- `Views/IAPWindow.xaml.cs`（新建）
- `ViewModels/MainViewModel.cs`
- `Views/MainWindow.xaml.cs`

### 3. 3D 检测流程改为串行：CAMERA3D_ON→移动→CAMERA3D_OFF→DETECT_3D

**背景**：3D 线扫需要精确控制一个 FOV 的取流窗口。原流程中 DETECT_3D 与移动到终点并行发送，无法保证 3D 采集时序。现在改为：到达起点后立即开启 3D 相机触发（`CAMERA3D_ON`），移动到终点后立即关闭触发（`CAMERA3D_OFF`），完成 FOV 采集后再发送 `DETECT_3D` 获取高度数据。

**改动**：

1. **`MainViewModel.cs` — 检测流程重写**（`StartCheckFlow` 步 4→6）：
   - 原步(4) 并行执行 `MoveToAndWaitStop(endX)` + `DETECT_3D` → 拆分为三步串行：
     - **(4) 开启触发**：到达起点后发送 `CAMERA3D_ON`，等待 `CAMERA3D_ON_OK`
     - **(5) 移动到终点**：`MoveToAndWaitStop(endX, moveY)` 移动过程中完成 FOV 线扫采集
     - **(6) 关闭触发+DETECT_3D**：到达终点后先发 `CAMERA3D_OFF`，再发 `DETECT_3D` 获取结果
   - 超时值：`CAMERA3D_ON/OFF` → 2000ms，`MoveToAndWaitStop` → 5000ms，`DETECT_3D` → 3000ms

2. **`检测流程.md` 文档同步更新**：
   - YOLO 流程（2.1节）和 VM 流程（3.1节）的 3D 线扫块改为五步串行流程
   - 主流程（4.1节）Step 3/Step 4 更新为新的时序描述
   - 代码结构（4.3节）更新为新的串行调用序列
   - 3D TCP 协议（5.2节）触发时机改为完整的时序图和文字描述
   - 版本历史追加 v6

**效果**：3D 相机触发由 IO 位精确控制起点/终点，FOV 采集窗口 = CAMERA3D_ON 到 CAMERA3D_OFF 的时间段，DETECT_3D 在采集完成后才发送，数据可靠。

**影响范围**：
- `ViewModels/MainViewModel.cs`：StartCheckFlow 3D 检测段
- `检测流程.md`：流程图、步骤说明、协议描述、版本历史

---

## 2026-05-27

### 多项功能新增与优化

**背景**：检测流程中发现 _imageManager 数据为空的问题（未加载方案就检测），以及 YOLO/VM 叠加层互相干扰的问题。同时新增相对焊盘偏移参数支持、VM 重叠率过滤、空针颜色自定义、检测自动切换结果页等功能。

**改动**：

1. **检测前检查**（`MainViewModel.cs:StartCheckFlow`）
   - 新增 `_imageManager.ImageCount == 0` 检查，未加载方案图片时提示用户

2. **YOLO/VM 叠加层互不干扰**（`MainViewModel.cs`）
   - 新增 `DeleteVmOverlaysFile()` 方法，检测开始时同时删除 YOLO 和 VM 叠加层文件
   - 修复因懒惰加载（`LoadYoloOverlays`/`LoadVmOverlays`）导致的旧叠加层重新显示问题

3. **VM 图片命名改为 GRAB_OK_时间戳**（`MainViewModel.cs:LoadProjectImagesAndDisplayAsync`）
   - 图片排序由正则提取末尾数字改为按文件名排序，兼容时间戳格式

4. **相对焊盘偏移参数**
   - `SysParamData.cs`：新增 `RelativeOffsetX`、`RelativeOffsetY` 字段（默认 0mm）
   - `ProjectData.cs`：新增 `RelativeOffsetX`、`RelativeOffsetY` 字段
   - `SysParamViewModel.cs`：新增绑定属性
   - `SysParamWindow.xaml`：界面增加两个输入框
   - `EditProjectWindow.xaml/.cs`：方案编辑界面增加两个字段+恢复系统参数
   - `MainWindow.xaml`：主视图显示偏移X/Y，与系统不一致时变红
   - `MainViewModel.cs`：YOLO 和 VM 检测的 dx/dy 计算公式统一改为：`idealX = padX + ProjectRelativeOffsetX`，`idealY = padY + ProjectRelativeOffsetY`
   - YOLO 的硬编码 `SHIFT_PIXELS(18px)` 已从 dx/dy 计算中移除（Python 脚本中的匹配逻辑仍保留）

5. **VM 重叠率过滤**（`MainViewModel.cs:StartCheckFlow` VM 检测路径）
   - VM 检测结果出来后增加有效区过滤，剔除落在重叠区的针脚，避免多检

6. **VM 空针 #NULL 个性化颜色**（`SysParamData.cs`、`PersonalizationViewModel.cs`、`PersonalizationWindow.xaml`）
   - 新增 `NullColor` 字段（默认橙色），在个性化设置界面可选颜色

7. **检测开始后自动切换到"检测结果"标签页**（`MainViewModel.cs`、`MainWindow.xaml`）
   - 新增 `SelectedTabIndex` 属性，检测开始时自动设置索引为 3

### Phase 3：理想针框叠加层 + 偏移重算 + 标注框显示开关

**背景**：用户要求在叠加层中增加理想针框（相对偏移焊盘框），并在修改相对偏移量时自动重新计算 dx/dy。同时为个性化设置界面增加标注框显示开关，让用户可以控制每种框的可见性。

**改动**：

1. **理想针框叠加层**（`MainViewModel.cs:DrawYoloRects/DrawVmRects`）
   - `YoloOverlayItem`、`VmOverlayItem` 新增 `IdealPinMmX`、`IdealPinMmY` 字段
   - YOLO 检测和 VM 检测时，理想针位置 = 焊盘坐标 + 相对偏移量
   - 绘制逻辑：在理想针位置绘制金色（gold）矩形框，受 `ShowIdealPinBox` 控制
   - `SysParamData.cs`：新增 `IdealPinColor`（默认 gold）
   - `PersonalizationViewModel.cs`：新增 `[SysParam] IdealPinColor` 绑定属性

2. **相对偏移重算 dx/dy**（`MainViewModel.cs:RecalculateOffsetDxDy`）
   - 新增 `RecalculateOffsetDxDy()` 方法：当 `ProjectRelativeOffsetX/Y` 改变时
     - 重新计算所有 YOLO/VM 叠加层的 `IdealPinMmX/Y`、`Dx/Dy`、`IsOK`
     - 同时更新 `PinResults` 的 `DiffX`/`DiffY`/`Result`
   - `ShowEditProjectDialog()` 中自动检测偏移量是否变化，决定调用 `RecalculateOffsetDxDy()` 或 `RecalculatePinResults()`

3. **标注框显示开关**（`PersonalizationWindow.xaml`、`PersonalizationViewModel.cs`）
   - `SysParamData.cs`：新增 `ShowPadBox`、`ShowPinBox`、`ShowIdealPinBox`（默认 true）
   - `PersonalizationViewModel.cs`：新增对应 `[SysParam]` 绑定属性
   - `DrawYoloRects`/`DrawVmRects`：每种框绘制前检查对应开关，未勾选则不绘制
   - 界面重新组织为"颜色设定"和"标注框绘制"两个区域，颜色设定含焊盘/针尖/理想针/NULL/文字背景色，标注框绘制含三个复选框

**影响范围**：
- `Models/SysParamData.cs`：新增 IdealPinColor、ShowPadBox/ShowPinBox/ShowIdealPinBox
- `ViewModels/PersonalizationViewModel.cs`：新增对应绑定属性
- `Views/PersonalizationWindow.xaml`：重新组织布局，新增理想针框颜色、标注框复选框
- `ViewModels/MainViewModel.cs`：RecalculateOffsetDxDy 方法、ShowEditProjectDialog 偏移检测、绘制方法理想针框+显示开关

## 2026-05-22

### 修复检测重拍 GRAB 超时：移除冗余 SET_PATH + 增加重试间隔

**背景**：检测流程中重拍（re-grab）的 GRAB 命令频繁超时，而全图采集中的 GRAB 正常。日志显示检测重拍 GRAB 每 10s 超时后立即重试无间隔，且每次重试都重新发送 SET_PATH（VM 实际忽略此指令）。连续无间隔的 GRAB 指令导致 VM 无法及时响应。

**改动**：
1. **移除检测重拍中的 SET_PATH 发送**（`MainViewModel.cs:1888`）：VM 实际忽略 SET_PATH，文件固定保存到 D:\GrabImage。发送多余指令可能干扰 VM 状态，直接 GRAB 更可靠。
2. **首次 GRAB 前增加 200ms 延迟**（`MainViewModel.cs:1889`）：给 VM 就绪时间。
3. **重试间增加 500ms 延迟**（`MainViewModel.cs:1896`）：全图采集已有 `Task.Delay(50)`，检测重拍完全无间隔，补上延迟让 VM 有恢复时间。

**影响范围**：
- `ViewModels/MainViewModel.cs`：检测重拍的 GRAB 重试逻辑

### 修复检测重拍图片找不到：增加 D:\GrabImage 兜底

**背景**：VM 实际忽略 SET_PATH，GRAB 文件固定保存到 D:\GrabImage 而非 Origin 目录。检测重拍的图片查找逻辑只检查 Origin，导致文件找不到时直接跳过，图片不被替换。全图采集的 `ParseAndSaveImageAsync` 已有 D:\GrabImage 兜底正常，检测重拍缺少同样逻辑。

**改动**：
1. **检测重拍图片查找改为两步检查**（`MainViewModel.cs:1934-1945`）：先查 Origin，找不到则从 D:\GrabImage 复制。
2. **文件找不到时改为 warn 日志而非静默跳过**（`MainViewModel.cs:1987`）：明确记录 VM 文件名方便定位问题。

**影响范围**：
- `ViewModels/MainViewModel.cs`：检测重拍的图片查找逻辑

## 2026-05-21

### 有效区过滤：剔除重叠区的重复检测结果

**背景**：由于 FOV 之间设了 15% 重叠率，YOLO 在相邻检测位的重叠区会独立检到同一根针，导致重复标注（多检），且同一针在两张不同图片中的 dx/dy 计算结果不一致（一个判 OK、一个判 NG），影响判定。

**改动**：
1. **`DetectWithYoloAsync`**：新增 `overlap` 参数，YOLO 检测完每根针的全局坐标后，计算有效区边界（`effHalfW/H = fovWidth/Height * (1 - overlap) / 2`），若 `|pinMmX - detectX| > effHalfW` 或 `|pinMmY - detectY| > effHalfH` 则 `continue` 丢弃该结果
2. **调用处**：在检测循环中传 `project.Overlap` 给 `DetectWithYoloAsync`

**原理**：有效区 = FOV 居中去除 overlap 比例的区域（如 overlap=15% 时去掉两侧各 7.5%，剩余居中 85%）。相邻有效区间完美邻接无重叠，每一根针恰好落在唯一一个有效区内，从根本上消除多检。

### 修改方案参数后同步刷新 Halcon 标注颜色

**背景**：在修改方案对话框调整允许 XY 偏差点击确定后，只有结果列表（DataGrid）更新了 OK/NG，Halcon 窗口上的绿色/红色标注文字没有更新，导致文字颜色与结果列表不一致。

**改动**：
1. **新增 `SyncYoloOverlayResults()`**：通过 `GridIndex` + `PinIndex` 匹配，将 `PinResults` 的最新 `Result` 同步到 `_yoloOverlays` 的 `IsOK` 字段
2. **`ShowEditProjectDialog` 增强**：`RecalculatePinResults` 后调用 `SyncYoloOverlayResults()` + 重绘 Halcon 窗口（清窗 → 显示底图 → 画矩形/文字 → RefreshDisplay），确保标注颜色实时更新

### 修复 RecalculatePinResults 中 YOLO 检测结果全部变 NG 的问题

**背景**：在修改方案对话框修改允许 XY 偏差后点击确定，所有检测结果都变成 NG。原因是纯 YOLO（2D）检测的针脚 `H=0`，而 `RecalculatePinResults` 中统一做了 `|H - idealHeight| <= allowedHDev` 的高度判定，`idealHeight` 默认 2.0mm，`|0 - 2.0| = 2.0` 远超 0.1mm 允许偏差，全部判 NG。

**改动**：
1. **`RecalculatePinResults`**（`MainViewModel.cs:2242`）：将高度判定改为 `pin.H == 0 || Math.Abs(pin.H - idealHeight) <= allowedHDev`。`H=0` 表示无 3D 数据（纯 YOLO 检测），此时跳过高度检查，恢复与初始检测一致的判定逻辑

### 修复选择模式下拖拽中选区矩形不显示的问题

**背景**：上个修复让选择模式下的拖拽可以工作了，但拖拽过程中白色虚线框和红色实线框不显示，只有释放鼠标后才出现。原因是拖拽中的 `DrawOverlays` 委托只画了 YOLO 框，没有画选区矩形。

**改动**：
1. **提取 `DrawSelectionRectangles(HWindow hw)`**：将选区矩形绘制逻辑从 `RefreshSelectionDisplay()` 中提取为独立方法，只画矩形不清窗/不显示底图，供多处调用
2. **`RefreshSelectionDisplay()` 简化**：清窗+显示底图后调用 `DrawSelectionRectangles`，消除代码重复
3. **`DrawOverlays` 委托增强**：在 `SetHalconWindow` 中的 `DrawOverlays` 委托增加选择模式判断，处于选择模式时先画选区矩形再画 YOLO 框，实现拖拽中实时显示

### 修复选择模式下缩放/拖拽/中键导致底图丢失的问题

**背景**：选择模式下滚轮缩放、右键拖拽平移、中键居中均被禁用，原因是这些操作会导致拼接底图丢失，只留下红色选区框和白色虚线。现在需要取消禁用并修复底图丢失的根因。

**改动**：
1. **`CustomHSmartWindowControl.OnMouseWheel`**：移除选择模式的直接返回，让基类处理缩放后通过 `NotifyImagePartChanged()` 触发全量重绘
2. **`CustomHSmartWindowControl.OnMouseRightButtonDown`**：移除选择模式的直接返回，走正常拖拽流程（拖拽中通过 `RedrawStitchedImage()` 保持底图可见，拖拽结束时通过 `NotifyImagePartChanged()` 重绘选区矩形）
3. **`CustomHSmartWindowControl.OnMouseDown`（中键）**：移除选择模式的直接返回，允许 `FitToWindow()` 居中后触发 `NotifyImagePartChanged()`
4. **`MainViewModel.OnImagePartChanged`**：新增选择模式判断，若处于选择模式则调用 `RefreshSelectionDisplay()` 完整重绘（底图 + 白色虚线框 + 红色实线框 + YOLO叠加层），而非仅重绘 YOLO

**原理**：缩放/平移改变了 Halcon 窗口的视口（SetPart），基类内部渲染周期会清窗导致 `DispObj` 显示的图像丢失。通过 `ImagePartChanged` 事件统一在视口变化后重新 `DispObj` 拼图 + 绘制所有叠加层，从根本上解决底图丢失问题。

### 修复叠加层缩放闪烁和拖拽清空问题

**背景**：检测结果条目较多时，滚轮缩放导致窗口闪烁（ClearWindow 让窗口短暂变空），右键拖拽平移后叠加层消失（拖拽不触发 ImagePartChanged）。两者根因各不同但都需要修复。

**改动**：
1. **防抖重绘**（`MainViewModel.cs`）：新增 `_overlayRedrawPending` 开关，连续缩放/平移只执行最后一次重绘，避免大量排队
2. **`OnImagePartChanged` 去掉清窗和 CopyImage**（`MainViewModel.cs`）：不再 `ClearWindow` + `DispObj(GetCurrentStitchedImage)`，基类缩放/平移后 Halcon 窗口图像依然存在，只需重绘叠加层 → 无闪烁
3. **拖拽中调 `base.OnMouseMove(e)`**（`CustomHSmartWindowControl.cs`）：拖拽时 SetPart 后通知基类同步内部状态，防止基类渲染周期用失效状态覆盖视口导致底图丢失
4. **恢复右键事件的 base 调用**（`CustomHSmartWindowControl.cs`）：`OnMouseRightButtonDown/Up` 的拖拽和菜单路径均调 `base.*`，保持基类事件状态平衡
5. **文字始终 `box=true`**（`MainViewModel.cs DrawYoloText`）：`TextBgColor` 为空时默认 `"black"`，覆盖缩放残留的旧文字，文字叠加问题根治
6. **批量绘制 / flush 控制**（`MainViewModel.cs DrawYoloRects/DrawYoloText`）：`SetWindowParam("flush", "false")` + `try-finally` 保证批处理刷新
7. **重绘优先级**：`DispatcherPriority.Background` → `DispatcherPriority.Render`

**影响范围**：
- `ViewModels/MainViewModel.cs`
- `Core/Controls/CustomHSmartWindowControl.cs`

## 2026-05-20

### 个性化设置独立界面 + 焊盘/针尖/文字背景颜色字段

**背景**：原个性化设置字段放在系统参数界面中，现拆分为独立窗口；新增焊盘框颜色、针尖框颜色、文字背景色3个字段。

**改动**：
1. **右键菜单"显示检测结果"改为"结果图层"**：改为复选框形式，初始状态为隐藏
2. **应用程序重启默认隐藏结果图层**：`_isOverlayVisible` 默认值改为 `false`，需用户手动点击"结果图层"显示
3. **按需加载叠加层数据**：移除所有项目加载时的 `LoadYoloOverlays()` 调用，改为在用户点击"结果图层"时按需加载
4. **切换项目时清除旧叠加层**：每个项目加载方法中增加 `_yoloOverlays.Clear()`
5. **右键菜单状态同步**：`CustomHSmartWindowControl` 新增 `IsOverlayVisible` 属性，右键菜单复选框状态跟随实际可见状态
6. **新增 `Views/PersonalizationWindow.xaml` + `PersonalizationWindow.xaml.cs`**：独立个性化设置窗口，风格模仿系统参数界面
2. **新增 `ViewModels/PersonalizationViewModel.cs`**：独立的 ViewModel，含反射同步 SysParamData
8. **SysParamWindow.xaml**：移除「界面结果显示设置」分组框
9. **SysParamViewModel.cs**：移除已迁移的 TextOffsetX/Y、OverlayFontSize/Name、AvailableFonts、LoadSystemFonts
10. **SysParamData.cs**：新增 `PadColor`(默认 cyan)、`PinColor`(默认 yellow)、`TextBgColor`(默认空=无背景)
6. **MainViewModel.cs `DrawYoloRects`**：改用 `_sysParam.Data.PadColor/PinColor`
7. **MainViewModel.cs `DrawYoloText`**：增加文字背景色支持（带 try-catch 兼容旧 Halcon）
8. **MainViewModel.cs `ShowPersonalization`**：关闭窗口后自动刷新叠加层，颜色/字体/偏移即时生效

**关于颜色立即生效**：点击确定后，代码自动获取当前拼图并重新绘制矩形+文字，新颜色即刻可见。文字背景色受 Halcon 版本支持限制，若旧版不支持则静默忽略。

**影响范围**：
- `Models/SysParamData.cs`
- `ViewModels/PersonalizationViewModel.cs`（新）
- `Views/PersonalizationWindow.xaml`（新）
- `Views/PersonalizationWindow.xaml.cs`（新）
- `ViewModels/MainViewModel.cs`
- `ViewModels/SysParamViewModel.cs`
- `Views/SysParamWindow.xaml`

## 2026-05-20

### 四个改动：保存自动退出选择、检测后自动绘制叠加层、YOLO持久化、方案历史重构

**改动 1 — 保存方案自动退出选择模式**：
- `MainViewModel.SaveProject()` 开头调用 `ExitSelectInspectionMode()`

**改动 2 — 检测位完成后自动绘制叠加层**：
- 每个检测位结果处理后（含 YOLO/VM 两种模式），统一调用 `DrawYoloRects` + `DrawYoloText`
- 移除 YOLO 分支中的独立绘制，合并到公共流程

**改动 3 — YOLO 叠加层数据持久化**：
- `_yoloOverlays` 保存到 `{ProjectPath}/Result/YoloOverlays.json`
- 新增 `SaveYoloOverlays()` / `LoadYoloOverlays()` / `DeleteYoloOverlaysFile()` 方法
- 检测完成时自动保存、方案加载时自动恢复、开始新检测时自动清除

**改动 4 — 方案历史界面重构**：
- 新增「方案路径」列显示完整绝对路径（带 ToolTip）
- 列顺序：方案名称 | 方案路径 | 方案大小 | 备注 | 修改时间
- 右键菜单新增「在文件资源管理器中查看」选项
- 窗口宽度从 900→1100

**影响范围**：
- `ViewModels/MainViewModel.cs`
- `Views/ProgramHistoryWindow.xaml`
- `Views/ProgramHistoryWindow.xaml.cs`

## 2026-05-20

### 新增个性化设置（界面结果显示设置）

**背景**：文字偏移X/Y、字体大小、字体名称原为硬编码值，无法在界面上调整。

**改动**：
1. **SysParamData.cs**：新增 `TextOffsetX`（默认10）、`TextOffsetY`（默认-7）、`OverlayFontSize`（默认10）、`OverlayFontName`（默认Consolas）
2. **SysParamViewModel.cs**：新增4个 `[SysParam]` 属性 + `AvailableFonts` 系统字体列表
3. **SysParamWindow.xaml**：新增「界面结果显示设置」分组框，含文字偏移X/Y、字体大小、字体选择
4. **MainViewModel.cs `DrawYoloText`**：改用 `_sysParam.Data` 读取上述参数替代硬编码值
5. **MainWindow.xaml.cs**：设置菜单新增「个性化设置」子按钮
6. 参数通过系统参数XML自动序列化持久化

**影响范围**：
- `Models/SysParamData.cs`
- `ViewModels/SysParamViewModel.cs`
- `Views/SysParamWindow.xaml`
- `ViewModels/MainViewModel.cs`
- `Views/MainWindow.xaml.cs`

## 2026-05-20

### 拆分 YOLO 叠加层绘制为两个独立方法（矩形/文字）

**背景**：矩形框（焊盘/针尖）在图像坐标系中绘制，缩放时 Halcon 自动适配，无需重绘；但文字须随缩放动态调整字号。原 `DrawYoloOverlays` 用一个方法+参数控制两种行为，逻辑耦合且文字累积未解决。

**改动**：
1. **MainViewModel.cs**：移除 `DrawYoloOverlays`，拆分为 `DrawYoloRects`（仅矩形）和 `DrawYoloText`（仅文字，每次重算字号）
2. 各调用方根据场景分别调用
3. **DrawYoloText** 文字偏移由 `fontSize * 1.2` 改为固定值 `17`（图像坐标），避免字号随缩放变化导致文字位移动

**影响范围**：
- `ViewModels/MainViewModel.cs`：方法拆分 + 所有调用处更新

### 修复缩放时YOLO叠加层文字累积

**背景**：`OnImagePartChanged` 在滚轮缩放时用 `skipClear: true` + `textOnly: true` 调用 `DrawYoloOverlays`，不清窗只追加文字，导致旧文字不消失、新文字叠加上去。

**改动**：
1. **MainViewModel.cs `OnImagePartChanged`**：改为获取拼图 → `ClearWindow` → `DispObj` → 完整重绘所有叠加层（焊盘+针尖+文字），消除文字累积

**影响范围**：
- `ViewModels/MainViewModel.cs`：`OnImagePartChanged` 重写

## 2026-05-20

### 修复方案检测参数未生效问题

**背景**：加载方案后运行检测，NG/OK 判断结果不正确，每次需要打开修改方案对话框点确定后才正确。根因是方案加载时 `LoadPinResults()` 从 JSON 文件加载了上次保存的检测结果（含旧的 OK/NG 状态），但未用当前方案参数重新评估。

**改动**：
1. **MainViewModel.cs**：在所有方案加载路径中，`LoadPinResults()` + `SyncPinIdealHeight()` 之后补充 `RecalculatePinResults()` 调用
   - `AutoLoadLastProject()`（line 440）
   - `ShowProgramHistory()`（line 2213）
   - `OpenProject()`（line 2313）
   - `OnProjectSelected()`（line 2497）

**影响范围**：
- `ViewModels/MainViewModel.cs`：4 处加载路径新增 `RecalculatePinResults()` 调用

### YOLO 检测结果改为 Halcon 叠加层绘制（移除渲染图方案）

**背景**：原 YOLO 渲染图方案存在以下问题：
- bug1: Render 中已存在的渲染图会被重新拿去检测
- bug2: 切换到渲染图要对所有图重新拼接，100+ 张时极慢
- bug3: 全图采集完后复制所有图片到 Render 目录耗时大
- 且 OpenCV 绘制的标记经 Halcon 缩放到拼图后模糊不清，颜色与灰度图性能存在冲突

**新方案**：Python 只返回原始像素坐标，C# 上位机根据检测位中心物理坐标 + FOV 参数换算针/焊盘的物理坐标(mm)，然后通过 Halcon 窗口直接绘制叠加层（`SetColor`/`DispRectangle2`/`DispText`），每检测完一个检测位立即增量绘制。

**改动**：

1. **`yolo_detect_server.py`** — 简化输出
   - `detect_pins()` 改为返回匹配对的原始像素坐标 `(pin_cx, pin_cy, pad_cx, pad_cy)` + 图像尺寸 `(h, w)`
   - 移除 `render_detection()` 函数（整函数删除）
   - 移除 `rendered_path` 相关逻辑
   - 保留匹配逻辑（SHIFT_PIXELS、SEARCH_RANGE_THRESHOLD）

2. **`Core/Managers/YoloDetector.cs`** — 新增原生返回结构
   - 新增 `YoloRawPin` 类：PinIndex、PinCx、PinCy、PadCx、PadCy
   - 新增 `YoloDetectionResult` 类：RawPins、ImageWidth、ImageHeight
   - `DetectAsync()` 改为返回 `YoloDetectionResult`，移除 `outputPath` 参数
   - 公开 `SHIFT_PIXELS` 常量供 C# 端计算 dx/dy

3. **`ViewModels/MainViewModel.cs`** — 核心改动
   - 新增 `YoloOverlayItem` 类存储针/焊盘的物理坐标 mm 及 OK/NG 状态
   - 新增 `_yoloOverlays` 列表，累积所有检测位的叠加层数据
   - `DetectWithYoloAsync()` 改为接收 detectX/Y、gridIndex、FOV、允许偏差等参数
     - 物理坐标公式：`pinMmX = (detectX - FovW/2) + pinPx * (FovW/imgW)`
     - 物理坐标公式：`pinMmY = (detectY + FovH/2) - pinPy * (FovH/imgH)`
     - dx/dy 在 C# 端以 mm 为单位计算
   - 新增 `DrawYoloOverlays()` 使用 Halcon 窗口绘制：
     - 焊盘 = 青色矩形框
     - 针尖 = 黄色矩形框
     - 文字 = OK 绿 / NG 红
   - StartCheckFlow 开始清空 `_yoloOverlays`，检测后调用 `DrawYoloOverlays()`

4. **Bug 4 修复：S 型右下角起始检测位位置映射错误**
   - `ToggleDetectPositionFromUI(int index)` 中 `index` 是网格索引（row*cols+col），但方法内直接将其当作 `GrabPositions` 的列表索引使用
   - 当采集模式为 S 型/右下角时，`GrabPositions` 列表顺序 = 遍历顺序 ≠ 网格顺序，导致点击网格 0（左上角）实际切换的是列表第 0 个元素（右下角）
   - 修复：改为 `GrabPositions.FirstOrDefault(p => p.Index == gridIndex)` 匹配网格索引

**新方案优点**：
- Halcon 矢量绘制随窗口缩放自动保持清晰，无模糊问题
- 叠加层绘制在灰度拼图之上，无需 RGB 通道，采集性能不变
- 无需 Render 目录和磁盘 IO，消除 bug1-bug3
- 全彩色标注（OK 绿/NG 红），无颜色 vs 性能取舍
- 每检测完一个检测位立即增量绘制，用户体验好
- dx/dy 以 mm 为单位，更直观

**影响范围**：
- `yolo_detect_server.py`：大幅简化，删除约 70 行渲染代码
- `Core/Managers/YoloDetector.cs`：返回值结构调整
- `ViewModels/MainViewModel.cs`：DetectWithYoloAsync/DrawYoloOverlays/ToggleDetectPositionFromUI 修改

### 修复叠加层重叠率偏移 + 移除残留渲染图代码

**背景**：
1. DrawYoloOverlays 中叠加层坐标未考虑重叠率裁剪。拼图显示时每个瓦片只显示中心 `FovWidth*(1-overlap) × FovHeight*(1-overlap)` 范围（即 cropW × cropH），但坐标换算用了 `cropW / FovWidth = pixelsPerMm * (1-overlap)` 作为比例系数，而非正确的 `pixelsPerMm`。导致针脚/焊盘标注向瓦片中心偏移，overlap 越大偏移越明显。
2. 移除渲染图后仍有残留代码：`_isRenderMode`、`SwitchViewMode`、`EnsureRenderFolder` 及其调用。

**改动**：
1. **MainViewModel.cs DrawYoloOverlays 修复重叠率偏移**
   - `cropW / project.FovWidth` → `pixelsPerMm`（正确比例）
   - `cropH / project.FovHeight` → `pixelsPerMm`
   - 移除未使用的 `imgW`/`imgH` 临时变量

2. **MainViewModel.cs 移除残留渲染图代码**
   - 移除 `_isRenderMode` 字段及对应 region
   - 移除 `SwitchViewMode()` 方法（约 90 行）
   - 移除 `EnsureRenderFolder()` 方法（约 20 行）
   - 移除 `LoadProjectImagesAndDisplayAsync()` 中的 `EnsureRenderFolder()` 调用
   - 移除 `StartFullImageGrabFlow()` 中的 `EnsureRenderFolder()` 调用

**影响范围**：
- `ViewModels/MainViewModel.cs`：DrawYoloOverlays 比例系数修正，删除约 120 行残留代码

## 2026-05-19

### 检测方式参数化：DetectionMode ComboBox + 检测流程解耦

**背景**：检测方式选择不应依赖 `_vision2DClient.IsConnected` 连接状态（VM 2D 连接用于采集，不等同于使用 VM 做检测）。改为系统参数 `DetectionMode` 显式选择"VM方案"或"YOLO方案"。

**改动**：
1. **SysParamData 新增 DetectionMode 属性**
   - `Models/SysParamData.cs`：新增 `DetectionMode`，默认 `"VM方案"`
   - 写入 `Config/SysParam.xml`，重启自动加载

2. **SysParamViewModel 新增检测方式字段**
   - `[SysParam] public string DetectionMode { get; set; }`
   - `DetectionModes` 列表：VM方案 / YOLO方案

3. **SysParamWindow.xaml 检测参数分组新增 ComboBox**
   - 检测参数 GroupBox 底部新增"检测方式"选择
   - 选项：VM方案 / YOLO方案

4. **StartCheckFlow 检测算法选择改为 DetectionMode**
   - 移除 `bool has2D = _vision2DClient.IsConnected` 的条件判断
   - 改为 `string detectionMode = _sysParam.Data.DetectionMode`
   - `canUseVM = detectionMode == "VM方案" && _vision2DClient.IsConnected`
   - `canUseYOLO = detectionMode == "YOLO方案" && _hasYoloDetector`
   - GRAB 重新采集仍通过 `_vision2DClient.IsConnected` 判断（采集依赖 VM 2D）
   - 移除 YOLO 分支注释中的"VM不可用时回退"描述

5. **检测流程.md 更新**
   - 检测方式选择逻辑图更新为 DetectionMode 判断
   - 附录新增"VM 需实现的 TCP 协议"章节（指令列表、GRAB/DETECT_2D 响应格式、错误场景处理）
   - 版本历史新增 v3

**影响范围**：
- `Models/SysParamData.cs`：新增 DetectionMode
- `ViewModels/SysParamViewModel.cs`：DetectionMode 属性 + DetectionModes 列表
- `Views/SysParamWindow.xaml`：检测参数分组新增下拉框
- `ViewModels/MainViewModel.cs`：StartCheckFlow 改用 DetectionMode 选择检测算法
- `检测流程.md`：逻辑图更新 + 附录新增 VM 协议文档

### YOLOv8 OBB 2D检测过渡方案集成

**背景**：VM 2D检测方案暂时无法就绪，需要使用 OpenCV 深度学习检测作为过渡方案。要求不修改现有的 VM 检测代码、3D检测代码，过渡方案代码独立封装，后续可整体删除。

**改动**：

1. **新增 Python YOLO 检测服务器**
   - `yolo_detect_server.py`：从 `pin6.py` 提取核心检测逻辑，独立为 stdin/stdout JSON Lines IPC 服务
   - 加载 YOLOv8 OBB 模型，检测 `pintop`（针尖）和 `pinpad`（焊盘）两类目标
   - 匹配逻辑：焊盘 X 方向偏移 SHIFT_PIXELS(18px)，搜索半径 SEARCH_RANGE_THRESHOLD(400px)，计算 dx/dy 偏移量
   - 通信协议：`{"image_path":"D:/img.bmp"}` → `{"pins":[{"pin_index":0,"dx":-0.52,"dy":1.23}],"error":null}`
   - 支持 image_path（文件路径）和 image（base64）两种输入方式

2. **新增 C# YoloDetector 管理器**
   - `Core/Managers/YoloDetector.cs`：单例模式，管理 Python 子进程生命周期
   - `Start()`：启动 Python 子进程，重定向 stdin/stdout
   - `DetectAsync()`：发送 JSON 请求，异步读取 JSON 响应（15s 超时）
   - `Shutdown()`：发送 `__SHUTDOWN__` 指令，3s 超时后 Kill
   - 超时自动 Restart() 清理挂起进程

3. **MainViewModel 集成**
   - 构造函数调用 `InitYoloDetector()`，启动时自动加载
   - `DetectWithYoloAsync()` 封装调用入口
   - `StartCheckFlow` 中新增 `else if (_hasYoloDetector)` 分支：当 VM 2D 未连接时走 YOLO 检测路径
   - 检测结果格式 `(PinIndex, Dx, Dy)` 与 VM `DETECT_2D_OK` 解析结果一致，3D检测流程无需修改
   - 窗口关闭时 `MainWindow.OnClosed` 中调用 `YoloDetector.Instance.Shutdown()` 清理进程

**影响范围**：
- `yolo_detect_server.py`：新增（过渡方案，后续可删除）
- `Core/Managers/YoloDetector.cs`：新增（过渡方案，后续可删除）
- `ViewModels/MainViewModel.cs`：InitYoloDetector、DetectWithYoloAsync、_hasYoloDetector 标记、StartCheckFlow 新增分支
- `Views/MainWindow.xaml.cs`：OnClosed 添加 Shutdown 调用
- `RBLAOI.csproj`：新增 YoloDetector.cs 编译项

### 检测流程重构：重新采集+替换拼图 + YOLO方案文档

**背景**：用户要求完善检测流程（1）VM 方案：每个检测位先移动到中心点重新采集图片，替换原拼图瓦片后做 2D 检测，再移动到 3D 起点做线扫；（2）YOLO 方案：写完整流程文档供审核。

**改动**：

1. **FullImageManager 重构 + 新增 ReplaceTile**
   - 提取公共方法 `ScaleAndCrop`（灰度化→缩放→裁剪）、`CalcCropSize`、`RebuildStitchedAndDisplay`
   - `StitchAndDisplay` 使用上述公共方法，移除重复代码
   - 新增 `ReplaceTile(gridIndex, newFilePath, displayAction, ...)`：替换单张瓦片，刷新拼图显示
   - 不重新拼接全图，复杂度 O(1)

2. **StartCheckFlow 修改为四步流程（VM方案）**
   - (1) 移动到检测位中心点 (detectX, detectY)
   - (1a) 发送 GRAB 到 VM 2D 重新采集图片
   - (1b) ParseAndSave → ReplaceTile 替换拼图瓦片
   - (2) 2D 检测（优先用新采集图片，失败回退到已存储图片）
   - (3) 移动到 3D 扫描起点 (startX, moveY)
   - (4) DETECT_3D + 移动到终点（并行）
   - YOLO路径统一到同一个 `if (has2D || _hasYoloDetector)` 分支中

3. **检测流程.md 更新**
   - 新增第十一章：YOLO 过渡方案检测流程
   - 包含完整流程图、与 VM 方案差异对比、渲染图规范、ReplaceTile 原理、代码入口

**影响范围**：
- `Core/Managers/FullImageManager.cs`：重构 + ReplaceTile 方法
- `ViewModels/MainViewModel.cs`：StartCheckFlow 四步流程 + 重新采集 + ReplaceTile
- `检测流程.md`：新增 YOLO 章节

### YOLO检测结果渲染图 + 2D/3D结果独立显示

**背景**：YOLO检测成功后需要将检测结果可视化渲染图替换拼图瓦片，同时2D检测和3D检测应互不干扰，一方无数据不影响另一方的结果展示。

**改动**：

1. **Python 服务器新增渲染图输出**
   - `yolo_detect_server.py`：`detect_pins()` 新增 `render_data` 返回（含像素坐标）
   - 新增 `render_detection()`：绘制针尖(红)、焊盘(蓝)、偏移焊盘(黄)、匹配连线(绿)、偏移标签
   - 检测完成后自动保存 `{原图名}_render.png`，路径在 `rendered_path` 字段返回

2. **MainViewModel 新增渲染图替换拼图**
   - YOLO检测成功后立即调用 `ReplaceTile()` 将渲染图替换到拼图显示
   - 渲染图路径根据原图路径计算：`{dir}/{stem}_render.png`

3. **2D/3D结果解耦**
   - 3D检测超时不再 `continue` 跳过整个检测位，2D结果仍然显示
   - 3D无数据时，仅使用2D数据生成PinResult（H=0，Result="NG"）
   - 2D无数据时，仅使用3D数据保持原有行为

**Bug修复**：
- `YoloDetector.cs` 错误判断条件反转：`error == null` 是成功，不是失败（导致YOLO检测结果被丢弃）

**影响范围**：
- `yolo_detect_server.py`：新增 render_detection + detect_pins 返回扩展
- `Core/Managers/YoloDetector.cs`：修正错误判断条件
- `ViewModels/MainViewModel.cs`：YOLO渲染图替换 + 2D/3D结果独立显示

### Images 目录结构重构：Origin + Render 双文件夹

**背景**：原始图片和检测渲染图混合存放在 Images 目录，导致管理混乱。将 Images 拆分为 Origin（采集原图）和 Render（渲染图 + 非检测位原图）两个子文件夹，加载和采集统一从 Origin 读写。

**改动**：

1. **ProjectManager 新增辅助方法**
   - `GetOriginPath()` → `Images/Origin/` 目录
   - `GetRenderPath()` → `Images/Render/` 目录

2. **加载/验证统一走 Origin**
   - `LoadProjectImagesAndDisplayAsync` 改用 `ResolveImageDir()`（优先 Origin，兼容旧版 Images/）
   - `ValidateImageCount` 同样走 `ResolveImageDir()`
   - `CopyImageToProjectFolder` 复制到 Origin

3. **全图采集和检测重采保存到 Origin**
   - 全图采集 `SET_PATH` 指向 Origin
   - 检测重采 `SET_PATH` 指向 Origin，GRAB 结果保存到 Origin
   - `EnsureRenderFolder()`：加载完成后从 Origin 复制非检测位图片到 Render

4. **YOLO 渲染图保存到 Render**
   - 渲染图输出路径改为 `Images/Render/{同名}`，通过 `output_path` 传给 Python 服务器
   - YOLO 检测完成后，`ReplaceTile` 使用 Render 路径替换拼图瓦片
   - Python 服务器 `yolo_detect_server.py` 支持 `output_path` 参数

**影响范围**：
- `Core/Managers/ProjectManager.cs`：新增 GetOriginPath / GetRenderPath
- `Core/Managers/YoloDetector.cs`：DetectAsync 新增 outputPath 参数
- `ViewModels/MainViewModel.cs`：加载/验证/采集/检测全链路 Origin/Render 适配
- `yolo_detect_server.py`：支持 output_path 请求参数

### Halcon 右键菜单：原图/渲染图切换 + 检测后自动切换

**背景**：由于加载的是 Origin 目录，渲染图永远不显示。需要提供右键菜单让用户自由切换 Origin（原图）和 Render（渲染图）视图。检测完成后自动切换到渲染图视图，方便查看结果。

**改动**：

1. **CustomHSmartWindowControl 新增事件和菜单项**
   - 新增 `SwitchToOriginRequested` / `SwitchToRenderRequested` 事件
   - 右键菜单新增"切换到原图"和"切换到渲染图"两个选项（分隔线分隔）
   - 选择模式和普通模式均显示

2. **MainViewModel 新增视图切换逻辑**
   - `_isRenderMode` 字段记录当前视图模式
   - `SwitchViewMode(bool useRender)`：从 Origin/Render 目录重新加载并拼接
   - 加载时恢复网格索引映射，确保拼图位置正确
   - `EnsureRenderFolder()` 改为复制所有文件（检测位初始也有，检测时覆盖为渲染图）

3. **检测完成后自动切换**
   - `StartCheckFlow` 结束后自动调用 `SwitchViewMode(true)` 切换到渲染图

4. **渲染图文字和标记加大**
   - `yolo_detect_server.py`：字体 0.45→0.55，圆半径 5px→6px

**影响范围**：
- `Core/Controls/CustomHSmartWindowControl.cs`：新增事件 + 右键菜单项
- `ViewModels/MainViewModel.cs`：SwitchViewMode + 事件处理 + 检测后自动切换
- `yolo_detect_server.py`：字体加大、圆形标记加大

### 渲染图标记改为灰度高对比度（取消 RGB 显示尝试）

**背景**：尝试将拼图管线改为 3 通道 RGB 以保留渲染图颜色，但 `ScaleAndCrop` 数据量翻 3 倍导致全图采集速度明显变慢。取消 RGB 方案，改为在 Python 端绘制灰度高对比度标记。

**改动**：

1. **FullImageManager 回退到 1 通道灰度**
   - `ScaleAndCrop` 恢复 `Rgb1ToGray` 转换（采集性能恢复）
   - 空占位图恢复 1 通道 `GenImageConst("byte")`

2. **yolo_detect_server.py 改为白+黑高对比度渲染**
   - 焊盘：白色空心圆 r=10
   - 针尖：黑色底 + 白色实心圆 r=8
   - 偏移焊盘：白色×标记
   - 匹配连线：白色实线 t=3
   - 文字：字体 0.8，黑色投影 + 白色描边，确保在任何背景下可读

**影响范围**：
- `Core/Managers/FullImageManager.cs`：恢复原始灰度逻辑
- `Core/Managers/YoloDetector.cs`：DetectAsync 新增 renderWidth/renderHeight 参数
- `ViewModels/MainViewModel.cs`：YOLO 调用时传入拼图瓦片尺寸
- `yolo_detect_server.py`：渲染改为高对比度黑白方案，支持先缩到目标尺寸再画标记

## 2026-05-09

### GET_IO 轮询下沉到 MotionIO 后台 + PTP/JOG 速度分离

**背景**：IO 状态轮询 (`GET_IO`) 原来依赖 `AxisIOControlWindow` 的 `DispatcherTimer`，窗口关闭后停止，导致无法实时监测机器状态（如异常报警）。

**改动**：

1. **MotionIO 新增后台 IO 轮询**
   - `Open()` 时自动启动 `IoPollLoop` 后台任务，每隔 `IoPollInterval` ms 发送一次 `GET_IO`
   - 结果缓存到 `CurrentIOState` 属性，通过 `IOStateUpdated` 事件通知订阅者
   - `Close()` / `Dispose()` 时自动停止轮询
   - `_isIoPollPending` 标记防止指令队列堆积
   - 轮询间隔通过系统参数 `IoPollInterval` 实时控制（保存即生效，无需重启）

2. **速度指令拆分为 PTP / JOG 模式**
   - `SetSpeed(percentage)` → `SetSpeedPtp(percentage)`：发送 `SPEED X...,Y...`
   - `SetSpeedJog(percentage)`：发送 `SPEED JOG X...,Y...`
   - `GetSpeed()` → `GetPtpSpeed()` / `GetJogSpeed()`：均返回原始脉冲数，不再做百分比换算
   - `MAX_PULSE_SPEED` 改为 `public const`，供外部换算百分比

3. **系统参数窗口**
   - 移除"步长系数"字段
   - 新增"最大速度 (mm/s)"字段（`MaxSpeed`，默认 600）
   - 新增"IO 状态轮询间隔 (ms)"字段（`IoPollInterval`，默认 200）
   - 轴速度失焦时校验不超过最大速度
   - 保存时发送 `SET_SPEED`（走 PTP 指令）

4. **IO 控制面板简化**
   - 移除 `_ioRefreshTimer` 和 `RefreshIOState()`，不再自己发 `GET_IO`
   - 订阅 `IOStateUpdated` 事件更新指示灯
   - 保留轻量 `_movingStateTimer`，仅用于 `GET_STATUS`（轴运动指示）
   - 滑块拖动发 `SPEED JOG` 指令（JOG 模式）
   - "⟳" 同步按钮读取 `GetJogSpeed()` 并换算百分比
   - 连接时初始速度也从 `SetSpeed` 改为 `SetSpeedJog`

**影响范围**：
- `Core/Motion/MotionIO.cs`：新增后台轮询逻辑、速度方法重命名
- `AxisIOControlWindow.xaml.cs`：移除 IO 定时器，改为事件驱动
- `SysParamWindow.xaml` / `.xaml.cs`：字段增删、校验、发送逻辑
- `Models/SysParamData.cs`：新增 `MaxSpeed`、`IoPollInterval`
- `ViewModels/SysParamViewModel.cs`：对应属性更新

### 速度参数改用 mm/s 直设，移除 MAX_PULSE_SPEED

**背景**：PTP 速度不需要百分比换算，JOG 的百分比应由调用方根据 MaxSpeed 自行计算，职责更清晰。

**改动**：

1. **MotionIO 速度方法改参数类型**
   - `SetSpeedPtp(double percentage)` → `SetSpeedPtp(double mmPerSec)`：直接用 mm/s，内部 `MmToPulse` 转脉冲
   - `SetSpeedJog(double percentage)` → `SetSpeedJog(double mmPerSec)`：同上
   - 移除 `MAX_PULSE_SPEED` 常量（不再需要百分比参考值）

2. **SysParamWindow 发送 PTP 速度**
   - `SendPtpSpeedAsync()` 直接传 `viewModel.AxisSpeed`（mm/s）
   - `Dialog()` 中保存时也会调用 `SendPtpSpeedAsync()`，退出/关闭时选了"是"同样生效

3. **IO 面板百分比 → mm/s 转换上移**
   - `AxisSpeedSD_ValueChanged`：`mmPerSec = sliderPct / 100 * MaxSpeed` → `SetSpeedJog(mmPerSec)`
   - 连接时初始速度同上处理
   - `SyncSpeedFromController`：`PulseToMm(pulse)` 得 mm/s → 反算百分比给滑块

**影响范围**：
- `Core/Motion/MotionIO.cs`：SetSpeedPtp/SetSpeedJog 参数类型变更，移除 MAX_PULSE_SPEED
- `AxisIOControlWindow.xaml.cs`：增加百分比 ↔ mm/s 换算
- `SysParamWindow.xaml.cs`：SendPtpSpeedAsync 简化

## 2026-05-11

### 方案加载增加模态进度对话框

**背景**：方案的 Images 目录通常有 ~100 张位图（~5GB），加载时界面卡顿且无反馈，用户不知道加载进度；且状态栏进度条不够醒目，用户在加载期间仍可操作界面，不安全。

**改动**：

1. **FullImageManager 支持进度回调**
   - `StitchAndDisplay` 新增可选参数 `IProgress<int> progress = null`
   - 每张图片加载/缩放完成后调用 `progress?.Report(idx + 1)`

2. **新增 ProgressDialog 模态对话框**
   - `Views/ProgressDialog.xaml`：无边框模态窗口，居中显示，禁止关闭
   - 包含 ProgressBar（百分比）和 TextBlock（x/100）
   - `MarkCompleted()` 设置完成标志后自动关闭，Alt+F4 被拦截

3. **MainViewModel 异步化加载**
   - `LoadProjectImagesAndDisplay()` → `async Task LoadProjectImagesAndDisplayAsync()`
   - 创建 `ProgressDialog` 并 `ShowDialog()` 模态显示，阻止用户操作主界面
   - 图片处理在 `Task.Run` 后台线程执行，Halcon 显示通过 `Dispatcher.Invoke` 封送到 UI 线程
   - `Progress<int>` 将后台进度自动转发到 UI 线程更新对话框
   - 后台任务完成后 `ContinueWith`（UI 线程）自动关闭对话框
   - 所有入口（AutoLoadLastProject、ShowProgramHistory、OpenProject、ReloadProject、RefreshDisplayAfterWindowReady）统一调用

4. **移除状态栏进度条**：`IsLoadingProject`、`LoadProgressValue`、`LoadProgressText` 属性和 XAML 控件一并移除

**影响范围**：
- `Views/ProgressDialog.xaml` + `.cs`：新增模态进度对话框
- `Core/Managers/FullImageManager.cs`：StitchAndDisplay 新增 IProgress 参数
- `ViewModels/MainViewModel.cs`：异步加载改用模态对话框
- `MainWindow.xaml`：移除状态栏进度条

## 2026-05-15

### 启动时加载VM方案 + SysParam窗口VM路径改为带选择按钮 + 采集前发送SPEED指令

**背景**：系统启动后需要自动与VM建立通信，避免全图采集前临时加载方案导致的延迟；同时全图采集前的运动需要确保下位机速度参数已生效。

**改动**：
1. **SysParamWindow.xaml**：VM方案文件字段从`LabeledTextBox`改为StackPanel（TextBlock标签 + Grid含TextBox和"选择..."按钮），支持文件对话框选择或手动输入
2. **SysParamWindow.xaml.cs**：新增`SelectVmSolution_Click`打开`.sol`文件对话框；移除保存时调用`SendPtpSpeedAsync`（`BtnSave_Click`和`Dialog()`中的调用全部删除，同时删除`SendPtpSpeedAsync`方法）
3. **MainViewModel.cs**：
   - 构造函数末尾新增`InitVmSolutionAtStartup()`：启动时自动加载VM方案（静默失败不影响启动）
   - 新增`IsVmSolutionLoaded()`：检查VM方案当前是否已加载
   - 新增`EnsureVmSolutionLoaded()`：采集前确保VM方案已加载（未加载时自动重载）
   - `SetVmCameraExposure()`：简化实现，改为先`EnsureVmSolutionLoaded()`再设置曝光时间
   - 新增`SendSpeedPtpAsync()`：发送轴速度SPEED指令给下位机
   - `StartFullImageGrabFlow()`：在设置曝光时间之后、移动循环之前添加`await SendSpeedPtpAsync()`

**影响范围**：
- `Views/SysParamWindow.xaml`
- `Views/SysParamWindow.xaml.cs`
- `ViewModels/MainViewModel.cs`

### 修复工业电脑部署：VM SDK 引用改为复制到输出目录

**背景**：在全图采集功能基本完成后，在工业电脑上运行报 `XamlParseException`（VmMainViewConfigControl 构造异常）和 `VmSolution.Load` 初始化异常。根因是 csproj 中所有 VM SDK 引用均为 `<Private>False</Private>`，DLL 不复制到输出目录，仅靠 GAC 解析。开发机有 VM Studio 安装环境所以正常，但工业电脑环境差异导致运行时程序集解析失败。

**改动**：
1. **RBLAOI.csproj**：将 7 个关键 VM SDK 引用改为 `<Private>True</Private>`
   - `VM.Core`、`VM.PlatformSDKCS`、`VMControls.BaseInterface`
   - `VMControls.WPF.Release`（直接包含 VmMainViewConfigControl）
   - `VMControls.Interface`、`VMControls.Winform.Release`
   - `VMControls.RenderInterface` 保持 `False`（运行时间接依赖）
2. **VmSolutionWindow.xaml.cs**：加载方案前先关闭已有方案，防止 `SetVmCameraExposure` 预加载的方案与新加载冲突

**影响范围**：
- `RBLAOI.csproj`
- `Views/VmSolutionWindow.xaml.cs`

## 2026-05-14

### VM曝光时间远程设置+图片直存方案目录

**背景**：全图采集前需要自动设置VM全局相机曝光时间，由系统参数CameraExposureTime（默认10000μs）控制。

**改动**：
1. SysParamData新增VmSolutionPath属性，配置VM方案文件(.sol)路径，默认VmSolution/test.sol
2. SysParamWindow新增"VM方案文件(.sol)"配置字段
3. MainViewModel新增SetVmCameraExposure()方法：全图采集前通过VM SDK加载.sol，将GlobalCameraParam.ExposureTime设为CameraExposureTime
4. 图片直存优化：通过SET_PATH通知VM将图片直接保存到方案Images目录，采集完成后复位默认路径
5. WaitForFileReadyAsync增加独占打开检测，提高文件就绪判断可靠性

**影响范围**：
- `Models/SysParamData.cs`
- `ViewModels/SysParamViewModel.cs`
- `ViewModels/MainViewModel.cs`
- `Views/SysParamWindow.xaml`

### VM方案控制窗口+方案删除/重命名

**背景**：需要能够在主界面直接操作VM可视化方案，以及方便地删除和重命名项目方案。

**改动**：
1. 新增VmSolutionWindow，布局参照VM SDK示例：左侧方案操作（加载/保存/关闭/锁定等），右侧VmMainViewConfigControl可视化配置
2. 编辑菜单新增"打开VM方案"按钮
3. 文件菜单新增"删除方案"按钮，删除前确认弹窗
4. 程序历史窗口增加右键菜单：支持删除和重命名方案
5. ProjectManager新增RenameProject方法，自动重命名文件夹并更新配置文件

**影响范围**：
- `Views/VmSolutionWindow.xaml`（新增）
- `Views/VmSolutionWindow.xaml.cs`（新增）
- `Views/MainWindow.xaml.cs`
- `Views/ProgramHistoryWindow.xaml`
- `Views/ProgramHistoryWindow.xaml.cs`
- `ViewModels/MainViewModel.cs`
- `Core/Managers/ProjectManager.cs`

## 2026-05-15

### StitchAndDisplay 性能优化 + ParseAndSaveImageAsync 简化 + 检测代码更新

**背景**：全图采集过程中 StitchAndDisplay 每次调用约 100ms，是全图采集（88 张图）的性能瓶颈；同时 ParseAndSaveImageAsync 中按序号重命名文件的逻辑增加了不必要的复杂性和延迟。

**改动**：

1. **StitchAndDisplay 移除 CopyImage 瓶颈**
   - 原实现（v1）在拼图循环中对每张图调用 `CopyImage`，每次约 50-70ms（含 GC 抖动），总耗时平均 86ms（范围 74-129ms）
   - 优化后（v2）直接引用 `_cachedScaledImages[i]`，避免冗余拷贝，总耗时平均 76ms（范围 70-84ms），且消除了 129ms 的 GC 尖峰
   - 尝试了占位图预缓存方案（v3），平均 84ms，不如 v2，已回退
   - 添加 Stopwatch 计时日志便于后续监控

2. **ParseAndSaveImageAsync 回退到 result 即文件名的原始方式**
   - VM 返回的 `GRAB_OK` 结果本身就是文件名（时间戳格式），无需按序号重命名
   - 移除了解析序号、构造序号的逻辑
   - 添加了 fallback 路径复制 + WaitForFileReadyAsync 确保文件就绪

3. **检测代码原图查找改为 GetImagePaths()**
   - 从 `{pos.GrabIndex:D6}` 文件名模式 + 按创建时间排序重命名的逻辑
   - 改为直接通过 `_imageManager.GetImagePaths()[GrabIndex]` 按采集顺序索引查找
   - 与 VM 时间戳命名方式解耦，无论文件名是什么格式都能正确找到原图

**影响范围**：
- `Core/Managers/FullImageManager.cs`：StitchAndDisplay 移除 CopyImage，v2 优化版本
- `ViewModels/MainViewModel.cs`：Stopwatch 计时、ParseAndSaveImageAsync 简化、检测代码更新

## 2026-05-16

### 修复 VM TCP 通信中文路径乱码（Encoding.UTF8 → Encoding.Default）

**背景**：`SET_PATH` 和 `DETECT_2D` 指令中包含中文项目名路径时，接收到的数据中路径部分显示为乱码（如 `娴嬭瘯鎷煎浘鍔犺浇1`），导致 VM 可能无法正确定位文件路径。

**根因**：VisionMaster（VM）运行在中文 Windows 上，TCP 通信使用系统默认编码（GBK/CodePage 936），而 `Vision2DClient` 和 `Vision3DClient` 的收发方法均硬编码为 `Encoding.UTF8`。 UTF-8 字节被当作 GBK 解码后产生乱码，且发送给 VM 的 UTF-8 中文路径 VM 无法正确理解。

**改动**：
1. **Vision2DClient.cs**：`SendCommand()` 和 `ReceiveData()` 的 `Encoding.UTF8` → `Encoding.Default`
2. **Vision3DClient.cs**：同上

`Encoding.Default` 在中文 Windows 上为 GBK（CodePage 936），与 VM 编码一致。纯 ASCII 指令（如 `GRAB`、`DETECT_3D`）不受影响。

**影响范围**：
- `Core/Vision/Vision2DClient.cs`
- `Core/Vision/Vision3DClient.cs`

## 2026-05-16

### UI 术语统一 + 方案历史新增大小列 + 全图采集前检查方案加载

**背景**：用户反馈（1）"程序历史" → "方案历史" 更符合实际用途；（2）方案列表缺少大小信息；（3）未加载方案时直接全图采集会报错。

**改动**：

1. **术语统一："工程" → "方案"**
   - 主菜单按钮：新建工程→新建方案、打开工程→打开方案、修改工程→修改方案、关闭工程→关闭方案
   - 窗口标题：新建工程、修改工程 对应更新
   - 按钮图标映射同步更新

2. **程序历史 → 方案历史 + 新增大小列**
   - 窗口标题/TitleBarControl 改为"方案历史"
   - 列表新增"方案大小"列，显示每个方案文件夹的磁盘占用（自动换算 B/KB/MB/GB）
   - `ProjectListItem` 新增 `Size` 属性，`LoadProjects()` 中通过 `GetDirectorySize()` 计算

3. **全图采集前检查方案是否已加载**
   - `StartFullImageGrabFlow()` 开始处增加 `CurrentProject == null` 判断
   - 未加载方案时弹出提示"请先新建或打开一个方案"并返回，避免后续空引用异常

**影响范围**：
- `Views/MainWindow.xaml` + `.cs`：菜单按钮文字、图标映射
- `Views/CreateProjectDialog.xaml`：窗口标题
- `Views/EditProjectWindow.xaml`：窗口标题、TitleBar
- `Views/ProgramHistoryWindow.xaml` + `.cs`：窗口标题、大小列、ProjectListItem
- `ViewModels/MainViewModel.cs`：StartFullImageGrabFlow 增加方案加载检查

## 2026-05-16

### 新增三种拍照移动模式 (Z/S/U) + 采集设置对话框

**背景**：全图采集默认 Z 型移动（每行同向，跨行需长距离回退），耗时较长。需要增加 S 型（逐行蛇形）和 U 型（逐列蛇形）以节省空程时间，并在界面上提供采集设置入口。

**改动**：

1. **SysParamData 新增采集设置字段**
   - `GrabStartCorner`：TopLeft / BottomRight，默认 TopLeft
   - `GrabMoveMode`：Z / S / U，默认 Z
   - 写入 `Config/SysParam.xml`，重启自动加载

2. **新增 CaptureSettingsWindow 对话框**
   - 遵循项目 UI 风格，使用 TitleBarControl + 现有样式
   - 起始角：左上角 / 右下角（单选）
   - 移动方式：Z型 / S型 / U型（单选，附说明文字）
   - 确定后保存到 SysParam.xml

3. **主界面设置菜单新增"采集设置"按钮**
   - 放在"运动参数"之后、"IO配置"之前
   - 图标 ⚙️

4. **CalculateGrabPositions 扩展三种移动模式**
   - 新增 `GetGrabOrder()` 方法生成遍历顺序
   - Z型（默认/当前）：逐行同向遍历
   - S型：逐行蛇形交替，省去每行末尾长距离回退
   - U型：逐列蛇形交替，省去每列末尾长距离回退
   - 起始角控制遍历起点和方向

5. **FullImageManager 拼图适配任意移动顺序**
   - 新增 `_imageGridIndices`：每张图关联其在拼接网格中的索引 (row * cols + col)
   - `AddImage(path)` → `AddImage(path, gridIndex)`：采集中按网格位置存图
   - `StitchAndDisplay` 改按 gridIndex 摆放图片到对应网格 slot，不再依赖采集顺序
   - 未拍到图像的 slot 自动填灰占位，与增量拼接兼容

6. **图片命名统一为网格序号**
   - 文件名 `GRAB_OK_序号` 中的序号固定为网格坐标序号（从左至右、从上至下），与采集顺序解耦
   - 无论 Z/S/U 哪种移动模式，`GRAB_OK_1` 始终对应网格左上角

**影响范围**：
- `Models/SysParamData.cs`：新增 GrabStartCorner、GrabMoveMode
- `Views/CaptureSettingsWindow.xaml` + `.cs`：新增采集设置对话框
- `Views/MainWindow.xaml.cs`：设置菜单添加按钮、图标映射、动作映射
- `ViewModels/MainViewModel.cs`：GetGrabOrder 方法、CalculateGrabPositions 扩展、ShowCaptureSettings
- `RBLAOI.csproj`：添加新文件引用

## 2026-05-18

### StitchAndDisplay 添加重叠裁剪支持 + 检测高亮对齐

**背景**：当 FOV 边缘刚好有针脚时，VM 检测图片边缘的针脚会不完整，影响检测效果。通过设置采集重叠率，让相邻 FOV 之间有冗余覆盖，同时需要让拼接显示正确裁剪重叠区域。

**改动**：

1. **FullImageManager.StitchAndDisplay 增加 overlap 参数**
   - 缩放后对每张图居中裁剪到 `step = fovWidth × (1 − overlap)` 尺寸
   - 所有 tile 统一大小，拼接图显示均匀
   - 空位占位图也使用裁剪后尺寸
   - `overlap` 默认 0，完全向后兼容

2. **MainViewModel 调用处传入 project.Overlap**
   - 方案加载显示：`StitchAndDisplay(..., project.Overlap)`
   - 采集循环中重绘：`StitchAndDisplay(..., project.Overlap)`
   - `HighlightDetectPositions` 检测位矩形使用裁剪后 tile 尺寸，与拼接图对齐

**影响范围**：
- `Core/Managers/FullImageManager.cs`：StitchAndDisplay 增加 overlap 参数 + 裁剪逻辑
- `ViewModels/MainViewModel.cs`：两处调用传入 overlap + HighlightDetectPositions 对齐

### 三个Bug修复：检测位高亮重叠、拍照位序号排序、VM空闲超时

**背景**：
1. 重叠裁剪后 `RefreshSelectionDisplay`/`RedisplayAndHighlight` 中的检测位矩形仍按全FOV尺寸绘制，与实际拼接图错位
2. S型/BottomRight 模式下，`GrabPosition.Index` 按采集顺序赋值（末行末列为0），但用户期望0行0列永远为序号0
3. 程序空闲一段时间后 VM SDK COM 对象过期，"设置VM相机曝光时间"抛出异常导致连续拍照超时

**改动**：
1. **检测位高亮尺寸修正**
   - `RefreshSelectionDisplay`：`fovWidthPx/fovHeightPx` 乘以 `overlapFactor`
   - `RedisplayAndHighlight`：矩形坐标改用 `tileWidth/tileHeight`
2. **拍照位序号按网格位置排序**
   - `CalculateGrabPositions`：`Index = row * cols + col`
   - 检测时2D图片查找改为 `gridToCaptureIdx` 字典（网格位置→采集顺序索引），与采集顺序解耦
3. **VM空闲超时恢复**
   - `SetVmCameraExposure` 捕获异常后强制重新加载 VM 方案并重试一次

**影响范围**：
- `ViewModels/MainViewModel.cs`：四处的矩形绘制、CalculateGrabPositions Index、检测2D图查找、SetVmCameraExposure 重试逻辑

### 移除显示/隐藏检测位功能 + 拼图补偿系统参数化

**背景**：显示/隐藏检测位功能与选择模式存在同步问题，右键菜单冗余；拼图补偿值（X=0.05, Y=0.1）硬编码在 CalculateGrabPositions 中，每台机器误差不同，需要可配置。

**改动**：

1. **移除显示/隐藏检测位功能**
   - `CustomHSmartWindowControl.cs`：移除 `ToggleHighlightRequested` 事件、`IsHighlightVisible` 属性、右键菜单"隐藏检测位/显示检测位"项
   - `MainViewModel.cs`：移除 `HighlightDetectPositions()`、`HideHighlightRects()`、`OnToggleHighlightRequested()`、`RedisplayAndHighlight()` 四个方法及所有调用
   - 简化 `OnGrabPositionDetectChanged()`、`ExitSelectInspectionMode()`、`RefreshSelectionDisplay()`

2. **拼图补偿系统参数化**
   - `SysParamData.cs`：新增 `StitchOffsetX`（默认 0.05）、`StitchOffsetY`（默认 0.1）
   - `SysParam.cs`：新增对应的便捷访问属性
   - `SysParamViewModel.cs`：新增 `[SysParam] StitchOffsetX / StitchOffsetY`
   - `SysParamWindow.xaml`：新增"🧩 拼图补偿"分组，含 X/Y 补偿输入框
   - `MainViewModel.cs`：`CalculateGrabPositions` 中硬编码的 `0.05`/`0.1` 替换为 `_sysParam.StitchOffsetX`/`_sysParam.StitchOffsetY`

**影响范围**：
- `Core/Controls/CustomHSmartWindowControl.cs`
- `Core/Managers/FullImageManager.cs`
- `Core/Managers/SysParam.cs`
- `Models/SysParamData.cs`
- `ViewModels/MainViewModel.cs`
- `ViewModels/SysParamViewModel.cs`
- `Views/SysParamWindow.xaml`

### Bug修复：退出选择叠加层残留 + 检测位overlap偏移 + 鼠标点击坐标不一致

**背景**：移除显示/隐藏检测位功能后出现三个bug：（1）退出选择模式后虚线框和红框残留；（2）带 overlap 时检测位矩形尺寸和点击区域未按裁剪后 tile 大小更新；（3）鼠标点击有时需要两下才响应。

**改动**：

1. **ExitSelectInspectionMode 清除叠加层**（Bug 1）
   - 退出时获取当前拼接图，ClearWindow + DispObj 重新显示纯净拼接图，覆盖所有绘制的矩形
   - 调用 `_vision.RefreshDisplay()` 强制 Halcon 控件刷新布局，使其内部状态与显示一致

2. **RefreshSelectionDisplay 矩形尺寸适配 overlap**（Bug 2）
   - `fovWidthPx`/`fovHeightPx` 乘以 `overlapFactor = 1 - project.Overlap`，与实际裁剪后 tile 尺寸匹配

3. **鼠标点击区域适配 overlap**（Bug 3 + 点击不一致修复）
   - `EnterSelectInspectionMode` 中 `FovWidthPixels`/`FovHeightPixels` 乘以 overlapFactor，使 `HandlePositionClick` 中的行列计算与拼接图 tile 位置对齐
   - `EnterSelectInspectionMode` 结束时调用 `_vision.RefreshDisplay()` 确保 Halcon 内部状态就绪，`GetPart` 返回正确的窗口视口坐标

**影响范围**：
- `ViewModels/MainViewModel.cs`

### 检测结果列表 X/Y 物理坐标

**背景**：检测结果列表中针尖的 X/Y 坐标一直显示为 0，现需要填充针尖的物理坐标（mm）。

**改动**：
1. **rawPins2D 元组扩展**：从 (PinIndex, Dx, Dy) 扩展为 (PinIndex, Dx, Dy, X, Y)，传递针尖物理坐标
2. **DetectWithYoloAsync**：返回参数增加 pinMmX、pinMmY（YOLO 检测时已计算的物理坐标）
3. **ParseDetect2DResponse**：VM 方案返回 X=0、Y=0（VM 仅返回偏差，暂无物理坐标）
4. **PinResult 创建**：3D 路径使用 pin2D?.X ?? 0、pin2D?.Y ?? 0；2D-only 路径直接使用 pin2D.X、pin2D.Y

**影响范围**：
- ViewModels/MainViewModel.cs

### Halcon 颜色列表重组

**背景**：原个性化设置中 Halcon 颜色选项只有 12 种基础色，部分颜色在 Halcon 中无效会报 Unknown color 错误。现按 Halcon 官方支持的完整颜色名称列表重新设计。

**改动**：
1. **AvailableColors 扩展**：从 12 种基础色扩展为 60+ 种，按色系分类（基础色、珊瑚色系、植物绿色系、蓝色系、紫色/粉红系、大地色系、灰色系），每个颜色带中文标注
2. **BgColorOptions 扩展**：在原有 8 种基础上增加橙色、粉红、紫罗兰，共 12 种常用色 + 无背景
3. **颜色名称验证**：所有颜色名称均使用 Halcon set_color 算子支持的有效名称，含空格的名称（如 spring green）Halcon 可正常识别

**影响范围**：
- ViewModels/PersonalizationViewModel.cs

## 2026-05-21

### 任务2：轴回零检测、结果列表列排序、覆盖层重载修复、个性化扩展、定位到PIN

**背景**：多项功能增强和bug修复：全图采集/检测前需确保XY轴已回零；结果列表X/Y列应放末尾；重载方案后叠加层不显示；个性化设置需添加检测位选择样式；检测结果需支持定位到PIN。

**改动**：

1. **轴回零状态检测（X/Y）**
   - `MainViewModel` 新增 `_axisHomed` bool 数组（[0]=X, [1]=Y, [2]=Z, [3]=H），Z/H 始终视为已回零
   - 新增 `CheckAndHomeAxesAsync()` 方法：流程前检查 `IsXyHomed`，未回零时弹窗提示用户确定/取消；确定后自动执行回零，成功后标记为已回零
   - `ServoHome()` 回零成功后设置 `_axisHomed[AXIS_X/Y] = true`
   - `StartFullImageGrabFlow()` 和 `StartCheckFlow()` 开始处调用 `await CheckAndHomeAxesAsync()`，取消时阻止流程

2. **检测结果列表 X/Y 列移到末尾**
   - `MainWindow.xaml`：列顺序调为 PINID | 检测位 | 位 | DX | DY | DH | 结果 | X | Y

3. **修复重载方案后叠加层不显示**
   - `LoadProjectImagesAndDisplayAsync()` 末尾：当 `_isOverlayVisible` 为 true 时，自动重新加载 YOLO 叠加层数据并重绘

4. **个性化设置添加"检测位选择"分组框**
   - `SysParamData.cs`：新增 `SelectionBoxColor`（默认 red）、`SelectionBoxSize`（默认 1）
   - `PersonalizationViewModel.cs`：新增对应 `[SysParam]` 属性
   - `PersonalizationWindow.xaml`：新增"🎯 检测位选择"分组框（颜色 ComboBox + 大小输入框）
   - `DrawSelectionRectangles`：改用 `_sysParam.Data.SelectionBoxColor/SelectionBoxSize` 替代硬编码
   - `ShowPersonalization`：关闭后立即刷新选择框样式和叠加层

5. **检测结果列表右键菜单"定位到该PIN"**
   - `MainViewModel` 新增 `NavigateToDetectIndex(int detectIndex)`：计算该检测位在拼接图中的 tile 位置，`SetPart` 放大居中显示并留 10% 边距，同时刷新选区和叠加层
   - `MainWindow.xaml`：DataGrid 行样式新增 ContextMenu，含"定位到该PIN"菜单项
   - `MainWindow.xaml.cs`：新增 `MenuItem_LocatePin_Click` 处理右键点击
   - `LoadPinResults`：加载时按 `DetectIndex` 排序，默认按检测位序号排列

**影响范围**：
- `ViewModels/MainViewModel.cs`
- `Views/MainWindow.xaml`
- `Views/MainWindow.xaml.cs`
- `Views/PersonalizationWindow.xaml`
- `ViewModels/PersonalizationViewModel.cs`
- `Models/SysParamData.cs`

## 2026-05-25

### 相机像素当量标定 + Fov自动推算 + YOLO像素转换优化

**背景**：完成相机标定，获得准确像素当量（0.004780825 mm/px）。用户界面输入像素密度（209.2236 px/mm），内部统一使用像素当量计算。全图采集后自动从第一张图的实际像素尺寸推算 FovWidth/FovHeight。YOLO检测的像素坐标→物理坐标转换改为直接使用标定后的像素当量，消除对 FovWidth/ImageWidth 推算的间接依赖。

**改动**：

1. **SysParamData 默认值更新**（`SysParamData.cs`）
   - `CameraPixelEquivalent` 默认值 0.01 → 0.004780825（mm/px）
   - 注释补充单位标注

2. **SysParamViewModel 新增像素密度转换属性**（`SysParamViewModel.cs`）
   - `CameraPixelEquivalent` 改为完整属性，setter 触发 `CameraPixelDensity` PropertyChanged
   - 新增 `CameraPixelDensity`（无 `[SysParam]`）：界面输入 px/mm，内部自动转存 mm/px

3. **SysParamWindow 界面更新**（`SysParamWindow.xaml`）
   - "相机像素当量" → "像素密度 (px/mm)"
   - 绑定改为 `CameraPixelDensity`，用户输入 209.2236

4. **YOLO 检测像素转换优化**（`MainViewModel.cs`）
   - `DetectWithYoloAsync()`：移除 `mmPerPxX = fovWidth / ImageWidth`，改用 `CameraPixelEquivalent`
   - `actualFovWidth/Height` 从 `ImageWidth * CameraPixelEquivalent` 计算
   - 方法签名简化：移除 `fovWidth`、`fovHeight` 参数

5. **全图采集后 Fov 自动推算**（`MainViewModel.cs`）
   - 全图采集完成后，读取第一张图的像素尺寸
   - `FovWidth = imageWidth_px × CameraPixelEquivalent`
   - `FovHeight = imageHeight_px × CameraPixelEquivalent`
   - 自动写入 project 并保存

6. **EditProjectWindow Fov 改为只读显示**（`EditProjectWindow.xaml`）
   - FOV宽/FOV高从 LabeledTextBox 改为只读 TextBlock，显示自动计算值

**影响范围**：
- `Models/SysParamData.cs`
- `ViewModels/SysParamViewModel.cs`
- `Views/SysParamWindow.xaml`
- `ViewModels/MainViewModel.cs`
- `Views/EditProjectWindow.xaml`

## 2026-05-25（续）

### VM 2D 检测协议改为 PAD/PIN 分离格式 + Fov 前置推算

**背景**：VM 2D 检测方案通信协议重新设计，不再返回 dx/dy 偏差值，改为直接返回焊盘坐标和针脚坐标及空针状态，由上位机自行计算偏差。同时 FovWidth/FovHeight 改为由相机分辨率 × 像素当量自动推算，在全图采集/拼图/检测前统一计算。

**改动**：

1. **PinResult 模型新增字段**（`Models/PinResult.cs`）
   - `PadX` / `PadY`：焊盘物理坐标 (mm)，VM 方案检测返回
   - `IsEmptyPin`：空针标记（焊盘有但针脚缺失），VM 方案检测返回
   - `SetIdealHeight(double)`：新增方法设置理想高度，用于计算 `DiffH`

2. **重写 ParseDetect2DResponse 解析新协议**（`MainViewModel.cs`）
   - 旧格式：`DETECT_2D_OK:pinIdx,dx,dy;...`
   - 新格式：`DETECT_2D_OK:PAD:x&y;x&y;...|PIN:x&yOK;x&yNG;...`
   - 解析 PAD 和 PIN 部分，按顺序 1:1 匹配
   - `x&yNG` 标记为空针，设置 `IsEmptyPin=true`
   - 8字段元组：`(PinIndex, Dx, Dy, X, Y, PadX, PadY, IsEmptyPin)`

3. **YOLO 检测元组扩展**（`MainViewModel.cs`）
   - YOLO 返回的 PinResult 统一设置 `PadX=0, PadY=0, IsEmptyPin=false`

4. **RecalculatePinResults 增加空针判定**（`MainViewModel.cs`）
   - `if (pin.IsEmptyPin) { pin.Result = "NG"; continue; }` 空针直接 NG

5. **Fov 前置推算取代后置推算**（`MainViewModel.cs`）
   - 新增 `SyncFovFromCameraParams()`：`FovWidth = CameraResolutionW × CameraPixelEquivalent`
   - 调用时机：`LoadProjectToUI()`、`CreateNewProject()`、`StartFullImageGrabFlow()`
   - 移除全图采集结束后的后置 Fov 推算代码

6. **检测流程.md 更新**
   - VM 2D 协议附录改为 PAD/PIN 新格式
   - 空针错误场景处理文档
   - 2D+3D 合并逻辑更新为含 PadX/PadY/IsEmptyPin 的代码
   - 版本历史新增 v4

**影响范围**：
- `Models/PinResult.cs`：新增 PadX/PadY/IsEmptyPin + SetIdealHeight
- `ViewModels/MainViewModel.cs`：ParseDetect2DResponse 重写、YOLO 元组扩展、RecalculatePinResults 空针判定、SyncFovFromCameraParams
- `检测流程.md`：协议文档更新

---

### 像素当量标定更新 + 相机分辨率字段

**背景**：标定获得准确像素当量 0.004780825 mm/px（209.2236 px/mm）。界面用户输入像素密度（px/mm），代码内部统一用像素当量（mm/px）。新增相机分辨率字段 5472×3648。

**改动**：

1. **SysParamData 默认值更新**（`SysParamData.cs`）
   - `CameraPixelEquivalent`：0.01 → **0.004780825**（mm/px）
   - 新增 `CameraResolutionW = 5472`、`CameraResolutionH = 3648`

2. **SysParamViewModel 像素密度转换**（`SysParamViewModel.cs`）
   - `CameraPixelEquivalent` 改为完整属性，setter 触发 `CameraPixelDensity` PropertyChanged
   - 新增 `CameraPixelDensity`（无 `[SysParam]`）：`get => 1.0 / CameraPixelEquivalent`、`set => CameraPixelEquivalent = 1.0 / value`
   - 新增 `[SysParam] CameraResolutionW`、`[SysParam] CameraResolutionH`

3. **SysParamWindow.xaml 界面更新**
   - "相机像素当量" → "像素密度 (px/mm)"，绑定 `CameraPixelDensity`
   - 新增分辨率行：分辨率宽/高

4. **EditProjectWindow Fov 只读显示**
   - FovWidth/FovHeight 改为 TextBlock 只读显示，StringFormat={}{0:F2}

**影响范围**：
- `Models/SysParamData.cs`
- `ViewModels/SysParamViewModel.cs`
- `Views/SysParamWindow.xaml`
- `Views/EditProjectWindow.xaml`

## 2026-05-26

### VM 2D 检测结果可视化：叠加层绘制 + 结果列表 PadX/PadY 列

**背景**：VM 2D 检测已返回 PAD/PIN 坐标数据，但结果仅存储在 PinResults 中，未在 Halcon 窗口上可视化。需要在拼接图上绘制焊盘/针脚的矩形框，并在结果列表中添加 pad 坐标列。

**改动**：

1. **新增 VM 叠加层数据结构**（`MainViewModel.cs`）
   - `VmOverlayItem` 类：存储 GridIndex、PinIndex、PinMmX/Y、PadMmX/Y、Dx/Dy、IsOK、IsEmptyPin
   - `_vmOverlays` 列表：累积所有检测位的 VM 结果叠加层数据

2. **新增 VM 叠加层绘制方法**（`MainViewModel.cs`）
   - `DrawVmRects(HWindow)`：焊盘 = 蓝色固定矩形(5×5)、针尖 = 红色固定矩形(3×3)、空针 = 橙色×标记
   - `DrawVmText(HWindow)`：文字带 "VM" 前缀，OK 绿 / NG 红，空针橙色显示"空针"
   - 坐标从 VM 返回像素值 × CameraPixelEquivalent 转为物理 mm

3. **检测流程集成**（`MainViewModel.cs`）
   - VM 检测结果解析后，立即将像素坐标换算为物理 mm 并填充 `_vmOverlays`
   - OK/NG 判定规则：`|dx|≤XDev && |dy|≤YDev`（dx/dy 已转为 mm）
   - 每个检测位完成后刷新叠加层（与 YOLO 共享同一绘图管道）

4. **所有叠加层刷新路径增加 VM 绘制**
   - `OnToggleOverlayRequested`（切换叠加层可见）
   - `RefreshSelectionDisplay`（刷新选择+叠加）
   - `OnImagePartChanged`（缩放/平移后重绘）
   - `SetHalconWindow DrawOverlays` 回调
   - `NavigateToDetectIndex`（定位到检测位）
   - `ShowPersonalization`（个性化设置刷新）
   - `ShowEditProjectDialog`（参数修改后刷新）
   - 检测位完成后自动刷新

5. **VM 叠加层数据持久化**
   - `SaveVmOverlays()` / `LoadVmOverlays()`：保存到 `Result/VmOverlays.json`
   - 检测完成时自动保存，加载方案时自动恢复
   - `SyncVmOverlayResults()`：参数修改后同步 OK/NG 状态

6. **结果列表新增 PadX/PadY 列**（`MainWindow.xaml`）
   - 在 X/Y 列之前插入 PadX、PadY 列，显示焊盘坐标 (mm)

**影响范围**：
- `ViewModels/MainViewModel.cs`：VmOverlayItem、DrawVmRects/Text、检测流程集成、持久化、所有刷新路径
- `Views/MainWindow.xaml`：结果列表新增 PadX/PadY 列

---

### 适配 VM 2D 最新通信协议（2026-05-27）

**背景**：VM 2D 通信协议从 PAD/PIN 分离格式改为自包含格式，每组数据包含焊盘坐标、有针标志和针脚坐标，不再分 PAD 和 PIN 两段传输。

**改动**：

1. **重写 ParseDetect2DResponse 解析新协议**（`MainViewModel.cs`）
   - 旧格式：`DETECT_2D_OK:PAD:x&y;...|PIN:x&yOK;x&yNG;...`
   - 新格式：`DETECT_2D_OK:x&y&0&x&y;x&y&1&x&y;...`
   - 每组数据自包含 `{PadX}&{PadY}&{HasPin}&{PinX}&{PinY}`
   - `HasPin=0` 时直接设置 `IsEmptyPin=true`，PinX/PinY 忽略

2. **DrawVmRects 空针不显示匹配框**（`MainViewModel.cs`）
   - `IsEmptyPin=true` 的针脚跳过焊盘蓝框和针脚红框绘制
   - 叠加层仅显示 NULL 文字，不显示任何矩形/交叉标记

3. **检测流程.md 更新**
   - 协议格式文档更新为新格式示例和说明
   - 错误场景表：空针场景更新为 `HasPin=0` 描述
   - 版本历史新增 v5

**影响范围**：
- `ViewModels/MainViewModel.cs`：ParseDetect2DResponse 重写、DrawVmRects 空针跳过
- `检测流程.md`：协议文档更新

## 2026-07-22 新增 — 多类型针标记工具栏

### 背景
方案的 PluginFeatureCount（特征个数，1~10）决定针型数量。需要一个 CAD 风格工具栏嵌入在 Halcon 窗口区域内，让用户标记每个检测位包含哪些针型。

### 解决方案
1. **Model 层**：`GrabPosition.cs` 和 `DetectPosition.cs` 新增 `PinTypes` 属性（string，如 "A"、"AB"）
2. **ViewModel 层**（`MainViewModel.cs`）：
   - 新增标记模式常量/属性/事件（MARKER_MODE_NONE/DELETE、ActiveMarkerIndex、MarkerColors 颜色表）
   - `OnPositionToggled` 替代 `ToggleDetectPositionFromUI`：支持标记模式切换字母、删除模式清除、无模式回退原行为
   - `DrawSelectionRectangles`：在选中框左上角绘制彩色标记字母
   - `UpdateDetectPositions`：Select 投影中复制 PinTypes
   - `SetMarkerMode`/`SetDeleteMode`/`ClearAllMarkers`：标记模式控制
   - `EnterSelectInspectionMode`：默认激活标记 A
3. **控件层**（`CustomHSmartWindowControl.cs`）：
   - 新增 `PluginFeatureCount` 属性、`MarkerModeSelected`/`ClearAllMarkersRequested` 事件
   - 右键菜单"从界面上选择检测位"改为子菜单 A/B/C...；选择模式下新增"清空所有标记"
4. **视图层**：
   - `MainWindow.xaml`：Halcon 窗口上方新增 `RowDefinition Auto` + `MarkerToolbar` StackPanel
   - `MainWindow.xaml.cs`：`UpdateMarkerToolbar()` 动态生成 A/B/C 按钮 + 删除/清空按钮；`OnMarkerToolbarStateChanged` 更新按钮激活样式

### 数据流
用户点击工具栏 A → SetMarkerMode(0) → 激活标记模式 → 点击 Halcon 窗口检测位 → PinTypes += "A" → 绘制红框 + 左上角彩色 "A"

### 验证要点
1. PluginFeatureCount=1 → 显示 A 按钮；3 → A, B, C
2. 点击标记字母 → 点击检测位 → 显示/隐藏对应字母
3. 删除模式 → 清除检测位所有标记
4. 清空全部 → 所有标记清除
5. 旧功能不受影响（无标记模式时点击恢复原行为）

**影响范围**：
- `Models/DetectPosition.cs`
- `Models/GrabPosition.cs`
- `ViewModels/MainViewModel.cs`
- `Core/Controls/CustomHSmartWindowControl.cs`
- `Views/MainWindow.xaml`
- `Views/MainWindow.xaml.cs`