# 更新日志 2026-08-13：检测流程 StartCheckFlow 三层状态机化改造

## 时间
2026-08-13

## 标题
检测流程（StartCheckFlow）改造为三层状态机：设备状态 / 壳层 FlowRunState / 业务层 CheckState

## 背景
检测流程是项目代码量最大（1200+ 行单体方法）的核心流程，也是最后一个未状态机化的流程：检测位循环内物理动作（移动/拍照/3D扫描）、视觉等待（2D/3D）、解析合并（匈牙利匹配）全部平铺，暂停/取消靠手工插入 6 处 `CheckPauseAndCancelAsync` 检查点，三色灯绿灯用 `!_pauseCheckEvent.IsSet` 近似表示。用户要求参照已完成的**全图采集三层状态机**改造，保证原有暂停（6 检查点粒度）、复位（终止+物理复位）、停止、报警功能可用，三色灯随流程运行状态精确变化。

## 解决方案

### 架构（与全图采集完全同构）
- **设备状态层**（DeviceMonitor.DeviceState）：不动
- **壳层 FlowRunState**（Init/Running/Paused/Terminated）：RunStep 局部函数与全图采集逐行同构，仅暂停事件换 `_pauseCheckEvent`；`FlowLoopAsync(RunStep)` 驱动
- **业务层 CheckState**（新增私有枚举）：`Init → CapturePos → GrabTile → Start2D → MoveScanStart → StartScan3D → Wait2D → WaitScan3D → Merge → NextPos → End/Err`

### 关键设计
1. **6 个暂停检查点位置精确保留**（对齐原 CheckPauseAndCancelAsync 调用点）：①CapturePos 入口 ②Start2D 入口 ③MoveScanStart 移动后 mid-case ④StartScan3D 入口 ⑤WaitScan3D 入口 ⑥Merge 入口；恢复从当前状态重做（游标 i 仅 NextPos ++）
2. **2D/3D 并行任务句柄闭包跨状态传递**：`_vmDetectTask/_yoloDetectTask`（Start2D 写→Wait2D 读）、`_3dScanTask`（StartScan3D 写→WaitScan3D 读）；`scan3DTimeout` 闭包标志 = response=="TIMEOUT"，Merge 内 `? 纯2D兜底 : 完整3D合并` 分支（3D 超时/未配置/扫描失败降级语义不变）
3. **终态收尾放循环外**（不放 End case 内）：中途终止/报错同样执行去重+保存（保持原"break 后走完整收尾"保存部分结果行为）；状态栏文案三分支区分：完成（原文案）/ 异常 / 已终止
4. **灯控精确化**：新增 `_checkBizRunning` 字段（GrabTile 入口置位=绿灯；准备/暂停/位间/终态收尾/异常清零），`RegisterFlowStateProviders` Check 分支从 `!_pauseCheckEvent.IsSet` 改为 `_checkBizRunning`
5. **业务失败 Err 化**（用户确认）：移动到3D扫描起点失败（重试3次后）由"MessageBox+break（状态栏仍显示检测完成）"改为转 `CheckState.Err` → 收尾弹窗+红灯+状态栏"检测异常"；**返回值仍 true，连续模式不受影响**（照常出板）
6. **外部接口零变化**：`StartCheckFlow(bool isSubFlow)` 签名/返回值语义不变（前置失败/异常 false；完成/取消/Err true）；`_checkCts/_pauseCheckEvent/_checkFlowCompleted` 全部使用点不动（StopCheckFlow/PauseCheck/PauseCheckOnly/ResetMachine/OnAlarmTriggered 零修改）；`_checkFlowCompleted.Set()` 仍在 finally（连续 WaitCheck/演示/精度 5 个等待方不变）
7. **准备段搬入 try 内 Init case**：原 :3181-3234 清理准备段在 try 外（异常会逃逸且 isWorkFlowRun[Check] 卡 true），搬入后异常走 catch 弹窗+finally 清标志（顺带修 bug）
8. **迁移展望**：连续模式（ContinuousState 已有 Step 无壳层）后续迁移 FlowRunState 壳层时可参考本实现；壳层 RunStep 仅 ~30 行、每流程自包含，暂不抽公共类（等第三个接入者出现再抽）

### 行为矩阵（改造前后对比）
| 场景 | 改造前 | 改造后 |
|---|---|---|
| 单板/连续检测正常跑 | 平铺循环+检查点 | 状态机逐拍，文案一致 |
| 6 处暂停→恢复 | CheckPauseAndCancelAsync 等待 | 壳层 Paused 分支等待，恢复重做当前状态 |
| 停止/复位 | Cancel→下检查点 break→完整收尾 | Cancel→壳层 Terminated→完整收尾（文案"已终止，已检测 i 个位点"） |
| 报警 | 检查点拦截→break→收尾 | MarkAlarmCancel→Err→红灯+不重复弹窗 |
| 移动失败 | 弹窗+break+状态栏"检测完成" | Err→弹窗+红灯+状态栏"检测异常"（返回值不变） |
| 绿灯 | 运行中即亮（暂停近似） | 业务执行中精确（准备/暂停/收尾不亮） |

## 总结
检测流程完成三层状态机化，与全图采集架构完全对齐（壳层 RunStep 同构、FlowLoopAsync 复用），为连续模式后续迁移 FlowRunState 壳层铺路。6 检查点粒度、外部接口、返回值语义零变化；新增 Err 化语义（移动失败）与灯控精确化。仅改 MainViewModel.cs（+1 字段、+1 枚举、方法体重构、委托 1 行）。编译通过（0 错误）。
