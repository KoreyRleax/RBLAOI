# 更新日志

## 时间
2026-07-22

## 标题
多类型针检测第二阶段：检测流程重构与参数引用改造

## 背景
阶段一新增了 `PinTypeParameter`，将检测参数从全局 `ProjectData` 字段（`XAllowedDeviation`、`YAllowedDeviation`、`IdealPinHeight`、`AllowedHeightDeviation`、`RelativeOffsetX/Y`、`FocusParams`）迁移到每针型独立的 `PinTypeParams[]`。但由于这些字段已被删除，`MainViewModel.cs` 中大量代码引用了已删除的属性，导致编译失败。

## 解决方案
完成以下核心改造：

### 1. 旧方案兼容（`EnsurePinTypeParams`）
- 在 `LoadProjectToUI` 中调用 `EnsurePinTypeParams`，加载旧方案时自动生成 `PinTypeParams` 默认值
- 从系统参数（`SysParam.Data`）获取默认容差/高度值

### 2. 全局属性删除（MainViewModel）
- 删除了 6 个全局属性及 backing fields：`ProjectXAllowedDeviation`、`ProjectYAllowedDeviation`、`ProjectIdealPinHeight`、`ProjectAllowedHeightDeviation`、`ProjectRelativeOffsetX`、`ProjectRelativeOffsetY`
- 保留了 `Is*Modified` 标志位，改为以 `PinTypeParams[0]` 为基准与系统参数对比

### 3. 检测参数读取改为逐针型
- `RecalculatePinResults()`、`RecalculateOffsetDxDy()`、`SyncPinIdealHeight()` 改为通过 `pin.PinType` 查找对应的 `PinTypeParameter`
- VM 检测结果处理中，按 `pos.PinTypes[pin.PinIndex]` 确定每根针的针型，并使用对应 `PinTypeParam` 的 `IdealOffsetX/Y`、`XyTolerance`
- 3D 匹配判定（匈牙利算法）逐针读取 `PinTypeParams`，替代固定的全局容差

### 4. `ShowEditProjectDialog` 重构
- 改为直接传递 `PinTypeParameter[]` 给 `EditProjectWindow`
- 保存时直接写入 `project.PinTypeParams`
- 删除旧的 `FocusParams`、`XAllowedDeviation` 等传参

### 5. Blob 模式多针抓图
- 新方法 `GrabOneAsync(double focusZ, string saveSubDir, ...)`：单次抓图，支持任意子目录
- 新方法 `GrabAndReplaceMultiAsync(...)`：抓取板面图 + 遍历 `pos.PinTypes` 逐型抓取针尖图
- 删除旧的 `GrabAndReplaceWithFocusAsync`
- `DETECT2D_BLOB` 命令改为多针型格式：`DETECT2D_BLOB:{boardPath}|A:{pinPathA}|B:{pinPathB}`

### 6. 数据模型扩展
- `PinResult.cs` 新增 `PinType` 属性（string，默认 "A"），标记每根针所属针型
- `Pin2DData` 内部类新增 `PinType` 属性，流动到 `PinResult` 创建处
- 新增 `GetPinTypeParam()`、`GetDefaultDetectParams()` 辅助方法

## 改动文件

| 文件 | 操作 |
|------|------|
| `Models/PinResult.cs` | 新增 `PinType` 属性 |
| `ViewModels/MainViewModel.cs` | 删除6个全局属性；新增 EnsurePinTypeParams/GetPinTypeParam/GetDefaultDetectParams 辅助方法；重构 LoadProjectToUI/SaveUIToProject/UpdateDetectionParamModifiedFlags；重构 ShowEditProjectDialog；新增 GrabOneAsync/GrabAndReplaceMultiAsync；删除 GrabAndReplaceWithFocusAsync；重构检测流程中的参数引用；更新 PinResult/Pin2DData 创建处添加 PinType；重构 RecalculatePinResults/RecalculateOffsetDxDy/SyncPinIdealHeight；更新 DETECT2D_BLOB 命令格式；更新 FOV 抓图 FocusParams 引用 |

## 总结
编译通过（0 个 C# 编译错误），所有旧属性引用已清除。检测流程现在按每针型读取独立参数（XY容差、理想高度、高度容差、理想偏移）。旧方案加载时自动生成 `PinTypeParams` 默认值，确保向后兼容。
