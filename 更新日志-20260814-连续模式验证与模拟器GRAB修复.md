# 更新日志 2026-08-14：连续模式完整验证 + 模拟器 GRAB 删源修复

## 时间
2026-08-14 下午

## 标题
连续模式自动化验证全部通过；修复模拟器 GRAB 回复名导致 Origin 瓦片库少图的缺陷

## 背景
- 2026-08-14 上午的 5 项代码改动（拍照失败直接异常/同名瓦片短路/TransportBoard 暂停响应/暂停冻结计时/复位立即终止）中，**连续模式验证未完成**（11:50 在未回零下错误启动），需按正确流程（真实回零）重验。
- 验证过程中发现：连续模式每跑一轮检测，`Images\Origin` 瓦片库就少 1 张图（12911→11498），最终触发"图片丢失"弹窗，阻塞验证。

## 解决方案
1. **按手册流程重验连续模式**（模拟器+上位机重启、Win32 弹窗处理、真实回零）：
   - 前置：全图采集 150 张 + 图片加载完成 + G28 真实回零（`X/Y/Z/H_HOME_OK`）
   - 入口有板时正确回零路径：启动回零弹窗点**否**（有板会被 BoardBlockingHome 拒绝）→ 连续模式→开始→"轴未回零"点确定 → `CheckAndHomeAxesAsync` 的 `HomeXYZ()` **不检查传送带** → G28 回零成功 → 流程继续
   - 验证结果：
     - ✅ 连续模式日志链：入板→检测(3检测位)→出板→出口防堵→循环，稳定跑 4+ 块
     - ✅ **出口防堵暂停冻结计时**：SysParam 实际防堵超时=10s；暂停 32s（>10s）后恢复，6s 内无 Err（冻结生效），恢复后 ~9.8s 正常超时（冻结≠取消）
     - ✅ **运送中暂停**：增量读日志抓"去基板"时机（延迟 229ms）→ 点停止 → MOTOR_STOP 0.36s 响应、传送带停、黄灯、计时冻结；恢复后无超时继续
2. **修复模拟器 GRAB 删源缺陷**（`C:\Users\59538\Desktop\f407_gui_simulator.py`）：
   - 根因：自动模式 GRAB 优先返回 DetectImages 队列文件名，该名 ∈ 瓦片库；目标瓦片≠GRAB 名时，上位机瓦片替换"删目标→复制→**删源文件**"把瓦片库同名文件删除 → Origin 少图。真实 VM 每次拍照=新时间戳文件名（∉瓦片库），删源只删临时文件，无此问题——属模拟器缺陷。
   - 修复：GRAB 回复名改为新生成的 `GRAB_OK_SIM{seq:06d}.bmp`（模拟 VM 新时间戳行为），源图内容仍从队列取。
   - 验证：连续模式跑 4+ 块后 Origin **保持 150 张**，无"图片丢失"。
   - 备份：`f407_gui_simulator.py.bak_grab`（delay 3.0 原版）
3. 测试辅助（已还原）：临时把模拟器 `_schedule_take_away` 取板延迟 3.0→120.0 秒构造出口防堵场景（备份 `.bak_delay3`，已还原 3.0）。

## 总结
- 连续模式 5 项改动全部验证通过（含出口防堵暂停冻结计时、运送中暂停两个核心场景）。
- 发现并修复模拟器 GRAB 删源缺陷——**测试检测类流程前先跑全图采集的规则仍然有效，但 Origin 数量不再因检测而减少**。
- 新增验证经验（已写入记忆）：菜单项是 Button 非 MenuItem；btnRun 需激活窗口+鼠标点击；等图片加载前必须先关完回零/轨宽弹窗；SysParam 实际防堵超时=10s；入口无板时重启失败的处理方式。
