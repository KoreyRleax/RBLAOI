# 更新日志 2026-08-13：报警时终止运动流程（安全保护机制）

## 时间
2026-08-13

## 标题
报警保护机制：报警激活时立即终止运行中的运动流程（全图采集/检测/连续/运送），停止未完成指令

## 背景
原报警只做两件事：弹窗/蜂鸣/状态栏提示 + `AlarmBlocked` 入口闸门（防"开始"不防"进行中"）。**正在运行的流程不会因报警而停止，报警期间上位机继续发运动指令**，存在安全隐患。

## 解决方案

### 职责划分（用户拍板）
- **下位机**：报警时机器不动（硬件/固件兜底所有电机）——已由下位机处理
- **上位机**：只停止"报警前一时刻发送且未完成的指令"——`MotionIO.Flush()`（排空指令队列+等待者 ERROR 确定性完成+清缓冲，纯上位机操作，零协议指令需求）；流程取消后检查点退出不再发新指令
- 统一模式：每个涉及运动的流程报警时只做两件事——① 停止流程（状态机→Err）② 停止未完成指令（统一入口 Flush；运送流程自身 StopMotorAsync 兜底停传送带——传送带是"发后持续运转"指令）

### 追加：安全操作检查统一封装 CheckSafeOperation（2026-08-13 同日）
- 新增 `CheckSafeOperation(bool isSubFlow = false)`：统一检查顺序 ① 互斥流程（isSubFlow 跳过）② 报警状态（文案固定"设备处于报警状态，请先复位报警后再操作"，不再带 action 名称）③ 需回零（急停/轴报警区分文案）；返回 true 可继续
- 替换原 `AlarmBlocked(action)`（删除）与各处散落的互斥/回零检查，8 处流程入口统一调用：全图采集/检测/运送基板/送出口/送入口/调整轨道/连续模式/采集FOV
- 行为变化（更严格更统一）：送出口/送入口/采集FOV 补齐回零检查；调整轨道补齐互斥+回零检查

### 追加：安全操作检查对齐插针机 isSafeOperation（2026-08-13 同日）
- **急停联动回零**：急停按下（InputEmergency 激活）→ `NeedHomeX/Y/Z/H` 四轴全置位 + `NeedHomeByEmergency` 标志（伺服使能关闭、位置不可信，对齐插针机 isNeedHome 语义）——急停解除后执行流程会被"需先回零"拦截，**文案按源区分**：急停触发显示"急停后需先回零"，轴报警显示"轴报警后需先回零"（4 处流程入口统一）；回零后标志清除
- **安全门/光幕报警**（现场已接线，仿插针机 bChkSafeDoorAlarm/bChkLightAlarm）：
  - 系统参数新增 `EnableSafetyDoorAlarm`（安全门 X11）/`EnableLightCurtainAlarm`（光幕 X10），功能开关区 CheckBox，默认勾选
  - DeviceMonitor 报警源新增两条（门禁 ON=安全门已开启、光幕 ON=被遮挡），`AlarmItem.Enabled` 委托按参数开关运行时可改即时生效（未勾选则跳过检查，不误报）
- 对照：急停/气压/轴报警检查（AlarmBlocked 已有）✓；互斥/回零/连接检查（已有）✓——插针机 isSafeOperation 全部要素本机已齐

### 具体改动（2 个文件，MotionIO.cs 零修改）
1. **DeviceMonitor.cs**：新增 `AlarmTriggered`/`AlarmCleared` 事件（AxisAlarm 边沿跳变触发，去抖 200ms；"复位但信号仍在"期间不触发 Cleared）
2. **MainViewModel.cs**：
   - 新增 `_alarmStopping`（volatile）标志 + `OnAlarmTriggered`（轮询线程，线程安全原语）：置位 + Cancel 全部 CTS（_grabCts/_checkCts/_continuousCts/_demoCts/_precisionCts）+ Set 暂停事件 + `Flush()`
   - 全图采集：检查点取消时报警→`bizState=Err`（MarkAlarmCancel 辅助，err="设备报警，采集已终止"）；Err 收尾报警时跳过弹窗
   - 检测（非状态机）：`CheckPauseAndCancelAsync` 加 `_alarmStopping` 拦截（StopCheckFlow 原语）
   - 连续模式：Step 顶部报警穿透→Err（不等检测完、不强行出板，finally 停带+ResetIO 兜底）；Err 收尾跳过弹窗；入板失败文案区分
   - 运送三流程（TransportBoard/SendToExit/SendToEntrance）：Step 顶部报警检查→`StopMotorAsync()` 兜底停传送带+转 Err

### 行为矩阵
| 场景 | 行为 |
|---|---|
| 全图采集移动/拍照中报警 | Flush 在途等待立即 ERROR 返回；检查点 Err → "设备报警，采集已终止"、无弹窗 |
| 全图采集暂停中报警 | Set+Cancel → OCE → Err 化立即退出 |
| 单板检测中报警 | 下一检查点退出 → finally 置 _checkFlowCompleted |
| 连续模式报警 | Step 顶 ≤200ms Err，不等检测完、不出板 |
| 运送基板/送出口/送入口中报警 | 50ms 节拍内停带转 Err |
| 演示/精度中报警 | 外层 CTS 取消退出 |
| 报警消失/复位后 | AlarmCleared → _alarmStopping=false，流程可重新开始 |

## 总结
报警从"只提示不干预"升级为"提示+自动终止流程+停止未完成指令"：上位机 `Flush + 流程取消` 治指令流，下位机治物理运动，各司其职；所有状态机流程统一"Err + 停止指令"响应模式。MotionIO.cs 零修改（仅调用现有 public API）。编译通过（0 错误）。
