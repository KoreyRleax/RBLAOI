# 交接文档（2026-08-21 16:26 更新）

> 交接范围：RBLAOI 上位机（.NET Framework 4.7.2 / WPF / x64）。
> 本次更新重点：**3D 匹配/重扫链路** 的下午改动（匈牙利修复 + 3D 异常自动重扫 + 数据等待缩短），以及显示性能优化的尝试与回退。
> 上午改动（3D 扫描超时解耦/快照兜底）见 `更新日志-20260821-3D扫描超时解耦与快照兜底.md`。

## 一、当前代码状态（今天下午新增改动）

**均未提交 git（沙箱下 git ref 写入不稳定，commit 对象 86265e3 已建但分支 ref 丢失风险；回退以文件备份为准）。**

### ⭐ 1. 匈牙利匹配修复（DetectionFlow.cs ~L1061）
- **问题**：2D/3D 数量不等时（如 2D=16、3D=18），补的"虚拟行"代价设 `INF=1e9`，导致算法产生次优解——虚拟行抢占本可匹配的 3D 点，真实针被挤到 20mm 外（实例：PinIdx=8 被配到 20.41mm 的 3D#16，错失 1.50mm 的 3D#8，误判无匹配 NG）。
- **修复**：`else hungCost[pi, ci] = INF;` → `= 0;`（虚拟行/列代价 0 = 允许 2D 无 3D、允许多余 3D 被跳过）。
- **影响**：每个 2D 针要么配到全局最优 3D 点(≤3mm)，要么明确"无 3D"；不会再出现"匹配了但距离 20mm"的荒谬结果。已用 Python 复现验证（`analyze_hungarian.py`，输出与日志 100% 一致）。

### ⭐ 2. 3D 异常自动重扫（DetectionFlow.cs，核心新增）
- **需求**：3D 数据异常（①全部匹配失败 ②匹配成功但高度全 0 ③扫描成功但无数据）→ 判定 3D 图像源/VM 异常 → 本拍结果不显示不落表，**自动重新扫描**，直到至少 1 个针有有效高度(h>0)；上限 `max3DRescanRetry=3` 次仍无效 → 走原逻辑降级显示。
- **实现**：
  - 新增 `int retry3DPos`（每检测位计数，CapturePos 重置）+ `const int max3DRescanRetry = 3`；
  - 新增 `int mergeValidH3D`（匹配成功且 h>0 的针数）；
  - 新增 `bool scan3DNoData`（WaitScan3D 写：扫描成功但数据超时=true、轴失败=false）；
  - Merge 两处重扫判断：
    - if 分支（有 3D 数据）：`!scan3DTimeout && mergeTotal2D>0 && mergeValidH3D==0` → 重扫；
    - else 分支（3D 超时）：`scan3DNoData && validPins2D.Count>0` → 重扫（轴失败不重扫，走弹窗降级）。
  - **重扫必须跳 `CheckState.MoveScanStart` 而非 StartScan3D**：重扫时轴停在扫描终点，直接 SCAN3D 到同一点轴不移动（X_COMPLETE 27ms 即回），3D 相机不产生新点云→收不到数据（16:17 实测踩坑）。跳 MoveScanStart 才会先移回起点再完整移动+扫描。
- **重扫链路**：MoveScanStart(移回起点) → StartScan3D(SCAN3D 整段) → Wait2D(2D 重解析，无害) → WaitScan3D → Merge。每次重扫 ≈4s。

### 3. 数据超时等待缩短（WaitScan3D ~L787）
- 扫描完成后的数据等待窗口：`Math.Min(Wait3DResultTimeout, 2000)` —— 正常数据扫描完成前已到（实测 23ms~1.5s）不受影响；**超 2s 无数据立即判"无数据"→重扫**，不再干等 5s。

### 4. 无匹配日志区分两种原因（~L1152）
- 有分配但距离超阈值 → `NO 无匹配(匈牙利距离=xxmm >阈值3mm)`；
- 未分配到任何 3D 点（3D 点数不足）→ `NO 无匹配(3D点数不足，无点可配)`（不再显示 NaN）。

### 5. 显示性能优化已回退（用户决定，重绘效果不好）
- 尝试过：显示段 `GetCurrentStitchedImage→GetRawStitchedImage`（免拷贝）+ `DrawVmRects` 改 `DispRectangle2`（直绘）→ **已全部回退**，恢复 `GetCurrentStitchedImage`/`GenRectangle2ContourXld` 原样。
- 实测数据：扫描完成→显示落表平均 ~200-250ms，偶发尖峰 0.5~1.6s；解析+匹配仅 20-30ms，慢在 UI 全量重绘（拼接大图 DispObj + 全量画框）。
- **下一步提速方向**（未实施）：①去掉 Wait2D 重复刷新（每拍 2 次全量重绘→1 次）；②拼接图降采样显示。备份在 `.workbuddy/backup/save-node-1-display-opt/`。

### ⭐ 6. 显示刷新异步化：不再阻塞检测流程（用户决策，替代提速方案）
- **需求**：不优化显示刷新速度，而是让显示刷新**异步执行**——渲染在后台间隙进行，检测流程直接推进下一个检测位，不等渲染完成。
- **实现**：
  - `VisionOverlay.cs` 新增 `RefreshOverlayDisplayAsync()`：`Dispatcher.InvokeAsync` fire-and-forget（不 await）+ **`_vmOverlays` 快照**（渲染排队期间 _vmOverlays 已被下一检测位修改，快照保证画的是发起时刻内容）；拼接图克隆与 Dispose 均在渲染任务内；
  - `DrawVmRects/DrawVmText` 增加带 `List<VmOverlayItem>` 参数重载（快照绘制），无参重载保留转发；
  - `DetectionFlow.cs` Wait2D 后 / Merge 后两处显示段 `await InvokeAsync` → `RefreshOverlayDisplayAsync()`；
  - Merge 段 overlay.H/IsOK/PinId 更新**移出 UI 委托**（放流程上下文），保证异步刷新的快照拍到最终 H。
- **效果**：单拍不再干等显示 200ms~1.6s；渲染在流程 await 间隙（运动/扫描等待）由 UI 线程执行；FIFO 顺序 Wait2D 帧→Merge 帧。
- ⚠️ **未编译**（沙箱拦 MSBuild），需本机 `_build.bat` 验证：编译通过、画面与原先一致、`[⏱] 总耗时` 不再含显示等待、连续模式/暂停/复位/重扫场景显示正常。
- 详细见 `更新日志-20260821-显示异步化不阻塞检测流程.md`。

## 二、核心关注点（长期，每日必看）

### ⭐ 时刻关注：3D 扫描是否失败、是否重试、是否自动重扫
- 用 `分析3D延迟.bat`（`analyze_3d_delay.py`）检查日志：
  - 双扫率（`SCAN3D 发送数/成拍数`）应为 0%；
  - 重试次数（`第一次尝试失败`）应为 0；
  - 快照兜底（`轴等待超时但快照确认已到位`）允许出现，持续增长需关注；`快照未确认到位` = 真失败，**出现即报警**。
- **新增观察点**：`第 N/3 次重扫`（3D 数据异常自动重扫）——允许出现（3D 偶发异常），但若高频出现说明 3D 图像源/VM 持续异常，需排查；重扫时应看到 SCAN3D → X_COMPLETE 间隔 >1s（真移动），若 <100ms 说明重扫链路又退化。
- **判定**：双扫率 >0 或"快照未确认"或"重扫高频" → 优先排查。

## 三、未完成事务（按优先级）

### 🟢 P1-1：WaitAxisComplete 快照轮询提前判完成（✅ 已实施 + 已验证 2026-08-21 17:41）
- **动机**：快照兜底拍仍白等满 `Scan3DTimeout`（6s）；改为等待期间轮询 GET_STATE 快照（19~21Hz），一旦到位即提前返回 → 兜底拍 6s→<1s。
- **实施**：`Core/Motion/MotionIO.cs`——`WaitAxisComplete(axis, timeoutMs, targetPulse?)` 增加可选目标脉冲，等待期间 `PollSnapshotArrivalAsync` 每 200ms 查快照（`AxisArrivedBySnapshot`：快照新鲜 && 轴空闲 && |pos−目标|≤500 脉冲 → 提前返回 true）；`Task.WhenAny(事件, 快照, 超时)` 三路竞争。`Scan3DToAndWaitOK`（L880）与 `MoveToAndWaitOK`（L772）均已传目标脉冲启用；回零等未知目标点不传 targetPulse，行为与旧版一致。
- ✅ **新 exe 日志验证**（log_20260821_172511.txt，17:09 编译 exe，17:40 跑 9 拍）：
  - **"轴等待超时但快照确认已到位" = 0 次**（旧 exe 同规模 16 次）——X_COMPLETE 偶发丢失时的白等路径消失，轮询提前返回生效；
  - "轴等待超时且快照未确认到位" = 0 次（无真失败）、SCAN3D 第一次尝试失败 = 0 次、重扫 = 0 次、3D数据超时 = 1 次（首拍预热，2D 0 针正确降级）；
  - 后续 8 拍 VM 推送 87~114ms，全部 <1s；D(3D扫描完成)≈4.1s 为 7mm/s 正常扫描时长（23mm 行程），非等待。

### 🟠 P1-2：VM 空推送（DETECT_3D_OK:|| / DETECT2D_RECT_OK: / DETECT2D_BLOB_OK:padC:blankC:pinC:）
- **现象**：VM 在 CAMERA3D_ON/GRAB 期间高频推送空结果（GRAB 流程串联执行 2D 检测产生），日志膨胀、触发全部 handler。实测频率（log_20260821_162440.txt 61 拍）：DETECT_3D_OK:|| 65 次 / DETECT2D_RECT_OK: 123 次 / DETECT2D_BLOB_OK:pad 184 次。
- **结论**：查官方文档确认 VM 3D 4.4.40 **全局脚本无发送拦截事件**（只有 OnReceiveCommunicateDataEvent 接收 + SendCommDeviceData 主动发送），脚本方案不可行。
- **正解**（待实施，二选一）：
  - VM 软件中解除 GRAB 流程与 RECT检测/BLOB检测 的流程串联（根因，推荐）；
  - 上位机 `Vision2DClient/3DClient` 按"无匹配等待者的空推送丢弃"过滤（方案已设计过，回退了）。

### 🟢 P1-3：检测位"解析→显示"刷新提速（✅ 已实施 + 已验证 2026-08-21 17:41）
- ~~现状：扫描完成→显示落表平均 ~200-250ms，偶发尖峰 0.5~1.6s；解析+匹配仅 20-30ms，慢在 UI 全量重绘~~ → 2026-08-21 已改为**显示异步化**（`RefreshOverlayDisplayAsync` fire-and-forget + 快照），显示不再阻塞检测流程（见上文"当前代码状态 §6"）。
- ✅ **新 exe 日志验证**（log_20260821_172511.txt，9 拍）：`E(3D结果获取)=D(3D扫描完成)`（同拍 4169ms，结果在扫描完成即取到），总耗时稳定 7.1~7.8s 无 0.5~1.6s 尖峰；扫描完成→落表不再有显示等待叠加（旧版 E 恒比 D 晚 200ms~1.6s）。画面显示需人工确认。
- 若后续仍需**进一步**提速（画面更新节奏跟不上）：①去掉 Wait2D 重复刷新（每拍 2 次全量重绘→1 次，Merge 统一刷）；②拼接图降采样显示（DispObj 前按窗口比例缩放或缓存缩小版）；③DrawVmRects 只重绘当前检测位新增叠加层。
- **涉及文件**：`MainViewModel.DetectionFlow.cs`（显示段）、`MainViewModel.VisionOverlay.cs`（DrawVmRects/Text、RefreshOverlayDisplayAsync）、`FullImageManager.cs`（拼接图获取/缓存）。

### 🟡 P2-1：参数最终确认
- `Scan3DTimeout=6000`、`Scan3DRetryTimeout=3000`、`Wait3DResultTimeout=5000`（数据等待实际按 min(5000,2000)=2s 生效）、`MatchThreshold3D=3`。
- 2s 数据等待窗口是否合适（偶发数据延迟>2s 会被误判重扫）。
- ✅ **日志佐证**：①log_20260821_162440.txt（61 拍）5 次超时中 3 次 2D 有针→重扫成功恢复，2 次 2D 0 针→重扫条件不满足正确降级，**无 2s 误判**；②log_20260821_172511.txt（9 拍）仅 1 次超时（首拍预热 2D 0 针正确降级）。→ **2s 窗口可保持**。

### 🟢 P2-2：代码提交/归档（✅ 已完成 2026-08-21 17:22）
- 已推送远程 `fix/multi-pintype-blob-parser`（c3ec443→34638a2，含匈牙利修复/3D重扫/显示异步化/快照轮询，排除 .workbuddy/）。保存节点1 文件备份在 `.workbuddy/backup/save-node-1-display-opt/`。

### 🔴 P2-3：检测位12 3D 匹配 Y 向 4mm 系统偏差（已分析定位 2026-08-21 18:10，未修改代码）
- **现象**：17 检测位方案中检测位12（扫描行 Y=90.614，X 109.696~132.696）12 针中 11 针"匈牙利距离 3.90~6.52mm >阈值3mm"无匹配 NG，每次都超出（17:44 与 18:07 两轮结果完全一致，偏差恒定）。
- **数据**：其他 16 位匹配距离平均 **1.57mm**（n=263，max 2.53mm）全部正常；检测位12 正确对位后 **dx=-0.29mm（正常）、dy=-4.03mm（系统性）**——2D 针 Y 中心 251.2 vs 3D 转板后 Y 中心 247.2。
- **定位**：3D 转板公式 `board = detectPos + Vm + calibOffset(全局-11.6,25.4)`。已排除：全局偏移（16 位正常，**不可调全局 Y+4，会毁掉其他位**）、检测位配置 Y（排布规律正常，同行 11/13/14 差仅 0.1~0.4mm）、偶发（两轮完全一致）。剩余嫌疑：①该位置 VM 3D 输出 VmY 系统性偏小 4mm（3D 标定非线性/该区域精度）；②该位置 2D 拼接图坐标偏大 4mm。
- **伴随问题**：PinIdx=5 错配到相邻针（dist=1.48mm <3mm 但配错对象），H 值取的是隔壁针的高度——**检测结果是错的**。
- **区分方法（不改代码）**：拼接大图上目视检查该区域（板坐标 X 161~164, Y 244~257）针位置与相邻区域/板框几何关系是否正常：正常→2D 对、VM 3D 标定偏；异常→2D 拼接偏。
- **修复方向（待定）**：若 3D 侧偏 → 检修 VM 3D 方案 RangeTransform 的 Y 标定，或启用"检测位级 3D 校准偏移"（DetectionFlow.cs 中注释掉的逐 FOV 投票校准即此思路）；若 2D 侧偏 → 检查全图采集拼接在该区域的基准。
- **分析工具**：`analyze_pos12.py`（按行号范围精确对位分析 2D↔3D 偏差分量）。

## 四、环境与工具注意事项

- **构建**：`_build.bat`（VS2019 MSBuild）。沙箱拦截 MSBuild/cmd，需本机编译。
- **3D 延迟分析**：`分析3D延迟.bat` / `analyze_3d_delay.py`（用法：`python analyze_3d_delay.py [日志目录] [文件名关键字] [文件数]`）。
- **匈牙利复现脚本**：`analyze_hungarian.py`（验证/调试 3D-2D 匹配用）。
- **日志关键字**：
  - 快照兜底生效：`[Scan3DToAndWaitOK] 轴等待超时但快照确认已到位`
  - 真失败（查固件）：`[Scan3DToAndWaitOK] 轴等待超时且快照未确认到位`
  - 3D 数据异常重扫：`全部无有效高度...第 1/3 次重扫` / `3D数据超时无有效高度...第 1/3 次重扫`
  - 无匹配原因：`匈牙利距离=xxmm >阈值`（有分配超距）/ `3D点数不足，无点可配`（无分配）
  - 诊断 Origin 文件数：`[诊断] 检测位i 抓图后Origin文件数变化`

## 五、历史遗留参考（非交接重点）

- Origin 图片数量减少问题：已确认非上位机"持续删除"，唯一批量清空点为全图采集开始的 `ClearProjectImagesFolder()`（设计行为）；用户反馈已解决，不再跟进。
- 08-20 双扫描问题全链路分析记录于 `.workbuddy/memory/2026-08-20.md`。
