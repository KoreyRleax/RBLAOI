# 更新日志 2026-08-24：单检测位执行时间优化（1a+2+3+5+8）

> 依据交接文档 §9（2026-08-24 分析）实施前三项低风险优化。目标：压缩单检测位「拍照→扫描前」A 链路的非物理开销，期望收益 ~350~650ms/位（含确定性 200ms）。
> ⚠️ 沙箱拦截 MSBuild，**未编译**；Roslyn 语义验证通过（目标文件真实错误 0，GlobalCameraModuleCs 为已知环境噪声）。**待用户本机 `_build.bat` 编译 + 模拟器/实机回归。**

## 改动（3 个文件位置）

### 1a. 删除移动前固定 Task.Delay(200)（确定性省 200ms/位）
- 文件：`ViewModels/MainViewModel.RunCommands.cs`（GrabAndReplaceTileAsync 开头）
- 原：`UpdateStatus(...)` 后 `await Task.Delay(200);` 再移动
- 理由：`MoveToAndWaitOK` 内部已等轴完成，200ms 纯白等；UpdateStatus 为属性绑定不依赖此延迟
- 保留 SET_PATH 后的 `Task.Delay(100)`（VM 传播保险，属 1b 未动）
- ⚠️ **8-24 15:52 补删 Blob 路径**：`GrabOneAsync`（RunCommands.cs:1124，Blob 多针模式板面图+每型针尖图使用）的同一 `Task.Delay(200)`——首轮验证发现用户跑的是 Blob 方案（样板单针检测方案），该路径 delay200×2=400ms/拍 未删，已补删

### 2. 移除每拍诊断 Directory.GetFiles×2（10~400ms/位）
- 文件：`ViewModels/MainViewModel.DetectionFlow.cs`（GrabTile case）
- 删除：拍照前 `originBefore` 枚举（原 L424）+ 拍照后 `originAfter` 枚举与 `[诊断]` 日志（原 L475-480）
- 理由：8-14 排查"Origin 文件数变化"问题的临时诊断，该问题已关闭（8-21 交接文档五节）；全目录枚举与 VM 写文件竞争磁盘
- 保留 `[⏱] 检测位i A(移动+拍照)` 计时日志不变

### 3. PauseAwareWaitAsync 轮询尾差消除（统计性 ~150ms/位，均值）
- 文件：`ViewModels/MainViewModel.DetectionFlow.cs`（PauseAwareWaitAsync 方法体）
- 原：`while(!task.IsCompleted){ 检查; await Task.Delay(50); } return await task;` —— task 在 delay 挂起期间完成要多等 0~50ms（平均 25ms/次），单检测位 6 个调用点合计 ~150ms
- 新：`while(true){ if(task.IsCompleted) return await task; 检查; if(await Task.WhenAny(task, Task.Delay(50)) == task) return await task; }` —— 任务完成立即返回（零尾差）
- **语义不变**：暂停 ≤50ms 响应（每轮检查 isPaused）、取消立即返回 default、task 异常经 await 上抛（调用方 catch 语义不变）

## 预期收益（非 Blob 主线，单检测位 ~7.5s）
| 项 | 收益 |
|---|---|
| 1a | 200ms 确定性 |
| 2 | 10~400ms（现场 Origin 目录规模相关，期望 ~100ms） |
| 3 | 平均 150ms（0~300ms 统计性，Blob 多针型模式更大） |
| 合计 | 确定性 ~200ms+，期望 ~450~650ms/位（6%~8.7%）；17 位整板期望 ~8s |

## 回归重点（⚠️ 必做）
1. **优化 3 是 14 处共享的公共方法**（检测/全图采集/传送带）——回归：单板/连续/全图采集/演示模式 的**暂停/恢复、复位立即终止、取消**场景（模拟器手册 `.claude/memory/simulator-test-rules-20260814.md` 场景模板）
2. `[⏱] 检测位i A(移动+拍照)` 应比优化前显著下降；`[诊断]` 行应消失
3. 模拟器验证法（§9 配套）：改前/改后同环境各跑 ≥10 拍，对比 A 段均值与总耗时均值

## 未做（留待后续）
- 1b（SET_PATH 缓存 + delay100 缩短）：需先确认 VM 是否有 SET_PATH 回执协议
- 6（文件等待与移起点并行）与 7（扫描提速）：结构性改动，待优化 5 稳定后评估

## 优化 5：ReplaceTile 重拼+显示异步化（8-24 16:40 已实施，未编译待验证）
- 文件：`Core/Managers/FullImageManager.cs`（调用方零改动）
- 改动：`ReplaceTile` 同步部分只保留缓存更新（ReadImage+ScaleAndCrop），全量重拼（ConcatObj+TileImages）与显示（DispObj）移入 `RebuildStitchedAsync`：
  - 纯计算 `BuildStitched` 在 `Task.Run` 后台线程执行（Halcon HOperatorSet 线程安全）
  - 完成后 `Dispatcher.InvokeAsync` 在 UI 线程「先显示成功再替换 `_currentStitchedImage`」（与原同步顺序一致）
  - 无 UI 上下文（Application.Current 为 null）时退回同步（原行为）；显示失败仅日志不再终止流程
- 竞态处理：`_currentStitchedImage` 的 Dispose/赋值/克隆全部收敛 UI 线程；`_cachedScaledImages` 后台只读 vs 下拍同步写——拍间隔 ~7s ≫ 重拼 ~1s 时序隔离
- 保留同步版 `RebuildStitchedAndDisplay`（全图采集 `StitchAndDisplay` 仍用，行为不变）
- 收益：A 段 ~1580ms → 预期 ~930ms（省 ~650ms/拍）；画面效果不变（同样的全图 DispObj+叠加层），显示时机变为重拼完成即显示（扫描期覆盖）
- ⚠️ 与 8-21 回退的"免拷贝/直绘"不同：只改计算线程，不改显示方式，画面零变化
- 验证：Roslyn 通过（FullImageManager.cs 真实错误 0）。待编译 + 模拟器 A 段对比 + 画面目视 + 暂停/复位回归（重点：恢复后画面一致、复位无残留图）

## 优化 8：移动中心(X/Y)+调Z 合并为一次指令（8-24 16:55 已实施，未编译待验证）
- 文件：`ViewModels/MainViewModel.RunCommands.cs`（GrabOneAsync，Blob 模式板面图/针尖图共用）
- 改动：原两段串行（`MoveToAndWaitOK(x,y)` 再 `MoveToAndWaitOK(z)`，各带重试3次+状态栏各一条）→ 合并为一次 `MoveToAndWaitOK(x, y, z: focusZ>0?(double?)focusZ:null)`：
  - 多轴并发运动，省一次指令往返与串行等待；状态栏合并为一条"移动到中心点并调焦"
  - 超时统一用 MoveToCenterTimeout（与 MoveZTimeout 同为 2000ms）；失败日志合并"移动到中心点/调焦失败"
  - 调Z后稳定等待 `Task.Delay(100)` 保留（仅 focusZ>0，曝光前光学稳定保险）
  - 暂停/重试/取消语义不变：移动中暂停→返回 null 外层转 Paused，恢复后整拍重做
- 注意：XY 与 Z 同时运动，**需用户确认机构安全**（调焦轴与水平轴并发无干涉）
- 收益：Blob 每张图省一次移动等待（模拟器/实机均受益，实机估 100-500ms/拍）
- 验证：Roslyn 通过（RunCommands.cs 真实错误 0）。待编译 + 模拟器 A 段对比 + 机构安全确认
