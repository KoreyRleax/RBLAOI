# 交接文档 2026-08-25：3D 结果等待异步化 与 UI 阻塞问题排查

> 交接给新会话。覆盖：3D 结果等待异步化的完整方案与实施（快照+回退），以及随之暴露并解决的 **UI 线程阻塞链路**（三层问题）。
> 分支：`fix/multi-pintype-blob-parser`（e5a4f7f 之上，**本次改动未提交**）
> 改动文件：`ViewModels/MainViewModel.DetectionFlow.cs`、`ViewModels/MainViewModel.CollectionFlow.cs`（枚举）
> 状态：**待用户编译实机验证**（沙盒环境无 VS2019）

---

## 一、背景与目标

- 原流程 `WaitScan3D` 扫描完成后**同步等待 DETECT_3D_OK（中位 137ms / p90 191ms / 偶发 1.5s）**，期间轴空闲；
- 目标：等待与"移动到下一检测位"（A 段 ~1.8s）并行，**数据到即显示、零推迟**；
- 收益预期：~167ms/拍（等数据 + Merge 均被 A 段隐藏），40 位排 -6.7s。

## 二、最终方案（快照+回退，Merge 570 行零改动）

```
WaitScan3D(i): 只等扫描完成 → 快照 i 上下文(_pending*) + 早开数据监听 → NextPos（不等数据）
CapturePos(i+1)/GrabTile(i+1): A段移动+拍照（期间 D_i 后台到达）
MergePending(i+1 A段后): 恢复 i 快照 → 取数(剩余窗口 max(0,2s-已过)) → Merge(i) 原样执行
   ├ 正常 → NextPos(i++) → _skipAForNext → 恢复 i+1 上下文 → MergePending(无挂起) → Start2D(i+1)
   └ 重扫 → i 已恢复 → MoveScanStart(i)（用快照 startX）+ swPos.Restart() → 重扫 → 同上
末位: NextPos(i=N) → MergePending 补 N-1 的 Merge → End
```

**为什么选快照+回退而非参数化 Merge**：Merge 570 行重度依赖流程变量（i/pos/detectX/startX/rawPins2D），参数化改 ~20 处易错；快照+回退让 Merge **原样执行**（重扫分支零改动），风险集中在状态流转外围。

### 状态机改动点（DetectionFlow.cs）

| # | 位置 | 内容 |
|---|---|---|
| 1 | 字段区（CapturePos 前） | 新增 `_pendingMergeIdx/_pendingPos/_pendingDetectX/Y/_pendingScanHalfWidth/_pendingStartX/EndX/MoveY/_pendingPins2D/_pending3DDataTask/_pendingScanDoneUtc/_skipAForNext`（12 个） |
| 2 | BizStep 内 | 局部函数 `SnapshotPending3D(idx, pins2D)`（快照挂起上下文） |
| 3 | `WaitScan3D` case | 重写：**监听早开**（await 扫描前创建，防 VM 提前推送漏数据）→ 只等扫描完成 → 快照 → NextPos；扫描失败丢监听（scan3DNoData=false 不重扫）；总耗时日志移位至此 |
| 4 | 新增 `MergePending` case | 检查点⑦：恢复 i 上下文 → 取数（剩余窗口）→ 写 scan3DTimeout/scan3DNoData → 置 _skipAForNext → Merge；无挂起 → Start2D/End |
| 5 | `GrabTile` 末尾 | `Start2D` → `MergePending` |
| 6 | `Merge` case | 删 2 处总耗时日志；重扫 2 处补 `swPos.Restart()` |
| 7 | `NextPos` case | i++ 后：i>=N → MergePending；`_skipAForNext` → **恢复当前位上下文**（pos/坐标/行程，防 Start2D 用错 PinTypes）→ MergePending；否则 CapturePos |

## 三、⚠️ UI 阻塞问题全链路（本交接重点，三层问题）

异步化把"等数据"从关键路径移除后，**依次暴露并解决了两层 UI 阻塞问题**。每层都导致实测不升反降，均已在实机日志定位根因。

### 第 1 层：Merge 落表 `await Dispatcher` → 轴空等 886ms
- **现象**：A 段完成 → Merge 的 3D 匹配日志（25ms）→ DETECT2D_BLOB 延迟 886ms 才发出；
- **根因**：Merge 内 `await Application.Current.Dispatcher.InvokeAsync`（落表）在等 UI 线程；而 A 段拍照期间 `ReplaceTile → RebuildStitchedAsync` 正在 **UI 线程渲染 19MB 拼接大图**（优化 5 重拼在后台，但**显示仍走 Dispatcher**）→ 落表排队等渲染 → 轴空等；
- **修复**：Merge 落表（2 处）与 GrabTile 清旧结果（1 处）改为**不 await**（`Dispatcher.InvokeAsync` 队列 FIFO 保序）。

### 第 2 层：不 await 后 `PinResults` 跨线程竞态 → A/B+C 段 +180/+240ms
- **现象**：修复第 1 层后，总耗时反升（4767→5028）；同位置 A 段 +182ms、B+C +238ms；但 A 段内部每步正常（移动 304ms、GRAB 249ms、VM 写图 177ms、文件等待 175ms）→ 磁盘/VM/轴全排除；
- **根因**：落表不 await 后，**UI 线程异步执行 `PinResults.Add/Remove`**，而流程线程的 `UpdateStatistics()`（DetectionFlow.cs L2122）**直接枚举 PinResults（ObservableCollection）** → 跨线程集合竞态拖慢流程。await 版有时序隔离（落表完成才统计），不 await 后竞态暴露；
- **修复**（4 处）：① GrabTile 清旧结果、② Merge 正常路径、③ Merge 超时降级——`UpdateStatistics` 均**移入对应 Dispatcher 回调**（与 Add/Remove 同批，UI 线程枚举）；④ `End` 分支加 `await Dispatcher.InvokeAsync(()=>{})` **flush**（收尾统计前清空落表队列）。

### 经验教训（写进代码注释，勿再踩）
1. **fire-and-forget 化 Dispatcher 时，必须连带迁移所有"读 ObservableCollection 的流程线程代码"**（UpdateStatistics 是典型）——否则 UI 写与流程读并发；
2. Dispatcher 的 `InvokeAsync` 排队 FIFO 可保顺序，但**不保证执行时机**——流程线程读 UI 集合必须以"同批回调"或"await 时序隔离"串行化；
3. UI 渲染（大图重拼显示）占用 Dispatcher 队列时，任何 `await Dispatcher` 都会让**轴空等**——检测流程内的 UI 操作一律 fire-and-forget + 快照绘制。

## 四、实测数据（16mm/s，39 位完整轮）

| 轮次 | 状态 | A 中位 | B+C 中位 | D 中位 | 总中位 | 备注 |
|---|---|---|---|---|---|---|
| 11:38 基线（优化11后） | 同步 | 1838 | 2251 | 2391 | 4625 | — |
| 15:18（等数据异步化+pos修复） | 第1层阻塞 | 1887 | 2599 | 2222 | 4767 | Merge await UI 886ms |
| 15:26（+Dispatcher不await） | 第2层竞态 | 2080 | 2990 | 2119 | 5028 | PinResults 跨线程竞态 |
| **预期（修复后）** | 待测 | ~1850 | ~2350 | ~2120 | **~3800-4000** | D 段 -270 已验证；A/B+C 待回落确认 |

- **已验证收益**：D 段中位 2391→2119（-272ms，数据等待完全隐藏）；
- **待验证**：修复第 2 层后 A/B+C 是否回落基线（1800/2250 水平）→ 总中位应到 ~3900；
- 若验证通过：40 位排 ~156-160s（对比 8/21 基线 309s，累计 **-48%**）。

## 五、其他本次改动

- **日志格式变化**：`E(3D结果获取)` 行已移除；`总耗时` 行移到 WaitScan3D 扫描完成时打印（体现异步化后真实节拍）；
- **分析脚本适配**：`analyze_135_dspeed.py` E 列已容错（E 行缺失时显示 "—"）；`analyze_a_segment.py`/`analyze_data_wait.py` 依赖旧日志格式，仅对旧日志有效；
- **相关方案文档**：`优化方案-3D结果等待异步化.md`（v2）、`更新日志-20260825-3D结果等待异步化.md`（实施记录）。

## 六、待办与遗留风险

1. **实机验证**（用户执行）：总耗时中位预期 ~3900；重扫路径（i 回退+不重拍）；暂停/恢复；末位落表；
2. **重扫路径风险最高**：偶发场景验证频次低，模拟器可用"模拟空 3D 数据"构造触发；
3. **UpdateStatistics 竞态**：本次修复 3+1 处，但**流程线程还有没有其他枚举 PinResults 的代码**需全量排查（L1500 去重/L1628 汇总在 End flush 后执行 ✓）；
4. **暂停恢复 swPos"总="值异常**（7533ms，15:13 轮观察到）：暂停恢复路径的 swPos.Restart 时机疑似错位，低优先待查；
5. **3D 偶发重试**（已知遗留）：检测位 23 本轮再现 1 次，单次损失 4-6s，仍建议抓相机侧日志立项。

## 七、环境坑（沙盒 git，沿用）

- git 写 `refs/heads/fix/` 被沙盒静默拦截 → 分支可能 unborn；`git log --oneline -1` 异常时用 reflog 取 SHA（`tail .git/logs/HEAD`）手动重建 `refs/heads/fix/multi-pintype-blob-parser`；
- 远程 tracking ref 写不进（`[origin/...: gone]`），push 走 URL 不受影响；push 后 `git ls-remote origin <branch>` 验证。
