## 更新日志

**时间**：2026-07-22
**标题**：第一阶段：多类型针检测 -- 数据模型重构和方案编辑UI

**背景**：
原检测参数为全局单一值（单个 X/Y 允许偏差、单一理想高度等），无法支持多类型针（A/B/C...）各自独立的检测参数。同时 `FocusParams` 数组结构不灵活（焦距+ROIW+ROIH 硬编码），不利于扩展。

**改动内容**：

### 1. 新增 `Models/PinTypeParameter.cs`
- 单种针型的检测参数类，继承 `NotificationObject`
- 包含：Label(标记)、Focus(焦距)、XyTolerance、IdealOffsetX/Y、HeightTolerance、IdealHeight

### 2. 修改 `Models/ProjectData.cs`
- 删除：`FocusParams`(double[])、`XAllowedDeviation`、`YAllowedDeviation`、`IdealPinHeight`、`AllowedHeightDeviation`、`RelativeOffsetX`、`RelativeOffsetY`
- 新增：`PinTypeParams`(PinTypeParameter[])，替代以上全部

### 3. 修改 `Models/SysParamData.cs`
- 删除：`XAllowedDeviation`、`YAllowedDeviation`、`IdealPinHeight`、`AllowedHeightDeviation`、`RelativeOffsetX`、`RelativeOffsetY`
- 保留全局参数：`DetectionMode`、`EmptyPinThreshold`、`MatchThreshold3D`

### 4. 修改 `ViewModels/SysParamViewModel.cs`
- 删除对应 6 个 `[SysParam]` 标注属性

### 5. 修改 `Views/SysParamWindow.xaml`
- 删除"检测参数"分组中的 6 个 LabeledTextBox 控件（X偏差/Y偏差/理想高度/高度偏差/X偏移/Y偏移）

### 6. 修改 `Views/EditProjectWindow.xaml.cs`
- 构造函数参数简化：删除 6 个旧检测参数，改为 `PinTypeParameter[] pinTypeParams`
- 删除 `FocusParams` 属性，新增 `PinTypeParams`(ObservableCollection`<PinTypeParameter>`)
- 删除 `BtnFocusSetting_Click`、`BtnResetParams_Click` 方法

### 7. 修改 `Views/EditProjectWindow.xaml`
- 删除"焦距设定"按钮和"恢复系统参数"按钮
- 用 DataGrid 动态表格替代旧的 LabeledTextBox 固定布局
- 每列对应 PinTypeParameter 的 7 个属性，支持编辑

### 8. 删除 `Views/FocusSettingWindow.xaml` + `.xaml.cs`
- 旧的焦距设定弹窗不再需要，功能整合到 EditProjectWindow 的 DataGrid 中

### 9. 清理 `RBLAOI.csproj`
- 移除 FocusSettingWindow 的 Compile 和 Page 引用
- 添加 PinTypeParameter.cs 的 Compile 引用

**注意事项**：
- `MainViewModel.cs` 中引用旧属性的代码尚未修改，留到阶段二处理
- 旧方案加载兼容性逻辑（从旧版 FocusParams 迁移到 PinTypeParams）在阶段二实现
- 未改动 `MotionIO.cs`

**编译状态**：
- `EditProjectWindow` 自身改动已确保编译正确
- 全项目编译因 `MainViewModel.cs` 引用已删除属性而暂不可用（阶段二修复）
