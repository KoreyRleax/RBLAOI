# 更新日志：AOI 连续模式自动运行（入板→检测→出板循环）

**时间**：2026-08-12  
**标题**：连续模式自动运行（参照插针机连续模式业务设计）

## 背景

AOI 点击「开始」只检测一块板就结束（单板模式）。参照插针机连续模式（loadPlate 入板/出板 + autoRun 主状态机 + 流片任务防堵），为 AOI 开发连续模式：自动循环"入板→检测→出板"，连续检测多块板。

**已确认决策（方案 a）**：运行功能栏新增「单板模式」「连续模式」互斥高亮按钮切换模式；固定按钮「开始」按当前模式执行。

## 解决方案

### 1. 工作模式切换（方案 a）

- `IsContinuousMode` 模式状态 + `SetWorkMode` + `ModeChanged` 事件；运行栏新增「单板模式」「连续模式」按钮（互斥高亮，当前模式蓝色激活），默认单板模式
- 固定按钮「开始」按模式分发：单板=现有检测；连续=启动/继续连续流程
- 固定按钮「停止」Tag 改 `stop_work`：连续模式运行中=优雅停止（当前板检测完→出板→结束，不卡板）；单板=原有暂停/继续

### 2. 连续模式状态机（ContinuousRunInternal，仿插针机 autoRun 主状态机）

```
Init(ResetIO准备) → LoadBoard(入板) → WaitCheck(等检测完成)
→ UnloadBoard(降顶板+出板) → WaitOutletClear(出口防堵) → 循环回 LoadBoard
End / Err（终态）
```

- 停止语义：取消后 WaitCheck 不打断（当前板检测完才出板），其余段立即收尾；安全收尾 MOTOR_STOP + ResetIO
- 循环用 CTS 取消 + FlowLoopAsync 驱动（与现有传送带流程同模式）

### 3. 子流程抽核（最小改动）

`TransportBoard` / `StartCheckFlow` / `SendToExit` 由 `async void` 改为 `Task<bool>`（成功/失败返回），新增 `bool isSubFlow` 参数跳过全互斥检查（外壳持 ContinuousRun 标志）；公共入口行为不变（调用点 `_ =` 兼容）。

### 4. 入口/出口防堵（仿插针机"出口2堵板不入板"联锁）

- **出口**：新板入板前检查出口传感器，有板则等待取走（`ContinuousOutletClearTimeout` 30s 超时报警结束），防堆板
- **入口**：连续模式下入口无板时**纯等待前机放板**（新增 `waitEntryOnly` 参数，不 MOTOR REV 反向，避免与前机送板冲突），超时 `ContinuousEntryWaitTimeout`（60s）

### 5. 新增系统参数（可现场配置）

`ContinuousOutletClearTimeout`（出口防堵 30s）、`ContinuousEntryWaitTimeout`（入口等板 60s），[SysParam] 反射同步 + 系统参数界面输入框。

## 总结

连续模式按插针机业务思想落地：入板（等前机放板→传送→工作位→顶板）→ 检测 → 出板（降顶板→送出口）→ 防堵循环；模式切换 UI 高亮互斥；停止语义优雅（不卡板）；检测流程未重构（保持现状，Task<bool> 返回已为将来状态机化留衔接点）。

## 验证

- 编译通过（x64）。
- 待现场/虚拟串口走查：连续模式启动→入板→检测→出板→下一块；停止→当前板收尾出板后结束；出口堵板→等待取走/超时报警；入口无板→纯等待前机放板；模式切换高亮互斥。

---

## 补充更新（同日）：连续模式停止语义修正

### 背景

原设计"停止=当前板检测完→出板→结束"经确认不符合操作习惯。

### 解决方案

连续模式下「停止」按钮改为**暂停当前动作**（检测段立即暂停，传送带段完成当前动作后段间等待），点「开始」继续（`PauseContinuousFlow` / `ResumeContinuousFlow` + 状态机段间暂停检查）；结束连续模式仍由「切回单板模式」触发（优雅收尾：当前板检测完→出板→结束）。

### 验证

- 编译通过（x64）。
