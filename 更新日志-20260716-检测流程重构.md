# 更新日志

## 时间
2026-07-16

## 标题
检测流程重构：Blob方案双GRAB+Z轴调焦+双路径协议

## 背景
原有的检测流程中，GrabAndReplaceTileAsync 只做一次 GRAB 拍照和瓦片替换。Blob 检测方案需要分别采集焊盘图（用于轮廓匹配）和针尖图（用于Blob分析），两次拍照需要不同的 Z 轴焦距（焊盘焦距 / 针尖焦距）。同时 `DETECT2D_BLOB` 协议需要支持双路径输入。

## 解决方案

### 涉及文件

1. **ProjectManager.cs** - 新增 `GetPinPath()` 方法获取 Pin 目录
2. **MainViewModel.cs** - 核心改动：
   - 新增 `SetVmSavePathToPin()` 方法
   - 新增 `GrabAndReplaceWithFocusAsync()` 双GRAB+Z轴调焦方法
   - 新增 `GlobalDedup()` 全局去重方法
   - StartCheckFlow 中为 Blob 模式调用双 GRAB 流程
   - DETECT2D_BLOB 指令改为 `{焊盘路径}|{针尖路径}` 双路径格式
   - 全局去重阈值从 0.2mm 改为 0.5mm
3. **UserGlobalScript.cs** - DETECT2D_BLOB 解析逻辑改为支持 `|` 分隔双路径，分别设置图像源1（焊盘图）和图像源2（针尖图）
4. **检测流程.md** - 更新流程文档

### 关键技术点

- **双GRAB流程**：移动到检测位中心 → Z轴焊盘焦距 → 第一次GRAB(焊盘图) → ReplaceTile → Z轴针尖焦距 → 第二次GRAB(针尖图) → 保存针尖路径
- **SetVmSavePathToPin()**：设置 VM 存储路径到 `{ProjectDir}/Images/Pin/`
- **双路径协议**：`DETECT2D_BLOB:{padPath}|{pinPath}`，VM端分别设置图像源1和图像源2
- **全局去重**：阈值 0.5mm，优先保留有3D高度的针

## 总结
完成了 Blob 检测方案的完整流程重构，包括 Z 轴调焦双 GRAB 拍照、双路径 TCP 协议、VM 端脚本解析、全局去重优化和文档更新。所有改动均编译通过（预存在的 VM SDK 引用缺失警告不影响代码正确性）。
