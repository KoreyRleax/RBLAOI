# 更新日志：模拟器拍照存图修复 + 同名瓦片替换修复（2026-08-14）

## 时间
2026-08-14

## 标题
模拟器未能模拟 VM 存图导致拍照超时的修复

## 背景
用户反馈：模拟器未能模拟 VM 将图片放到指定路径，导致上位机拍照超时。日志证据（log_20260814_110438.txt）：11:05-11:12 期间模拟器**持续回裸 `GRAB_OK`**（无图片文件名），上位机 `[GrabOne] 文件等待超时: GRAB_OK` 连续 27 次——上位机拿 "GRAB_OK" 当文件名等 `...\Origin\GRAB_OK` 文件必然超时。

模拟器回裸 `GRAB_OK` 的两种情况：图片队列为空（未加载路径/加载失败）、复制到目标路径失败。且**模拟器 GUI 日志不落盘**，问题无法事后追溯。

另发现上位机侧关联 Bug：`GrabAndReplaceTileAsync`/`GrabOneAsync` 替换瓦片时"删除旧文件→复制新文件"，当 GRAB 返回文件名与旧瓦片同名（如模拟器固定文件名）时，删除步骤把源文件也删了 → 复制必然失败 → 拍照失败（此 Bug 因 2026-08-14"拍照失败直接转异常"修改从隐藏变显性）。

## 解决方案

### 模拟器（C:\Users\59538\Desktop\f407_gui_simulator.py）
1. **`_load_config` 自动加载图片队列**：配置路径存在时直接建立图片队列（与 `__init__` 已有的自动加载形成双保险），启动即就绪，无需手动点"选目录"；
2. **新增文件日志**：`log()` 同步写入脚本同目录 `f407_gui_simulator.log`（加锁、多线程安全）——模拟器行为可事后追溯；
3. 新增 `_refresh_queue_label()` 辅助方法。

### 上位机（ViewModels/MainViewModel.cs，待部署）
4. `GrabAndReplaceTileAsync`/`GrabOneAsync` 替换瓦片处：**源==目标（同名文件）时跳过删除/复制**（文件已就位），直接返回源路径。

## 总结
模拟器启动即加载图片队列 + 文件日志落盘；上位机同名瓦片替换短路。模拟器侧已通过 `py_compile` 语法验证、配置路径有效。上位机修改已编译通过（0 错误），**bin 因运行中的程序（PID 12188）锁定暂未部署**——需关闭程序后重新编译部署。验证路径：重启模拟器 → 启动程序 → 检测应能正常拍照（模拟器回 `GRAB_OK_*.bmp` 文件名 + 文件就绪），排障时可查 `f407_gui_simulator.log`。
