# 更新日志 2026-08-13：全图采集流程三层状态机化

## 时间
2026-08-13

## 标题
全图采集流程状态机化改造（三层架构：设备状态 / 流程运行状态 / 流程业务状态）+ 主按钮语义统一

## 背景
全图采集（`StartFullImageGrabFlow`）原为「bool 互斥标志 + `_grabCts` 取消令牌」混合控制：
- 无显式状态、**无暂停能力**（固定"停止"按钮对采集无效，UI 上 `stop` tag 为死代码）
- 复位只能间接 Cancel，无"终止"概念
- 用户要求：状态机应聚焦业务流程（同传送带/连续模式），设备状态（初始化/运行/暂停/终止）由独立层维护

## 解决方案

### 三层架构（用户拍板）
```
设备状态机（已有 DeviceMonitor.DeviceState，本次补 Demo）
流程运行状态（壳层 FlowRunState：Init → Running ⇄ Paused → Terminated，通用命名，本次仅全图采集接入）
流程业务状态（GrabState：Init → MoveToPos → GrabImage → SaveStitch → NextPos → End/Err，仿 TransportState）
```

### 具体改动
1. **MainViewModel.cs**
   - 新增 `_pauseGrabEvent`（ManualResetEventSlim，与 `_pauseCheckEvent` 对称）
   - `flowPaused` 委托扩展为"检测暂停 或 全图采集暂停"→ 采集暂停时设备状态显示"暂停"
   - `StartFullImageGrabFlow` 重构为壳层（`RunStep`）驱动业务层（`BizStep`）双局部函数 + `FlowLoopAsync` 驱动；游标 `i` 仅整拍完成后 `++`，暂停恢复后从当前点位重做
   - 暂停检查点 = 每点位顶部（MoveToPos 入口）；暂停/恢复不刷状态栏（保持原业务文字：移动中/拍照中/开始/完成/调焦距/就绪）
   - 新增 `IsFullImageGrabRunning` / `PauseFullImageGrab()` / `ResumeFullImageGrab()` / `PauseCheckOnly()`
   - `PauseContinuousFlow()` 内部 `PauseCheck()` → `PauseCheckOnly()`（纯暂停，重复点击不误恢复）
   - `ResetMachine()` 加 `_pauseGrabEvent.Set()`：唤醒暂停等待 → 立即进入 Terminated（不等待流程退出）
   - `StopFullImageGrabFlow()` 改终止语义（Cancel + Set 唤醒）
2. **DeviceMonitor.cs**：`DeviceState` 枚举补 `Demo`（预留，演示模式本次不联动）；`StateText` 补"演示"
3. **MainWindow.xaml.cs**：固定"停止"按钮改纯暂停分发（采集→暂停采集；连续→暂停当前动作；单板检测→纯暂停，恢复只能点"开始"）
4. **三色灯统一语义**（DeviceMonitor.UpdateLights 重构 + VM 委托扩展）：
   - 绿灯 = 流程业务执行中（`flowBizRunning` 委托：全图采集按业务阶段标志、检测按暂停、连续按暂停标志、其余流程运行中即执行）
   - 红灯 = 轴报警/急停/气压 或 流程报错（`flowError` 委托：Err 终态收尾弹窗期间亮红）
   - 黄灯 = 其余（就绪/手动/未就绪/准备阶段/暂停/终止收尾/完成收尾）
   - 灯色改为每轮询周期评估（不再只在设备状态变化时下发），红绿黄灯仍仅变化时下发指令
   - 顺带：`flowPaused` 纳入 `_continuousPaused`（连续模式暂停时设备状态文字也显示"暂停"）

### 按钮行为矩阵（含方案A定稿）
> 定稿修正（消除"开始"按钮歧义）：**开始=检测专用**；**采集的暂停/恢复由「采集全图」按钮承担**——暂停时按钮解除禁用，点击=从当前点位恢复。

| 按钮 | 采集运行 | 采集暂停 | 连续模式 | 单板检测 | 空闲 |
|---|---|---|---|---|---|
| 停止 | → Paused（按钮解除禁用） | 无动作 | 暂停当前动作 | 纯暂停（不再切换） | 无动作 |
| 开始 | 提示"全图采集运行中" | 提示"已暂停，请点「采集全图」继续" | 现有 | 现有 | 现有 |
| 采集全图 | 禁用（兜底提示） | 恢复采集（当前点位重做，按钮重新禁用） | — | — | 启动新采集 |
| 复位 | → Terminated + 物理复位 → 空闲 | 同（≤200ms 退出） | 现有 | 现有 | 现有 |

## 总结
全图采集从"标志+令牌"升级为与传送带/连续模式同构的三层状态机架构：壳层管生命周期（暂停/终止），业务层管点位执行，设备状态机对外呈现（采集暂停 → 设备"暂停"）。主按钮语义固定无歧义："开始"=检测专用、"采集全图"=采集专用（含暂停恢复）、"停止"=纯暂停、"复位"=终止+物理复位。编译通过（0 错误，仅预存在警告）。
