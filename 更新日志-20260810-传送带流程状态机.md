# 更新日志

- **时间**：2026-08-10
- **标题**：传送带流程状态机 —— 运送基板/送到入口/送到出口改造（MOTOR FWD/REV/STOP 指令）

## 背景

下位机（F401 运动板）新增三条 ASCII 连续运转指令：`MOTOR FWD`（正向连续运行）、`MOTOR REV`（反向连续运行）、`MOTOR_STOP`（停止并取消正在运行的 MOVE_* 状态机），成功响应分别为 `MOTOR_FWD_OK` / `MOTOR_REV_OK` / `MOTOR_STOP_OK`。

原"运送基板 / 送到出口 / 送到入口"三个动作是简单的 `MOVE_MID` / `MOVE_OUT` / `MOVE_IN` 定时等待（固定 10s），不判断出入口传感器（待料位 X0=入口、出料位 X4=出口），无法表达"等待板到位即停 / 带中无板超时"的流程。

## 解决方案

### 1. 轻量状态机 `Core/Motion/ConveyorFlowController.cs`（新建，未触碰 MotionIO.cs）

每个流程为"发指令 → 等信号/等时长 → 转移"，超时/异常确保 `MOTOR_STOP` 停传送：

- **运送基板** `TransportBoardAsync`：
  - 入口无料 → `MOTOR REV` 反向等待板到入口（超时 `ConveyorWaitEntryTimeout`）：
    - (a) 超时入口仍无料 → 返回 Timeout（传送带中无板）；
    - (b) 期间入口有料 → 停反向 → 打开阻挡气缸 → `MOTOR FWD` 正向传送 `ConveyorForwardDuration`（3s）→ `MOTOR_STOP` → 顶板（顶板1/2 顶起）；
  - 入口初始有料 → 打开阻挡气缸 → `MOTOR FWD` 正向 3s → `MOTOR_STOP` → 顶板。
- **送到入口** `SendToEntranceAsync`：阻挡降下 → `MOTOR REV` 反向，入口有料即 `MOTOR_STOP`；超时（5s）入口无料 → Timeout。
- **送到出口** `SendToExitAsync`：阻挡降下 → `MOTOR FWD` 正向，出口有料即 `MOTOR_STOP`；超时（5s）出口无料 → Timeout。

关键实现点：
- 传感器轮询读 GET_STATE 综合快照（`GetIOState`，50ms 间隔），不发额外查询指令；
- MOTOR 指令经 `SendCommandAndWaitAsync` 走现有串口队列（串行化，日志已有 [Send]）；
- 所有超时/失败路径先 `MOTOR_STOP` 再返回，保证传送带必停。

### 2. 超时参数入系统参数（可配置，不重编译）

`SysParamData` 新增 4 个字段（默认：入口等待 10000ms、正向传送 3000ms、送到入口/出口 5000ms），`SysParamViewModel` 同名 `[SysParam]` 反射同步，系统参数界面"⚙️ 其他"分组新增一行（2 列 × 2 行）配置输入框。

### 3. MainViewModel 三个动作转发状态机

`TransportBoard` / `SendToExit` / `SendToEntrance` 保留原外壳（isWorkFlowRun 防并发、CheckMotionConnected、ActionBusyStateChanged 按钮状态、UpdateStatus 状态栏），内部改为调用状态机并按 `ConveyorResult`（成功/超时/下位机无回复）更新状态与日志。

## 注意

- 需求原文两处"执行 MOTOR REV 正向传送"经确认系笔误，已按 **MOTOR FWD（正向传送）** 实现；
- 状态机每步状态转移记 `[Conveyor]` 日志，上机排查方向/超时在哪一步一目了然。

## 总结

三个传送带动作从"固定超时的 MOVE_* 指令"升级为"传感器信号驱动的轻量状态机"：入口无料时自动反向找板、到位后正向 3s 顶板、送到入口/出口有料即停、带中无板明确超时提示。全部时序参数可在系统参数界面现场调整；协议指令与 IO 全部走 MotionIO 现有公开接口，未违反"禁止改动 MotionIO.cs"规则。

## 验证

- 待编译通过；硬件验证待上机：运送基板（入口有/无料两路径）、送到入口、送到出口，观察 [Conveyor] 日志状态转移与 MOTOR 指令收发。

---

## 补充更新（方案 A 落地）：通用状态机底座 —— 轮子下沉，流程只注册规则表

### 背景

第一版状态机把"轮询等待/超时/日志"直接写进了 ConveyorFlowController 的每个流程方法，编排逻辑与业务状态耦合；且三条 MOTOR 指令的协议层散落在控制器内，与 MotionIO 的 MOVE_*/CLAMP 系列不统一。目标：底座下沉为通用框架，后续顶板流程/排料流程各自建控制器注册规则表，互不污染。

### 解决方案

1. **新建 `Core/StateMachine/FlowStateMachine.cs`**（通用底座，零业务状态）：
   - `FlowResult { Success, Timeout, Failed, NotConnected, Cancelled }` 通用结果枚举（替代原 ConveyorResult）；
   - `FlowTransition<TState>` 规则项：From + Guard(分支) + Enter(进入动作) + Wait(轮询条件/固定时长) + TimeoutMs + Next；
   - 底座职责：找可用规则 → 执行进入动作 → 轮询等待（50ms，可取消）→ 状态转移，统一记 `[StateMachine] 流程名: A → B` 日志；
   - **OnAbort 全局收尾**：任何一步超时/失败/取消先执行（通常=MOTOR_STOP），保证传送带必停；
   - `CancelAsync()` 取消令牌，供 MOTOR_STOP 中断；MaxSteps=32 循环保护防规则表回跳。
2. **MotionIO.cs 授权追加 3 个 MOTOR 方法**（2026-08-10 用户授权，仅追加未动现有逻辑）：
   `StartMotorForwardAsync` / `StartMotorReverseAsync` / `StopMotorAsync`（MOTOR FWD/REV/STOP + 对应 _OK 校验），协议层归 MotionIO 统一承担，后续任何流程复用这三条指令不重复写协议。
3. **ConveyorFlowController 重写为纯规则注册表**：保留文件名/类名，内部三个私有流程子类（TransportBoardFlow / SendToEntranceFlow / SendToExitFlow）继承 `FlowStateMachine<ConveyorState>` 各注册一张规则表（共用 ConveyorState 枚举），门面方法只做连接检查 + new + RunAsync。
4. **MainViewModel** 三处 switch 改用 `FlowResult`，新增 `Cancelled` 分支（"已取消"状态栏提示）。
5. **CLAUDE.md 规则同步**："不允许改动的文件 MotionIO.cs" 补充说明——除新增运动指令封装方法外禁止修改现有逻辑，新增指令须在更新日志注明。

### 总结

"编排框架"（轮询/超时/取消/日志）与"业务规则"（什么状态做什么）彻底分离：底座零业务、控制器零协议、MotionIO 只追加不修改。后续新流程（顶板/排料）只需新建一个 Flow 子类注册规则表即可复用全部时序能力。

### 验证

- 编译通过（AnyCPU；x64 需程序关闭后复制）。
- 上机验证同前：三流程状态转移日志 `[StateMachine] 运送基板: ReverseWait → StopReverse` 等，超时/取消路径均先 MOTOR_STOP 再返回。

---

## 补充更新（架构简化）：撤销底座与 Controller，状态机直接写在 MainViewModel

### 背景

方案 A 落地下来的层级是 MainViewModel → ConveyorFlowController → FlowStateMachine 底座 → MotionIO 四层，且需要注册规则表、建多个类。用户认为完全没必要：运送基板等流程的状态数很少（去入口/等待到入口/去基板/等待到基板/完成），直接在 MainViewModel 定义状态枚举、写状态业务逻辑最直观；YAGNI——遇到需要复用时再考虑复用，不一开始过度设计。

### 解决方案

1. **删除** `Core/StateMachine/FlowStateMachine.cs`（通用底座）与 `Core/Motion/ConveyorFlowController.cs`（控制器）两个文件及 csproj 注册项、MainViewModel 中的 `_conveyor` 字段；
2. **运送基板**：MainViewModel 内新增私有状态枚举 `TransportState { GoToEntry, WaitEntry, GoToBoard, WaitBoard, Done }`（按用户命名），`TransportBoard()` 内 while + switch 直接驱动：
   - GoToEntry：入口有料 → 直接去基板；入口无料 → MOTOR REV 反向找板；
   - WaitEntry：轮询入口传感器（50ms，GET_STATE 快照），超时 = 带中无板（先 MOTOR_STOP 再返回）；
   - GoToBoard：开阻挡气缸（等 200ms 到位）→ MOTOR FWD 正向；
   - WaitBoard：正向固定时长 → MOTOR_STOP；
   - Done：顶板1/2 顶起。
   失败/超时路径都显式先 MOTOR_STOP（与 MOVE_* 状态机取消语义一致）；
3. **送到入口/送到出口**：流程简单无分支，直接顺序代码（阻挡降下 → MOTOR → 等待传感器 → STOP），不再建状态枚举；
4. **公共轮询**：`WaitForIOSignalAsync(InputSignal, target, timeoutMs)` 私有辅助方法，三个动作共用（读 GET_STATE 快照轮询，不发额外指令）；
5. **保留**：MotionIO.cs 追加的 `StartMotorForwardAsync / StartMotorReverseAsync / StopMotorAsync`（协议层与 MOVE_*/CLAMP 同构，MainViewModel 直接调用，仍是两层结构）。

### 总结

层级回到 MainViewModel（状态逻辑）→ MotionIO（指令）两层：状态枚举与业务逻辑集中在一个类里，改流程、加状态都在同一处，视觉上一目了然；删除了底座与规则表注册的全部间接层。后续若出现真正需要复用的流程（多个流程共享同一段时序），再考虑抽公共方法，不预设抽象。

### 验证

- 编译通过（AnyCPU + x64）。
- 上机验证：运送基板（入口有/无料两路径）、送到入口、送到出口，观察 `[Conveyor]` 日志与 MOTOR 指令收发。

---

## 补充更新（同日）：等待 IO 信号改事件驱动，删除自写轮询

### 背景

原 `WaitForIOSignalAsync` 自己写"GetIOState + 50ms Delay"轮询循环。经核实代码：`MotionIO.Open()` 连接成功即启动 GET_STATE 后台无限轮询（~20ms 刷新 `CurrentIOState` 并触发 `IOStateUpdated` 事件），`GetIOState` 只是读内存快照、不发指令——自写轮询是多余代码。

### 解决方案

1. `WaitForIOSignalAsync` 改为**事件驱动**：先读 `CurrentIOState` 快照（已满足立即返回）→ 订阅 `IOStateUpdated`（后台轮询每 ~20ms 触发）→ 信号到位即退订返回 true；超时（Task.Delay）退订返回 false；并发安全（`TrySetResult` 只生效一次）；
2. **CLAUDE.md 新增规则**：IO 状态由后台 GET_STATE 轮询持续维护，读 IO 直接读 `CurrentIOState`/`GetIOState`；**等待 IO 信号订阅 `IOStateUpdated` 事件驱动，不要自己写轮询循环**——防止未来出现多余代码。

### 总结

等待传感器从"自写 50ms 轮询"改为"响应后台轮询事件"，零轮询、零额外指令、响应 ~20ms 内；规则已固化到 CLAUDE.md。

---

## 补充更新（同日）：状态机终版 —— 循环驱动 + Stopwatch 计时器，删除 TCS 等待

### 背景

事件驱动等待（TCS + IOStateUpdated）仍是多余写法：状态机循环本身就是"等待"的载体——每轮迭代重新检查条件、转移状态；超时用 Stopwatch 计时在状态内判断，比任何等待机制都直观。且原状态枚举没有终态（End/Err）不规范。

### 解决方案

三个流程全部改为**循环驱动状态机**（MainViewModel 内，同构模式）：

- **状态枚举带 End / Err 终态**：
  - 运送基板 `TransportState { GoToEntry, WaitEntry, GoToBoard, WaitBoard, LiftPlate, End, Err }`
  - 送到入口 `EntranceState { BlockOff, ReverseWait, End, Err }`
  - 送到出口 `ExitState { BlockOff, ForwardWait, End, Err }`
- **驱动结构**：`while (state != End && state != Err) { switch (state) { ... } }`——每轮迭代即"等待"：传感器条件用 `GetIOState` 直接读快照（后台 GET_STATE 轮询维护，~20ms 刷新）；超时是"`sw.ElapsedMilliseconds >= n` 的条件判断"（Stopwatch 在发完指令的状态里 `Restart()` 开始计时），不是阻塞等待；
- **Err 前必停电机**：超时/指令失败路径都先 `MOTOR_STOP` 再转移 Err；
- 循环节拍 `Task.Delay(50)`（未转移时才走，不空转 CPU）；
- **删除 `WaitForIOSignalAsync`**（TCS 事件等待整个移除）。

### 总结

状态机终版形态：枚举（含 End/Err 终态）+ 每轮条件判断 + Stopwatch 计时超时。状态机天然自带等待与超时，无任何多余等待包装；三个流程同构，改一个其他照抄；主流程只判终态（End=成功 / Err=异常，异常原因在 err 字符串）。

---

## 补充更新（同日）：switch 封装局部函数 + FlowLoopAsync 驱动循环

### 背景

状态机逐轮驱动（while + switch）与多执行器并行设备的 `commandMonitor` 轮询框架做过对比论证：本项目单工位串行（同一时刻一个动作，isWorkFlowRun 互斥已解决冲突），流程长且每步 await（串口/TCP 回复），轮询框架需拆"发起+回调置标志"两段式，是纯负担，不采用。但 switch 应封装为独立方法，且每个状态机外面都包 while 显得重复。

### 解决方案

1. **switch 封装为局部函数** `Step()`（C# 局部函数，闭包直接读写 sw/state/err，无需参数传递），每步处理当前状态一轮判断并转移，到达 End/Err 终态返回 true；
2. **新增 3 行公共驱动循环** `FlowLoopAsync(Func<Task<bool>> stepOne)`：每轮调用单步直到返回 true——各流程只写一行 `await FlowLoopAsync(Step);`，不再各自写 while（仅消 while 样板，非执行器框架：互斥/状态栏/日志仍在各流程外壳）；
3. 三个流程（运送基板/送到入口/送到出口）统一此形态。

### 约定（记忆已记录）

状态机数量多起来（约 10+ 个流程）后再抽统一执行器（互斥/状态栏/日志样板），现在不抽（YAGNI）。

### 验证

- 编译通过（AnyCPU + x64）。
